---
phase: 27-admin-slots-reservas-usu-rios
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/admin/ui/user_detail_sheet.dart
  - lib/features/admin/ui/users_management_tab.dart
autonomous: true
requirements:
  - ADMN-20
  - ADMN-21
must_haves:
  truths:
    - "Aba Usuários exibe rows hairline com avatar circular (laranja admin / ink usuário)"
    - "Avatar usa inicial Anton 20px em CircleAvatar 40×40 como fallback quando photoUrl ausente"
    - "Row exibe nome em Manrope 14px bold, email em JBM 11px, contador 'N reservas' em JBM 11px concrete"
    - "Tap em row abre UserDetailSheet"
    - "UserDetailSheet exibe drag handle + avatar grande 64px + nome + email + contador"
    - "UserDetailSheet exibe SportBtn 'PROMOVER A ADMIN' ou 'REMOVER ADMIN' com confirmação antes da ação"
    - "Ações de promover/remover chamam Firestore diretamente (via AuthCubit.promoteUser ou update direto)"
    - "Nenhum gradiente, sem ListTile padrão, sem inline promote button, sem primaryGreen"
  artifacts:
    - path: "lib/features/admin/ui/user_detail_sheet.dart"
      provides: "UserDetailSheet — novo bottom sheet Arena"
      exports: ["UserDetailSheet"]
    - path: "lib/features/admin/ui/users_management_tab.dart"
      provides: "UsersManagementTab reescrito com hairline rows + tap → sheet"
    - path: "test/features/admin/ui/user_detail_sheet_test.dart"
      provides: "Widget tests para ADMN-20 (avatar fallback)"
    - path: "test/features/admin/ui/user_row_test.dart"
      provides: "Widget tests para ADMN-21 (row layout)"
  key_links:
    - from: "users_management_tab.dart"
      to: "user_detail_sheet.dart"
      via: "showModalBottomSheet onTap"
    - from: "UserDetailSheet"
      to: "AuthCubit.promoteUser / FirebaseFirestore update role:client"
      via: "_handleAction → authCubit.promoteUser ou firestore.update"
---

<objective>
Criar UserDetailSheet (novo bottom sheet Arena) e redesenhar UsersManagementTab com rows hairline + avatar circular Arena. Tap em row abre sheet; sheet tem ações de promover/remover admin.

Purpose: Implementa ADMN-20 (avatar circular laranja admin / ink usuário, sem gradiente) e ADMN-21 (rows hairline: avatar + Manrope bold + mono email + mono contador), consolidando ações de admin em bottom sheet (D-14, D-15).
Output: user_detail_sheet.dart (novo), users_management_tab.dart (reescrito), testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-CONTEXT.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-UI-SPEC.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-RESEARCH.md

<interfaces>
<!-- Interfaces que o executor precisa. Extraídas do codebase. -->

From lib/core/theme/app_theme.dart:
```dart
static const Color orange   = Color(0xFFFF4D17);
static const Color ink      = Color(0xFF0E0E0C);
static const Color concrete = Color(0xFF6B6B66);
static const Color lineHair = Color(0xFFEAE3CE);
static const Color paper    = Color(0xFFFBF8F0);
static TextStyle display({double size = 32, Color? color})  // Anton, height: 0.92
static TextStyle ui({double size = 14, FontWeight weight, Color? color})  // Manrope
static TextStyle mono({double size = 11, FontWeight weight, Color? color})  // JBM Mono
```

From lib/core/widgets/sport_btn.dart:
```dart
SportBtn.filled(String label, {required VoidCallback? onPressed})
// FilledButton orange background, paper text, StadiumBorder, minimumSize(double.infinity, 52)
```

From lib/core/models/user_model.dart:
```dart
class UserModel extends Equatable {
  final String uid;
  final String email;
  final String displayName;
  final String role; // "client" or "admin"
  final String? phone;

  bool get isAdmin => role == 'admin';
}
// NOTA: UserModel NÃO tem photoUrl nem bookingCount — esses campos não existem no modelo
// Exibir apenas o que existe: displayName, email, role (isAdmin)
// Para bookingCount: NÃO mostrar (campo não existe) — omitir da sheet
```

From lib/features/auth/cubit/auth_cubit.dart:
```dart
Future<void> promoteUser(String uid) async {
  await _firestore.collection('users').doc(uid).update({'role': 'admin'});
}
// demoteUser NÃO existe — implementar inline na sheet:
// await FirebaseFirestore.instance.collection('users').doc(uid).update({'role': 'client'});
```

From lib/features/booking/ui/hairline_booking_row.dart (padrão hairline — adaptar para UserRow):
```dart
DecoratedBox(
  decoration: BoxDecoration(
    border: index == 0 ? null : const Border(
      top: BorderSide(color: AppTheme.lineHair, width: 0.5),
    ),
  ),
  child: InkWell(
    onTap: ...,
    child: Padding(
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
      child: Row(...),
    ),
  ),
)
```

Existing users_management_tab.dart logic to preserve:
```dart
// _loadUsers() — FirebaseFirestore.instance.collection('users').orderBy('displayName').get()
// _onSearchChanged(String query) — filter by displayName/email
// State: _users, _filtered, _isLoading
// Search controller (opcional — manter se quiser, ou remover por ser deferred)
```

IMPORTANTE: CONTEXT.md "Deferred Ideas" inclui "Filtros/busca aba Reservas" — mas NOT explícito para Usuários.
No entanto, a search TextField atual usa `OutlineInputBorder` e `primaryGreen` etc.
Por segurança: MANTER a busca funcional (não remover), mas atualizar para AppTheme.
O campo de busca existente NÃO é parte do escopo do redesign (não mencionado em ADMN-20/21).
Manter busca como está (apenas remover referências a primaryGreen/hardcoded colors se houver).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar user_detail_sheet.dart — bottom sheet Arena</name>
  <read_first>
    - lib/core/models/user_model.dart (campos disponíveis — IMPORTANTE: sem photoUrl, sem bookingCount)
    - lib/core/widgets/sport_btn.dart (SportBtn.filled para ações)
    - lib/core/theme/app_theme.dart (tokens AppTheme)
    - lib/features/auth/cubit/auth_cubit.dart (promoteUser — verificar se demoteUser existe)
    - lib/features/booking/ui/client_booking_detail_sheet.dart (padrão bottom sheet DraggableScrollableSheet)
  </read_first>
  <files>
    lib/features/admin/ui/user_detail_sheet.dart
    test/features/admin/ui/user_detail_sheet_test.dart
  </files>
  <behavior>
    - Test ADMN-20a: UserDetailSheet com user.isAdmin=true renderiza CircleAvatar com backgroundColor AppTheme.orange
    - Test ADMN-20b: UserDetailSheet com user.isAdmin=false renderiza CircleAvatar com backgroundColor AppTheme.ink
    - Test ADMN-20c: CircleAvatar renderiza Text com inicial do displayName (displayName[0].toUpperCase())
    - Test ADMN-20d: Avatar texto usa AppTheme.display(size: 32, color: AppTheme.paper)
    - Test ADMN-20e: UserDetailSheet com user.isAdmin=false renderiza Text 'PROMOVER A ADMIN'
    - Test ADMN-20f: UserDetailSheet com user.isAdmin=true renderiza Text 'REMOVER ADMIN'
    - Test ADMN-20g: Drag handle renderizado: Container com width=32, height=4, color=AppTheme.lineHair
  </behavior>
  <action>
    PASSO 1 — Criar test/features/admin/ui/user_detail_sheet_test.dart com os 7 testes. Usar flutter_test + mocktail para AuthCubit. Rodar — todos devem FALHAR (RED).

    PASSO 2 — Criar lib/features/admin/ui/user_detail_sheet.dart:

    ```dart
    import 'package:cloud_firestore/cloud_firestore.dart';
    import 'package:flutter/material.dart';
    import 'package:flutter_bloc/flutter_bloc.dart';
    import 'package:vida_ativa/core/models/user_model.dart';
    import 'package:vida_ativa/core/theme/app_theme.dart';
    import 'package:vida_ativa/core/utils/snack_helper.dart';
    import 'package:vida_ativa/core/widgets/sport_btn.dart';
    import 'package:vida_ativa/features/auth/cubit/auth_cubit.dart';

    class UserDetailSheet extends StatefulWidget {
      final UserModel user;

      const UserDetailSheet({super.key, required this.user});

      @override
      State<UserDetailSheet> createState() => _UserDetailSheetState();
    }

    class _UserDetailSheetState extends State<UserDetailSheet> {
      bool _isSubmitting = false;

      @override
      Widget build(BuildContext context) {
        final user = widget.user;
        final isAdmin = user.isAdmin;
        final initial = user.displayName.isNotEmpty
            ? user.displayName[0].toUpperCase()
            : '?';
        final avatarBg = isAdmin ? AppTheme.orange : AppTheme.ink;
        final actionLabel = isAdmin ? 'REMOVER ADMIN' : 'PROMOVER A ADMIN';

        return DraggableScrollableSheet(
          expand: false,
          initialChildSize: 0.45,
          minChildSize: 0.35,
          maxChildSize: 0.65,
          builder: (context, controller) => SingleChildScrollView(
            controller: controller,
            child: SafeArea(
              child: Padding(
                padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.center,
                  children: [
                    // Drag handle
                    Center(
                      child: Container(
                        width: 32,
                        height: 4,
                        decoration: BoxDecoration(
                          color: AppTheme.lineHair,
                          borderRadius: BorderRadius.circular(2),
                        ),
                      ),
                    ),
                    const SizedBox(height: 20),
                    // Avatar grande 64×64 (radius 32)
                    CircleAvatar(
                      radius: 32,
                      backgroundColor: avatarBg,
                      child: Text(
                        initial,
                        style: AppTheme.display(size: 32, color: AppTheme.paper),
                      ),
                    ),
                    const SizedBox(height: 16),
                    Text(user.displayName, style: AppTheme.ui(size: 14, weight: FontWeight.w700), textAlign: TextAlign.center),
                    const SizedBox(height: 4),
                    Text(user.email, style: AppTheme.mono(size: 11), textAlign: TextAlign.center),
                    const SizedBox(height: 24),
                    // Ação
                    SportBtn.filled(
                      actionLabel,
                      onPressed: _isSubmitting ? null : () => _handleAction(context, user),
                    ),
                  ],
                ),
              ),
            ),
          ),
        );
      }

      Future<void> _handleAction(BuildContext context, UserModel user) async {
        // Confirmação antes da ação
        final confirmMsg = user.isAdmin
            ? 'Deseja remover privilégios de admin? Ação não pode ser desfeita.'
            : 'Deseja promover ${user.displayName} a admin?';
        final confirmed = await showDialog<bool>(
          context: context,
          builder: (ctx) => AlertDialog(
            title: Text(user.isAdmin ? 'Remover admin' : 'Promover a admin'),
            content: Text(confirmMsg),
            actions: [
              TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Cancelar')),
              FilledButton(onPressed: () => Navigator.pop(ctx, true), child: const Text('Confirmar')),
            ],
          ),
        );
        if (confirmed != true || !context.mounted) return;

        setState(() => _isSubmitting = true);
        try {
          if (user.isAdmin) {
            // demoteUser não existe em AuthCubit — chamar Firestore diretamente
            await FirebaseFirestore.instance
                .collection('users')
                .doc(user.uid)
                .update({'role': 'client'});
          } else {
            await context.read<AuthCubit>().promoteUser(user.uid);
          }
          if (context.mounted) {
            SnackHelper.success(
              context,
              user.isAdmin
                  ? '${user.displayName} não é mais admin'
                  : '${user.displayName} agora é admin',
            );
            Navigator.pop(context);
          }
        } catch (e) {
          if (context.mounted) {
            SnackHelper.error(context, 'Erro ao atualizar usuário');
          }
        } finally {
          if (mounted) setState(() => _isSubmitting = false);
        }
      }
    }
    ```

    NOTAS IMPORTANTES:
    - UserModel NÃO tem photoUrl nem bookingCount — não renderizar esses campos
    - Não usar `authCubit` como parâmetro do widget — usar `context.read<AuthCubit>()` internamente
    - Verificar se SnackHelper.error() existe; se não, usar ScaffoldMessenger.of(context).showSnackBar diretamente

    PASSO 3 — Rodar testes: todos devem PASSAR (GREEN).
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter test test/features/admin/ui/user_detail_sheet_test.dart --reporter compact</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DraggableScrollableSheet" lib/features/admin/ui/user_detail_sheet.dart` retorna 1 linha
    - `grep -n "CircleAvatar(radius: 32" lib/features/admin/ui/user_detail_sheet.dart` retorna 1 linha
    - `grep -n "AppTheme.orange\|AppTheme.ink" lib/features/admin/ui/user_detail_sheet.dart` retorna pelo menos 2 linhas
    - `grep -n "AppTheme.display(size: 32" lib/features/admin/ui/user_detail_sheet.dart` retorna 1 linha
    - `grep -n "PROMOVER A ADMIN\|REMOVER ADMIN" lib/features/admin/ui/user_detail_sheet.dart` retorna 2 linhas
    - `grep -n "Container(.*width: 32.*height: 4\|width: 32,.*height: 4" lib/features/admin/ui/user_detail_sheet.dart` retorna 1 linha (drag handle)
    - `grep -n "gradiente\|primaryGreen\|Color(0x" lib/features/admin/ui/user_detail_sheet.dart` retorna vazio
    - `flutter analyze lib/features/admin/ui/user_detail_sheet.dart` retorna 0 erros
    - Todos os 7 testes em test/features/admin/ui/user_detail_sheet_test.dart passam
  </acceptance_criteria>
  <done>UserDetailSheet criado com DraggableScrollableSheet, avatar CircleAvatar 64px, drag handle, SportBtn.filled para promover/remover, confirmação AlertDialog, sem photoUrl nem bookingCount</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever users_management_tab.dart — hairline rows + tap → UserDetailSheet</name>
  <read_first>
    - lib/features/admin/ui/users_management_tab.dart (arquivo atual — preservar lógica _loadUsers, _onSearchChanged)
    - lib/features/admin/ui/user_detail_sheet.dart (novo sheet — criado na Task 1)
    - lib/core/models/user_model.dart (campos disponíveis)
    - lib/core/theme/app_theme.dart (tokens AppTheme)
    - lib/features/booking/ui/hairline_booking_row.dart (padrão DecoratedBox hairline)
  </read_first>
  <files>
    lib/features/admin/ui/users_management_tab.dart
    test/features/admin/ui/user_row_test.dart
  </files>
  <behavior>
    - Test ADMN-21a: _UserRow com user.isAdmin=true renderiza CircleAvatar com backgroundColor AppTheme.orange
    - Test ADMN-21b: _UserRow com user.isAdmin=false renderiza CircleAvatar com backgroundColor AppTheme.ink
    - Test ADMN-21c: _UserRow renderiza displayName em Manrope 14px bold
    - Test ADMN-21d: _UserRow renderiza email em JBM 11px (AppTheme.mono(size: 11))
    - Test ADMN-21e: _UserRow com index > 0 tem DecoratedBox com Border top (AppTheme.lineHair, width 0.5)
    - Test ADMN-21f: _UserRow não usa ListTile nem gradiente
    - Test ADMN-21g: _UserRow renderiza Icon(Icons.chevron_right)
  </behavior>
  <action>
    PASSO 1 — Criar test/features/admin/ui/user_row_test.dart com os 7 testes. Rodar — todos devem FALHAR (RED).

    PASSO 2 — Criar widget `_UserRow` (privado, no mesmo arquivo users_management_tab.dart):
    ```dart
    class _UserRow extends StatelessWidget {
      final UserModel user;
      final int index;
      final VoidCallback onTap;

      const _UserRow({required this.user, required this.index, required this.onTap});

      @override
      Widget build(BuildContext context) {
        final initial = user.displayName.isNotEmpty
            ? user.displayName[0].toUpperCase()
            : '?';
        final avatarBg = user.isAdmin ? AppTheme.orange : AppTheme.ink;

        return DecoratedBox(
          decoration: BoxDecoration(
            border: index == 0
                ? null
                : const Border(top: BorderSide(color: AppTheme.lineHair, width: 0.5)),
          ),
          child: InkWell(
            onTap: onTap,
            child: Padding(
              padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
              child: Row(
                crossAxisAlignment: CrossAxisAlignment.center,
                children: [
                  CircleAvatar(
                    radius: 20,
                    backgroundColor: avatarBg,
                    child: Text(
                      initial,
                      style: AppTheme.display(size: 20, color: AppTheme.paper),
                    ),
                  ),
                  const SizedBox(width: 16),
                  Expanded(
                    child: Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        Text(user.displayName, style: AppTheme.ui(size: 14, weight: FontWeight.w600)),
                        Text(user.email, style: AppTheme.mono(size: 11)),
                        if (user.isAdmin)
                          Text('Admin', style: AppTheme.mono(size: 11, color: AppTheme.orange)),
                      ],
                    ),
                  ),
                  Icon(Icons.chevron_right, color: AppTheme.concrete, size: 20),
                ],
              ),
            ),
          ),
        );
      }
    }
    ```

    NOTAS:
    - Avatar inicial usa Anton 20px (não 32px) — 20px corresponde ao radius 20 do CircleAvatar row (40×40 total)
    - Sem "N reservas" porque UserModel não tem bookingCount — mostrar 'Admin' em mono laranja para admins, nada para users comuns
    - Sem ListTile, sem gradiente, sem FilledButton inline

    PASSO 3 — Reescrever `_UsersManagementTabState.build()`:
    - Manter `_loadUsers()` (Firestore query ordenada) — sem mudança
    - Manter `_onSearchChanged()` — sem mudança
    - REMOVER TextField de busca? NÃO — manter (filtro de usuários é funcional; deferred é para aba Reservas, não Usuários)
    - Atualizar TextField: remover `border: OutlineInputBorder()` se usar a default do AppTheme (UnderlineInputBorder)
    - Substituir ListView.builder:
      ```dart
      // ANTES: ListTile com leading: CircleAvatar default, trailing: Chip/FilledButton
      // DEPOIS:
      ListView.builder(
        itemCount: _filtered.length,
        itemBuilder: (context, index) {
          final user = _filtered[index];
          return _UserRow(
            user: user,
            index: index,
            onTap: () => showModalBottomSheet(
              context: context,
              isScrollControlled: true,
              builder: (_) => BlocProvider.value(
                value: context.read<AuthCubit>(),
                child: UserDetailSheet(user: user),
              ),
            ).then((_) => _loadUsers()), // reload after sheet closes (role may have changed)
          );
        },
      )
      ```
    - Remover `_confirmPromote()` — ação movida para UserDetailSheet
    - Adicionar import de `user_detail_sheet.dart`
    - Remover import de `snack_helper` se não mais usado em tab (movido para sheet)

    PASSO 4 — Rodar testes: todos devem PASSAR (GREEN).
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter test test/features/admin/ui/user_row_test.dart --reporter compact</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ListTile\|primaryGreen\|Color(0x\|gradiente" lib/features/admin/ui/users_management_tab.dart` retorna vazio
    - `grep -n "UserDetailSheet" lib/features/admin/ui/users_management_tab.dart` retorna pelo menos 1 linha
    - `grep -n "_UserRow\|UserRow" lib/features/admin/ui/users_management_tab.dart` retorna pelo menos 1 linha
    - `grep -n "AppTheme.orange\|AppTheme.ink" lib/features/admin/ui/users_management_tab.dart` retorna pelo menos 2 linhas
    - `grep -n "BorderSide(color: AppTheme.lineHair, width: 0.5)" lib/features/admin/ui/users_management_tab.dart` retorna pelo menos 1 linha
    - `grep -n "CircleAvatar(radius: 20" lib/features/admin/ui/users_management_tab.dart` retorna pelo menos 1 linha
    - `flutter analyze lib/features/admin/ui/users_management_tab.dart` retorna 0 erros
    - Todos os 7 testes em test/features/admin/ui/user_row_test.dart passam
  </acceptance_criteria>
  <done>users_management_tab.dart redesenhado com _UserRow hairline (CircleAvatar 40px, Manrope bold, mono email), tap abre UserDetailSheet, ações de promover/remover consolidadas na sheet; testes passam</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserDetailSheet → Firestore | Demote escreve role:client diretamente via Firestore SDK — requer auth token válido |
| UserDetailSheet → AuthCubit | Promote chama promoteUser(uid) que escreve role:admin — requer admin logado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Elevation of Privilege | UserDetailSheet._handleAction (demote/promote) | accept | Firestore security rules validam `request.auth.token.role == 'admin'` server-side antes de permitir update no documento users/{uid}; UI é apenas reflexo |
| T-27-03-02 | Repudiation | promoteUser / demoteUser | accept | AlertDialog de confirmação com texto explícito "Ação não pode ser desfeita" antes de qualquer modificação de role |
| T-27-03-03 | Information Disclosure | UsersManagementTab lista emails | accept | Tab visível apenas para admin (AdminScreen guarda com role check via AuthCubit); emails são dados operacionais admin-only |
</threat_model>

<verification>
1. `flutter analyze lib/features/admin/ui/users_management_tab.dart` — 0 erros
2. `flutter analyze lib/features/admin/ui/user_detail_sheet.dart` — 0 erros
3. `flutter test test/features/admin/ui/user_detail_sheet_test.dart` — todos passam
4. `flutter test test/features/admin/ui/user_row_test.dart` — todos passam
5. Grep confirma ausência de ListTile, primaryGreen, gradiente em ambos os arquivos
6. Grep confirma CircleAvatar 20px (row) e 32px (sheet), AppTheme tokens
</verification>

<success_criteria>
- users_management_tab.dart usa _UserRow hairline (sem ListTile, sem gradiente)
- Avatar circular: laranja para admin, ink para usuário comum, inicial Anton
- Tap em row abre UserDetailSheet via showModalBottomSheet
- UserDetailSheet exibe drag handle + avatar 64px + nome + email + SportBtn.filled
- Ações promover/remover têm AlertDialog de confirmação e recarregam lista ao fechar
- flutter analyze 0 erros, testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/27-admin-slots-reservas-usu-rios/27-03-SUMMARY.md`
</output>
