---
phase: 27-admin-slots-reservas-usu-rios
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/admin/ui/admin_booking_row.dart
  - lib/features/admin/ui/booking_management_tab.dart
  - lib/features/admin/ui/admin_booking_card.dart
autonomous: true
requirements:
  - ADMN-18
  - ADMN-19
must_haves:
  truths:
    - "AdminBookingRow exibe hora em Anton 36px (cor ink)"
    - "AdminBookingRow exibe nome do cliente em Manrope 14px bold"
    - "AdminBookingRow exibe participantes em Manrope 14px concrete"
    - "AdminBookingRow exibe status em JetBrains Mono 11px uppercase colorido"
    - "Pills CONFIRMAR/RECUSAR visíveis apenas quando status == 'pending'"
    - "Hairline top border 0.5px lineHair entre rows (suprimida no index 0)"
    - "AdminBookingCard.dart deletado — nenhuma referência remanescente"
    - "booking_management_tab.dart usa AdminBookingRow, não AdminBookingCard"
  artifacts:
    - path: "lib/features/admin/ui/admin_booking_row.dart"
      provides: "AdminBookingRow widget hairline"
      exports: ["AdminBookingRow"]
      contains: "_statusColor"
    - path: "lib/features/admin/ui/booking_management_tab.dart"
      provides: "BookingManagementTab reescrito usando AdminBookingRow"
    - path: "test/features/admin/ui/admin_booking_row_test.dart"
      provides: "Widget tests para ADMN-18 e ADMN-19"
  key_links:
    - from: "booking_management_tab.dart"
      to: "admin_booking_row.dart"
      via: "import + ListView.builder itemBuilder"
    - from: "AdminBookingRow"
      to: "AdminBookingCubit.confirmBooking / rejectBooking"
      via: "onConfirm / onReject callbacks"
---

<objective>
Criar AdminBookingRow (novo widget hairline), substituir AdminBookingCard por ele em BookingManagementTab, e deletar admin_booking_card.dart (dead code).

Purpose: Implementa ADMN-18 (booking rows Anton 36px + Manrope + mono status) e ADMN-19 (pills Confirmar/Recusar só pending, outline sem fundo), eliminando o Card antigo com sombra e cores hardcoded.
Output: admin_booking_row.dart (novo), booking_management_tab.dart (atualizado), admin_booking_card.dart (deletado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-CONTEXT.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-UI-SPEC.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-RESEARCH.md

<interfaces>
<!-- Interfaces que o executor precisa. Extraídas do codebase. -->

From lib/core/theme/app_theme.dart:
```dart
static const Color orange    = Color(0xFFFF4D17);
static const Color orangeDk  = Color(0xFFC9300A);
static const Color ink       = Color(0xFF0E0E0C);
static const Color concrete  = Color(0xFF6B6B66);
static const Color lineHair  = Color(0xFFEAE3CE);
static const Color court     = Color(0xFF1B5E2A);
static const Color sun       = Color(0xFFFFB800);
static TextStyle display({double size = 32, Color? color})  // Anton, height: 0.92
static TextStyle ui({double size = 14, FontWeight weight, Color? color})  // Manrope
static TextStyle mono({double size = 11, FontWeight weight, Color? color})  // JetBrains Mono
```

From lib/core/widgets/sport_btn.dart:
```dart
SportBtn.outlined(String label, {required VoidCallback? onPressed})
// OutlinedButton, ink border 1.5px, StadiumBorder, minimumSize(double.infinity, 52)
```

From lib/core/models/booking_model.dart:
```dart
booking.id              // String
booking.startTime       // String? "HH:mm"
booking.userDisplayName // String?
booking.participants    // String? (free-text list)
booking.status          // String: 'pending' | 'pending_payment' | 'confirmed' | 'expired' | 'rejected'
booking.paymentMethod   // String? 'pix' | 'on_arrival'
booking.sport           // String?
bool get isPending => status == 'pending'
```

From lib/features/admin/cubit/admin_booking_cubit.dart:
```dart
cubit.confirmBooking(String bookingId)
cubit.rejectBooking(String bookingId)
cubit.selectDate(DateTime date)
cubit.setConfirmationMode(String mode)
```

From lib/features/admin/cubit/admin_booking_state.dart:
```dart
class AdminBookingLoaded extends AdminBookingState {
  final List<BookingModel> bookings;
  final DateTime selectedDate;
  final String confirmationMode; // 'automatic' | 'manual'
}
```

Status color map (from existing admin_booking_card.dart — replicate exactly):
```dart
Color _statusColor(String status, String? paymentMethod) {
  return switch ((status, paymentMethod)) {
    ('pending', _) => AppTheme.orange,
    ('pending_payment', _) => AppTheme.sun,
    ('confirmed', 'pix') => AppTheme.court,
    ('confirmed', 'on_arrival') => AppTheme.ink,
    ('confirmed', _) => AppTheme.court,
    ('expired', _) => AppTheme.concrete,
    ('rejected', _) => AppTheme.orangeDk,
    _ => AppTheme.concrete,
  };
}
```

Status label map (uppercase mono — new in AdminBookingRow):
```dart
String _statusLabel(String status, String? paymentMethod) {
  return switch ((status, paymentMethod)) {
    ('pending', _) => 'AGUARDANDO',
    ('pending_payment', _) => 'AGUARDANDO PIX',
    ('confirmed', 'pix') => 'PIX PAGO',
    ('confirmed', 'on_arrival') => 'PAGAR NA HORA',
    ('confirmed', _) => 'CONFIRMADO',
    ('expired', _) => 'EXPIRADO',
    ('rejected', _) => 'RECUSADO',
    _ => status.toUpperCase(),
  };
}
```

Hairline row pattern (from lib/features/booking/ui/hairline_booking_row.dart):
```dart
DecoratedBox(
  decoration: BoxDecoration(
    border: index == 0 ? null : const Border(
      top: BorderSide(color: AppTheme.lineHair, width: 0.5),
    ),
  ),
  child: Padding(
    padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
    child: Row(...),
  ),
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar admin_booking_row.dart + deletar admin_booking_card.dart</name>
  <read_first>
    - lib/features/admin/ui/admin_booking_card.dart (entender status colors/labels a migrar; será deletado)
    - lib/features/booking/ui/hairline_booking_row.dart (padrão hairline DecoratedBox a replicar)
    - lib/core/widgets/sport_btn.dart (assinatura SportBtn.outlined para pills)
    - lib/core/theme/app_theme.dart (tokens AppTheme)
    - lib/core/models/booking_model.dart (campos do BookingModel)
  </read_first>
  <files>
    lib/features/admin/ui/admin_booking_row.dart
    lib/features/admin/ui/admin_booking_card.dart
    test/features/admin/ui/admin_booking_row_test.dart
  </files>
  <behavior>
    - Test ADMN-18a: AdminBookingRow renderiza Text com startTime em estilo Anton 36px (AppTheme.display(size: 36))
    - Test ADMN-18b: AdminBookingRow renderiza userDisplayName em Manrope 14px bold (AppTheme.ui(size: 14, weight: FontWeight.w600))
    - Test ADMN-18c: AdminBookingRow renderiza status 'pending' como texto 'AGUARDANDO' em AppTheme.mono com cor AppTheme.orange
    - Test ADMN-18d: AdminBookingRow renderiza status 'confirmed'/'pix' como texto 'PIX PAGO' em cor AppTheme.court
    - Test ADMN-18e: AdminBookingRow com index > 0 tem DecoratedBox com Border top (AppTheme.lineHair, width 0.5)
    - Test ADMN-19a: AdminBookingRow com status 'pending' renderiza texto 'CONFIRMAR' e 'RECUSAR'
    - Test ADMN-19b: AdminBookingRow com status 'confirmed' NÃO renderiza 'CONFIRMAR' nem 'RECUSAR'
    - Test ADMN-19c: Tap em 'CONFIRMAR' chama onConfirm callback
    - Test ADMN-19d: Tap em 'RECUSAR' chama onReject callback
  </behavior>
  <action>
    PASSO 1 — Criar test/features/admin/ui/admin_booking_row_test.dart com os 9 testes listados em <behavior>. Usar mocktail para callbacks. Rodar — todos devem FALHAR (RED).

    PASSO 2 — Criar lib/features/admin/ui/admin_booking_row.dart:

    ```dart
    import 'package:flutter/material.dart';
    import 'package:vida_ativa/core/models/booking_model.dart';
    import 'package:vida_ativa/core/theme/app_theme.dart';
    import 'package:vida_ativa/core/widgets/sport_btn.dart';

    class AdminBookingRow extends StatelessWidget {
      final BookingModel booking;
      final int index;
      final VoidCallback? onConfirm;
      final VoidCallback? onReject;

      const AdminBookingRow({
        super.key,
        required this.booking,
        required this.index,
        this.onConfirm,
        this.onReject,
      });

      Color _statusColor(String status, String? paymentMethod) { /* switch pattern acima */ }
      String _statusLabel(String status, String? paymentMethod) { /* switch pattern acima */ }

      @override
      Widget build(BuildContext context) {
        final statusColor = _statusColor(booking.status, booking.paymentMethod);
        final statusLabel = _statusLabel(booking.status, booking.paymentMethod);
        final isPending = booking.isPending;
        final clientName = booking.userDisplayName ?? 'Cliente';
        final timeDisplay = booking.startTime ?? '';

        return DecoratedBox(
          decoration: BoxDecoration(
            border: index == 0
                ? null
                : const Border(top: BorderSide(color: AppTheme.lineHair, width: 0.5)),
          ),
          child: Padding(
            padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
            child: Row(
              crossAxisAlignment: CrossAxisAlignment.center,
              children: [
                Text(timeDisplay, style: AppTheme.display(size: 36)),
                const SizedBox(width: 16),
                Expanded(
                  child: Column(
                    crossAxisAlignment: CrossAxisAlignment.start,
                    children: [
                      Text(clientName, style: AppTheme.ui(size: 14, weight: FontWeight.w600)),
                      if (booking.participants != null && booking.participants!.isNotEmpty)
                        Text(booking.participants!, style: AppTheme.ui(size: 14, color: AppTheme.concrete), overflow: TextOverflow.ellipsis, maxLines: 1),
                      const SizedBox(height: 4),
                      Text(statusLabel, style: AppTheme.mono(size: 11, color: statusColor)),
                    ],
                  ),
                ),
                if (isPending) ...[
                  const SizedBox(width: 8),
                  Column(
                    mainAxisSize: MainAxisSize.min,
                    children: [
                      SizedBox(
                        height: 36,
                        child: OutlinedButton(
                          onPressed: onConfirm,
                          style: OutlinedButton.styleFrom(
                            foregroundColor: AppTheme.court,
                            side: const BorderSide(color: AppTheme.court, width: 1.5),
                            padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
                            shape: const StadiumBorder(),
                            textStyle: AppTheme.mono(size: 10),
                          ),
                          child: const Text('CONFIRMAR'),
                        ),
                      ),
                      const SizedBox(height: 4),
                      SizedBox(
                        height: 36,
                        child: OutlinedButton(
                          onPressed: onReject,
                          style: OutlinedButton.styleFrom(
                            foregroundColor: AppTheme.orangeDk,
                            side: const BorderSide(color: AppTheme.orangeDk, width: 1.5),
                            padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
                            shape: const StadiumBorder(),
                            textStyle: AppTheme.mono(size: 10),
                          ),
                          child: const Text('RECUSAR'),
                        ),
                      ),
                    ],
                  ),
                ],
              ],
            ),
          ),
        );
      }
    }
    ```

    NOTAS SOBRE OS PILLS:
    - Não usar SportBtn.outlined pois SportBtn tem minimumSize(double.infinity, 52) — muito alto para pills inline em row
    - Usar OutlinedButton diretamente com height 36px e padding compacto
    - CONFIRMAR: cor court (verde — confirmação positiva), RECUSAR: cor orangeDk (vermelho — ação destrutiva)
    - Sem fundo colorido em nenhum dos dois (outline only — cumpre D-10 "outline pill sem fundo colorido")

    PASSO 3 — Deletar lib/features/admin/ui/admin_booking_card.dart:
    - Usar `dart` de delete de arquivo ou apenas esvaziar o arquivo — NÃO. Deletar o arquivo fisicamente.
    - Comando: `rm lib/features/admin/ui/admin_booking_card.dart` (ou equivalente no OS)

    PASSO 4 — Rodar testes: todos devem PASSAR (GREEN).
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter test test/features/admin/ui/admin_booking_row_test.dart --reporter compact</automated>
  </verify>
  <acceptance_criteria>
    - `ls lib/features/admin/ui/admin_booking_card.dart` retorna "No such file" (arquivo deletado)
    - `grep -rn "admin_booking_card" lib/` retorna vazio (nenhuma referência remanescente)
    - `grep -n "AppTheme.display(size: 36" lib/features/admin/ui/admin_booking_row.dart` retorna 1 linha
    - `grep -n "AppTheme.lineHair" lib/features/admin/ui/admin_booking_row.dart` retorna pelo menos 1 linha
    - `grep -n "isPending\|booking.status == .pending" lib/features/admin/ui/admin_booking_row.dart` retorna pelo menos 1 linha
    - `grep -n "Card\|IntrinsicHeight\|Color(0x" lib/features/admin/ui/admin_booking_row.dart` retorna vazio
    - `flutter analyze lib/features/admin/ui/admin_booking_row.dart` retorna 0 erros
    - Todos os 9 testes em test/features/admin/ui/admin_booking_row_test.dart passam
  </acceptance_criteria>
  <done>AdminBookingRow criado com hairline row, hora Anton 36px, nome Manrope bold, status mono colorido, pills Confirmar/Recusar apenas para pending; admin_booking_card.dart deletado; testes passam</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever booking_management_tab.dart usando AdminBookingRow</name>
  <read_first>
    - lib/features/admin/ui/booking_management_tab.dart (arquivo atual — ver estrutura com AdminBookingCard)
    - lib/features/admin/ui/admin_booking_row.dart (novo widget — criado na Task 1)
    - lib/features/admin/cubit/admin_booking_cubit.dart (métodos confirmBooking, rejectBooking, selectDate)
    - lib/features/admin/cubit/admin_booking_state.dart (AdminBookingLoaded fields)
    - lib/features/admin/ui/admin_booking_detail_sheet.dart (usado no onTap de cada row)
  </read_first>
  <files>
    lib/features/admin/ui/booking_management_tab.dart
  </files>
  <action>
    Substituir AdminBookingCard por AdminBookingRow na lista de bookings. Manter toda a lógica de seleção de data, cubit, modo de confirmação.

    MUDANÇAS ESPECÍFICAS em booking_management_tab.dart:

    1. Remover import de `admin_booking_card.dart` (arquivo deletado na Task 1)
    2. Adicionar import de `admin_booking_row.dart`

    3. Em `_BookingManagementView.build()`:
       - Manter o date selector row (← / texto data / →) — preservar funcionalidade
       - Manter o SwitchListTile de confirmação automática — preservar funcionalidade
       - Substituir o ListView.builder:
         ```dart
         // ANTES:
         GestureDetector(
           onTap: () => _showBookingDetailSheet(context, booking),
           child: AdminBookingCard(booking: booking, bookingCubit: cubit),
         )

         // DEPOIS:
         AdminBookingRow(
           booking: booking,
           index: index,
           onConfirm: () async {
             final confirmed = await showDialog<bool>(
               context: context,
               builder: (ctx) => AlertDialog(
                 title: const Text('Confirmar reserva?'),
                 content: const Text('Deseja confirmar esta reserva? Ação não pode ser desfeita.'),
                 actions: [
                   TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Cancelar')),
                   FilledButton(onPressed: () => Navigator.pop(ctx, true), child: const Text('Confirmar')),
                 ],
               ),
             );
             if (confirmed == true && context.mounted) {
               await cubit.confirmBooking(booking.id);
             }
           },
           onReject: () async {
             final confirmed = await showDialog<bool>(
               context: context,
               builder: (ctx) => AlertDialog(
                 title: const Text('Recusar reserva?'),
                 content: const Text('Deseja recusar esta reserva? Ação não pode ser desfeita.'),
                 actions: [
                   TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Cancelar')),
                   FilledButton(onPressed: () => Navigator.pop(ctx, true), child: const Text('Recusar')),
                 ],
               ),
             );
             if (confirmed == true && context.mounted) {
               await cubit.rejectBooking(booking.id);
             }
           },
         )
         ```

    4. Remover o GestureDetector wrapper externo — AdminBookingRow não tem tap geral para sheet.
       Se o admin quiser ver detalhe, pode tap no row excluindo a área dos pills. Para simplicidade desta fase:
       envolver AdminBookingRow com InkWell(onTap: () => _showBookingDetailSheet(context, booking))
       e os OutlinedButton nos pills terão seus próprios onPressed — GestureDetector absorve o tap nos botões corretamente.
       Na prática: usar GestureDetector(behavior: HitTestBehavior.opaque, onTap: ...) fora, e botões internos absorvem.

    5. Remover import de `app_spacing` se não usado mais em mais nenhum lugar

    6. Manter todos os estados de loading/error do BlocBuilder intactos.

    NOTA: Não há test específico para booking_management_tab nesta fase — o test de AdminBookingRow já cobre o widget. `flutter analyze` é a verificação desta task.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ui/booking_management_tab.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "admin_booking_card\|AdminBookingCard" lib/features/admin/ui/booking_management_tab.dart` retorna vazio
    - `grep -n "AdminBookingRow" lib/features/admin/ui/booking_management_tab.dart` retorna pelo menos 1 linha
    - `flutter analyze lib/features/admin/ui/booking_management_tab.dart` retorna 0 erros
    - `flutter analyze lib/features/admin/ui/` retorna 0 erros (nenhuma referência quebrada a admin_booking_card)
  </acceptance_criteria>
  <done>booking_management_tab.dart usa AdminBookingRow; nenhuma referência a AdminBookingCard; flutter analyze 0 erros</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → AdminBookingCubit | confirmBooking/rejectBooking escrevem no Firestore — requer role admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Spoofing | AdminBookingRow onConfirm/onReject | accept | AuthCubit garante admin view mode antes de renderizar tab; Firestore rules validam role server-side |
| T-27-02-02 | Repudiation | confirmBooking / rejectBooking | accept | Ações irreversíveis têm AlertDialog de confirmação inline com texto "Ação não pode ser desfeita." |
</threat_model>

<verification>
1. `flutter analyze lib/features/admin/ui/` — 0 erros, sem referências ao arquivo deletado
2. `flutter test test/features/admin/ui/admin_booking_row_test.dart` — todos passam
3. Grep confirma ausência de AdminBookingCard em todo lib/
4. Grep confirma AdminBookingRow em booking_management_tab.dart
</verification>

<success_criteria>
- admin_booking_card.dart deletado sem referências remanescentes
- AdminBookingRow exibe hora Anton 36px + nome Manrope bold + status mono colorido
- Pills CONFIRMAR/RECUSAR visíveis apenas para status pending
- Pills usam OutlinedButton court/orangeDk sem fundo colorido
- booking_management_tab.dart usa AdminBookingRow com callbacks de confirm/reject
- flutter analyze 0 erros, testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/27-admin-slots-reservas-usu-rios/27-02-SUMMARY.md`
</output>
