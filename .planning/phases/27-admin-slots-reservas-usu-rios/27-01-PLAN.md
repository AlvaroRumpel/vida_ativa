---
phase: 27-admin-slots-reservas-usu-rios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/admin/ui/slot_management_tab.dart
autonomous: true
requirements:
  - ADMN-16
  - ADMN-17
must_haves:
  truths:
    - "Aba Slots exibe slots como rows hairline sem Card colorido"
    - "Hora do slot usa Anton 32px — laranja se reservado, ink se vazio"
    - "Slot vazio mostra preço + switch ativo/inativo à direita"
    - "Slot reservado mostra nome do reservante em Manrope + esporte"
    - "Tap em slot vazio abre SlotFormSheet existente"
    - "Tap em slot reservado abre AdminBookingDetailSheet existente"
    - "Day selector usa underline laranja 2px + botões ← → para navegar semanas"
    - "Nenhum ChoiceChip, nenhum backgroundColor hardcoded, nenhum DayView de calendar_view"
  artifacts:
    - path: "lib/features/admin/ui/slot_management_tab.dart"
      provides: "SlotManagementTab reescrito com hairline rows e AdminDaySelector"
      contains: "AdminDaySelector"
    - path: "test/features/admin/ui/slot_management_tab_test.dart"
      provides: "Widget tests para ADMN-16 e ADMN-17"
  key_links:
    - from: "slot_management_tab.dart"
      to: "AdminSlotCubit"
      via: "context.read<AdminSlotCubit>()"
    - from: "_SlotRow"
      to: "SlotFormSheet / AdminBookingDetailSheet"
      via: "InkWell.onTap → showModalBottomSheet"
    - from: "AdminDaySelector"
      to: "AdminSlotCubit.loadSlotsForWeek"
      via: "widget.onDateChanged callback"
---

<objective>
Redesenhar slot_management_tab.dart com identidade Arena Esportivo: rows hairline sem Card, hora Anton 32px (laranja se reservado, ink se vazio), switch AppTheme-themed, e day selector underline laranja + ← →.

Purpose: Implementa ADMN-16 (slot rows hairline) e ADMN-17 (day selector underline + navegação), substituindo DayView calendar_view + ChoiceChip por layout hairline simples.
Output: slot_management_tab.dart reescrito + test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-CONTEXT.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-UI-SPEC.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-RESEARCH.md

<interfaces>
<!-- Interfaces que o executor precisa. Extraídas do codebase. -->

From lib/core/theme/app_theme.dart:
```dart
static const Color orange    = Color(0xFFFF4D17);
static const Color ink       = Color(0xFF0E0E0C);
static const Color concrete  = Color(0xFF6B6B66);
static const Color lineHair  = Color(0xFFEAE3CE);
static const Color paper     = Color(0xFFFBF8F0);
static const Color court     = Color(0xFF1B5E2A);
static TextStyle display({double size = 32, Color? color, double? letterSpacing})
  // GoogleFonts.anton, height: 0.92
static TextStyle ui({double size = 14, FontWeight weight = FontWeight.w400, Color? color})
  // GoogleFonts.manrope
static TextStyle mono({double size = 11, FontWeight weight = FontWeight.w700, Color? color})
  // GoogleFonts.jetBrainsMono
```

From lib/features/admin/cubit/admin_slot_cubit.dart (verified usage pattern):
```dart
context.read<AdminSlotCubit>().loadSlotsForWeek(DateTime weekStart)
context.read<AdminSlotCubit>().setSlotActive(String slotId, bool value)
```

From lib/features/admin/cubit/admin_booking_cubit.dart (for opening detail sheet):
```dart
context.read<AdminBookingCubit>()
// used to pass to AdminBookingDetailSheet
```

From lib/core/models/slot_model.dart (verified fields used in existing code):
```dart
slot.id          // String
slot.date        // String "YYYY-MM-DD"
slot.startTime   // String "HH:mm"
slot.isActive    // bool
slot.price       // double?
slot.sport       // String? (optional)
```

From lib/features/admin/ui/slot_form_sheet.dart (existing, do not modify):
```dart
SlotFormSheet({SlotModel? existing, required AdminSlotCubit slotCubit, DateTime? initialDate})
```

From lib/features/admin/ui/admin_booking_detail_sheet.dart (existing, do not modify):
```dart
AdminBookingDetailSheet({required BookingModel booking, required AdminBookingCubit adminBookingCubit})
```

From lib/core/models/booking_model.dart (fields needed for booking lookup):
```dart
static String generateId(String slotId, String date)
booking.userDisplayName  // String?
booking.sport           // String?
```

Hairline row pattern (from lib/features/booking/ui/hairline_booking_row.dart):
```dart
DecoratedBox(
  decoration: BoxDecoration(
    border: index == 0 ? null : const Border(
      top: BorderSide(color: AppTheme.lineHair, width: 0.5),
    ),
  ),
  child: InkWell(
    onTap: ...,
    child: Padding(
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
      child: Row(...),
    ),
  ),
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescrever slot_management_tab.dart — hairline rows + AdminDaySelector</name>
  <read_first>
    - lib/features/admin/ui/slot_management_tab.dart (arquivo atual — ver toda a lógica existente)
    - lib/features/booking/ui/hairline_booking_row.dart (padrão hairline DecoratedBox)
    - lib/core/theme/app_theme.dart (tokens de cor e tipografia)
    - lib/core/models/slot_model.dart (campos do SlotModel)
    - lib/core/models/booking_model.dart (generateId para lookup de reserva)
    - lib/features/admin/ui/slot_form_sheet.dart (assinatura para tap em slot vazio)
    - lib/features/admin/ui/admin_booking_detail_sheet.dart (assinatura para tap em slot reservado)
  </read_first>
  <files>
    lib/features/admin/ui/slot_management_tab.dart
    test/features/admin/ui/slot_management_tab_test.dart
  </files>
  <behavior>
    - Test ADMN-16a: _SlotRow com slot vazio renderiza Text com estilo Anton 32px e cor AppTheme.ink
    - Test ADMN-16b: _SlotRow com slot reservado renderiza Text com estilo Anton 32px e cor AppTheme.orange
    - Test ADMN-16c: _SlotRow com slot vazio renderiza Switch
    - Test ADMN-16d: _SlotRow com slot reservado renderiza bookedByName em Text com AppTheme.ui(14)
    - Test ADMN-16e: _SlotRow não usa Card nem Container com backgroundColor não-null (exceto background paper)
    - Test ADMN-17a: AdminDaySelector renderiza 7 GestureDetector para os 7 dias da semana
    - Test ADMN-17b: AdminDaySelector renderiza botão ← (Icons.chevron_left) e → (Icons.chevron_right)
    - Test ADMN-17c: AdminDaySelector renderiza Container(width:20, height:2, color:AppTheme.orange) para o dia selecionado
  </behavior>
  <action>
    Reescrever slot_management_tab.dart completamente. Manter lógica BLoC/Firestore existente, substituir apenas o visual.

    ESTRUTURA NOVA:

    1. Criar widget `AdminDaySelector` (StatefulWidget, pode ser classe privada `_AdminDaySelector` no mesmo arquivo):
       - Props: `selectedDate: DateTime`, `onDateChanged: ValueChanged<DateTime>`
       - Estado interno: `_weekStart` (Monday da semana selecionada), calculado com `date.subtract(Duration(days: date.weekday - 1))`
       - Build: Row com IconButton(Icons.chevron_left) + Expanded(Row 7 dias) + IconButton(Icons.chevron_right)
       - Para cada dia: GestureDetector → Column([Text(abbr, AppTheme.mono(11)), Text(day, AppTheme.display(32)), if(selected) Container(width:20, height:2, color:AppTheme.orange)])
       - Abreviações: ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom']
       - Navegação ← : `_weekStart = _weekStart.subtract(Duration(days:7))` + chamar `widget.onDateChanged(_weekStart.add(Duration(days: weekday-1)))` para manter dia selecionado em nova semana
       - Navegação → : `_weekStart = _weekStart.add(Duration(days:7))` + idem
       - Padding horizontal da Row principal: 8px; espaçamento entre dias: `MainAxisAlignment.spaceEvenly`

    2. Criar widget `_SlotRow` (StatelessWidget privado, mesmo arquivo):
       - Props: `slot: SlotModel`, `isBooked: bool`, `bookedByName: String?`, `sport: String?`, `index: int`, `onTap: VoidCallback`, `onSwitchToggle: ValueChanged<bool>?`
       - Build: DecoratedBox hairline (index == 0 ? null : Border(top: BorderSide(color: AppTheme.lineHair, width: 0.5)))
       - InkWell(onTap: widget.onTap)
       - Padding(horizontal: 16, vertical: 8)
       - Row children:
         * Text(slot.startTime, style: AppTheme.display(size: 32, color: isBooked ? AppTheme.orange : AppTheme.ink))
         * SizedBox(width: 16)
         * Expanded(Column([
             if (isBooked && bookedByName != null) Text(bookedByName!, style: AppTheme.ui(size: 14, weight: FontWeight.w600)),
             if (isBooked && sport != null) Text(sport!, style: AppTheme.ui(size: 14, color: AppTheme.concrete)),
             if (!isBooked && slot.price != null) Text(NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$').format(slot.price), style: AppTheme.ui(size: 14, color: AppTheme.concrete)),
           ]))
         * Switch(value: slot.isActive, onChanged: isBooked ? null : onSwitchToggle)
         // Switch desativado (onChanged: null) quando slot está reservado — sem toggle de reserva

    3. Reescrever `_SlotDayView`:
       - Manter estado: `_selectedDate: DateTime` (usando DateTime completo, não int), `_selectedWeekStart: DateTime`, `_bookedSlotIds: Set<String>`, `_bookedByNames: Map<String, String>`, `_bookedBySports: Map<String, String?>`
       - Remover toda a lógica de `EventController<SlotModel>` e `DayView` — não usar calendar_view
       - `_loadBookingsForDay()`: busca bookings por data, popula `_bookedSlotIds`, `_bookedByNames` (slotId → userDisplayName), `_bookedBySports` (slotId → sport)
       - Build: Column([
           _AdminDaySelector(selectedDate: _selectedDate, onDateChanged: (d) { setState(() => _selectedDate = d); _slotCubit.loadSlotsForWeek(_getMonday(d)); _loadBookingsForDay(); }),
           Expanded(ListView.builder para slots do dia selecionado)
         ])
       - Filtrar slots: `widget.slots.where((s) => s.date == _toDateString(_selectedDate)).toList()`
       - Para cada slot → _SlotRow(slot, isBooked, bookedByName, sport, index, onTap, onSwitchToggle)
       - onSwitchToggle: `context.read<AdminSlotCubit>().setSlotActive(slot.id, value)`
       - onTap: preservar lógica existente `_openSheet(slot)` (abre SlotFormSheet se vazio, AdminBookingDetailSheet se reservado)
       - Manter FABs (batch + single) existentes

    4. Remover imports de: `calendar_view`, `app_spacing` (substituir por valores literais)
       Manter imports de: `cloud_firestore`, `flutter_bloc`, `intl`, `slot_model`, `booking_model`, `app_theme`, cubits, sheets

    PITFALLS A EVITAR:
    - NÃO usar SizedBox com altura fixa para envolver texto Anton (Pitfall 8 — height: 0.92 clipará)
    - NÃO usar ChoiceChip (remover completamente)
    - NÃO usar DayView nem EventController de calendar_view
    - NÃO usar AppTheme.primaryGreen (usar AppTheme.court se necessário, ou AppTheme.orange)
    - NÃO usar Colors.grey nem Color(0xFF...) hardcoded

    TEST SCAFFOLD (criar test/features/admin/ui/slot_management_tab_test.dart):
    - Use flutter_test + mocktail
    - Criar `FakeAdminSlotCubit` e `FakeAdminBookingCubit` com mocktail
    - Testes listados em <behavior> acima
    - Rodar RED antes de implementar (testes devem falhar primeiro)
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter test test/features/admin/ui/slot_management_tab_test.dart --reporter compact</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChoiceChip" lib/features/admin/ui/slot_management_tab.dart` retorna vazio
    - `grep -n "DayView\|EventController\|calendar_view" lib/features/admin/ui/slot_management_tab.dart` retorna vazio
    - `grep -n "Color(0x\|Colors\.grey\|primaryGreen" lib/features/admin/ui/slot_management_tab.dart` retorna vazio
    - `grep -n "AdminDaySelector\|_AdminDaySelector" lib/features/admin/ui/slot_management_tab.dart` retorna pelo menos 1 linha
    - `grep -n "AppTheme.display(size: 32" lib/features/admin/ui/slot_management_tab.dart` retorna pelo menos 1 linha
    - `grep -n "AppTheme.orange\|AppTheme.ink" lib/features/admin/ui/slot_management_tab.dart` retorna pelo menos 2 linhas
    - `grep -n "BorderSide(color: AppTheme.lineHair, width: 0.5)" lib/features/admin/ui/slot_management_tab.dart` retorna pelo menos 1 linha
    - `flutter analyze lib/features/admin/ui/slot_management_tab.dart` retorna 0 erros
    - Todos os testes em test/features/admin/ui/slot_management_tab_test.dart passam (verde)
  </acceptance_criteria>
  <done>slot_management_tab.dart reescrito sem ChoiceChip/DayView/hardcoded colors; _SlotRow usa hairline DecoratedBox; _AdminDaySelector usa underline laranja 2px + navegação ← →; todos os testes passam</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Firestore | Tab faz query direta bookings/slotId para checar reservas — só leitura, regras Firestore restringem escrita |
| Admin UI → AdminSlotCubit | setSlotActive escreve no Firestore — requer role admin verificado pelo backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Spoofing | slot_management_tab → setSlotActive | accept | AuthCubit verifica role admin antes de renderizar AdminScreen; Firestore rules bloqueiam escrita não-admin |
| T-27-01-02 | Information Disclosure | _loadBookingsForDay query | accept | Retorna apenas slotId, userDisplayName, sport de bookings do dia — dados operacionais admin-only; Firestore rules restringem |
</threat_model>

<verification>
1. `flutter analyze lib/features/admin/ui/slot_management_tab.dart` — 0 erros
2. `flutter test test/features/admin/ui/slot_management_tab_test.dart` — todos passam
3. Grep confirma ausência de ChoiceChip, DayView, Color(0xFF...), Colors.grey, primaryGreen
4. Grep confirma presença de AppTheme.display(size: 32), AppTheme.orange, lineHair, AdminDaySelector
</verification>

<success_criteria>
- aba Slots mostra lista hairline de slots para o dia selecionado
- hora em Anton 32px laranja (reservado) ou ink (vazio)
- slot vazio: preço Manrope + Switch AppTheme-themed
- slot reservado: nome Manrope bold + esporte Manrope concrete
- tap abre sheet correto (SlotFormSheet ou AdminBookingDetailSheet)
- day selector underline laranja 2px no dia ativo + ← → navegam semanas
- flutter analyze 0 erros, testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/27-admin-slots-reservas-usu-rios/27-01-SUMMARY.md`
</output>
