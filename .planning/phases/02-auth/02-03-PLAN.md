---
phase: 02-auth
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - lib/features/auth/ui/splash_screen.dart
  - lib/features/auth/ui/profile_screen.dart
  - lib/features/auth/ui/access_denied_screen.dart
  - lib/core/router/app_router.dart
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "User sees green splash screen with 'Vida Ativa' while auth initializes"
    - "Splash resolves to /home when user has cached session (AUTH-05)"
    - "Splash resolves to /login when no cached session"
    - "Profile tab shows circular avatar with Google photo or name initial"
    - "Profile tab shows user's display name and email"
    - "Profile tab has 'Sair da conta' logout button"
    - "Client accessing /admin sees 'Acesso negado' with back button"
    - "After logout, user is redirected to /login"
  artifacts:
    - path: "lib/features/auth/ui/splash_screen.dart"
      provides: "Splash screen with green branding"
      contains: "class SplashScreen"
    - path: "lib/features/auth/ui/profile_screen.dart"
      provides: "Profile tab with avatar, name, email, logout"
      contains: "class ProfileScreen"
    - path: "lib/features/auth/ui/access_denied_screen.dart"
      provides: "Access denied screen for non-admin users"
      contains: "class AccessDeniedScreen"
  key_links:
    - from: "lib/features/auth/ui/profile_screen.dart"
      to: "lib/features/auth/cubit/auth_cubit.dart"
      via: "context.read<AuthCubit>().signOut()"
      pattern: "signOut"
    - from: "lib/features/auth/ui/profile_screen.dart"
      to: "lib/features/auth/cubit/auth_state.dart"
      via: "BlocBuilder reading AuthAuthenticated.user"
      pattern: "AuthAuthenticated"
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/auth/ui/profile_screen.dart"
      via: "/profile route builder"
      pattern: "ProfileScreen"
---

<objective>
Build the splash screen, profile screen, and access denied screen. Replace remaining placeholder references in the router. Finish with a human verification checkpoint of the full auth flow.

Purpose: These are the remaining UI surfaces. Splash prevents cold-start flash (AUTH-05), profile provides logout and user info display, access-denied enforces admin boundary. Checkpoint confirms the complete auth flow works end-to-end.

Output: Three production screens, router fully wired with no placeholders in auth routes, verified auth flow.
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/phases/02-auth/02-RESEARCH.md
@.planning/phases/02-auth/02-01-SUMMARY.md
@.planning/phases/02-auth/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and Plan 02 -->

From lib/features/auth/cubit/auth_state.dart:
```dart
sealed class AuthState extends Equatable { const AuthState(); }
class AuthInitial extends AuthState { ... }
class AuthLoading extends AuthState { ... }
class AuthAuthenticated extends AuthState { final UserModel user; ... }
class AuthUnauthenticated extends AuthState { ... }
class AuthError extends AuthState { final String message; ... }
```

From lib/features/auth/cubit/auth_cubit.dart:
```dart
class AuthCubit extends Cubit<AuthState> {
  Future<void> signOut() async { ... }
  // ... other methods
}
```

From lib/core/models/user_model.dart:
```dart
class UserModel extends Equatable {
  final String uid;
  final String email;
  final String displayName;
  final String role;
  final String? phone;
  bool get isAdmin => role == 'admin';
}
```

From lib/core/theme/app_theme.dart:
```dart
class AppTheme {
  static const Color primaryGreen = Color(0xFF2E7D32);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SplashScreen, ProfileScreen, and AccessDeniedScreen</name>
  <files>lib/features/auth/ui/splash_screen.dart, lib/features/auth/ui/profile_screen.dart, lib/features/auth/ui/access_denied_screen.dart</files>
  <read_first>
    - lib/features/auth/ui/splash_screen.dart (Plan 01 placeholder)
    - lib/features/auth/ui/profile_placeholder_screen.dart
    - lib/features/auth/ui/access_denied_screen.dart (Plan 01 placeholder)
    - lib/features/auth/cubit/auth_cubit.dart
    - lib/features/auth/cubit/auth_state.dart
    - lib/core/models/user_model.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/02-auth/02-CONTEXT.md (splash, profile, access-denied decisions)
  </read_first>
  <action>
**File 1: `lib/features/auth/ui/splash_screen.dart`** (REPLACE Plan 01 placeholder)

Simple StatelessWidget splash screen per CONTEXT.md decisions:
- Background: `AppTheme.primaryGreen` (#2E7D32) — use `Scaffold(backgroundColor: AppTheme.primaryGreen)`
- Centered content: Column with:
  - Text "Vida Ativa" in white, fontSize 36, fontWeight bold
  - SizedBox(height: 8)
  - Text "Reserve sua quadra" in white70 (Colors.white70), fontSize 16
  - SizedBox(height: 40)
  - `CircularProgressIndicator(color: Colors.white)` — signals loading
- No navigation logic here — GoRouter redirect handles all transitions

Imports: `package:flutter/material.dart`, `package:vida_ativa/core/theme/app_theme.dart`

**File 2: `lib/features/auth/ui/profile_screen.dart`** (NEW FILE — replaces profile_placeholder_screen.dart)

StatelessWidget using `BlocBuilder<AuthCubit, AuthState>` to read the authenticated user.

Layout:
- Scaffold with no AppBar (tab already has context)
- SafeArea wrapping a Column with center alignment
- SizedBox(height: 40) at top
- **Avatar:** `CircleAvatar` with radius 48:
  - If `user.displayName.isNotEmpty`: show first letter of displayName as fallback text (fontSize 36, white on primaryGreen background)
  - backgroundImage: check if FirebaseAuth.instance.currentUser?.photoURL is not null — if so, use `NetworkImage(photoURL)` for the Google profile photo
  - The BlocBuilder provides `AuthAuthenticated.user` (UserModel). For photoURL, access `FirebaseAuth.instance.currentUser?.photoURL` directly since UserModel doesn't store it.
- SizedBox(height: 16)
- Text: `user.displayName` in `textTheme.headlineSmall`
- SizedBox(height: 4)
- Text: `user.email` in `textTheme.bodyMedium`, grey color
- SizedBox(height: 32)
- Divider
- SizedBox(height: 16)
- **Logout button:** `OutlinedButton.icon` with Icons.logout, text "Sair da conta", `style: OutlinedButton.styleFrom(foregroundColor: Colors.red)`. On press: `context.read<AuthCubit>().signOut()`.

Handle non-authenticated state: if `state is! AuthAuthenticated`, show a loading indicator or empty container (GoRouter redirect will handle navigation away).

Imports: `package:flutter/material.dart`, `package:flutter_bloc/flutter_bloc.dart`, `package:firebase_auth/firebase_auth.dart` (for photoURL), `package:vida_ativa/features/auth/cubit/auth_cubit.dart`, `package:vida_ativa/features/auth/cubit/auth_state.dart`, `package:vida_ativa/core/theme/app_theme.dart`

**File 3: `lib/features/auth/ui/access_denied_screen.dart`** (REPLACE Plan 01 placeholder)

StatelessWidget per CONTEXT.md: "tela Acesso negado com mensagem clara e botao Voltar para Agenda".

Layout:
- Scaffold with centered Column:
  - Icon: `Icons.block`, size 64, color `Colors.red.shade300`
  - SizedBox(height: 16)
  - Text "Acesso negado" in `textTheme.headlineSmall`
  - SizedBox(height: 8)
  - Text "Voce nao tem permissao para acessar esta area." in `textTheme.bodyMedium`, grey, textAlign center. Use proper diacritics: "Voce nao tem permissao para acessar esta area." -> with accents.
  - SizedBox(height: 24)
  - `FilledButton` text "Voltar para Agenda", on press: `context.go('/home')`

Imports: `package:flutter/material.dart`, `package:go_router/go_router.dart`

**Cleanup:** Delete `lib/features/auth/ui/profile_placeholder_screen.dart` after creating profile_screen.dart.

Run `flutter analyze lib/features/auth/ui/` to confirm zero errors.
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/ui/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/auth/ui/splash_screen.dart contains `AppTheme.primaryGreen`
    - lib/features/auth/ui/splash_screen.dart contains `Vida Ativa`
    - lib/features/auth/ui/splash_screen.dart contains `CircularProgressIndicator`
    - lib/features/auth/ui/profile_screen.dart contains `class ProfileScreen`
    - lib/features/auth/ui/profile_screen.dart contains `signOut`
    - lib/features/auth/ui/profile_screen.dart contains `Sair da conta`
    - lib/features/auth/ui/profile_screen.dart contains `CircleAvatar`
    - lib/features/auth/ui/profile_screen.dart contains `BlocBuilder`
    - lib/features/auth/ui/profile_screen.dart contains `photoURL`
    - lib/features/auth/ui/access_denied_screen.dart contains `Acesso negado`
    - lib/features/auth/ui/access_denied_screen.dart contains `Voltar para Agenda`
    - lib/features/auth/ui/access_denied_screen.dart contains `go('/home')`
    - lib/features/auth/ui/profile_placeholder_screen.dart does NOT exist (deleted)
    - `flutter analyze lib/features/auth/ui/` reports 0 issues
  </acceptance_criteria>
  <done>SplashScreen shows green branding with spinner. ProfileScreen shows avatar, name, email, and logout. AccessDeniedScreen shows block message with back navigation. Profile placeholder deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update router to use real ProfileScreen and remove all auth placeholder imports</name>
  <files>lib/core/router/app_router.dart</files>
  <read_first>
    - lib/core/router/app_router.dart
    - lib/features/auth/ui/profile_screen.dart
    - lib/features/auth/ui/splash_screen.dart
    - lib/features/auth/ui/access_denied_screen.dart
  </read_first>
  <action>
Update `lib/core/router/app_router.dart` imports and route builders:

1. **Remove** import for `profile_placeholder_screen.dart`
2. **Add** import for `profile_screen.dart`: `import 'package:vida_ativa/features/auth/ui/profile_screen.dart';`
3. **Change** the /profile branch builder from `ProfilePlaceholderScreen()` to `ProfileScreen()`
4. **Verify** all other route builders point to real screens:
   - /splash -> SplashScreen (should be done in Plan 01)
   - /login -> LoginScreen (should be done in Plan 02)
   - /register -> RegisterScreen (should be done in Plan 01/02)
   - /access-denied -> AccessDeniedScreen (should be done in Plan 01)
   - /profile -> ProfileScreen (this task)
5. **Verify** no remaining imports reference any `*_placeholder_screen.dart` files except `schedule_placeholder_screen.dart` and `my_bookings_placeholder_screen.dart` (those are Phase 3 and 4 concerns) and `admin_placeholder_screen.dart` (Phase 5).

Run `flutter analyze lib/` to confirm zero errors across the full project.
  </action>
  <verify>
    <automated>flutter analyze lib/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/router/app_router.dart contains `import` for `profile_screen.dart`
    - lib/core/router/app_router.dart contains `ProfileScreen()` (not ProfilePlaceholderScreen)
    - lib/core/router/app_router.dart does NOT contain `ProfilePlaceholderScreen`
    - lib/core/router/app_router.dart does NOT contain `LoginPlaceholderScreen`
    - lib/core/router/app_router.dart does NOT contain `login_placeholder_screen.dart`
    - lib/core/router/app_router.dart does NOT contain `profile_placeholder_screen.dart`
    - `flutter analyze lib/` reports 0 issues
  </acceptance_criteria>
  <done>Router uses all real auth screens. No auth placeholder imports remain. Full project analyzes cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow in browser</name>
  <files>none</files>
  <action>
Human verification of the complete auth system built in Plans 01-03. No code changes — this is a visual and functional check.

Run `flutter run -d chrome --web-port 7357` to launch the app, then follow the verification steps below.
  </action>
  <what-built>
Complete authentication system:
- Splash screen (green, shows while Firebase initializes)
- Login screen (Google + email/password, forgot password, register link)
- Register screen (name, email, password, confirm password)
- Profile tab (avatar, name, email, logout button)
- Access denied screen (for non-admin users accessing /admin)
- Route guards (unauthenticated -> /login, client + /admin -> /access-denied, splash on cold start)
- Session persistence (close and reopen browser)
  </what-built>
  <how-to-verify>
1. Run `flutter run -d chrome --web-port 7357`
2. **Cold start:** App should show green splash screen briefly, then redirect to /login
3. **Login screen layout:** Verify "Vida Ativa" branding, Google button, "ou" separator, email/password fields, "Esqueci minha senha" link, "Criar" link
4. **Register flow:** Click "Criar", verify /register screen with 4 fields, create an account with test email, verify redirect to /home after success
5. **Logout:** Go to Perfil tab, verify avatar (initial letter), name, email displayed. Click "Sair da conta", verify redirect to /login
6. **Email login:** Log in with the account just created, verify redirect to /home
7. **Google login:** Click "Entrar com Google", verify popup opens (may need Firebase Console Google provider enabled)
8. **Session persistence:** Close browser tab, reopen localhost:7357, verify splash then /home (no re-login required)
9. **Admin guard:** Navigate to localhost:7357/admin in URL bar while logged in as client, verify "Acesso negado" screen with "Voltar para Agenda" button
10. **Error display:** Try wrong password on login, verify inline red error below password field

Note: Google Sign-In requires Google provider to be enabled in Firebase Console > Authentication > Sign-in method. If not enabled, Google button will show an error — this is expected and can be configured after verification.
  </how-to-verify>
  <verify>
    <automated>flutter analyze lib/ 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms: splash works, login/register flow works, profile shows user data, logout works, admin guard works, session persists.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `flutter analyze lib/` reports 0 issues
- `flutter test` passes (stubs from Plan 01)
- All auth routes use real screens (no auth placeholders in router)
- Profile shows user data from AuthCubit state
- Splash prevents cold-start flash
- Access denied shows for non-admin clients at /admin
</verification>

<success_criteria>
- SplashScreen shows green branding while auth initializes, then resolves correctly
- ProfileScreen displays avatar, name, email, and working logout
- AccessDeniedScreen blocks non-admin users with clear message and back navigation
- Router has zero auth placeholder references
- Human verifies full auth flow works end-to-end in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-03-SUMMARY.md`
</output>
