---
phase: 02-auth
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - lib/features/auth/ui/login_screen.dart
  - lib/features/auth/ui/register_screen.dart
  - lib/core/router/app_router.dart
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "User sees Google sign-in button at top of login screen"
    - "User sees email and password fields below a separator on login screen"
    - "User sees 'Esqueci minha senha' link below password field"
    - "User sees 'Nao tem conta? Criar' link navigating to /register"
    - "User sees register screen with name, email, password, confirm password fields"
    - "Login errors appear inline in red below the field with the problem"
    - "Login screen shows 'Vida Ativa' branding with 'Reserve sua quadra' subtitle"
  artifacts:
    - path: "lib/features/auth/ui/login_screen.dart"
      provides: "Full login screen with Google + email/password"
      contains: "class LoginScreen"
    - path: "lib/features/auth/ui/register_screen.dart"
      provides: "Registration screen with 4 fields"
      contains: "class RegisterScreen"
  key_links:
    - from: "lib/features/auth/ui/login_screen.dart"
      to: "lib/features/auth/cubit/auth_cubit.dart"
      via: "context.read<AuthCubit>().signInWithGoogle()"
      pattern: "signInWithGoogle"
    - from: "lib/features/auth/ui/login_screen.dart"
      to: "lib/features/auth/cubit/auth_cubit.dart"
      via: "context.read<AuthCubit>().signInWithEmailPassword()"
      pattern: "signInWithEmailPassword"
    - from: "lib/features/auth/ui/register_screen.dart"
      to: "lib/features/auth/cubit/auth_cubit.dart"
      via: "context.read<AuthCubit>().registerWithEmailPassword()"
      pattern: "registerWithEmailPassword"
    - from: "lib/features/auth/ui/login_screen.dart"
      to: "/register"
      via: "GoRouter.of(context).go('/register')"
      pattern: "go\\('/register'\\)"
---

<objective>
Build the login and registration screens with full AuthCubit integration, replacing the login placeholder. These are the primary user-facing auth surfaces.

Purpose: Users need to see and interact with the authentication flow. Login screen handles Google sign-in, email/password login, forgot password, and navigation to register. Register screen handles account creation.

Output: Two production screens (LoginScreen, RegisterScreen) wired to AuthCubit, with inline error display and all navigation links.
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/phases/02-auth/02-RESEARCH.md
@.planning/phases/02-auth/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From lib/features/auth/cubit/auth_state.dart:
```dart
sealed class AuthState extends Equatable { const AuthState(); }
class AuthInitial extends AuthState { ... }
class AuthLoading extends AuthState { ... }
class AuthAuthenticated extends AuthState { final UserModel user; ... }
class AuthUnauthenticated extends AuthState { ... }
class AuthError extends AuthState { final String message; ... }
```

From lib/features/auth/cubit/auth_cubit.dart:
```dart
class AuthCubit extends Cubit<AuthState> {
  Future<void> signInWithGoogle() async { ... }
  Future<void> signInWithEmailPassword(String email, String password) async { ... }
  Future<void> registerWithEmailPassword({required String name, required String email, required String password}) async { ... }
  Future<void> sendPasswordReset(String email) async { ... }
  Future<void> signOut() async { ... }
}
```

From lib/core/theme/app_theme.dart:
```dart
class AppTheme {
  static const Color primaryGreen = Color(0xFF2E7D32);
  static const Color primaryBlue = Color(0xFF0175C2);
  static final ThemeData lightTheme = ThemeData(...);
}
```

From lib/core/router/app_router.dart (after Plan 01):
```dart
GoRouter createRouter(AuthCubit authCubit) { ... }
// Routes: /splash, /login, /register, /access-denied, /admin, StatefulShellRoute(/home, /bookings, /profile)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginScreen with Google button, email/password form, and forgot password</name>
  <files>lib/features/auth/ui/login_screen.dart</files>
  <read_first>
    - lib/features/auth/ui/login_placeholder_screen.dart
    - lib/features/auth/cubit/auth_cubit.dart
    - lib/features/auth/cubit/auth_state.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/02-auth/02-CONTEXT.md (login decisions)
    - .planning/phases/02-auth/02-RESEARCH.md (inline error pattern, autofill hints)
  </read_first>
  <action>
Create `lib/features/auth/ui/login_screen.dart` — a StatefulWidget (needs TextEditingControllers and local error state).

**Layout (top to bottom, centered, max width 400px, scrollable SingleChildScrollView):**

1. **Branding:** Text "Vida Ativa" in `Theme.of(context).textTheme.headlineLarge` with `color: AppTheme.primaryGreen`, fontWeight bold. Below it: Text "Reserve sua quadra" in `textTheme.bodyLarge`, color grey.

2. **Spacer** (SizedBox height 40)

3. **Google Sign-In button:** `ElevatedButton.icon` with Google "G" icon (use `Icons.g_mobiledata` or a simple "G" text as icon since no image dependency per CONTEXT.md decisions), label "Entrar com Google", full width, style with white background and dark text. On press: `context.read<AuthCubit>().signInWithGoogle()`. Disable button when `state is AuthLoading` (use BlocBuilder).

4. **Separator:** Row with Divider — Text "ou" — Divider pattern (horizontal line, text "ou", horizontal line).

5. **Email field:** `TextField` with:
   - controller: `_emailController`
   - decoration: `InputDecoration(labelText: 'Email', errorText: _emailError)`
   - keyboardType: `TextInputType.emailAddress`
   - autofillHints: `[AutofillHints.email]`
   - Wrap in `AutofillGroup` (the whole form)

6. **Password field:** `TextField` with:
   - controller: `_passwordController`
   - decoration: `InputDecoration(labelText: 'Senha', errorText: _passwordError)`
   - obscureText: true
   - autofillHints: `[AutofillHints.password]`

7. **Forgot password link:** `TextButton` aligned right, text "Esqueci minha senha", always visible. On press: if `_emailController.text.isNotEmpty`, call `context.read<AuthCubit>().sendPasswordReset(_emailController.text.trim())` and show a brief message (can use ScaffoldMessenger for this one success case — it is NOT an error). If email empty, set `_emailError = 'Informe o email para recuperar a senha'`.

8. **Login button:** `FilledButton` full width, text "Entrar", `AppTheme.primaryGreen` background. On press: validate non-empty fields, then call `context.read<AuthCubit>().signInWithEmailPassword(email.trim(), password)`. Disable when loading.

9. **Register link:** Row with Text "Nao tem conta?" + TextButton "Criar" that calls `context.go('/register')`.

**Error handling:**
- Use `BlocListener<AuthCubit, AuthState>` to listen for `AuthError` state:
  - If error message contains 'Email' or 'email': set `_emailError = state.message`, clear `_passwordError`
  - If error message contains 'Senha' or 'senha' or 'incorretos': set `_passwordError = state.message`, clear `_emailError`
  - Default: set `_passwordError = state.message`
  - Call `setState` to trigger rebuild with error text showing inline
- On `AuthLoading`: show loading indicator (CircularProgressIndicator in place of button text, or disable buttons)
- On `AuthAuthenticated`: no action needed — GoRouter redirect handles navigation

**Import paths:**
- `package:flutter/material.dart`
- `package:flutter_bloc/flutter_bloc.dart`
- `package:go_router/go_router.dart`
- `package:vida_ativa/features/auth/cubit/auth_cubit.dart`
- `package:vida_ativa/features/auth/cubit/auth_state.dart`
- `package:vida_ativa/core/theme/app_theme.dart`

Use proper Portuguese with diacritics in all user-facing strings (nao -> nao with tilde if needed, but Flutter renders UTF-8 fine: use "Nao tem conta?" with proper accent: "Nao tem conta?" -> actually check CONTEXT.md: it says "Nao tem conta? Criar" — use exactly that phrasing with proper accents: "Nao" should be with tilde "N\u00e3o").

Delete `lib/features/auth/ui/login_placeholder_screen.dart` after creating the new file.
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/ui/login_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/auth/ui/login_screen.dart exists and contains `class LoginScreen`
    - login_screen.dart contains `signInWithGoogle`
    - login_screen.dart contains `signInWithEmailPassword`
    - login_screen.dart contains `sendPasswordReset`
    - login_screen.dart contains `Vida Ativa`
    - login_screen.dart contains `Reserve sua quadra`
    - login_screen.dart contains `Esqueci minha senha`
    - login_screen.dart contains `go('/register')`
    - login_screen.dart contains `AutofillHints.email`
    - login_screen.dart contains `AutofillHints.password`
    - login_screen.dart contains `errorText:`
    - login_screen.dart contains `BlocListener` or `BlocConsumer`
    - lib/features/auth/ui/login_placeholder_screen.dart does NOT exist (deleted)
    - `flutter analyze lib/features/auth/ui/login_screen.dart` reports 0 issues
  </acceptance_criteria>
  <done>LoginScreen renders Google button, email/password form with inline errors, forgot password link, and register navigation. Placeholder deleted. Analyzes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create RegisterScreen and update router imports</name>
  <files>lib/features/auth/ui/register_screen.dart, lib/core/router/app_router.dart</files>
  <read_first>
    - lib/features/auth/ui/login_screen.dart (match style/patterns)
    - lib/features/auth/ui/register_screen.dart (Plan 01 placeholder to replace)
    - lib/features/auth/cubit/auth_cubit.dart
    - lib/features/auth/cubit/auth_state.dart
    - lib/core/router/app_router.dart
    - .planning/phases/02-auth/02-CONTEXT.md (register decisions)
  </read_first>
  <action>
**File 1: `lib/features/auth/ui/register_screen.dart`** (REPLACE the Plan 01 placeholder)

Create a StatefulWidget with the registration form.

**Layout (same container pattern as LoginScreen — centered, max width 400px, SingleChildScrollView):**

1. **AppBar** (or just a back button): IconButton with arrow_back that calls `context.go('/login')`. Text "Criar conta" as title.

2. **Name field:** `TextField` with:
   - controller: `_nameController`
   - decoration: `InputDecoration(labelText: 'Nome completo', errorText: _nameError)`
   - autofillHints: `[AutofillHints.name]`
   - textCapitalization: `TextCapitalization.words`

3. **Email field:** `TextField` with:
   - controller: `_emailController`
   - decoration: `InputDecoration(labelText: 'Email', errorText: _emailError)`
   - keyboardType: `TextInputType.emailAddress`
   - autofillHints: `[AutofillHints.email]`

4. **Password field:** `TextField` with:
   - controller: `_passwordController`
   - decoration: `InputDecoration(labelText: 'Senha', errorText: _passwordError)`
   - obscureText: true
   - autofillHints: `[AutofillHints.newPassword]`

5. **Confirm password field:** `TextField` with:
   - controller: `_confirmPasswordController`
   - decoration: `InputDecoration(labelText: 'Confirmar senha', errorText: _confirmError)`
   - obscureText: true

6. **Register button:** `FilledButton` full width, text "Criar conta", AppTheme.primaryGreen background. On press:
   - Validate: name not empty (set `_nameError = 'Informe seu nome'`), email not empty, password >= 6 chars (set `_passwordError = 'Minimo 6 caracteres'`), passwords match (set `_confirmError = 'Senhas nao conferem'`). Use proper diacritics.
   - If valid: `context.read<AuthCubit>().registerWithEmailPassword(name: name.trim(), email: email.trim(), password: password)`
   - Disable when loading

7. **Login link:** Row with Text "Ja tem conta?" + TextButton "Entrar" that calls `context.go('/login')`. Use proper diacritics.

**Error handling:** Same BlocListener pattern as LoginScreen — listen for AuthError, map to field-level _emailError or _passwordError based on message content.

**File 2: `lib/core/router/app_router.dart`** (UPDATE IMPORTS ONLY)

Update the router to import the real LoginScreen instead of the placeholder:
- Change: `import 'package:vida_ativa/features/auth/ui/login_placeholder_screen.dart';` -> `import 'package:vida_ativa/features/auth/ui/login_screen.dart';`
- Change the /login route builder: `LoginPlaceholderScreen()` -> `LoginScreen()`
- The /register route should already point to RegisterScreen from Plan 01 — verify and fix if needed

Run `flutter analyze lib/` to confirm zero errors.
  </action>
  <verify>
    <automated>flutter analyze lib/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/auth/ui/register_screen.dart contains `class RegisterScreen`
    - register_screen.dart contains `registerWithEmailPassword`
    - register_screen.dart contains `Nome completo`
    - register_screen.dart contains `Confirmar senha`
    - register_screen.dart contains `go('/login')`
    - register_screen.dart contains `BlocListener` or `BlocConsumer`
    - register_screen.dart contains `AutofillHints.name`
    - lib/core/router/app_router.dart contains `LoginScreen()` (not LoginPlaceholderScreen)
    - lib/core/router/app_router.dart contains `import` for `login_screen.dart`
    - lib/core/router/app_router.dart does NOT contain `LoginPlaceholderScreen`
    - `flutter analyze lib/` reports 0 issues
  </acceptance_criteria>
  <done>RegisterScreen with 4 validated fields wired to AuthCubit. Router updated to use real LoginScreen. Full lib/ analyzes cleanly.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/` reports 0 issues
- lib/features/auth/ui/login_screen.dart has Google button + email form + forgot password + register link
- lib/features/auth/ui/register_screen.dart has name + email + password + confirm fields
- Router imports real LoginScreen, not placeholder
- Both screens use BlocListener for inline error display
- Both screens use AutofillGroup + autofillHints
</verification>

<success_criteria>
- LoginScreen matches all CONTEXT.md decisions: Google at top, "ou" separator, email/password below, "Esqueci minha senha" always visible, inline errors, "Nao tem conta? Criar" link
- RegisterScreen matches CONTEXT.md: separate screen, 4 fields, name becomes displayName, inline errors
- Both screens are wired to AuthCubit methods
- No placeholder screens remain for login
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-02-SUMMARY.md`
</output>
