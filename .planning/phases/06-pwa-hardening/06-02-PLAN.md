---
phase: 06-pwa-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - build/web/
autonomous: false
requirements:
  - PWA-01
  - INFRA-01

must_haves:
  truths:
    - "flutter build web completes without errors"
    - "firebase deploy completes and produces a working production URL"
    - "Live URL shows login screen or home screen when accessed"
    - "Browser tab shows Vida Ativa"
    - "Firebase Console shows isAdmin() function in deployed Firestore rules"
    - "Firestore rules are deployed (not just local)"
  artifacts:
    - path: "build/web/index.html"
      provides: "Built Flutter web app"
  key_links:
    - from: "firebase.json"
      to: "build/web/"
      via: "hosting.public config"
      pattern: "\"public\": \"build/web\""
---

<objective>
Build the Flutter web app and deploy to Firebase Hosting with Firestore rules. Verify the live production URL works.

Purpose: Ship the hardened app to production so real users can access it at vida-ativa-94ba0.web.app.
Output: Live production deployment at the Firebase Hosting default URL.
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pwa-hardening/06-CONTEXT.md
@.planning/phases/06-pwa-hardening/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Flutter web and deploy to Firebase Hosting</name>
  <files>build/web/</files>
  <read_first>
    - firebase.json (confirms hosting.public = "build/web" and firestore.rules path)
    - firestore.rules (confirms the production rules from Plan 01 are in place)
  </read_first>
  <action>
Run the two-step deploy sequence. These are the exact commands per CONTEXT.md locked decision:

```bash
flutter build web --no-tree-shake-icons
```

Wait for the build to complete successfully. Then:

```bash
firebase deploy --only hosting,firestore:rules
```

The `--no-tree-shake-icons` flag is required because the app uses dynamic IconData (e.g., admin slot forms).

The `--only hosting,firestore:rules` deploys BOTH the built web app AND the Firestore security rules in a single command.

If `firebase deploy` fails with an auth error, pause and inform the user they need to run `firebase login` first. This is expected if the Firebase CLI is not yet authenticated on this machine.

After deploy completes, note the Hosting URL from the output (expected: `vida-ativa-94ba0.web.app`).
  </action>
  <verify>
    <automated>ls build/web/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `flutter build web --no-tree-shake-icons` exits with code 0
    - `firebase deploy --only hosting,firestore:rules` exits with code 0
    - Deploy output contains a Hosting URL (vida-ativa-94ba0.web.app or similar)
    - build/web/index.html exists after the build
  </acceptance_criteria>
  <done>Flutter web app built and deployed to Firebase Hosting; Firestore rules deployed to production</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production deployment</name>
  <files>N/A</files>
  <action>
Human verification of the live production deployment. Present the verification steps below to the user and wait for approval.

What was built: Complete PWA hardening — restrictive Firestore rules with isAdmin() checking .data.role == "admin", iOS install SnackBar, corrected app title "Vida Ativa", production deployment to Firebase Hosting.
  </action>
  <verify>User confirms "approved"</verify>
  <done>User has verified the production deployment works correctly</done>
  <what-built>Complete PWA hardening: restrictive Firestore rules, iOS install banner, corrected app title, production deployment to Firebase Hosting</what-built>
  <how-to-verify>
    1. Open https://vida-ativa-94ba0.web.app in a browser
    2. Verify the page loads and shows the login screen (or home screen if already authenticated)
    3. Verify the browser tab title shows "Vida Ativa" (not "vida_ativa")
    4. Log in and verify the schedule, bookings, and profile tabs all work
    5. If you have an iOS device: open the URL in Safari and verify the install SnackBar appears at the bottom
    6. Go to Firebase Console > Firestore > Rules tab and verify the deployed rules show the isAdmin() function with `.data.role == "admin"`
    7. (Optional) Use the Firebase Rules Playground in the console to test: attempt a slot write without admin role — should be denied
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Production URL loads the app
2. Browser tab shows "Vida Ativa"
3. All app features (schedule, bookings, profile, admin) work on the live URL
4. Firebase Console shows the restrictive Firestore rules are deployed
</verification>

<success_criteria>
- The app is live at vida-ativa-94ba0.web.app
- Firestore rules are deployed and enforce role-based access
- The PWA installs cleanly (correct title, icons)
- Phase 6 success criteria from ROADMAP.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/06-pwa-hardening/06-02-SUMMARY.md`
</output>
