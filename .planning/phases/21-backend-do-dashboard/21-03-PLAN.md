---
phase: 21-backend-do-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
  - 21-02-PLAN.md
files_modified:
  - lib/features/admin/ui/admin_screen.dart
  - firestore.rules
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-09
  - DASH-10
  - DASH-11
  - DASH-12
tags:
  - firestore-rules
  - bloc-provider
  - admin
  - security
must_haves:
  truths:
    - "DashboardCubit provisionado em AdminScreen via BlocProvider envolvendo o Scaffold (D-15)"
    - "Phase 22 UI consegue acessar DashboardCubit via context.read/watch dentro de qualquer tab"
    - "Cliente Flutter (não admin) recebe PERMISSION_DENIED ao ler /config/dashboard/periods/week"
    - "Cliente Flutter admin recebe leitura permitida em /config/dashboard/periods/week"
    - "Qualquer cliente Flutter (admin ou não) recebe PERMISSION_DENIED ao escrever em /config/dashboard/periods/week"
    - "Cloud Function via admin SDK consegue escrever em /config/dashboard/periods/week (admin SDK bypassa rules)"
  artifacts:
    - path: "lib/features/admin/ui/admin_screen.dart"
      provides: "BlocProvider<DashboardCubit> envolvendo Scaffold; import de DashboardCubit"
      contains: "BlocProvider(create: (_) => DashboardCubit"
    - path: "firestore.rules"
      provides: "Bloco /config/dashboard/{period} com read: isAdmin() e write: false ANTES do wildcard /config/{docId}"
      contains: "match /config/dashboard/{period}"
  key_links:
    - from: "lib/features/admin/ui/admin_screen.dart"
      to: "DashboardCubit"
      via: "BlocProvider(create: (_) => DashboardCubit(firestore: FirebaseFirestore.instance))"
      pattern: "BlocProvider\\(\\s*create:[^)]*DashboardCubit"
    - from: "firestore.rules"
      to: "/config/dashboard/{period}"
      via: "match block before /config/{docId} wildcard"
      pattern: "match /config/dashboard/\\{period\\}"
---

<objective>
Conectar tudo: (1) provisionar `DashboardCubit` em `AdminScreen` para que a UI da Phase 22 possa consumir o stream; (2) adicionar regra Firestore que restringe leitura de `/config/dashboard/{period}` a admin e bloqueia escrita do cliente Flutter (apenas CFs via admin SDK escrevem).

Purpose: Wiring final + security hardening. Sem este plano, Phase 22 não acessa o cubit E o cliente poderia ler dados de receita.

Output: 2 arquivos modificados + checkpoint manual de verificação em emulator/staging (regras Firestore exigem teste fora de unit tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-backend-do-dashboard/21-CONTEXT.md
@.planning/phases/21-backend-do-dashboard/21-RESEARCH.md
@lib/features/admin/ui/admin_screen.dart
@firestore.rules
@lib/features/admin/cubit/dashboard_cubit.dart
@lib/features/admin/cubit/sport_config_cubit.dart

<interfaces>
<!-- AdminScreen.build() atual envolve Scaffold em BlocProvider.value(_fcmCubit). -->
<!-- DashboardCubit precisa estar acima do Scaffold inteiro porque a aba Dashboard (Phase 22) será um dos tabs. -->

Padrão atual (admin_screen.dart linhas 73-77):
```dart
@override
Widget build(BuildContext context) {
  return BlocProvider.value(
    value: _fcmCubit,
    child: Scaffold(
      ...
    ),
  );
}
```

Padrão de DashboardCubit construtor:
```dart
DashboardCubit({required FirebaseFirestore firestore})
```

Padrão de regra Firestore /config/mercadopago (firestore.rules:51):
```javascript
match /config/mercadopago {
  allow read: if false;
  allow write: if isAdmin();
}
match /config/{docId} {
  allow read: if isAuthenticated();
  allow write: if isAdmin();
}
```

Regra atual em /config/{docId} (firestore.rules:46-49):
```javascript
match /config/{docId} {
  allow read: if isAuthenticated();
  allow write: if isAdmin();
}
```

D-16 LITERAL: "/config/dashboard read restrito a admin; escrita bloqueada para cliente Flutter (apenas CFs escrevem via admin.firestore()). Regra específica sobrepõe o wildcard /config/{docId}."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provisionar DashboardCubit em AdminScreen</name>
  <files>lib/features/admin/ui/admin_screen.dart</files>
  <read_first>
    - lib/features/admin/ui/admin_screen.dart (estado atual — entender estrutura do build())
    - lib/features/admin/cubit/dashboard_cubit.dart (assinatura do construtor — criada em Plan 01)
    - lib/features/admin/cubit/sport_config_cubit.dart (padrão similar de cubit firestore-based)
    - .planning/phases/21-backend-do-dashboard/21-CONTEXT.md §D-15
  </read_first>
  <action>
    Modificar `lib/features/admin/ui/admin_screen.dart`:

    PASSO 1 — Adicionar import (após o último import existente, mantendo ordem alfabética por package):

    Localizar:
    ```dart
    import 'package:vida_ativa/features/admin/cubit/admin_fcm_cubit.dart';
    ```

    Adicionar imediatamente após:
    ```dart
    import 'package:vida_ativa/features/admin/cubit/dashboard_cubit.dart';
    ```

    PASSO 2 — Envolver o `BlocProvider.value(value: _fcmCubit, ...)` com um `BlocProvider<DashboardCubit>`.

    Localizar o método `build` (começa em linha ~74):
    ```dart
    @override
    Widget build(BuildContext context) {
      return BlocProvider.value(
        value: _fcmCubit,
        child: Scaffold(
    ```

    Substituir por:
    ```dart
    @override
    Widget build(BuildContext context) {
      return BlocProvider(
        create: (_) => DashboardCubit(firestore: FirebaseFirestore.instance),
        child: BlocProvider.value(
          value: _fcmCubit,
          child: Scaffold(
    ```

    PASSO 3 — Encontrar o fechamento correspondente. O `Scaffold(...)` original termina com `),` antes de `);` no final do `build`. A estrutura final fica:

    ```dart
    @override
    Widget build(BuildContext context) {
      return BlocProvider(
        create: (_) => DashboardCubit(firestore: FirebaseFirestore.instance),
        child: BlocProvider.value(
          value: _fcmCubit,
          child: Scaffold(
            // ... appBar, body Column inalterados ...
          ),
        ),
      );
    }
    ```

    PASSO 4 — Verificar que `FirebaseFirestore` está importado (já está via `import 'package:cloud_firestore/cloud_firestore.dart';` na linha 6).

    PASSO 5 — Rodar análise estática e suite de testes:
    ```
    flutter analyze lib/features/admin/ui/admin_screen.dart
    flutter test
    ```

    Commit: `feat(21-03): provision DashboardCubit at AdminScreen root via BlocProvider (D-15)`
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/admin_screen.dart &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/admin/ui/admin_screen.dart` contém `import 'package:vida_ativa/features/admin/cubit/dashboard_cubit.dart';`
    - `lib/features/admin/ui/admin_screen.dart` contém substring `BlocProvider(` seguida em proximidade de `create: (_) =>` e `DashboardCubit(firestore: FirebaseFirestore.instance)`
    - `lib/features/admin/ui/admin_screen.dart` mantém `BlocProvider.value(` com `value: _fcmCubit,` aninhado dentro do novo BlocProvider
    - `flutter analyze lib/features/admin/ui/admin_screen.dart` sem novos errors/warnings
    - `flutter test` exit code 0
  </acceptance_criteria>
  <done>AdminScreen tem DashboardCubit disponível via context para Phase 22 consumir.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar regra Firestore /config/dashboard/{period}</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules (estado atual — ver linhas 46-54 para padrão de override)
    - .planning/phases/21-backend-do-dashboard/21-CONTEXT.md §D-16
    - .planning/phases/21-backend-do-dashboard/21-RESEARCH.md §"Pattern 5: Firestore Rules"
  </read_first>
  <action>
    Modificar `firestore.rules`.

    PASSO 1 — Localizar o bloco existente (linhas 46-54):
    ```javascript
    match /config/{docId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }

    match /config/mercadopago {
      allow read: if false;
      allow write: if isAdmin();
    }
    ```

    PASSO 2 — O path real Firestore (per RESEARCH Q-1 e Plan 02) é `/config/dashboard/periods/{period}` (4 segmentos com subcollection chamada `periods`). Adicionar IMEDIATAMENTE ANTES do bloco `/config/{docId}` (regra mais específica primeiro, conforme Pattern 5 + Pitfall 5):

    ```javascript
        match /config/dashboard/periods/{period} {
          allow read: if isAdmin();
          allow write: if false; // Apenas Cloud Functions via admin SDK escrevem (D-16)
        }

        match /config/dashboard {
          // Doc placeholder — não usado como doc real, mas necessário para hierarchy
          allow read: if isAdmin();
          allow write: if false;
        }

        match /config/{docId} {
          allow read: if isAuthenticated();
          allow write: if isAdmin();
        }
    ```

    Manter o bloco `/config/mercadopago` exatamente como está (não tocar).

    PASSO 3 — Verificar sintaxe (Firestore não tem linter local; depender de validação no deploy do Task 3):
    ```
    grep -E "match /config/dashboard/periods/\{period\}" firestore.rules
    ```

    PASSO 4 — NÃO deployar ainda. Deploy é parte do checkpoint manual (Task 3).

    Commit: `feat(21-03): restrict /config/dashboard/periods to admin read + no client write (D-16, T-21-01)`
  </action>
  <verify>
    <automated>grep -E "match /config/dashboard/periods/\{period\}" firestore.rules</automated>
  </verify>
  <acceptance_criteria>
    - `firestore.rules` contém `match /config/dashboard/periods/{period}`
    - `firestore.rules` contém `allow read: if isAdmin();` dentro do bloco `/config/dashboard/periods/{period}`
    - `firestore.rules` contém `allow write: if false;` dentro do bloco `/config/dashboard/periods/{period}`
    - `firestore.rules` posiciona o bloco `/config/dashboard/periods/{period}` ANTES do bloco `/config/{docId}` (Pitfall 5)
    - `firestore.rules` mantém o bloco existente `/config/{docId}` inalterado em conteúdo (mesmas regras read/write)
    - `firestore.rules` mantém o bloco existente `/config/mercadopago` inalterado
  </acceptance_criteria>
  <done>Regra Firestore adicionada na ordem correta; deploy real fica para checkpoint manual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy staging + verificação manual da rule e CFs</name>
  <files>functions/index.js, firestore.rules</files>
  <read_first>
    - functions/index.js (estado pós-Plan 02 — exports onBookingStateChange e scheduledDailyAggregation prontos)
    - firestore.rules (estado pós-Task 2 — bloco /config/dashboard/periods/{period} adicionado)
    - C:/Users/alvar/.claude/projects/f---geral-Projetos-vida-ativa/memory/feedback_firebase_deploy.md (REGRA: deploy SEMPRE em vida-ativa-staging)
  </read_first>
  <action>
    Executar os passos manualmente em sequência. NÃO autoritativo — o executor Claude orquestra, mas o usuário humano valida cada step com olhos no Firebase Console.

    **STEP 1 — Deploy functions e rules em staging:**

    Conforme `memory/feedback_firebase_deploy.md`: deploy SEMPRE em `vida-ativa-staging`, NUNCA em prod.

    ```bash
    firebase deploy --only functions:onBookingStateChange,functions:scheduledDailyAggregation,firestore:rules --project vida-ativa-staging
    ```

    Expected: Deploy completes successfully. Output mostra `Function URL: ...` para `onBookingStateChange` e `scheduledDailyAggregation`. Rules deploy mostra "released".

    Se deploy falhar com syntax error em rules, voltar para Task 2 e corrigir.
    Se deploy falhar com syntax error em functions, voltar para Plan 02 e corrigir.

    **STEP 2 — Trigger inicial via criação de booking (DASH-01, DASH-02):**

    No app staging (https://vida-ativa-staging.web.app):
    1. Fazer login como admin
    2. Criar 1 reserva on_arrival confirmada para HOJE (data dentro da semana corrente)
    3. Anotar o `price` da reserva (ex: 80.00)

    No Firebase Console → Firestore → `config/dashboard/periods/week`:
    - `confirmedBookings` deve ser >= 1
    - `totalBookings` deve ser >= 1
    - `totalSlotsBooked` deve ser >= 1
    - `totalRevenue` deve ser >= price (ex: 80.0)
    - `onArrivalRevenue` deve ser >= price (ex: 80.0)
    - `pixRevenue` inalterado
    - `updatedAt` deve ser timestamp recente

    Repetir para `month` e `year` (mesma reserva afeta os 3 docs porque hoje está em todos os 3 períodos correntes).

    **STEP 3 — Verificar bloqueio de write do cliente (T-21-01 / D-16):**

    No Firebase Console → Rules Playground:
    - Path: `/config/dashboard/periods/week`
    - Authenticated: yes
    - Auth.uid: qualquer userId admin existente
    - Operation: `update`
    - Resource data: `{}`, request data: `{ totalRevenue: 9999 }`
    - Click "Run"

    Expected: `Simulated denied` (porque `allow write: if false`).

    Repetir com Operation: `read`, mesmo userId admin → Expected: `Simulated allowed`.
    Repetir com Operation: `read`, userId de cliente comum (não admin) → Expected: `Simulated denied`.

    **STEP 4 — Acionar scheduled manualmente (DASH-09..12):**

    ```bash
    firebase functions:shell --project vida-ativa-staging
    > scheduledDailyAggregation()
    ```

    Aguardar conclusão (~5-30s). No Firebase Console → Firestore → `config/dashboard/periods/week`:
    - `totalSlotsAvailable` deve ser > 0 (slots ativos da semana)
    - `occupancyRate` entre 0 e 1
    - `avgTicket` > 0 se há confirmados
    - `topClients` array (pode estar vazio em staging)
    - `revenueBySport` array
    - `uniqueClients`, `newClients`, `returnRate`, `noShowRate`, `conversionRate` devem existir

    **STEP 5 — Cancelar a reserva criada no Step 2 (transição confirmed→cancelled):**

    Cancelar a reserva via UI admin staging. Em `config/dashboard/periods/week`:
    - `confirmedBookings` decrementou em 1
    - `cancelledBookings` incrementou em 1
    - `totalRevenue` decrementou em price
    - `onArrivalRevenue` decrementou em price
    - `totalSlotsBooked` decrementou em 1

    **STEP 6 — Reportar resultados ao usuário humano.**

    Se algum Step falhar, descrever:
    - O Step que falhou
    - O comportamento observado vs. esperado
    - Logs relevantes (Firebase Console → Functions → Logs)

    Commit (após approval): `chore(21-03): verify staging deploy + dashboard CFs + rules (manual checkpoint)`
  </action>
  <what-built>
    - `DashboardCubit` provisionado em `AdminScreen` (Task 1)
    - Regra Firestore `/config/dashboard/periods/{period}` restringindo read a admin e bloqueando write client (Task 2)
    - Cloud Functions `onBookingStateChange` e `scheduledDailyAggregation` prontas no `functions/index.js` (Plan 02)
  </what-built>
  <how-to-verify>
    Seguir os 6 STEPS descritos em `<action>` acima:
    1. Deploy staging com `firebase deploy --only functions:onBookingStateChange,functions:scheduledDailyAggregation,firestore:rules --project vida-ativa-staging`
    2. Criar booking confirmed → verificar contadores incrementaram em /config/dashboard/periods/week
    3. Rules Playground: admin read = allowed, client read = denied, admin write = denied
    4. `firebase functions:shell` → `scheduledDailyAggregation()` → verificar campos calculados populados
    5. Cancelar booking → verificar decrement
    6. Reportar.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — see how-to-verify steps 1-6"</automated>
  </verify>
  <acceptance_criteria>
    - Deploy staging completou sem erros
    - `/config/dashboard/periods/week` doc existe após Step 2
    - Rules Playground confirma: admin read allowed, non-admin read denied, any write denied
    - Após Step 4, doc week contém campos: `totalSlotsAvailable`, `occupancyRate`, `avgTicket`, `topClients`, `revenueBySport`, `uniqueClients`, `newClients`, `returnRate`, `noShowRate`, `conversionRate`
    - Após Step 5, decrements correspondem ao price da reserva cancelada
    - Usuário humano respondeu "approved"
  </acceptance_criteria>
  <done>Deploy staging validado, rules + CFs verificados em ambiente real, usuário aprovou.</done>
  <resume-signal>Digite "approved" se todos os 6 steps passaram, ou descreva problemas encontrados (qual step, comportamento observado, logs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente Flutter → /config/dashboard/periods | Untrusted; bloqueio aplicado neste plano |
| Cloud Function (admin SDK) → /config/dashboard/periods | Trusted; admin SDK bypassa rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | Cliente Flutter (admin ou não) escreve direto em /config/dashboard/periods → contadores corrompidos | mitigate | `allow write: if false` na regra `/config/dashboard/periods/{period}` (Task 2); validação manual no Step 3 do checkpoint |
| T-21-10 | Information Disclosure | Cliente NÃO admin lê dados de receita via /config/dashboard/periods | mitigate | `allow read: if isAdmin()` na regra; validação manual no Step 3 do checkpoint com Rules Playground |
| T-21-11 | Privilege Escalation | Regra de wildcard `/config/{docId}` (`allow read: if isAuthenticated()`) vence a regra específica → não-admin lê | mitigate | Bloco específico posicionado ANTES do wildcard (Pitfall 5); validação no Step 3 do checkpoint |
</threat_model>

<verification>
- Task 1 verificado por `flutter analyze` + `flutter test` (automated)
- Task 2 verificado por grep do match block (automated)
- Task 3 verificado manualmente em staging — 6 steps cobrem: deploy ok, onBookingStateChange incrementa, rule bloqueia write client, scheduled popula campos calculados, transição confirmed→cancelled decrementa
</verification>

<success_criteria>
- AdminScreen.build() retorna BlocProvider<DashboardCubit> envolvendo o fluxo existente
- firestore.rules tem bloco `/config/dashboard/periods/{period}` ANTES do wildcard
- Deploy staging bem-sucedido com os 2 exports novos
- Cliente não-admin recebe denied em read; admin recebe allowed
- Qualquer cliente recebe denied em write
- onBookingStateChange incrementa contadores em booking de teste real
- scheduledDailyAggregation popula campos calculados em booking de teste real
- Transição confirmed→cancelled decrementa contadores corretamente
</success_criteria>

<output>
After completion, create `.planning/phases/21-backend-do-dashboard/21-03-SUMMARY.md`
</output>
