---
phase: 21-backend-do-dashboard
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/core/models/dashboard_data.dart
  - lib/features/admin/cubit/dashboard_state.dart
  - lib/features/admin/cubit/dashboard_cubit.dart
  - test/core/models/dashboard_data_test.dart
  - test/features/admin/cubit/dashboard_cubit_test.dart
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-09
  - DASH-10
  - DASH-11
  - DASH-12
tags:
  - flutter
  - cubit
  - firestore
  - dashboard
must_haves:
  truths:
    - "DashboardData.fromMap(null) retorna instância com contadores zerados (sem throw)"
    - "DashboardData.fromMap(map_completo) parseia todos os 9 contadores simples + 9 campos calculados"
    - "DashboardData.fromMap parseia topClients como List<TopClientEntry> e revenueBySport como List<RevenueBySportEntry>"
    - "DashboardCubit emite DashboardLoading no construtor"
    - "DashboardCubit emite DashboardLoaded(week, month, year) quando subcollection periods recebe snapshot com 3 docs"
    - "DashboardCubit emite DashboardError em Sentry quando stream falha"
    - "DashboardCubit cancela subscription em close()"
  artifacts:
    - path: "lib/core/models/dashboard_data.dart"
      provides: "DashboardData immutable model + TopClientEntry + RevenueBySportEntry + fromMap factory tolerante a null"
      contains: "class DashboardData"
    - path: "lib/features/admin/cubit/dashboard_state.dart"
      provides: "DashboardInitial, DashboardLoading, DashboardLoaded, DashboardError sealed-like states"
      contains: "class DashboardLoaded"
    - path: "lib/features/admin/cubit/dashboard_cubit.dart"
      provides: "Cubit lendo /config/dashboard/periods collection via StreamSubscription<QuerySnapshot>"
      contains: "class DashboardCubit"
    - path: "test/core/models/dashboard_data_test.dart"
      provides: "Coverage de DashboardData.fromMap (null, vazio, completo, parcial, sport=null em revenueBySport)"
      min_lines: 40
    - path: "test/features/admin/cubit/dashboard_cubit_test.dart"
      provides: "Coverage de stream → DashboardLoaded com 3 períodos, erro → DashboardError, close cancela sub"
      min_lines: 60
  key_links:
    - from: "lib/features/admin/cubit/dashboard_cubit.dart"
      to: "FirebaseFirestore.instance.collection('config').doc('dashboard').collection('periods')"
      via: "snapshots() listener"
      pattern: "collection\\('config'\\)\\.doc\\('dashboard'\\)\\.collection\\('periods'\\)"
    - from: "lib/features/admin/cubit/dashboard_cubit.dart"
      to: "DashboardData.fromMap"
      via: "factory invocation per QueryDocumentSnapshot"
      pattern: "DashboardData\\.fromMap"
---

<objective>
Criar o lado Flutter do dashboard: model `DashboardData` (com todos os campos do schema definido em D-03), states `DashboardLoading/Loaded/Error`, e `DashboardCubit` que escuta a subcollection `/config/dashboard/periods` em tempo real. Implementar TDD: testes primeiro, código depois.

Purpose: Fornecer a camada de leitura reativa que a UI da Phase 22 vai consumir. Cobre DASH-01..04 e DASH-09..12 via parsing tolerante a null (doc pode não existir no primeiro deploy).

Output: 3 arquivos de produção + 2 arquivos de teste, todos os testes verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-backend-do-dashboard/21-CONTEXT.md
@.planning/phases/21-backend-do-dashboard/21-RESEARCH.md
@lib/features/admin/cubit/pricing_cubit.dart
@lib/features/admin/cubit/sport_config_cubit.dart
@test/features/admin/cubit/pricing_cubit_test.dart
@lib/core/models/booking_model.dart

<interfaces>
<!-- Padrões diretos do codebase — replicar sem inventar. -->

Padrão de Cubit com StreamSubscription (de pricing_cubit.dart):
```dart
class PricingCubit extends Cubit<PricingState> {
  final FirebaseFirestore _firestore;
  StreamSubscription<DocumentSnapshot<Map<String, dynamic>>>? _sub;

  PricingCubit({required FirebaseFirestore firestore})
      : _firestore = firestore,
        super(const PricingInitial()) {
    _startStream();
  }

  void _startStream() {
    _sub = _firestore.collection('config').doc('pricing').snapshots().listen(
      (snap) { emit(PricingLoaded(...)); },
      onError: (e, s) {
        Sentry.captureException(e, stackTrace: s);
        emit(const PricingError('Erro ao carregar preços.'));
      },
    );
  }

  @override
  Future<void> close() { _sub?.cancel(); return super.close(); }
}
```

Padrão de Fakes de teste (de pricing_cubit_test.dart): `_FakeFirestore` → `_FakeCollRef` → `_FakeDocRef`. Adaptar criando `_FakeQuerySnapshot` + `_FakeQueryDocSnapshot` para escutar subcollection.

Schema dos contadores (D-03, RESEARCH §Pattern 4):
```dart
// Contadores simples (set por onBookingStateChange)
totalBookings, confirmedBookings, cancelledBookings, pendingBookings,
totalSlotsBooked: int
totalRevenue, pixRevenue, onArrivalRevenue: double

// Calculados pelo scheduled (NULLABLE — podem não existir no primeiro dia)
totalSlotsAvailable: int?
occupancyRate, avgTicket, conversionRate, noShowRate, returnRate: double?
uniqueClients, newClients: int?
topClients: List<TopClientEntry>?  // {userId, displayName, bookingCount}
revenueBySport: List<RevenueBySportEntry>?  // {sport, revenue}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir DashboardData model + entries + testes (RED→GREEN)</name>
  <files>
    lib/core/models/dashboard_data.dart,
    test/core/models/dashboard_data_test.dart
  </files>
  <read_first>
    - lib/core/models/booking_model.dart (padrão de fromFirestore + Equatable)
    - lib/core/models/price_tier_model.dart (padrão de listFromFirestore + toMap)
    - lib/core/models/slot_model.dart (padrão de Equatable simples)
    - .planning/phases/21-backend-do-dashboard/21-CONTEXT.md §"Estrutura dos Campos do Documento de Dashboard"
    - .planning/phases/21-backend-do-dashboard/21-RESEARCH.md §"Pattern 4: DashboardData"
  </read_first>
  <behavior>
    - Test 1: `DashboardData.fromMap(null)` retorna instância com period='', startDate='', endDate='', updatedAt=null, e TODOS os contadores int = 0, double = 0.0, e todos os campos calculados (occupancyRate etc) = null, topClients = null, revenueBySport = null.
    - Test 2: `DashboardData.fromMap({})` (map vazio) tem mesmo comportamento que null exceto que period/startDate/endDate continuam vazios.
    - Test 3: `DashboardData.fromMap(mapCompleto)` com todos os campos preenchidos parseia corretamente — totalBookings=10, confirmedBookings=8, cancelledBookings=2, pendingBookings=0, totalSlotsBooked=8, totalRevenue=800.0, pixRevenue=500.0, onArrivalRevenue=300.0, totalSlotsAvailable=20, occupancyRate=0.4, avgTicket=100.0, conversionRate=0.8, noShowRate=0.1, uniqueClients=5, newClients=2, returnRate=0.6.
    - Test 4: `topClients` parseia List<TopClientEntry> de `[{userId: 'u1', displayName: 'João', bookingCount: 5}]` → entrada com userId='u1', displayName='João', bookingCount=5. Aceita lista vazia → topClients=[].
    - Test 5: `revenueBySport` parseia `[{sport: 'Vôlei', revenue: 200.0}, {sport: null, revenue: 100.0}]` → entrada com sport=null preservada (representa "Não informado" da DASH-12).
    - Test 6: Map com apenas contadores simples (sem campos calculados) → calculados todos null mas contadores corretos. Simula primeira execução antes do scheduled rodar.
    - Test 7: `updatedAt` parseado de `Timestamp` Firestore quando presente; null quando ausente.
    - Test 8: Equatable — duas instâncias com mesmos campos são `==`.
  </behavior>
  <action>
    PASSO RED — Criar `test/core/models/dashboard_data_test.dart`:

    Importar `package:cloud_firestore/cloud_firestore.dart` para `Timestamp`, `package:flutter_test/flutter_test.dart`, e `package:vida_ativa/core/models/dashboard_data.dart`.

    Estrutura de testes em `void main() { group('DashboardData.fromMap', () { ... }); group('TopClientEntry/RevenueBySportEntry', () { ... }); }`.

    Test 1 (null map):
    ```dart
    test('fromMap(null) returns zeroed instance with nullable calculated fields null', () {
      final d = DashboardData.fromMap(null);
      expect(d.period, '');
      expect(d.startDate, '');
      expect(d.endDate, '');
      expect(d.updatedAt, isNull);
      expect(d.totalBookings, 0);
      expect(d.confirmedBookings, 0);
      expect(d.cancelledBookings, 0);
      expect(d.pendingBookings, 0);
      expect(d.totalSlotsBooked, 0);
      expect(d.totalRevenue, 0.0);
      expect(d.pixRevenue, 0.0);
      expect(d.onArrivalRevenue, 0.0);
      expect(d.totalSlotsAvailable, isNull);
      expect(d.occupancyRate, isNull);
      expect(d.avgTicket, isNull);
      expect(d.conversionRate, isNull);
      expect(d.noShowRate, isNull);
      expect(d.uniqueClients, isNull);
      expect(d.newClients, isNull);
      expect(d.returnRate, isNull);
      expect(d.topClients, isNull);
      expect(d.revenueBySport, isNull);
    });
    ```

    Test 3 (mapa completo):
    ```dart
    test('fromMap parses all fields when present', () {
      final ts = Timestamp.fromDate(DateTime.utc(2026, 5, 20, 6, 0));
      final d = DashboardData.fromMap({
        'period': 'week',
        'startDate': '2026-05-19',
        'endDate': '2026-05-25',
        'updatedAt': ts,
        'totalBookings': 10,
        'confirmedBookings': 8,
        'cancelledBookings': 2,
        'pendingBookings': 0,
        'totalSlotsBooked': 8,
        'totalRevenue': 800.0,
        'pixRevenue': 500.0,
        'onArrivalRevenue': 300.0,
        'totalSlotsAvailable': 20,
        'occupancyRate': 0.4,
        'avgTicket': 100.0,
        'conversionRate': 0.8,
        'noShowRate': 0.1,
        'uniqueClients': 5,
        'newClients': 2,
        'returnRate': 0.6,
        'topClients': [
          {'userId': 'u1', 'displayName': 'João', 'bookingCount': 5},
        ],
        'revenueBySport': [
          {'sport': 'Vôlei', 'revenue': 200.0},
          {'sport': null, 'revenue': 100.0},
        ],
      });
      expect(d.period, 'week');
      expect(d.startDate, '2026-05-19');
      expect(d.endDate, '2026-05-25');
      expect(d.updatedAt, ts.toDate());
      expect(d.totalBookings, 10);
      expect(d.confirmedBookings, 8);
      expect(d.totalRevenue, 800.0);
      expect(d.pixRevenue, 500.0);
      expect(d.onArrivalRevenue, 300.0);
      expect(d.totalSlotsAvailable, 20);
      expect(d.occupancyRate, 0.4);
      expect(d.avgTicket, 100.0);
      expect(d.conversionRate, 0.8);
      expect(d.noShowRate, 0.1);
      expect(d.uniqueClients, 5);
      expect(d.newClients, 2);
      expect(d.returnRate, 0.6);
      expect(d.topClients, hasLength(1));
      expect(d.topClients!.first.userId, 'u1');
      expect(d.topClients!.first.displayName, 'João');
      expect(d.topClients!.first.bookingCount, 5);
      expect(d.revenueBySport, hasLength(2));
      expect(d.revenueBySport![0].sport, 'Vôlei');
      expect(d.revenueBySport![0].revenue, 200.0);
      expect(d.revenueBySport![1].sport, isNull);
      expect(d.revenueBySport![1].revenue, 100.0);
    });
    ```

    Cobrir também Test 2, 4, 5, 6, 7, 8 conforme `<behavior>`.

    Rodar `flutter test test/core/models/dashboard_data_test.dart` → deve FALHAR (file doesn't exist) — confirma RED.

    PASSO GREEN — Criar `lib/core/models/dashboard_data.dart`:

    ```dart
    import 'package:cloud_firestore/cloud_firestore.dart';
    import 'package:equatable/equatable.dart';

    class TopClientEntry extends Equatable {
      final String userId;
      final String displayName;
      final int bookingCount;
      const TopClientEntry({
        required this.userId,
        required this.displayName,
        required this.bookingCount,
      });
      factory TopClientEntry.fromMap(Map<String, dynamic> m) => TopClientEntry(
            userId: (m['userId'] as String?) ?? '',
            displayName: (m['displayName'] as String?) ?? '',
            bookingCount: (m['bookingCount'] as num?)?.toInt() ?? 0,
          );
      @override
      List<Object?> get props => [userId, displayName, bookingCount];
    }

    class RevenueBySportEntry extends Equatable {
      final String? sport; // null = "Não informado" (DASH-12)
      final double revenue;
      const RevenueBySportEntry({required this.sport, required this.revenue});
      factory RevenueBySportEntry.fromMap(Map<String, dynamic> m) =>
          RevenueBySportEntry(
            sport: m['sport'] as String?,
            revenue: (m['revenue'] as num?)?.toDouble() ?? 0.0,
          );
      @override
      List<Object?> get props => [sport, revenue];
    }

    class DashboardData extends Equatable {
      // Identificação
      final String period;
      final String startDate;
      final String endDate;
      final DateTime? updatedAt;

      // Contadores simples (mantidos por onBookingStateChange)
      final int totalBookings;
      final int confirmedBookings;
      final int cancelledBookings;
      final int pendingBookings;
      final int totalSlotsBooked;
      final double totalRevenue;
      final double pixRevenue;
      final double onArrivalRevenue;

      // Calculados pelo scheduledDailyAggregation — NULLABLE
      final int? totalSlotsAvailable;
      final double? occupancyRate;
      final double? avgTicket;
      final double? conversionRate;
      final double? noShowRate;
      final int? uniqueClients;
      final int? newClients;
      final double? returnRate;
      final List<TopClientEntry>? topClients;
      final List<RevenueBySportEntry>? revenueBySport;

      const DashboardData({
        required this.period,
        required this.startDate,
        required this.endDate,
        required this.updatedAt,
        required this.totalBookings,
        required this.confirmedBookings,
        required this.cancelledBookings,
        required this.pendingBookings,
        required this.totalSlotsBooked,
        required this.totalRevenue,
        required this.pixRevenue,
        required this.onArrivalRevenue,
        required this.totalSlotsAvailable,
        required this.occupancyRate,
        required this.avgTicket,
        required this.conversionRate,
        required this.noShowRate,
        required this.uniqueClients,
        required this.newClients,
        required this.returnRate,
        required this.topClients,
        required this.revenueBySport,
      });

      factory DashboardData.empty(String period) => DashboardData(
            period: period,
            startDate: '',
            endDate: '',
            updatedAt: null,
            totalBookings: 0,
            confirmedBookings: 0,
            cancelledBookings: 0,
            pendingBookings: 0,
            totalSlotsBooked: 0,
            totalRevenue: 0.0,
            pixRevenue: 0.0,
            onArrivalRevenue: 0.0,
            totalSlotsAvailable: null,
            occupancyRate: null,
            avgTicket: null,
            conversionRate: null,
            noShowRate: null,
            uniqueClients: null,
            newClients: null,
            returnRate: null,
            topClients: null,
            revenueBySport: null,
          );

      factory DashboardData.fromMap(Map<String, dynamic>? map) {
        if (map == null) return DashboardData.empty('');
        final updatedAtRaw = map['updatedAt'];
        final topClientsRaw = map['topClients'] as List<dynamic>?;
        final revenueBySportRaw = map['revenueBySport'] as List<dynamic>?;
        return DashboardData(
          period: (map['period'] as String?) ?? '',
          startDate: (map['startDate'] as String?) ?? '',
          endDate: (map['endDate'] as String?) ?? '',
          updatedAt: updatedAtRaw is Timestamp ? updatedAtRaw.toDate() : null,
          totalBookings: (map['totalBookings'] as num?)?.toInt() ?? 0,
          confirmedBookings: (map['confirmedBookings'] as num?)?.toInt() ?? 0,
          cancelledBookings: (map['cancelledBookings'] as num?)?.toInt() ?? 0,
          pendingBookings: (map['pendingBookings'] as num?)?.toInt() ?? 0,
          totalSlotsBooked: (map['totalSlotsBooked'] as num?)?.toInt() ?? 0,
          totalRevenue: (map['totalRevenue'] as num?)?.toDouble() ?? 0.0,
          pixRevenue: (map['pixRevenue'] as num?)?.toDouble() ?? 0.0,
          onArrivalRevenue: (map['onArrivalRevenue'] as num?)?.toDouble() ?? 0.0,
          totalSlotsAvailable: (map['totalSlotsAvailable'] as num?)?.toInt(),
          occupancyRate: (map['occupancyRate'] as num?)?.toDouble(),
          avgTicket: (map['avgTicket'] as num?)?.toDouble(),
          conversionRate: (map['conversionRate'] as num?)?.toDouble(),
          noShowRate: (map['noShowRate'] as num?)?.toDouble(),
          uniqueClients: (map['uniqueClients'] as num?)?.toInt(),
          newClients: (map['newClients'] as num?)?.toInt(),
          returnRate: (map['returnRate'] as num?)?.toDouble(),
          topClients: topClientsRaw
              ?.map((e) => TopClientEntry.fromMap(e as Map<String, dynamic>))
              .toList(),
          revenueBySport: revenueBySportRaw
              ?.map((e) => RevenueBySportEntry.fromMap(e as Map<String, dynamic>))
              .toList(),
        );
      }

      @override
      List<Object?> get props => [
            period, startDate, endDate, updatedAt,
            totalBookings, confirmedBookings, cancelledBookings, pendingBookings,
            totalSlotsBooked, totalRevenue, pixRevenue, onArrivalRevenue,
            totalSlotsAvailable, occupancyRate, avgTicket, conversionRate,
            noShowRate, uniqueClients, newClients, returnRate,
            topClients, revenueBySport,
          ];
    }
    ```

    Rodar `flutter test test/core/models/dashboard_data_test.dart` → todos os testes verdes.

    Commit 1 (RED): `test(21-01): add failing tests for DashboardData.fromMap`
    Commit 2 (GREEN): `feat(21-01): add DashboardData model with nullable calculated fields (DASH-01..04, DASH-09..12)`
  </action>
  <verify>
    <automated>flutter test test/core/models/dashboard_data_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/models/dashboard_data.dart` contém `class DashboardData extends Equatable`
    - `lib/core/models/dashboard_data.dart` contém `class TopClientEntry extends Equatable`
    - `lib/core/models/dashboard_data.dart` contém `class RevenueBySportEntry extends Equatable`
    - `lib/core/models/dashboard_data.dart` contém `factory DashboardData.fromMap(Map<String, dynamic>? map)`
    - `lib/core/models/dashboard_data.dart` contém `factory DashboardData.empty(String period)`
    - `lib/core/models/dashboard_data.dart` contém os 18 campos: `totalBookings, confirmedBookings, cancelledBookings, pendingBookings, totalSlotsBooked, totalRevenue, pixRevenue, onArrivalRevenue, totalSlotsAvailable, occupancyRate, avgTicket, conversionRate, noShowRate, uniqueClients, newClients, returnRate, topClients, revenueBySport`
    - `test/core/models/dashboard_data_test.dart` contém pelo menos 6 `test(...)` blocks
    - `flutter test test/core/models/dashboard_data_test.dart` exit code 0
  </acceptance_criteria>
  <done>DashboardData parseia mapa nulo, vazio, parcial e completo; commits RED+GREEN separados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardState + DashboardCubit + testes (RED→GREEN)</name>
  <files>
    lib/features/admin/cubit/dashboard_state.dart,
    lib/features/admin/cubit/dashboard_cubit.dart,
    test/features/admin/cubit/dashboard_cubit_test.dart
  </files>
  <read_first>
    - lib/core/models/dashboard_data.dart (criado em Task 1)
    - lib/features/admin/cubit/pricing_cubit.dart (padrão StreamSubscription + Sentry)
    - lib/features/admin/cubit/pricing_state.dart (padrão de states com Equatable)
    - lib/features/admin/cubit/sport_config_cubit.dart (mesma família, mais recente)
    - test/features/admin/cubit/pricing_cubit_test.dart (Fake Firestore template)
    - .planning/phases/21-backend-do-dashboard/21-CONTEXT.md §D-13..D-15
    - .planning/phases/21-backend-do-dashboard/21-RESEARCH.md §"Pattern 3: DashboardCubit"
  </read_first>
  <behavior>
    - Test 1: Cubit no construtor emite `DashboardLoading()` como estado inicial (ou `DashboardInitial()` se preferir mesmo padrão de PricingInitial).
    - Test 2: Ao receber snapshot da subcollection `/config/dashboard/periods` com 3 docs (week, month, year), emite `DashboardLoaded` com os 3 DashboardData parseados corretamente — `state.week.totalBookings`, `state.month.totalRevenue`, `state.year.confirmedBookings`.
    - Test 3: Snapshot com apenas o doc `week` (month e year ainda não criados) → emite `DashboardLoaded` com week populado e month/year = `DashboardData.empty('month')` / `DashboardData.empty('year')`.
    - Test 4: Stream emite erro → emite `DashboardError('Erro ao carregar dashboard.')`.
    - Test 5: `close()` cancela a subscription (sem throw em writes subsequentes ao controller).
    - Test 6 (DASH-04 nullable resilience): Snapshot com doc week SEM campo `noShowRate` → `state.week.noShowRate` é null (não throw).
  </behavior>
  <action>
    PASSO 1 — Criar `lib/features/admin/cubit/dashboard_state.dart`:

    ```dart
    import 'package:equatable/equatable.dart';
    import 'package:vida_ativa/core/models/dashboard_data.dart';

    abstract class DashboardState extends Equatable {
      const DashboardState();
      @override
      List<Object?> get props => [];
    }

    class DashboardInitial extends DashboardState {
      const DashboardInitial();
    }

    class DashboardLoading extends DashboardState {
      const DashboardLoading();
    }

    class DashboardLoaded extends DashboardState {
      final DashboardData week;
      final DashboardData month;
      final DashboardData year;
      const DashboardLoaded({
        required this.week,
        required this.month,
        required this.year,
      });
      @override
      List<Object?> get props => [week, month, year];
    }

    class DashboardError extends DashboardState {
      final String message;
      const DashboardError(this.message);
      @override
      List<Object?> get props => [message];
    }
    ```

    PASSO 2 (RED) — Criar `test/features/admin/cubit/dashboard_cubit_test.dart`:

    Replicar estrutura de `pricing_cubit_test.dart` mas adaptar Fakes para `CollectionReference` + `QuerySnapshot` em subcollection. Estrutura mínima:

    ```dart
    import 'dart:async';
    import 'package:cloud_firestore/cloud_firestore.dart';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:vida_ativa/core/models/dashboard_data.dart';
    import 'package:vida_ativa/features/admin/cubit/dashboard_cubit.dart';
    import 'package:vida_ativa/features/admin/cubit/dashboard_state.dart';

    class _FakeQueryDocSnapshot extends Fake
        implements QueryDocumentSnapshot<Map<String, dynamic>> {
      final String _id;
      final Map<String, dynamic> _data;
      _FakeQueryDocSnapshot(this._id, this._data);
      @override
      String get id => _id;
      @override
      Map<String, dynamic> data() => _data;
    }

    class _FakeQuerySnapshot extends Fake
        implements QuerySnapshot<Map<String, dynamic>> {
      final List<_FakeQueryDocSnapshot> _docs;
      _FakeQuerySnapshot(this._docs);
      @override
      List<QueryDocumentSnapshot<Map<String, dynamic>>> get docs => _docs.cast();
    }

    class _FakePeriodsColl extends Fake
        implements CollectionReference<Map<String, dynamic>> {
      final StreamController<QuerySnapshot<Map<String, dynamic>>> ctrl =
          StreamController.broadcast();
      @override
      Stream<QuerySnapshot<Map<String, dynamic>>> snapshots({
        bool includeMetadataChanges = false,
        ListenSource source = ListenSource.defaultSource,
      }) => ctrl.stream;
    }

    class _FakeDashboardDoc extends Fake
        implements DocumentReference<Map<String, dynamic>> {
      final _FakePeriodsColl periods;
      _FakeDashboardDoc(this.periods);
      @override
      CollectionReference<Map<String, dynamic>> collection(String path) {
        if (path == 'periods') return periods;
        throw StateError('Unexpected subcollection: $path');
      }
    }

    class _FakeConfigColl extends Fake
        implements CollectionReference<Map<String, dynamic>> {
      final _FakeDashboardDoc dashboardDoc;
      _FakeConfigColl(this.dashboardDoc);
      @override
      DocumentReference<Map<String, dynamic>> doc([String? path]) {
        if (path == 'dashboard') return dashboardDoc;
        throw StateError('Unexpected doc: $path');
      }
    }

    class _FakeFirestore extends Fake implements FirebaseFirestore {
      final _FakeConfigColl configColl;
      _FakeFirestore(this.configColl);
      @override
      CollectionReference<Map<String, dynamic>> collection(String path) {
        if (path == 'config') return configColl;
        throw StateError('Unexpected collection: $path');
      }
    }

    void main() {
      late _FakePeriodsColl periods;
      late _FakeFirestore firestore;

      setUp(() {
        periods = _FakePeriodsColl();
        firestore = _FakeFirestore(_FakeConfigColl(_FakeDashboardDoc(periods)));
      });

      test('initial state is DashboardLoading', () {
        final cubit = DashboardCubit(firestore: firestore);
        expect(cubit.state, isA<DashboardLoading>());
        cubit.close();
      });

      test('emits DashboardLoaded with 3 periods parsed from snapshot', () async {
        final cubit = DashboardCubit(firestore: firestore);
        periods.ctrl.add(_FakeQuerySnapshot([
          _FakeQueryDocSnapshot('week', {
            'period': 'week',
            'totalBookings': 10,
            'confirmedBookings': 8,
            'totalRevenue': 800.0,
            'noShowRate': 0.1,
          }),
          _FakeQueryDocSnapshot('month', {
            'period': 'month',
            'totalBookings': 40,
            'totalRevenue': 3200.0,
          }),
          _FakeQueryDocSnapshot('year', {
            'period': 'year',
            'confirmedBookings': 350,
            'totalRevenue': 28000.0,
          }),
        ]));
        await pumpEventQueue();
        final state = cubit.state as DashboardLoaded;
        expect(state.week.totalBookings, 10);
        expect(state.week.confirmedBookings, 8);
        expect(state.week.totalRevenue, 800.0);
        expect(state.week.noShowRate, 0.1);
        expect(state.month.totalRevenue, 3200.0);
        expect(state.year.confirmedBookings, 350);
        await cubit.close();
      });

      test('emits DashboardLoaded with empty fallbacks when periods missing', () async {
        final cubit = DashboardCubit(firestore: firestore);
        periods.ctrl.add(_FakeQuerySnapshot([
          _FakeQueryDocSnapshot('week', {
            'period': 'week',
            'totalBookings': 5,
          }),
        ]));
        await pumpEventQueue();
        final state = cubit.state as DashboardLoaded;
        expect(state.week.totalBookings, 5);
        expect(state.month.period, 'month');
        expect(state.month.totalBookings, 0);
        expect(state.year.period, 'year');
        expect(state.year.totalBookings, 0);
        await cubit.close();
      });

      test('emits DashboardError on stream error', () async {
        final cubit = DashboardCubit(firestore: firestore);
        periods.ctrl.addError(Exception('boom'));
        await pumpEventQueue();
        expect(cubit.state, isA<DashboardError>());
        expect((cubit.state as DashboardError).message, 'Erro ao carregar dashboard.');
        await cubit.close();
      });

      test('parses doc with missing optional fields without throwing', () async {
        final cubit = DashboardCubit(firestore: firestore);
        periods.ctrl.add(_FakeQuerySnapshot([
          _FakeQueryDocSnapshot('week', {
            'period': 'week',
            'totalBookings': 3,
            // no noShowRate, no occupancyRate, no topClients
          }),
        ]));
        await pumpEventQueue();
        final state = cubit.state as DashboardLoaded;
        expect(state.week.noShowRate, isNull);
        expect(state.week.occupancyRate, isNull);
        expect(state.week.topClients, isNull);
        await cubit.close();
      });
    }
    ```

    Rodar `flutter test test/features/admin/cubit/dashboard_cubit_test.dart` → FALHA (DashboardCubit não existe). RED confirmado.

    PASSO 3 (GREEN) — Criar `lib/features/admin/cubit/dashboard_cubit.dart`:

    ```dart
    import 'dart:async';

    import 'package:cloud_firestore/cloud_firestore.dart';
    import 'package:flutter_bloc/flutter_bloc.dart';
    import 'package:sentry_flutter/sentry_flutter.dart';
    import 'package:vida_ativa/core/models/dashboard_data.dart';
    import 'package:vida_ativa/features/admin/cubit/dashboard_state.dart';

    class DashboardCubit extends Cubit<DashboardState> {
      final FirebaseFirestore _firestore;
      StreamSubscription<QuerySnapshot<Map<String, dynamic>>>? _sub;

      DashboardCubit({required FirebaseFirestore firestore})
          : _firestore = firestore,
            super(const DashboardLoading()) {
        _startStream();
      }

      void _startStream() {
        _sub = _firestore
            .collection('config')
            .doc('dashboard')
            .collection('periods')
            .snapshots()
            .listen(
          (snap) {
            final byId = <String, DashboardData>{};
            for (final doc in snap.docs) {
              byId[doc.id] = DashboardData.fromMap(doc.data());
            }
            emit(DashboardLoaded(
              week: byId['week'] ?? DashboardData.empty('week'),
              month: byId['month'] ?? DashboardData.empty('month'),
              year: byId['year'] ?? DashboardData.empty('year'),
            ));
          },
          onError: (e, s) {
            Sentry.captureException(e, stackTrace: s);
            emit(const DashboardError('Erro ao carregar dashboard.'));
          },
        );
      }

      @override
      Future<void> close() {
        _sub?.cancel();
        return super.close();
      }
    }
    ```

    Rodar `flutter test test/features/admin/cubit/dashboard_cubit_test.dart` → GREEN (todos passam).

    Rodar suite completa: `flutter test`.

    Commits:
    - `test(21-01): add failing tests for DashboardCubit stream + close`
    - `feat(21-01): add DashboardState + DashboardCubit reading /config/dashboard/periods (D-13..D-15)`
  </action>
  <verify>
    <automated>flutter test test/features/admin/cubit/dashboard_cubit_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/admin/cubit/dashboard_state.dart` contém `class DashboardInitial`, `class DashboardLoading`, `class DashboardLoaded`, `class DashboardError`
    - `lib/features/admin/cubit/dashboard_state.dart` contém `final DashboardData week;`, `final DashboardData month;`, `final DashboardData year;`
    - `lib/features/admin/cubit/dashboard_cubit.dart` contém `class DashboardCubit extends Cubit<DashboardState>`
    - `lib/features/admin/cubit/dashboard_cubit.dart` contém `.collection('config').doc('dashboard').collection('periods').snapshots()`
    - `lib/features/admin/cubit/dashboard_cubit.dart` contém `Sentry.captureException`
    - `lib/features/admin/cubit/dashboard_cubit.dart` contém `_sub?.cancel()` em `close()`
    - `test/features/admin/cubit/dashboard_cubit_test.dart` contém pelo menos 5 `test(...)` blocks
    - `flutter test test/features/admin/cubit/dashboard_cubit_test.dart` exit code 0
    - `flutter test` (suite completa) exit code 0
  </acceptance_criteria>
  <done>DashboardCubit lê subcollection periods, emite Loaded com 3 períodos, erro vira DashboardError, close cancela sub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client Flutter → Firestore | Cliente lê `/config/dashboard/periods/*` — leitura restrita a admin via rules (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | DashboardCubit lê dados de receita via subcollection periods | mitigate | Regra `allow read: if isAdmin()` aplicada na Plan 03 em `/config/dashboard/{period}`; este plano apenas lê — rule do Firestore impede usuário não-admin de receber dados |
| T-21-02 | Denial of Service | StreamSubscription nunca cancelada vaza memória ao trocar de tela | mitigate | `close()` cancela `_sub` (acceptance criteria explícito) |
| T-21-03 | Tampering | Parse de map com tipo inesperado (ex: bookingCount=String) explode app | mitigate | `(num?)?.toInt() ?? 0` em todos os parses numéricos no `DashboardData.fromMap` — tolerante a tipos divergentes |
</threat_model>

<verification>
- `flutter test test/core/models/dashboard_data_test.dart` exit 0
- `flutter test test/features/admin/cubit/dashboard_cubit_test.dart` exit 0
- `flutter test` (suite completa) exit 0
- `flutter analyze` sem novos warnings/errors nos arquivos criados
</verification>

<success_criteria>
- DashboardData model existe com 22 campos (4 metadata + 8 contadores + 10 calculados)
- DashboardCubit emite Loading → Loaded(week, month, year) ao receber snapshot
- DashboardCubit emite DashboardError em falha de stream
- Testes RED-GREEN-com-commits-separados conforme TDD protocol
</success_criteria>

<output>
After completion, create `.planning/phases/21-backend-do-dashboard/21-01-SUMMARY.md`
</output>
