---
phase: 17-pix-qr-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - lib/features/booking/ui/booking_confirmation_sheet.dart
  - lib/features/booking/ui/booking_card.dart
  - lib/features/booking/ui/my_bookings_screen.dart
  - lib/features/booking/ui/pix_payment_screen.dart
autonomous: true
requirements:
  - PIX-01
  - PIX-02

must_haves:
  truths:
    - "BookingConfirmationSheet exibe dois botoes: 'Pagar com Pix' e 'Pagar na hora'"
    - "Pagar com Pix: bookSlot(paymentMethod: 'pix') => Navigator.push para PixPaymentScreen"
    - "Pagar na hora: bookSlot(paymentMethod: 'on_arrival') => fecha sheet + snackbar 'Reserva confirmada!'"
    - "PixPaymentScreen abre com loading state (spinner + 'Gerando QR...'), chama createPixPayment CF"
    - "PixPaymentScreen exibe QR image via Image.memory(base64Decode()), texto 'Valido ate HH:mm', botao 'Copiar codigo'"
    - "Botao Copiar muda para 'Copiado' por 2 segundos apos copia"
    - "CF falhar: PixPaymentScreen exibe mensagem de erro + botao 'Tentar novamente'"
    - "Ao sair de PixPaymentScreen: navega para MyBookingsScreen (context.go('/bookings'))"
    - "BookingCard mostra badge 'Aguardando Pix' (amber) para pending_payment"
    - "BookingCard mostra badge 'Pagar na hora' (azul) para confirmed + on_arrival"
    - "BookingCard mostra badge 'Expirada' (cinza) para expired"
    - "Tap em card pending_payment abre PixPaymentScreen lendo PaymentRecord da subcollection"
  artifacts:
    - path: "lib/features/booking/ui/pix_payment_screen.dart"
      provides: "Tela dedicada de QR Pix"
      exports: ["PixPaymentScreen"]
      contains: "createPixPayment"
    - path: "lib/features/booking/ui/booking_confirmation_sheet.dart"
      provides: "Dois botoes de pagamento na confirmacao"
      contains: "Pagar com Pix"
    - path: "lib/features/booking/ui/booking_card.dart"
      provides: "Badges de status de pagamento"
      contains: "Aguardando Pix"
    - path: "lib/features/booking/ui/my_bookings_screen.dart"
      provides: "Tap em pending_payment abre PixPaymentScreen"
      contains: "isPendingPayment"
  key_links:
    - from: "lib/features/booking/ui/booking_confirmation_sheet.dart"
      to: "lib/features/booking/ui/pix_payment_screen.dart"
      via: "Navigator.push com MaterialPageRoute"
      pattern: "PixPaymentScreen"
    - from: "lib/features/booking/ui/pix_payment_screen.dart"
      to: "createPixPayment CF"
      via: "FirebaseFunctions.instance.httpsCallable('createPixPayment')"
      pattern: "httpsCallable"
    - from: "lib/features/booking/ui/my_bookings_screen.dart"
      to: "lib/features/booking/ui/pix_payment_screen.dart"
      via: "Navigator.push quando booking.isPendingPayment"
      pattern: "isPendingPayment"
---

<objective>
Implementar a UI completa do fluxo Pix: BookingConfirmationSheet com dois botoes de pagamento, PixPaymentScreen com QR code, badges no BookingCard, e tap para reabrir QR em Minhas Reservas.

Purpose: Com a camada de dados e CF prontos (Plan 01), a UI pode ser construida contra contratos definidos. O usuario precisa ver o QR, copiar o codigo, e ver o status da reserva corretamente.

Output: PixPaymentScreen nova, BookingConfirmationSheet modificada com dois botoes, BookingCard com novos badges, MyBookingsScreen com tap para pending_payment.
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-pix-qr-generation/17-CONTEXT.md
@.planning/phases/17-pix-qr-generation/17-01-SUMMARY.md

<interfaces>
<!-- Contratos do Plan 01 que esta UI consome. Executor: use esses diretamente. -->

De lib/core/models/booking_model.dart (apos Plan 01):
```dart
// Novos campos
final String? paymentMethod; // 'pix' | 'on_arrival' | null
final DateTime? expiresAt;
final String? paymentId;     // txId Mercado Pago

// Novos getters
bool get isPendingPayment => status == 'pending_payment';
bool get isExpired => status == 'expired';
bool get isOnArrival => isConfirmed && paymentMethod == 'on_arrival';
```

De lib/core/models/payment_record_model.dart (novo, Plan 01):
```dart
class PaymentRecordModel extends Equatable {
  final String id;           // txId = document ID
  final String qrCode;       // copia-e-cola string
  final String qrCodeBase64; // base64 para Image.memory()
  final DateTime expiresAt;
  final String status;       // 'pending' | 'paid' | 'expired'

  factory PaymentRecordModel.fromFirestore(DocumentSnapshot<Map<String, dynamic>> doc);
}
```

De lib/features/booking/cubit/booking_cubit.dart (apos Plan 01):
```dart
// bookSlot() agora aceita paymentMethod obrigatorio
Future<void> bookSlot({
  required String slotId,
  required String dateString,
  required double price,
  required String startTime,
  required String userDisplayName,
  required String paymentMethod, // 'pix' | 'on_arrival'
  String? participants,
  String? recurrenceGroupId,
}) async { ... }
```

Pattern de httpsCallable (de 17-RESEARCH.md):
```dart
final callable = FirebaseFunctions.instance.httpsCallable('createPixPayment');
final result = await callable.call({'bookingId': widget.bookingId});
final data = result.data as Map<String, dynamic>;
// data['qrCode'] as String
// data['qrCodeBase64'] as String
// data['expiresAt'] as String (ISO 8601 — parse com DateTime.parse())
```

Pattern de booking_confirmation_sheet.dart (estado atual — ler arquivo antes de modificar):
```dart
// _handleConfirm() atual: chama bookSlot() sem paymentMethod, depois fecha sheet
// Vai ser bifurcado em _handlePayPix() e _handlePayOnArrival()

// Padrao estabelecido no projeto:
bool _isSubmitting = false;
String? _errorMessage;
// BookingCubit capturado via widget.bookingCubit (fora do builder)
// mounted check obrigatorio apos await
```

Pattern de BookingCard (estado atual — ler arquivo antes de modificar):
```dart
Color _statusColor(String status) => switch (status) {
  'pending' => const Color(0xFFD4860A),
  'confirmed' => AppTheme.primaryGreen,
  'rejected' => Colors.red,
  _ => Colors.grey,
};

String _statusLabel(String status) => switch (status) {
  'pending' => 'Aguardando',
  'confirmed' => 'Confirmado',
  'rejected' => 'Recusado',
  _ => 'Cancelado',
};
// NOTA: badge usa booking.status; on_arrival precisa checar booking.isOnArrival
// (status 'confirmed' mas com paymentMethod 'on_arrival')
```

De go_router (padrao do projeto — ver my_bookings_screen.dart):
```dart
// MyBookingsScreen usa context.go() via GoRouter
// Para navegar para MyBookings de PixPaymentScreen: context.go('/bookings')
// (verificar rota exata no router da app antes de implementar)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar PixPaymentScreen</name>
  <files>
    lib/features/booking/ui/pix_payment_screen.dart
  </files>
  <read_first>
    - lib/features/booking/ui/recurrence_result_sheet.dart — ver padrao de StatefulWidget com _isSubmitting/_errorMessage usado no projeto
    - lib/core/theme/app_theme.dart — ver AppTheme.primaryGreen e cores disponiveis
    - lib/core/models/payment_record_model.dart — ver PaymentRecordModel.fromFirestore (ja existe apos Plan 01)
    - lib/app/router.dart (ou onde o GoRouter e configurado) — confirmar rota '/bookings' para navegacao no WillPopScope
  </read_first>
  <action>
Criar `lib/features/booking/ui/pix_payment_screen.dart` como StatefulWidget.

A tela e usada em dois contextos:
1. **Fluxo inicial** (logo apos booking): recebe `bookingId` + chama CF para gerar QR
2. **Tap em card** (Minhas Reservas): recebe `bookingId` + le PaymentRecord da subcollection (booking.paymentId)

Use parametro opcional `preloadedData` para distinguir os casos (nao usado nesta fase — o tap em card sempre busca da subcollection).

```dart
import 'dart:convert';

import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:cloud_functions/cloud_functions.dart';
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:go_router/go_router.dart';
import 'package:intl/intl.dart';
import 'package:sentry_flutter/sentry_flutter.dart';
import 'package:vida_ativa/core/models/payment_record_model.dart';
import 'package:vida_ativa/core/theme/app_theme.dart';

/// Dedicated screen to display Pix QR code after booking.
///
/// Used in two flows:
///   1. Post-booking (from BookingConfirmationSheet): receives bookingId,
///      calls createPixPayment CF, displays QR immediately.
///   2. Card tap (from MyBookingsScreen): receives bookingId + paymentId,
///      reads existing PaymentRecord from subcollection.
///
/// On exit, navigates to MyBookingsScreen ('/bookings').
class PixPaymentScreen extends StatefulWidget {
  final String bookingId;

  /// If provided, screen loads PaymentRecord from subcollection using this txId.
  /// If null, screen calls createPixPayment CF to generate a new QR.
  final String? paymentId;

  const PixPaymentScreen({
    super.key,
    required this.bookingId,
    this.paymentId,
  });

  @override
  State<PixPaymentScreen> createState() => _PixPaymentScreenState();
}

class _PixPaymentScreenState extends State<PixPaymentScreen> {
  bool _isLoading = true;
  String? _error;
  String? _qrCode;
  String? _qrCodeBase64;
  DateTime? _expiresAt;
  bool _copied = false;

  @override
  void initState() {
    super.initState();
    if (widget.paymentId != null) {
      _loadFromSubcollection();
    } else {
      _generateQr();
    }
  }

  /// Called on initial flow: invokes createPixPayment CF.
  Future<void> _generateQr() async {
    setState(() {
      _isLoading = true;
      _error = null;
    });
    try {
      final callable =
          FirebaseFunctions.instance.httpsCallable('createPixPayment');
      final result = await callable.call({'bookingId': widget.bookingId});
      final data = result.data as Map<String, dynamic>;
      if (!mounted) return;
      setState(() {
        _qrCode = data['qrCode'] as String;
        _qrCodeBase64 = data['qrCodeBase64'] as String;
        _expiresAt = DateTime.parse(data['expiresAt'] as String);
        _isLoading = false;
      });
    } on FirebaseFunctionsException catch (e) {
      if (!mounted) return;
      setState(() {
        _error = e.message ?? 'Erro ao gerar QR. Tente novamente.';
        _isLoading = false;
      });
    } catch (e, s) {
      await Sentry.captureException(e, stackTrace: s);
      if (!mounted) return;
      setState(() {
        _error = 'Erro inesperado. Tente novamente.';
        _isLoading = false;
      });
    }
  }

  /// Called when tapping pending_payment card: reads existing PaymentRecord.
  Future<void> _loadFromSubcollection() async {
    setState(() {
      _isLoading = true;
      _error = null;
    });
    try {
      final snap = await FirebaseFirestore.instance
          .collection('bookings')
          .doc(widget.bookingId)
          .collection('payment')
          .doc(widget.paymentId)
          .withConverter<PaymentRecordModel>(
            fromFirestore: (s, _) => PaymentRecordModel.fromFirestore(s),
            toFirestore: (_, __) => {},
          )
          .get();

      if (!snap.exists || snap.data() == null) {
        if (!mounted) return;
        setState(() {
          _error = 'QR code nao encontrado. Contate o suporte.';
          _isLoading = false;
        });
        return;
      }

      final record = snap.data()!;
      if (!mounted) return;
      setState(() {
        _qrCode = record.qrCode;
        _qrCodeBase64 = record.qrCodeBase64;
        _expiresAt = record.expiresAt;
        _isLoading = false;
      });
    } catch (e, s) {
      await Sentry.captureException(e, stackTrace: s);
      if (!mounted) return;
      setState(() {
        _error = 'Erro ao carregar QR. Tente novamente.';
        _isLoading = false;
      });
    }
  }

  Future<void> _copyCode() async {
    if (_qrCode == null) return;
    await Clipboard.setData(ClipboardData(text: _qrCode!));
    if (!mounted) return;
    setState(() => _copied = true);
    await Future.delayed(const Duration(seconds: 2));
    if (mounted) setState(() => _copied = false);
  }

  String _formatExpiresAt(DateTime dt) {
    return 'Valido ate ${DateFormat('HH:mm').format(dt)}';
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pagamento Pix'),
        leading: BackButton(
          onPressed: () {
            // Always navigate to MyBookings on exit (not just pop)
            // so the reactive stream shows the updated pending_payment booking
            context.go('/bookings');
          },
        ),
      ),
      body: PopScope(
        canPop: false,
        onPopInvokedWithResult: (didPop, _) {
          if (!didPop) context.go('/bookings');
        },
        child: SafeArea(
          child: _isLoading
              ? _buildLoading()
              : _error != null
                  ? _buildError()
                  : _buildQrContent(),
        ),
      ),
    );
  }

  Widget _buildLoading() {
    return const Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          CircularProgressIndicator(),
          SizedBox(height: 16),
          Text(
            'Gerando QR...',
            style: TextStyle(fontSize: 16, color: Color(0xFF757575)),
          ),
        ],
      ),
    );
  }

  Widget _buildError() {
    return Center(
      child: Padding(
        padding: const EdgeInsets.all(24),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(Icons.error_outline, size: 48, color: Color(0xFFC62828)),
            const SizedBox(height: 16),
            Text(
              _error!,
              textAlign: TextAlign.center,
              style: const TextStyle(fontSize: 16),
            ),
            const SizedBox(height: 24),
            FilledButton.icon(
              onPressed: widget.paymentId != null
                  ? _loadFromSubcollection
                  : _generateQr,
              icon: const Icon(Icons.refresh),
              label: const Text('Tentar novamente'),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildQrContent() {
    return SingleChildScrollView(
      padding: const EdgeInsets.all(24),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.center,
        children: [
          const SizedBox(height: 8),
          // Header instruction
          const Text(
            'Escaneie o QR code com seu app de banco',
            textAlign: TextAlign.center,
            style: TextStyle(fontSize: 16, fontWeight: FontWeight.w500),
          ),
          const SizedBox(height: 24),
          // QR image
          Container(
            padding: const EdgeInsets.all(16),
            decoration: BoxDecoration(
              color: Colors.white,
              borderRadius: BorderRadius.circular(16),
              boxShadow: [
                BoxShadow(
                  color: Colors.black.withValues(alpha: 0.08),
                  blurRadius: 16,
                  offset: const Offset(0, 4),
                ),
              ],
            ),
            child: Image.memory(
              base64Decode(_qrCodeBase64!),
              width: 240,
              height: 240,
              fit: BoxFit.contain,
            ),
          ),
          const SizedBox(height: 16),
          // Expiry
          if (_expiresAt != null)
            Text(
              _formatExpiresAt(_expiresAt!),
              style: const TextStyle(
                fontSize: 13,
                color: Color(0xFF757575),
              ),
            ),
          const SizedBox(height: 32),
          // Divider with "ou"
          Row(
            children: [
              const Expanded(child: Divider()),
              Padding(
                padding: const EdgeInsets.symmetric(horizontal: 12),
                child: Text(
                  'ou use o codigo',
                  style: TextStyle(
                    color: Colors.grey[600],
                    fontSize: 13,
                  ),
                ),
              ),
              const Expanded(child: Divider()),
            ],
          ),
          const SizedBox(height: 16),
          // Copia-e-cola
          Container(
            padding: const EdgeInsets.all(12),
            decoration: BoxDecoration(
              color: const Color(0xFFF5F5F5),
              borderRadius: BorderRadius.circular(12),
            ),
            child: Text(
              _qrCode!,
              maxLines: 3,
              overflow: TextOverflow.ellipsis,
              style: const TextStyle(
                fontSize: 12,
                fontFamily: 'monospace',
                color: Color(0xFF424242),
              ),
            ),
          ),
          const SizedBox(height: 12),
          SizedBox(
            width: double.infinity,
            child: OutlinedButton.icon(
              onPressed: _copyCode,
              icon: Icon(
                _copied ? Icons.check : Icons.copy,
                size: 18,
              ),
              label: Text(_copied ? 'Copiado' : 'Copiar codigo'),
              style: OutlinedButton.styleFrom(
                foregroundColor: _copied
                    ? AppTheme.primaryGreen
                    : AppTheme.primaryGreen,
                side: BorderSide(
                  color: _copied
                      ? AppTheme.primaryGreen
                      : AppTheme.primaryGreen.withValues(alpha: 0.5),
                ),
                padding: const EdgeInsets.symmetric(vertical: 14),
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
              ),
            ),
          ),
          const SizedBox(height: 32),
          // Info note
          Container(
            padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 10),
            decoration: BoxDecoration(
              color: const Color(0xFFFFF8E1),
              borderRadius: BorderRadius.circular(10),
              border: Border.all(color: const Color(0xFFFFB300), width: 1),
            ),
            child: const Row(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Icon(Icons.info_outline, size: 16, color: Color(0xFFE65100)),
                SizedBox(width: 8),
                Expanded(
                  child: Text(
                    'Apos pagar, sua reserva sera confirmada automaticamente.',
                    style: TextStyle(fontSize: 12, color: Color(0xFFE65100)),
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

**Verificar rota '/bookings' no GoRouter antes de usar context.go('/bookings').** Se a rota for diferente, usar a rota correta. Buscar o arquivo de router com:
```bash
grep -rn "bookings\|MyBookings" /f/_geral/Projetos/vida_ativa/lib/app/ --include="*.dart" | grep "path\|route\|go_router"
```
  </action>
  <verify>
    grep -n "PixPaymentScreen\|_generateQr\|_loadFromSubcollection\|httpsCallable\|base64Decode\|Clipboard" /f/_geral/Projetos/vida_ativa/lib/features/booking/ui/pix_payment_screen.dart
    flutter analyze /f/_geral/Projetos/vida_ativa/lib/features/booking/ui/pix_payment_screen.dart 2>&1 | tail -5
  </verify>
  <acceptance_criteria>
    - pix_payment_screen.dart existe em lib/features/booking/ui/
    - pix_payment_screen.dart contem `class PixPaymentScreen extends StatefulWidget`
    - pix_payment_screen.dart contem `FirebaseFunctions.instance.httpsCallable('createPixPayment')`
    - pix_payment_screen.dart contem `base64Decode(_qrCodeBase64!)`
    - pix_payment_screen.dart contem `Clipboard.setData(ClipboardData(text: _qrCode!))`
    - pix_payment_screen.dart contem `_copied ? 'Copiado' : 'Copiar codigo'`
    - pix_payment_screen.dart contem `context.go('/bookings')` (ou rota correta verificada)
    - pix_payment_screen.dart contem `Gerando QR...` (loading state text)
    - pix_payment_screen.dart contem `Tentar novamente` (error state button)
    - pix_payment_screen.dart contem `Valido ate` (expiresAt display)
    - `flutter analyze` sem erros neste arquivo
  </acceptance_criteria>
  <done>PixPaymentScreen funcional: loading => QR com Image.memory, copia-e-cola com feedback, error com retry, navegacao para MyBookings ao sair.</done>
</task>

<task type="auto">
  <name>Task 2: Modificar BookingConfirmationSheet e atualizar BookingCard + MyBookingsScreen</name>
  <files>
    lib/features/booking/ui/booking_confirmation_sheet.dart
    lib/features/booking/ui/booking_card.dart
    lib/features/booking/ui/my_bookings_screen.dart
  </files>
  <read_first>
    - lib/features/booking/ui/booking_confirmation_sheet.dart — ler COMPLETO; ver _handleConfirm() atual que sera substituido por dois handlers; ver _paymentWarningBanner() que sera removida ou adaptada; ver o botao FilledButton no build()
    - lib/features/booking/ui/booking_card.dart — ler COMPLETO; ver _statusColor() e _statusLabel() switch expressions que vao receber novos casos; ver o widget build() para entender onde adicionar tap em pending_payment
    - lib/features/booking/ui/my_bookings_screen.dart — ler COMPLETO; ver _showDetailSheet() e onTap do InkWell que precisara bifurcar para PixPaymentScreen quando booking.isPendingPayment
    - lib/core/models/booking_model.dart — confirmar getters isPendingPayment, isOnArrival, isExpired existem (Plan 01)
    - lib/core/utils/snack_helper.dart — ver SnackHelper.success() signature
  </read_first>
  <action>
**booking_confirmation_sheet.dart — substituir _handleConfirm() por dois handlers e atualizar o build:**

1. **Remover** o metodo `_handleConfirm()` existente inteiro.

2. **Remover** o metodo `_paymentWarningBanner()` (o banner antigo de aviso de pagamento sera substituido pela escolha real de metodo).

3. **Adicionar** dois novos handlers:

```dart
/// Fluxo Pix: cria booking com pending_payment, navega para PixPaymentScreen.
Future<void> _handlePayPix() async {
  setState(() {
    _isSubmitting = true;
    _errorMessage = null;
  });
  try {
    final authState = context.read<AuthCubit>().state as AuthAuthenticated;
    await widget.bookingCubit.bookSlot(
      slotId: widget.viewModel.slot.id,
      dateString: widget.viewModel.dateString,
      price: widget.viewModel.slot.price,
      startTime: widget.viewModel.slot.startTime,
      userDisplayName: authState.user.displayName,
      paymentMethod: 'pix',
      participants: _participantsController.text.trim().isEmpty
          ? null
          : _participantsController.text.trim(),
    );
    if (!mounted) return;
    Navigator.pop(context); // fecha a sheet
    // Aguarda um frame para garantir que a sheet fechou antes de push
    WidgetsBinding.instance.addPostFrameCallback((_) {
      if (mounted) {
        Navigator.push(
          context,
          MaterialPageRoute(
            builder: (_) => PixPaymentScreen(
              bookingId: BookingModel.generateId(
                widget.viewModel.slot.id,
                widget.viewModel.dateString,
              ),
            ),
          ),
        );
      }
    });
  } on Exception catch (e, s) {
    final str = e.toString();
    final isExpected =
        str.contains('slot_already_booked') || str.contains('slot_already_passed');
    if (!isExpected) await Sentry.captureException(e, stackTrace: s);
    final msg = str.contains('slot_already_booked')
        ? 'Este horario acabou de ser reservado.'
        : str.contains('slot_already_passed')
            ? 'Este horario ja passou.'
            : 'Falha na conexao. Tente novamente.';
    if (mounted) {
      setState(() {
        _isSubmitting = false;
        _errorMessage = msg;
      });
    }
  }
}

/// Fluxo presencial: cria booking com confirmed + on_arrival, fecha sheet.
Future<void> _handlePayOnArrival() async {
  setState(() {
    _isSubmitting = true;
    _errorMessage = null;
  });
  try {
    final authState = context.read<AuthCubit>().state as AuthAuthenticated;
    await widget.bookingCubit.bookSlot(
      slotId: widget.viewModel.slot.id,
      dateString: widget.viewModel.dateString,
      price: widget.viewModel.slot.price,
      startTime: widget.viewModel.slot.startTime,
      userDisplayName: authState.user.displayName,
      paymentMethod: 'on_arrival',
      participants: _participantsController.text.trim().isEmpty
          ? null
          : _participantsController.text.trim(),
    );
    if (!mounted) return;
    Navigator.pop(context);
    SnackHelper.success(context, 'Reserva confirmada!');
  } on Exception catch (e, s) {
    final str = e.toString();
    final isExpected =
        str.contains('slot_already_booked') || str.contains('slot_already_passed');
    if (!isExpected) await Sentry.captureException(e, stackTrace: s);
    final msg = str.contains('slot_already_booked')
        ? 'Este horario acabou de ser reservado.'
        : str.contains('slot_already_passed')
            ? 'Este horario ja passou.'
            : 'Falha na conexao. Tente novamente.';
    if (mounted) {
      setState(() {
        _isSubmitting = false;
        _errorMessage = msg;
      });
    }
  }
}
```

4. **Adicionar import** de PixPaymentScreen e BookingModel no topo do arquivo:
```dart
import 'package:vida_ativa/core/models/booking_model.dart';
import 'package:vida_ativa/features/booking/ui/pix_payment_screen.dart';
```

5. **No build() — substituir** o bloco do botao `FilledButton` unico (no final do Column) por dois botoes empilhados. Substituir `_paymentWarningBanner()` (se ainda estiver no Column) por um texto simples "Como voce prefere pagar?":

Localizar o widget `FilledButton` atual no final do Column e substituir por:
```dart
// Substituir o FilledButton unico pelo par de botoes
if (!_isRecurrent) ...[
  // Titulo da secao de pagamento
  const Text(
    'Como voce prefere pagar?',
    style: TextStyle(fontSize: 15, fontWeight: FontWeight.w500),
  ),
  const SizedBox(height: 12),
  // Botao Pix
  FilledButton.icon(
    onPressed: _isSubmitting ? null : _handlePayPix,
    icon: const Icon(Icons.qr_code, size: 20),
    label: const Text('Pagar com Pix'),
    style: FilledButton.styleFrom(
      minimumSize: const Size(double.infinity, 52),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(12),
      ),
    ),
  ),
  const SizedBox(height: 10),
  // Botao na hora
  OutlinedButton.icon(
    onPressed: _isSubmitting ? null : _handlePayOnArrival,
    icon: const Icon(Icons.handshake_outlined, size: 20),
    label: const Text('Pagar na hora'),
    style: OutlinedButton.styleFrom(
      minimumSize: const Size(double.infinity, 52),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(12),
      ),
    ),
  ),
] else ...[
  // Recorrente continua com um unico botao (bookRecurring)
  FilledButton(
    onPressed: _isSubmitting ? null : _handleConfirmRecurring,
    style: FilledButton.styleFrom(
      minimumSize: const Size(double.infinity, 52),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(12),
      ),
    ),
    child: _isSubmitting
        ? const SizedBox(
            width: 20,
            height: 20,
            child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white),
          )
        : Text(_availableRecurrenceEntries.isEmpty
            ? 'Reservar semanalmente'
            : 'Reservar ${_availableRecurrenceEntries.length} reserva${_availableRecurrenceEntries.length != 1 ? "s" : ""}'),
  ),
]
```

NOTA: `_handleConfirmRecurring()` ja passa `paymentMethod` a bookSlot? Verificar se bookRecurring tambem ganhou `paymentMethod` no Plan 01. Se sim, passar `paymentMethod: 'pix'` como padrao no `_handleConfirmRecurring()` (recorrente so suporta Pix por ora). Verificar antes de salvar.

---

**booking_card.dart — adicionar novos status:**

No metodo `_statusColor()`, adicionar casos antes do `_`:
```dart
Color _statusColor(String status) => switch (status) {
  'pending' => const Color(0xFFD4860A),
  'pending_payment' => const Color(0xFFE65100), // deep orange/amber
  'confirmed' => AppTheme.primaryGreen,
  'expired' => Colors.grey,
  'rejected' => Colors.red,
  _ => Colors.grey,
};
```

No metodo `_statusLabel()`:
```dart
String _statusLabel(String status) => switch (status) {
  'pending' => 'Aguardando',
  'pending_payment' => 'Aguardando Pix',
  'confirmed' => 'Confirmado',
  'expired' => 'Expirada',
  'rejected' => 'Recusado',
  _ => 'Cancelado',
};
```

ATENCAO: badge "Pagar na hora" nao pode usar apenas o status switch porque `isOnArrival` depende de status + paymentMethod. Substituir `_statusBadge(booking.status)` por uma versao que recebe o BookingModel:

Modificar a chamada de `_statusBadge` no build():
```dart
// Antes:
_statusBadge(booking.status),

// Depois:
_statusBadge(booking.status, booking: booking),
```

Modificar assinatura e implementacao de `_statusBadge`:
```dart
Widget _statusBadge(String status, {BookingModel? booking}) {
  // Special case: on_arrival confirmed bookings show different badge
  if (booking != null && booking.isOnArrival) {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
      decoration: BoxDecoration(
        color: const Color(0xFF1565C0).withValues(alpha: 0.12),
        borderRadius: BorderRadius.circular(20),
      ),
      child: const Text(
        'Pagar na hora',
        style: TextStyle(
          color: Color(0xFF1565C0),
          fontSize: 12,
          fontWeight: FontWeight.w600,
        ),
      ),
    );
  }
  final color = _statusColor(status);
  return Container(
    padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
    decoration: BoxDecoration(
      color: color.withValues(alpha: 0.15),
      borderRadius: BorderRadius.circular(20),
    ),
    child: Text(
      _statusLabel(status),
      style: TextStyle(
        color: color,
        fontSize: 12,
        fontWeight: FontWeight.w600,
      ),
    ),
  );
}
```

---

**my_bookings_screen.dart — bifurcar tap para pending_payment:**

No metodo `_showDetailSheet()` ou diretamente no `onTap` do InkWell na lista `upcoming`, adicionar bifurcacao:

Localizar o `onTap: () => _showDetailSheet(context, b, true)` no bloco upcoming e substituir por:
```dart
onTap: () {
  if (b.isPendingPayment && b.paymentId != null) {
    // Tap on pending_payment card reopens PixPaymentScreen with existing QR
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (_) => PixPaymentScreen(
          bookingId: b.id,
          paymentId: b.paymentId,
        ),
      ),
    );
  } else {
    _showDetailSheet(context, b, true);
  }
},
```

Adicionar import de PixPaymentScreen no topo:
```dart
import 'package:vida_ativa/features/booking/ui/pix_payment_screen.dart';
```
  </action>
  <verify>
    grep -n "Pagar com Pix\|Pagar na hora\|_handlePayPix\|_handlePayOnArrival\|PixPaymentScreen" /f/_geral/Projetos/vida_ativa/lib/features/booking/ui/booking_confirmation_sheet.dart
    grep -n "Aguardando Pix\|Pagar na hora\|pending_payment\|expired\|isOnArrival" /f/_geral/Projetos/vida_ativa/lib/features/booking/ui/booking_card.dart
    grep -n "isPendingPayment\|PixPaymentScreen\|paymentId" /f/_geral/Projetos/vida_ativa/lib/features/booking/ui/my_bookings_screen.dart
    flutter analyze /f/_geral/Projetos/vida_ativa/lib/features/booking/ui/ 2>&1 | tail -10
  </verify>
  <acceptance_criteria>
    - booking_confirmation_sheet.dart contem `_handlePayPix`
    - booking_confirmation_sheet.dart contem `_handlePayOnArrival`
    - booking_confirmation_sheet.dart contem `'Pagar com Pix'`
    - booking_confirmation_sheet.dart contem `'Pagar na hora'`
    - booking_confirmation_sheet.dart contem `paymentMethod: 'pix'`
    - booking_confirmation_sheet.dart contem `paymentMethod: 'on_arrival'`
    - booking_confirmation_sheet.dart contem `PixPaymentScreen(`
    - booking_confirmation_sheet.dart NAO contem `_handleConfirm` como metodo isolado (foi removido)
    - booking_card.dart contem `'pending_payment' => 'Aguardando Pix'`
    - booking_card.dart contem `'expired' => 'Expirada'`
    - booking_card.dart contem `booking.isOnArrival`
    - booking_card.dart contem `'Pagar na hora'` no widget _statusBadge
    - my_bookings_screen.dart contem `isPendingPayment`
    - my_bookings_screen.dart contem `PixPaymentScreen(`
    - `flutter analyze lib/features/booking/ui/` sem erros
  </acceptance_criteria>
  <done>BookingConfirmationSheet com dois botoes de pagamento. BookingCard com badges corretos para pending_payment/on_arrival/expired. MyBookingsScreen reopen QR ao tocar card pending_payment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fluxo completo de Pix QR Generation:
    - BookingConfirmationSheet com "Pagar com Pix" e "Pagar na hora"
    - PixPaymentScreen com loading, QR code, copia-e-cola
    - BookingCard com badges corretos
    - MyBookingsScreen com tap para reabrir QR
  </what-built>
  <how-to-verify>
    1. Abrir app no emulador/dispositivo
    2. Ir para agenda e selecionar um slot disponivel
    3. Na BookingConfirmationSheet: confirmar que aparecem dois botoes ("Pagar com Pix" e "Pagar na hora")

    **Fluxo "Pagar na hora":**
    4. Tocar "Pagar na hora" → sheet deve fechar → snackbar "Reserva confirmada!" deve aparecer
    5. Ir para Minhas Reservas → reserva deve aparecer com badge azul "Pagar na hora"

    **Fluxo "Pagar com Pix" (requer sandbox MP configurado):**
    6. Selecionar outro slot → tocar "Pagar com Pix"
    7. Sheet deve fechar → PixPaymentScreen deve abrir com spinner "Gerando QR..."
    8. Apos 1-5 segundos: QR code deve aparecer (imagem escaneavel) + texto "Valido ate HH:mm"
    9. Tocar "Copiar codigo" → botao deve mudar para "Copiado" por 2 segundos
    10. Tocar botao back (ou seta) → deve navegar para Minhas Reservas (nao para agenda)
    11. Em Minhas Reservas: reserva deve aparecer com badge amber "Aguardando Pix"
    12. Tocar no card "Aguardando Pix" → PixPaymentScreen deve reabrir com o mesmo QR (lido da subcollection)

    **Se MP sandbox nao configurado:** verificar apenas fluxo "Pagar na hora" + confirmar que PixPaymentScreen exibe estado de erro com botao "Tentar novamente" ao tocar "Pagar com Pix"
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
Verificacoes de integracao completas da fase:

1. `flutter analyze lib/` — sem erros em toda a lib/
2. `flutter build web --no-tree-shake-icons` — build sem erros
3. Fluxo Pix funcional no sandbox Mercado Pago (QR aparece)
4. Fluxo on_arrival funciona sem QR (confirmed direto)
5. pending_payment bloqueia slot na ScheduleScreen (outro usuario nao consegue reservar)
6. Badge "Aguardando Pix" aparece no BookingCard
7. Badge "Pagar na hora" aparece no BookingCard
8. Tap em card pending_payment reabre PixPaymentScreen
</verification>

<success_criteria>
- PixPaymentScreen criada e funcional
- BookingConfirmationSheet com dois botoes de pagamento
- BookingCard com 3 novos badges (Aguardando Pix, Pagar na hora, Expirada)
- MyBookingsScreen redireciona para PixPaymentScreen ao tocar pending_payment
- `flutter analyze lib/` sem erros
- Checkpoint humano aprovado
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/17-pix-qr-generation/17-02-SUMMARY.md`
</output>
