---
phase: 13-admin-semana-contextualizada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/admin/ui/slot_management_tab.dart
autonomous: true
requirements:
  - ADMN-10
must_haves:
  truths:
    - "Admin vê label da semana atual acima dos day chips (ex: 'Semana de 31 mar–6 abr')"
    - "Admin clica ← e o label muda para a semana anterior; os day chips mostram as datas dessa semana"
    - "Admin clica → e o label avança para a semana seguinte; os day chips mostram as datas corretas"
    - "Cada day chip exibe o rótulo do dia e a data numérica correspondente (ex: 'Seg' + '31')"
    - "O chip selecionado usa background primaryGreen (#7B5D0A) e texto branco; chips não selecionados usam #F0EDE8 e texto #4A4A4A"
  artifacts:
    - path: "lib/features/admin/ui/slot_management_tab.dart"
      provides: "SlotManagementTab com week navigation e date chips"
      contains: "_selectedWeekStart"
    - path: "lib/features/schedule/ui/week_header.dart"
      provides: "WeekHeader widget (existente, apenas importado)"
      exports: ["WeekHeader"]
  key_links:
    - from: "_SlotDayViewState._selectedWeekStart"
      to: "WeekHeader(weekStart: _selectedWeekStart)"
      via: "setState atualiza o widget WeekHeader"
      pattern: "_selectedWeekStart"
    - from: "_SlotDayViewState._getDateForDayOfWeek"
      to: "ChoiceChip label (date number)"
      via: "_selectedWeekStart.add(Duration(days: dayOfWeek - 1)).day"
      pattern: "_getDateForDayOfWeek"
---

<objective>
Adiciona navegação semanal e datas reais nos day chips da aba Slots do painel admin.

Purpose: Admin hoje vê apenas "Seg / Ter / Qua…" sem saber qual semana está visualizando. Após essa mudança, o admin vê "Semana de 31 mar–6 abr" com botões ← → e cada chip exibe a data real do dia (ex: "Seg\n31").

Output: `slot_management_tab.dart` modificado com WeekHeader integrado, estado `_selectedWeekStart`, e chips com data dupla (label + número do dia).
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-admin-semana-contextualizada/13-UI-SPEC.md
@.planning/phases/13-admin-semana-contextualizada/13-RESEARCH.md

<interfaces>
<!-- Interfaces críticas que o executor DEVE usar. Extraídas do codebase. -->

De lib/features/schedule/ui/week_header.dart:
```dart
class WeekHeader extends StatelessWidget {
  final DateTime weekStart;
  final VoidCallback? onPreviousWeek;
  final VoidCallback? onNextWeek;

  const WeekHeader({
    super.key,
    required this.weekStart,
    this.onPreviousWeek,
    this.onNextWeek,
  });
  // _weekLabel(): cross-month "Semana de 31 mar–6 abr", same-month "Semana de 1–7 abr"
}
```

De lib/core/theme/app_spacing.dart:
```dart
class AppSpacing {
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 16.0;
  static const double lg = 24.0;
  static const double xl = 32.0;
}
```

De lib/core/theme/app_theme.dart:
```dart
class AppTheme {
  static const Color primaryGreen = Color(0xFF7B5D0A);
  static const Color brandAmber = Color(0xFFD4A800);
}
```

Estado atual de _SlotDayViewState (campos relevantes):
```dart
class _SlotDayViewState extends State<_SlotDayView> {
  int _selectedDayOfWeek = 1;
  late EventController<SlotModel> _controller;

  // Referência fixa atual (a ser substituída):
  // final _referenceMonday = DateTime(2024, 1, 1); // definida no topo do arquivo
  DateTime _refDate(int dayOfWeek) =>
      _referenceMonday.add(Duration(days: dayOfWeek - 1));
}
```

Day chip atual (linhas 147–173 de slot_management_tab.dart):
```dart
ChoiceChip(
  label: Text(_dayLabels[i], style: const TextStyle(fontSize: 12)),
  selected: isSelected,
  showCheckmark: false,
  selectedColor: AppTheme.primaryGreen,
  backgroundColor: const Color(0xFFF0EDE8),
  labelStyle: TextStyle(
    color: isSelected ? Colors.white : const Color(0xFF4A4A4A),
    fontWeight: isSelected ? FontWeight.w700 : FontWeight.w500,
  ),
  side: isSelected
      ? BorderSide.none
      : const BorderSide(color: Color(0xFFCFC5B0), width: 0.5),
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
  onSelected: (_) => setState(() => _selectedDayOfWeek = dow),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar estado de semana e week navigation ao _SlotDayViewState</name>
  <read_first>
    - lib/features/admin/ui/slot_management_tab.dart (arquivo completo — ver estado atual e _refDate)
    - lib/features/schedule/ui/week_header.dart (WeekHeader widget e assinatura)
  </read_first>
  <files>lib/features/admin/ui/slot_management_tab.dart</files>
  <action>
Modificar `_SlotDayViewState` e o método `build` para suportar navegação semanal:

**1. Adicionar import de WeekHeader no topo do arquivo:**
```dart
import 'package:vida_ativa/features/schedule/ui/week_header.dart';
```

**2. Substituir a constante `_referenceMonday` no topo do arquivo:**
Remover a linha:
```dart
final _referenceMonday = DateTime(2024, 1, 1);
```
Essa constante deixa de existir — a lógica passa para `_selectedWeekStart` no estado.

**3. Adicionar `_selectedWeekStart` ao `_SlotDayViewState`:**
```dart
late DateTime _selectedWeekStart;
```

**4. Adicionar helper `_getMonday(DateTime date)` como método privado do state:**
```dart
DateTime _getMonday(DateTime date) {
  return date.subtract(Duration(days: date.weekday - 1));
}
```

**5. Inicializar `_selectedWeekStart` em `initState()`, antes de `_controller`:**
```dart
_selectedWeekStart = _getMonday(DateTime.now());
```

**6. Atualizar `_refDate(int dayOfWeek)` para usar `_selectedWeekStart`:**
```dart
DateTime _refDate(int dayOfWeek) =>
    _selectedWeekStart.add(Duration(days: dayOfWeek - 1));
```
(O restante do método permanece idêntico — `_syncEvents` e `DayView.initialDay` chamam `_refDate` sem alteração.)

**7. Adicionar callbacks de navegação semanal:**
```dart
void _onPreviousWeek() {
  setState(() {
    _selectedWeekStart =
        _selectedWeekStart.subtract(const Duration(days: 7));
  });
  WidgetsBinding.instance.addPostFrameCallback((_) {
    if (mounted) _syncEvents();
  });
}

void _onNextWeek() {
  setState(() {
    _selectedWeekStart = _selectedWeekStart.add(const Duration(days: 7));
  });
  WidgetsBinding.instance.addPostFrameCallback((_) {
    if (mounted) _syncEvents();
  });
}
```

**8. No método `build()`, inserir `WeekHeader` acima do `SingleChildScrollView` dos chips:**

Antes (início do Column.children em build):
```dart
children: [
  const SizedBox(height: AppSpacing.sm),
  SingleChildScrollView(  // chips
```

Depois:
```dart
children: [
  WeekHeader(
    weekStart: _selectedWeekStart,
    onPreviousWeek: _onPreviousWeek,
    onNextWeek: _onNextWeek,
  ),
  SingleChildScrollView(  // chips (sem o SizedBox.sm antes — WeekHeader já tem padding vertical: 8)
```

Remover o `const SizedBox(height: AppSpacing.sm)` que estava antes dos chips, pois `WeekHeader` já tem `padding: EdgeInsets.symmetric(vertical: 8)` internamente.
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ui/slot_management_tab.dart 2>&1 | grep -E "error|warning|No issues"</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/admin/ui/slot_management_tab.dart contém `late DateTime _selectedWeekStart`
    - lib/features/admin/ui/slot_management_tab.dart contém `_getMonday(`
    - lib/features/admin/ui/slot_management_tab.dart contém `_onPreviousWeek(`
    - lib/features/admin/ui/slot_management_tab.dart contém `_onNextWeek(`
    - lib/features/admin/ui/slot_management_tab.dart contém `WeekHeader(`
    - lib/features/admin/ui/slot_management_tab.dart NÃO contém `_referenceMonday` (constante removida)
    - lib/features/admin/ui/slot_management_tab.dart contém import `week_header.dart`
    - `flutter analyze` retorna 0 erros para esse arquivo
  </acceptance_criteria>
  <done>_SlotDayViewState rastreia _selectedWeekStart, WeekHeader aparece acima dos chips com callbacks ← →, análise estática limpa.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar day chips para exibir data real (label + número)</name>
  <read_first>
    - lib/features/admin/ui/slot_management_tab.dart (após Task 1 — ver estado atual dos chips)
  </read_first>
  <files>lib/features/admin/ui/slot_management_tab.dart</files>
  <action>
Atualizar o `ChoiceChip` no `List.generate(7, ...)` para exibir o rótulo do dia e a data numérica correspondente.

**Substituir o label do ChoiceChip de:**
```dart
label: Text(_dayLabels[i], style: const TextStyle(fontSize: 12)),
```

**Para:**
```dart
label: Column(
  mainAxisSize: MainAxisSize.min,
  children: [
    Text(
      _dayLabels[i],
      style: const TextStyle(fontSize: 12),
    ),
    Text(
      _refDate(dow).day.toString(),
      style: const TextStyle(fontSize: 12),
    ),
  ],
),
```

Isso usa `_refDate(dow)` que já foi atualizado na Task 1 para retornar datas reais baseadas em `_selectedWeekStart`.

**Nenhuma outra mudança nos chips** — `selectedColor`, `backgroundColor`, `labelStyle`, `side`, `shape`, `onSelected` permanecem idênticos ao código atual.

**Verificar visualmente (mentalmente):** 
- Segunda-feira da semana de 31/03–06/04 → chip mostra "Seg" + "31"
- Domingo da mesma semana → chip mostra "Dom" + "6"
- Ao navegar ← para semana anterior (24–30 mar) → chip segunda mostra "24", domingo mostra "30"
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ui/slot_management_tab.dart 2>&1 | grep -E "^  error|No issues"</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/admin/ui/slot_management_tab.dart contém `Column(` dentro do bloco `label:` do ChoiceChip (grep: `mainAxisSize: MainAxisSize.min`)
    - lib/features/admin/ui/slot_management_tab.dart contém `_refDate(dow).day.toString()`
    - lib/features/admin/ui/slot_management_tab.dart NÃO contém `label: Text(_dayLabels[i]` (forma original de label simples substituída)
    - `flutter analyze` retorna 0 erros para esse arquivo
  </acceptance_criteria>
  <done>Cada day chip mostra duas linhas: rótulo do dia (ex: "Seg") e data numérica real (ex: "31"), derivada de _selectedWeekStart.</done>
</task>

</tasks>

<verification>
Após ambas as tasks:
```bash
cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ui/slot_management_tab.dart
```
Deve retornar "No issues found" ou somente warnings (não erros).

Checkpoint visual (checkpoint:human-verify separado não necessário — navegação semanal é verificável via hot reload):
1. Abrir app como admin, ir para aba Slots
2. Verificar label "Semana de X mês–Y mês" aparece acima dos chips
3. Clicar ← e verificar label e datas nos chips retrocedem 7 dias
4. Clicar → e verificar label e datas nos chips avançam 7 dias
5. Cada chip deve mostrar dia abreviado + número (ex: "Seg\n31")
</verification>

<success_criteria>
- `slot_management_tab.dart` compila sem erros (`flutter analyze` clean)
- WeekHeader renderiza label semanal com navegação ← →
- Day chips exibem `_dayLabels[i]` + data numérica de `_selectedWeekStart.add(Duration(days: dow - 1)).day`
- Navegação semana-a-semana atualiza tanto o label quanto as datas dos chips
- Estilo dos chips mantido idêntico ao original (selectedColor, backgroundColor, labelStyle, side, shape)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-admin-semana-contextualizada/13-01-SUMMARY.md`
</output>
