---
phase: 13-admin-semana-contextualizada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/admin/ui/admin_booking_detail_sheet.dart
  - lib/features/admin/ui/booking_management_tab.dart
autonomous: true
requirements:
  - ADMN-11
must_haves:
  truths:
    - "Admin toca em qualquer card na aba Reservas → bottomsheet abre com nome do cliente, status badge, data, horário, preço e participantes"
    - "Bottomsheet exibe botões 'Confirmar' e 'Recusar' apenas para reservas com status 'pending'"
    - "Reservas confirmed/rejected mostram o bottomsheet sem botões de ação (somente detalhes)"
    - "Ao confirmar ou recusar, um AlertDialog pede confirmação antes de executar a ação"
    - "Se a ação falhar, mensagem de erro 'Falha ao confirmar/recusar reserva. Tente novamente.' aparece inline (vermelho, 12px); botões ficam habilitados novamente"
    - "Enquanto a ação está em progresso, o botão mostra CircularProgressIndicator (20×20, strokeWidth 2, branco) e fica desabilitado"
    - "Após sucesso, o bottomsheet fecha automaticamente; o card na lista atualiza via stream do Firestore"
  artifacts:
    - path: "lib/features/admin/ui/admin_booking_detail_sheet.dart"
      provides: "AdminBookingDetailSheet StatefulWidget"
      exports: ["AdminBookingDetailSheet"]
      contains: "_isSubmitting"
    - path: "lib/features/admin/ui/booking_management_tab.dart"
      provides: "BookingManagementTab com GestureDetector wrapping AdminBookingCard"
      contains: "_showBookingDetailSheet"
  key_links:
    - from: "booking_management_tab.dart GestureDetector.onTap"
      to: "AdminBookingDetailSheet"
      via: "showModalBottomSheet(isScrollControlled: true)"
      pattern: "_showBookingDetailSheet"
    - from: "AdminBookingDetailSheet._handleConfirm"
      to: "widget.adminBookingCubit.confirmBooking(widget.booking.id)"
      via: "Future<void> await (não emite state)"
      pattern: "confirmBooking"
    - from: "AdminBookingDetailSheet._handleReject"
      to: "widget.adminBookingCubit.rejectBooking(widget.booking.id)"
      via: "Future<void> await (não emite state)"
      pattern: "rejectBooking"
---

<objective>
Cria `AdminBookingDetailSheet` e conecta à aba Reservas via tap nos cards.

Purpose: Admin hoje precisa confirmar/recusar reservas diretamente nos cards, sem ver todos os detalhes em um único lugar. Com o bottomsheet, o admin toca qualquer reserva e vê nome do cliente, status, data, horário, preço e participantes — com ações confirmar/recusar para reservas pendentes.

Output: Novo arquivo `admin_booking_detail_sheet.dart` + `booking_management_tab.dart` atualizado com GestureDetector nos cards.
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-admin-semana-contextualizada/13-UI-SPEC.md
@.planning/phases/13-admin-semana-contextualizada/13-RESEARCH.md

<interfaces>
<!-- Interfaces críticas. Extraídas do codebase. -->

De lib/features/admin/cubit/admin_booking_cubit.dart:
```dart
class AdminBookingCubit extends Cubit<AdminBookingState> {
  // Mutations — NÃO emitem state. Stream subscription em selectDate() atualiza UI.
  Future<void> confirmBooking(String bookingId) async { ... }
  Future<void> rejectBooking(String bookingId) async { ... }
}
```

De lib/core/models/booking_model.dart (campos usados):
```dart
class BookingModel {
  final String id;
  final String status;           // 'pending' | 'confirmed' | 'rejected' | 'cancelled'
  final String? userDisplayName; // nome do cliente
  final String? startTime;       // ex: "08:00"
  final double? price;           // preço do slot
  final String? participants;    // texto livre, pode ser null ou empty
  final String date;             // "YYYY-MM-DD"
  bool get isPending => status == 'pending';
}
```

De lib/features/booking/ui/booking_confirmation_sheet.dart (padrão de bottomsheet a replicar):
```dart
// Drag handle:
Center(
  child: Container(
    width: 40, height: 4,
    decoration: BoxDecoration(
      color: const Color(0xFFD0CAC0),
      borderRadius: BorderRadius.circular(2),
    ),
  ),
),
// Info row:
Widget _infoRow(IconData icon, String text) {
  return Row(children: [
    Container(
      padding: const EdgeInsets.all(6),
      decoration: BoxDecoration(
        color: AppTheme.primaryGreen.withValues(alpha: 0.10),
        borderRadius: BorderRadius.circular(8),
      ),
      child: Icon(icon, size: 16, color: AppTheme.primaryGreen),
    ),
    const SizedBox(width: 12),
    Text(text),
  ]);
}
// Padding raiz: Padding(padding: EdgeInsets.fromLTRB(24, 16, 24, 24), ...)
// Título: TextStyle(fontSize: 22, fontWeight: FontWeight.w700)
// Botão: FilledButton com minimumSize: Size(double.infinity, 52), borderRadius: 12
// Loading: SizedBox(width: 20, height: 20, child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))
// Erro: TextStyle(color: Color(0xFFC62828))
```

De lib/features/admin/ui/admin_booking_card.dart (lógica de status a replicar):
```dart
Color _statusColor(String status) {
  return switch (status) {
    'pending' => Colors.orange,
    'confirmed' => AppTheme.primaryGreen,
    'rejected' => Colors.red,
    _ => Colors.grey,
  };
}
String _statusLabel(String status) {
  return switch (status) {
    'pending' => 'Aguardando',
    'confirmed' => 'Confirmado',
    'rejected' => 'Recusado',
    _ => 'Cancelado',
  };
}
// AlertDialog de confirmação (padrão exato de _confirmAction e _rejectAction):
showDialog<bool>(context: context, builder: (ctx) => AlertDialog(
  title: const Text('Confirmar reserva?'),
  content: const Text('Deseja confirmar esta reserva?'),
  actions: [
    TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Nao')),
    FilledButton(onPressed: () => Navigator.pop(ctx, true), child: const Text('Sim')),
  ],
));
// Para rejeitar: title: 'Recusar reserva?', content: 'Deseja recusar esta reserva?'
```

De lib/features/admin/ui/slot_management_tab.dart (padrão showModalBottomSheet):
```dart
void _openSheet(BuildContext context, SlotModel? existing) {
  final cubit = context.read<AdminSlotCubit>();
  showModalBottomSheet(
    context: context,
    isScrollControlled: true,
    builder: (_) => SlotFormSheet(existing: existing, slotCubit: cubit),
  );
}
```

De lib/features/admin/ui/booking_management_tab.dart (estado atual do ListView.builder):
```dart
ListView.builder(
  padding: const EdgeInsets.all(AppSpacing.sm),
  itemCount: state.bookings.length,
  itemBuilder: (context, index) {
    return AdminBookingCard(
      booking: state.bookings[index],
      bookingCubit: cubit,
    );
  },
),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar AdminBookingDetailSheet</name>
  <read_first>
    - lib/features/booking/ui/booking_confirmation_sheet.dart (padrão exato a replicar: padding, drag handle, _infoRow, loading, erro)
    - lib/features/admin/ui/admin_booking_card.dart (copiar _statusColor, _statusLabel, AlertDialog patterns)
    - lib/features/admin/cubit/admin_booking_cubit.dart (assinatura de confirmBooking e rejectBooking)
  </read_first>
  <files>lib/features/admin/ui/admin_booking_detail_sheet.dart</files>
  <action>
Criar o arquivo `lib/features/admin/ui/admin_booking_detail_sheet.dart` do zero como StatefulWidget.

**Imports necessários:**
```dart
import 'package:flutter/material.dart';
import 'package:intl/intl.dart';
import 'package:vida_ativa/core/models/booking_model.dart';
import 'package:vida_ativa/core/theme/app_theme.dart';
import 'package:vida_ativa/features/admin/cubit/admin_booking_cubit.dart';
```

**Classe principal:**
```dart
class AdminBookingDetailSheet extends StatefulWidget {
  final BookingModel booking;
  final AdminBookingCubit adminBookingCubit;

  const AdminBookingDetailSheet({
    super.key,
    required this.booking,
    required this.adminBookingCubit,
  });

  @override
  State<AdminBookingDetailSheet> createState() =>
      _AdminBookingDetailSheetState();
}
```

**Estado com _isSubmitting e _errorMessage:**
```dart
class _AdminBookingDetailSheetState extends State<AdminBookingDetailSheet> {
  bool _isSubmitting = false;
  String? _errorMessage;
```

**Helpers de status (copiar de admin_booking_card.dart — sem modificação):**
```dart
  Color _statusColor(String status) {
    return switch (status) {
      'pending' => Colors.orange,
      'confirmed' => AppTheme.primaryGreen,
      'rejected' => Colors.red,
      _ => Colors.grey,
    };
  }

  String _statusLabel(String status) {
    return switch (status) {
      'pending' => 'Aguardando',
      'confirmed' => 'Confirmado',
      'rejected' => 'Recusado',
      _ => 'Cancelado',
    };
  }
```

**Info row helper (copiar de booking_confirmation_sheet.dart — sem modificação):**
```dart
  Widget _infoRow(IconData icon, String text) {
    return Row(
      children: [
        Container(
          padding: const EdgeInsets.all(6),
          decoration: BoxDecoration(
            color: AppTheme.primaryGreen.withValues(alpha: 0.10),
            borderRadius: BorderRadius.circular(8),
          ),
          child: Icon(icon, size: 16, color: AppTheme.primaryGreen),
        ),
        const SizedBox(width: 12),
        Flexible(child: Text(text)),
      ],
    );
  }
```
Nota: usar `Flexible` em vez de `Text` direto para acomodar texto longo em participantes.

**Handler de confirmação (com AlertDialog, estado local, sem emitir cubit state):**
```dart
  Future<void> _handleConfirm() async {
    final confirmed = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: const Text('Confirmar reserva?'),
        content: const Text('Deseja confirmar esta reserva?'),
        actions: [
          TextButton(
            onPressed: () => Navigator.pop(ctx, false),
            child: const Text('Nao'),
          ),
          FilledButton(
            onPressed: () => Navigator.pop(ctx, true),
            child: const Text('Sim'),
          ),
        ],
      ),
    );
    if (confirmed != true) return;
    setState(() {
      _isSubmitting = true;
      _errorMessage = null;
    });
    try {
      await widget.adminBookingCubit.confirmBooking(widget.booking.id);
      if (mounted) Navigator.pop(context);
    } on Exception {
      if (mounted) {
        setState(() {
          _isSubmitting = false;
          _errorMessage = 'Falha ao confirmar reserva. Tente novamente.';
        });
      }
    }
  }
```

**Handler de rejeição (mesma estrutura, textos diferentes):**
```dart
  Future<void> _handleReject() async {
    final confirmed = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: const Text('Recusar reserva?'),
        content: const Text('Deseja recusar esta reserva?'),
        actions: [
          TextButton(
            onPressed: () => Navigator.pop(ctx, false),
            child: const Text('Nao'),
          ),
          FilledButton(
            onPressed: () => Navigator.pop(ctx, true),
            child: const Text('Sim'),
          ),
        ],
      ),
    );
    if (confirmed != true) return;
    setState(() {
      _isSubmitting = true;
      _errorMessage = null;
    });
    try {
      await widget.adminBookingCubit.rejectBooking(widget.booking.id);
      if (mounted) Navigator.pop(context);
    } on Exception {
      if (mounted) {
        setState(() {
          _isSubmitting = false;
          _errorMessage = 'Falha ao recusar reserva. Tente novamente.';
        });
      }
    }
  }
```

**Build method — layout completo:**
```dart
  @override
  Widget build(BuildContext context) {
    final booking = widget.booking;
    final statusColor = _statusColor(booking.status);
    final statusLabel = _statusLabel(booking.status);

    // Formatar data: "YYYY-MM-DD" → "Segunda, 31 de março"
    final parsedDate = DateTime.parse(booking.date);
    final rawFormatted =
        DateFormat("EEEE, d 'de' MMMM", 'pt_BR').format(parsedDate);
    final dateDisplay =
        '${rawFormatted[0].toUpperCase()}${rawFormatted.substring(1)}';

    // Formatar preço
    final priceDisplay = booking.price != null
        ? NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$')
            .format(booking.price)
        : '—';

    return Padding(
      padding: EdgeInsets.fromLTRB(
        24, 16, 24,
        24 + MediaQuery.of(context).viewInsets.bottom,
      ),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // Drag handle
          Center(
            child: Container(
              width: 40,
              height: 4,
              decoration: BoxDecoration(
                color: const Color(0xFFD0CAC0),
                borderRadius: BorderRadius.circular(2),
              ),
            ),
          ),
          const SizedBox(height: 16),
          // Título + status badge
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              const Text(
                'Detalhes da Reserva',
                style: TextStyle(fontSize: 22, fontWeight: FontWeight.w700),
              ),
              Container(
                padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                decoration: BoxDecoration(
                  color: statusColor.withValues(alpha: 0.15),
                  borderRadius: BorderRadius.circular(12),
                ),
                child: Text(
                  statusLabel,
                  style: TextStyle(
                    color: statusColor,
                    fontSize: 12,
                    fontWeight: FontWeight.w600,
                  ),
                ),
              ),
            ],
          ),
          const SizedBox(height: 16),
          // Info rows
          _infoRow(Icons.person, booking.userDisplayName ?? 'Cliente'),
          const SizedBox(height: 10),
          _infoRow(Icons.calendar_today, dateDisplay),
          const SizedBox(height: 10),
          _infoRow(Icons.access_time, booking.startTime ?? '—'),
          const SizedBox(height: 10),
          _infoRow(Icons.attach_money, priceDisplay),
          if (booking.participants != null &&
              booking.participants!.isNotEmpty) ...[
            const SizedBox(height: 10),
            _infoRow(Icons.group, booking.participants!),
          ],
          // Erro inline
          if (_errorMessage != null) ...[
            const SizedBox(height: 12),
            Text(
              _errorMessage!,
              style: const TextStyle(
                color: Color(0xFFC62828),
                fontSize: 12,
              ),
            ),
          ],
          // Botões apenas para reservas pendentes
          if (booking.isPending) ...[
            const SizedBox(height: 24),
            Row(
              children: [
                Expanded(
                  child: FilledButton(
                    onPressed: _isSubmitting ? null : _handleConfirm,
                    style: FilledButton.styleFrom(
                      backgroundColor: AppTheme.primaryGreen,
                      minimumSize: const Size(double.infinity, 52),
                      shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(12),
                      ),
                    ),
                    child: _isSubmitting
                        ? const SizedBox(
                            width: 20,
                            height: 20,
                            child: CircularProgressIndicator(
                              strokeWidth: 2,
                              color: Colors.white,
                            ),
                          )
                        : const Text('Confirmar'),
                  ),
                ),
                const SizedBox(width: 8),
                Expanded(
                  child: OutlinedButton(
                    onPressed: _isSubmitting ? null : _handleReject,
                    style: OutlinedButton.styleFrom(
                      foregroundColor: Colors.red,
                      minimumSize: const Size(double.infinity, 52),
                      shape: RoundedRectangleBorder(
                        borderRadius: BorderRadius.circular(12),
                      ),
                    ),
                    child: const Text('Recusar'),
                  ),
                ),
              ],
            ),
          ],
          const SizedBox(height: 8),
        ],
      ),
    );
  }
}
```

**Nota crítica sobre `_isSubmitting` com dois botões:** Quando `_isSubmitting = true`, ambos os botões ficam desabilitados (`onPressed: _isSubmitting ? null : _handle*`). Somente o botão Confirmar mostra o loading spinner — o Recusar fica apenas desabilitado. Isso é intencional (não há como saber qual botão foi pressionado para mostrar spinner no correto; simplificação aceitável para esta fase).
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ui/admin_booking_detail_sheet.dart 2>&1 | grep -E "^  error|No issues"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `lib/features/admin/ui/admin_booking_detail_sheet.dart` existe
    - Contém `class AdminBookingDetailSheet extends StatefulWidget`
    - Contém `bool _isSubmitting = false`
    - Contém `String? _errorMessage`
    - Contém `_handleConfirm(` e `_handleReject(`
    - Contém `widget.adminBookingCubit.confirmBooking(widget.booking.id)`
    - Contém `widget.adminBookingCubit.rejectBooking(widget.booking.id)`
    - Contém `if (booking.isPending)` para mostrar botões condicionalmente
    - Contém `'Detalhes da Reserva'` como título do sheet
    - Contém `'Falha ao confirmar reserva. Tente novamente.'`
    - Contém `'Falha ao recusar reserva. Tente novamente.'`
    - `flutter analyze` retorna 0 erros para esse arquivo
  </acceptance_criteria>
  <done>AdminBookingDetailSheet existe, compila sem erros, exibe todos os campos de detalhe, botões condicionais para status pending, loading + erro inline.</done>
</task>

<task type="auto">
  <name>Task 2: Conectar AdminBookingDetailSheet à aba Reservas (BookingManagementTab)</name>
  <read_first>
    - lib/features/admin/ui/booking_management_tab.dart (arquivo completo — ver ListView.builder atual)
    - lib/features/admin/ui/admin_booking_detail_sheet.dart (recém criado — verificar assinatura do construtor)
  </read_first>
  <files>lib/features/admin/ui/booking_management_tab.dart</files>
  <action>
Modificar `booking_management_tab.dart` para:

1. **Adicionar import** do novo sheet no topo do arquivo:
```dart
import 'package:vida_ativa/features/admin/ui/admin_booking_detail_sheet.dart';
```

2. **Adicionar método `_showBookingDetailSheet`** na classe `_BookingManagementView`:
```dart
void _showBookingDetailSheet(BuildContext context, booking) {
  final cubit = context.read<AdminBookingCubit>();
  showModalBottomSheet(
    context: context,
    isScrollControlled: true,
    builder: (_) => AdminBookingDetailSheet(
      booking: booking,
      adminBookingCubit: cubit,
    ),
  );
}
```

**Nota crítica:** `context.read<AdminBookingCubit>()` deve ser capturado ANTES de chamar `showModalBottomSheet` — a subtree do builder `(_)` não tem acesso ao BlocProvider. O método já captura `cubit` antes de `showModalBottomSheet`, portanto está correto.

3. **Envolver o `AdminBookingCard` com `GestureDetector`** no `ListView.builder`:

Substituir:
```dart
itemBuilder: (context, index) {
  return AdminBookingCard(
    booking: state.bookings[index],
    bookingCubit: cubit,
  );
},
```

Por:
```dart
itemBuilder: (context, index) {
  final booking = state.bookings[index];
  return GestureDetector(
    onTap: () => _showBookingDetailSheet(context, booking),
    child: AdminBookingCard(
      booking: booking,
      bookingCubit: cubit,
    ),
  );
},
```

**Nenhuma outra mudança no arquivo** — date picker, day navigation, SwitchListTile de confirmação automática, header de data, etc. permanecem intactos.

**Importante:** O `AdminBookingCard` existente ainda tem seus próprios botões "Confirmar" e "Recusar" inline para reservas pendentes. Esses botões permanecem — não os remover nesta fase. O card agora também abre o detail sheet via tap na área geral. Os dois mecanismos coexistem: tap no card → sheet; tap nos botões inline → ação direta. Isso é aceitável para Phase 13; remoção dos botões inline do card pode ser feita em refactor futuro.
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ui/booking_management_tab.dart 2>&1 | grep -E "^  error|No issues"</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/admin/ui/booking_management_tab.dart contém import `admin_booking_detail_sheet.dart`
    - lib/features/admin/ui/booking_management_tab.dart contém `_showBookingDetailSheet(`
    - lib/features/admin/ui/booking_management_tab.dart contém `GestureDetector(`
    - lib/features/admin/ui/booking_management_tab.dart contém `onTap: () => _showBookingDetailSheet(context, booking)`
    - lib/features/admin/ui/booking_management_tab.dart contém `showModalBottomSheet(`
    - lib/features/admin/ui/booking_management_tab.dart contém `isScrollControlled: true`
    - `flutter analyze` retorna 0 erros para esse arquivo
  </acceptance_criteria>
  <done>BookingManagementTab envolve cada AdminBookingCard em GestureDetector que abre AdminBookingDetailSheet via showModalBottomSheet. Analise estática limpa.</done>
</task>

</tasks>

<verification>
Após ambas as tasks, verificar o projeto completo:
```bash
cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/
```
Deve retornar "No issues found" ou somente warnings (não erros).

Checkpoint visual (verificável via hot reload):
1. Abrir app como admin, ir para aba Reservas
2. Tocar em qualquer card de reserva → bottomsheet abre com "Detalhes da Reserva"
3. Verificar que nome do cliente, data formatada, horário, preço e participantes aparecem
4. Para reserva pendente: verificar que botões "Confirmar" e "Recusar" aparecem
5. Para reserva confirmada/recusada: verificar que botões NÃO aparecem
6. Tocar "Confirmar" → AlertDialog aparece com "Confirmar reserva?" → confirmar → sheet fecha, card atualiza
7. Tocar "Recusar" → AlertDialog aparece com "Recusar reserva?" → confirmar → sheet fecha, card atualiza
</verification>

<success_criteria>
- `admin_booking_detail_sheet.dart` criado e compila sem erros
- `booking_management_tab.dart` atualizado com GestureDetector e `_showBookingDetailSheet`
- `flutter analyze lib/features/admin/` retorna sem erros
- AdminBookingDetailSheet exibe todos os campos: nome, status badge, data formatada em pt_BR, horário, preço em R$, participantes (se non-null/non-empty)
- Botões aparecem somente para `booking.isPending == true`
- Ações usam AlertDialog antes de executar e fecham o sheet com `Navigator.pop(context)` após sucesso
- Erros inline aparecem em vermelho (Color(0xFFC62828)) em 12px se ação falhar
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-admin-semana-contextualizada/13-02-SUMMARY.md`
</output>
