---
phase: 26-fluxo-de-reserva-cliente
plan: 03
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - lib/features/booking/ui/my_bookings_screen.dart
autonomous: true
requirements:
  - BOOK-10
  - BOOK-11
  - BOOK-12
must_haves:
  truths:
    - "MyBookingsScreen has no AppBar — inline header with wordmark 'VIDA ATIVA' (Anton ink + orange pill 'ATIVA') and eyebrow 'MINHAS RESERVAS' in JBM mono"
    - "First upcoming booking renders as hero block: eyebrow orange 'PRÓXIMO · HOJE/AMANHÃ/[DAY]', Anton 72px time, mono date below — no colored background"
    - "Hero block tap routes to PixPaymentScreen (if pending_payment + paymentId) or ClientBookingDetailSheet (isFuture:true)"
    - "Section 'EM SEGUIDA' header in JBM mono uppercase tracked appears before remaining upcoming bookings (excluding hero)"
    - "Section 'HISTÓRICO' header in JBM mono uppercase tracked appears before past/cancelled bookings"
    - "Remaining upcoming bookings and past bookings render as HairlineBookingRow with hairline top separator"
    - "Empty state shows JBM mono text + SportBtn.outlined 'VER AGENDA' → navigates to schedule tab"
    - "No AppSpacing import — uses literal values. No booking_card.dart import"
    - "No Color(0xFF...) literals in the modified file"
  artifacts:
    - path: "lib/features/booking/ui/my_bookings_screen.dart"
      provides: "Rewritten MyBookings with inline header, hero Próximo, hairline rows"
      contains: "AppTheme.display(size: 72"
  key_links:
    - from: "my_bookings_screen.dart"
      to: "lib/features/booking/ui/hairline_booking_row.dart"
      via: "import + HairlineBookingRow(booking:, bookingCubit:, index:, isFuture:)"
      pattern: "HairlineBookingRow"
    - from: "my_bookings_screen.dart"
      to: "lib/core/widgets/sport_btn.dart"
      via: "import + SportBtn.outlined('VER AGENDA')"
      pattern: "SportBtn.outlined"
    - from: "hero block"
      to: "ClientBookingDetailSheet / PixPaymentScreen"
      via: "GestureDetector.onTap"
      pattern: "GestureDetector"
---

<objective>
Rewrite MyBookingsScreen UI to Arena Esportivo identity: inline header (no AppBar), hero "Próximo" section with Anton 72px, section headers in JBM mono uppercase, HairlineBookingRow for all booking rows, and SportBtn empty state.

Purpose: Deliver BOOK-10 (hero Próximo 72px), BOOK-11 (hairline rows), BOOK-12 (section headers "EM SEGUIDA"/"HISTÓRICO"). Zero behavior/state changes — only UI layer.
Output: `lib/features/booking/ui/my_bookings_screen.dart` rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-fluxo-de-reserva-cliente/26-CONTEXT.md
@.planning/phases/26-fluxo-de-reserva-cliente/26-UI-SPEC.md
@.planning/phases/26-fluxo-de-reserva-cliente/26-01-SUMMARY.md

<interfaces>
<!-- AppTheme helpers -->
```dart
static TextStyle display({double size = 32, Color? color, double? letterSpacing});
static TextStyle ui({double size = 14, FontWeight weight = FontWeight.w400, Color? color});
static TextStyle mono({double size = 11, FontWeight weight = FontWeight.w700, Color? color, double? letterSpacing});

static const Color orange    = Color(0xFFFF4D17);
static const Color ink       = Color(0xFF0E0E0C);
static const Color concrete  = Color(0xFF6B6B66);
static const Color paper     = Color(0xFFFBF8F0);
static const Color lineHair  = Color(0xFFEAE3CE);
```

<!-- SportBtn (created Plan 01) -->
```dart
import 'package:vida_ativa/core/widgets/sport_btn.dart';
SportBtn.outlined('VER AGENDA', onPressed: () => StatefulNavigationShell.of(context).goBranch(0))
```

<!-- HairlineBookingRow (created Plan 01) -->
```dart
import 'package:vida_ativa/features/booking/ui/hairline_booking_row.dart';
HairlineBookingRow(
  booking: b,
  bookingCubit: context.read<BookingCubit>(),
  index: index,    // 0 = no top border, >0 = hairline top
  isFuture: true,  // or false for past bookings
)
```

<!-- Inline header pattern (Phase 24 schedule_screen.dart) -->
```dart
// Wordmark "VIDA ATIVA"
Row(
  children: [
    Text('VIDA', style: AppTheme.display(size: 18, color: AppTheme.ink)),
    const SizedBox(width: 4),
    Container(
      padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
      decoration: BoxDecoration(
        color: AppTheme.orange,
        borderRadius: BorderRadius.circular(4),
      ),
      child: Text('ATIVA', style: AppTheme.display(size: 18, color: AppTheme.paper)),
    ),
    const Spacer(),
    Text('MINHAS RESERVAS', style: AppTheme.mono(size: 11, color: AppTheme.concrete)),
  ],
)
```

<!-- BookingModel fields -->
```dart
class BookingModel {
  final String id;
  final String date;        // "YYYY-MM-DD" string
  final String startTime;   // "HH:mm" string
  final String status;      // 'pending' | 'confirmed' | 'pending_payment' | 'cancelled' | 'expired'
  final String? paymentId;
  bool get isPendingPayment => status == 'pending_payment';
  bool get isCancelled => status == 'cancelled';
  bool get isRefunded;      // check current field name in model
}
```

<!-- Navigation -->
```dart
// PixPaymentScreen
Navigator.push(context, MaterialPageRoute(
  builder: (_) => PixPaymentScreen(bookingId: b.id, paymentId: b.paymentId),
));

// ClientBookingDetailSheet
showModalBottomSheet(
  context: context,
  isScrollControlled: true,
  builder: (_) => ClientBookingDetailSheet(
    booking: booking, bookingCubit: bookingCubit, isFuture: isFuture,
  ),
);
```

<!-- Eyebrow logic for hero Próximo -->
```dart
String _heroEyebrow(String bookingDate) {
  final today = DateTime.now();
  final date = DateTime.parse(bookingDate);
  final todayNormalized = DateTime(today.year, today.month, today.day);
  final dateNormalized = DateTime(date.year, date.month, date.day);
  final diff = dateNormalized.difference(todayNormalized).inDays;
  if (diff == 0) return 'PRÓXIMO · HOJE';
  if (diff == 1) return 'PRÓXIMO · AMANHÃ';
  final abbr = ['SEG','TER','QUA','QUI','SEX','SÁB','DOM'][date.weekday - 1];
  return 'PRÓXIMO · $abbr';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite MyBookingsScreen — inline header, hero Próximo, hairline rows, section headers, empty state (BOOK-10, BOOK-11, BOOK-12, D-12..D-23)</name>
  <read_first>
    - lib/features/booking/ui/my_bookings_screen.dart (FULL FILE — current implementation, note _showDetailSheet and _confirmCancel methods)
    - lib/core/models/booking_model.dart (field names: date, startTime, status, paymentId, isRefunded exact getter name)
    - lib/features/schedule/ui/schedule_screen.dart (inline header pattern — copy wordmark Row exactly)
    - lib/features/booking/ui/hairline_booking_row.dart (HairlineBookingRow constructor — required params)
    - lib/core/widgets/sport_btn.dart (SportBtn.outlined signature)
  </read_first>
  <files>lib/features/booking/ui/my_bookings_screen.dart</files>
  <action>
Rewrite MyBookingsScreen completely. Keep business logic methods `_showDetailSheet()` and `_confirmCancel()`. Booking state logic (upcoming/past split) is preserved but modified to extract hero.

**Step 1: Convert to StatefulWidget**

MyBookingsScreen is currently a StatelessWidget. Keep it stateless — the screen itself manages no state, only BlocBuilder does.

**Step 2: Replace AppBar with inline header**

Remove `appBar: AppBar(title: const Text('Minhas Reservas'))`.
Replace Scaffold body structure with:
```dart
Scaffold(
  body: SafeArea(
    bottom: false,
    child: Column(
      children: [
        _buildHeader(),
        Expanded(
          child: BlocBuilder<BookingCubit, BookingState>(
            builder: (context, state) => switch (state) {
              BookingInitial() || BookingLoading() =>
                const Center(child: CircularProgressIndicator()),
              BookingError(:final message) => Center(
                  child: Text(message, style: AppTheme.ui(size: 14, color: AppTheme.orangeDk)),
                ),
              BookingLoaded(:final bookings) =>
                _buildBookingsList(context, bookings),
            },
          ),
        ),
      ],
    ),
  ),
)
```

**Step 3: Add `_buildHeader()` method (per D-12)**

```dart
Widget _buildHeader() {
  return Padding(
    padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
    child: Row(
      children: [
        Text('VIDA', style: AppTheme.display(size: 18, color: AppTheme.ink)),
        const SizedBox(width: 4),
        Container(
          padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
          decoration: BoxDecoration(
            color: AppTheme.orange,
            borderRadius: BorderRadius.circular(4),
          ),
          child: Text('ATIVA', style: AppTheme.display(size: 18, color: AppTheme.paper)),
        ),
        const Spacer(),
        Text('MINHAS RESERVAS',
            style: AppTheme.mono(size: 11, color: AppTheme.concrete)),
      ],
    ),
  );
}
```

**Step 4: Add `_heroEyebrow()` helper (per D-14)**

```dart
String _heroEyebrow(String bookingDate) {
  final today = DateTime.now();
  final date = DateTime.parse(bookingDate);
  final todayNorm = DateTime(today.year, today.month, today.day);
  final dateNorm = DateTime(date.year, date.month, date.day);
  final diff = dateNorm.difference(todayNorm).inDays;
  if (diff == 0) return 'PRÓXIMO · HOJE';
  if (diff == 1) return 'PRÓXIMO · AMANHÃ';
  final abbr = ['SEG','TER','QUA','QUI','SEX','SÁB','DOM'][date.weekday - 1];
  return 'PRÓXIMO · $abbr';
}
```

**Step 5: Add `_buildHeroBlock()` (per D-13, D-15)**

```dart
Widget _buildHeroBlock(BuildContext context, BookingModel booking) {
  final eyebrow = _heroEyebrow(booking.date);
  final timeDisplay = booking.startTime; // already "HH:mm"
  final dateFormatted = DateFormat('E, d MMM', 'pt_BR')
      .format(DateTime.parse(booking.date))
      .toUpperCase(); // "QUI, 27 MAI"

  void onTap() {
    if (booking.isPendingPayment && booking.paymentId != null) {
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => PixPaymentScreen(
          bookingId: booking.id,
          paymentId: booking.paymentId,
        ),
      ));
    } else {
      _showDetailSheet(context, booking, true);
    }
  }

  return GestureDetector(
    onTap: onTap,
    child: DecoratedBox(
      decoration: const BoxDecoration(
        border: Border(
          bottom: BorderSide(color: AppTheme.lineHair, width: 0.5),
        ),
      ),
      child: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(eyebrow, style: AppTheme.mono(size: 11, color: AppTheme.orange)),
            const SizedBox(height: 4),
            Text(timeDisplay, style: AppTheme.display(size: 72, color: AppTheme.ink)),
            const SizedBox(height: 4),
            Text(dateFormatted, style: AppTheme.mono(size: 11, color: AppTheme.concrete)),
          ],
        ),
      ),
    ),
  );
}
```

**Step 6: Add `_buildSectionHeader()` (per D-16)**

```dart
Widget _buildSectionHeader(String label) {
  return DecoratedBox(
    decoration: const BoxDecoration(
      border: Border(
        bottom: BorderSide(color: AppTheme.lineHair, width: 0.5),
      ),
    ),
    child: Padding(
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
      child: Text(
        label,
        style: AppTheme.mono(size: 10, color: AppTheme.concrete, letterSpacing: 1.6),
      ),
    ),
  );
}
```

**Step 7: Rewrite `_buildBookingsList()` (per D-13, D-17, D-18, D-23)**

```dart
Widget _buildBookingsList(BuildContext context, List<BookingModel> bookings) {
  final today = DateTime.now();
  final todayString =
      '${today.year}-${today.month.toString().padLeft(2, '0')}-${today.day.toString().padLeft(2, '0')}';

  final upcoming = bookings
      .where((b) => b.date.compareTo(todayString) >= 0 && !b.isCancelled && !b.isRefunded)
      .toList()
    ..sort((a, b) => a.date.compareTo(b.date));

  final past = bookings
      .where((b) => b.date.compareTo(todayString) < 0 || b.isCancelled || b.isRefunded)
      .toList()
    ..sort((a, b) => b.date.compareTo(a.date));

  if (upcoming.isEmpty && past.isEmpty) {
    return Center(
      child: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 32),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(Icons.event_busy, size: 48, color: AppTheme.concrete),
            const SizedBox(height: 16),
            Text('Nenhuma reserva',
                style: AppTheme.ui(size: 16, weight: FontWeight.w700, color: AppTheme.ink)),
            const SizedBox(height: 8),
            Text(
              'Vá para a agenda para reservar um horário.',
              style: AppTheme.ui(size: 14, color: AppTheme.concrete),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 24),
            SportBtn.outlined(
              'VER AGENDA',
              onPressed: () => StatefulNavigationShell.of(context).goBranch(0),
            ),
          ],
        ),
      ),
    );
  }

  // Hero = first upcoming booking (per D-13)
  final hero = upcoming.isNotEmpty ? upcoming.first : null;
  final remainingUpcoming = upcoming.length > 1 ? upcoming.sublist(1) : <BookingModel>[];

  return ListView(
    children: [
      // Hero block (per D-13)
      if (hero != null)
        _buildHeroBlock(context, hero),

      // "EM SEGUIDA" section (per D-16, D-17)
      if (remainingUpcoming.isNotEmpty) ...[
        _buildSectionHeader('EM SEGUIDA'),
        ...remainingUpcoming.asMap().entries.map(
          (entry) => HairlineBookingRow(
            booking: entry.value,
            bookingCubit: context.read<BookingCubit>(),
            index: entry.key,
            isFuture: true,
          ),
        ),
      ],

      // "HISTÓRICO" section (per D-16, D-17)
      if (past.isNotEmpty) ...[
        _buildSectionHeader('HISTÓRICO'),
        ...past.asMap().entries.map(
          (entry) => HairlineBookingRow(
            booking: entry.value,
            bookingCubit: context.read<BookingCubit>(),
            index: entry.key,
            isFuture: false,
          ),
        ),
      ],
    ],
  );
}
```

**Step 8: Update imports**

Add:
```dart
import 'package:intl/intl.dart';
import 'package:vida_ativa/core/theme/app_theme.dart';
import 'package:vida_ativa/core/widgets/sport_btn.dart';
import 'package:vida_ativa/features/booking/ui/hairline_booking_row.dart';
```

Remove:
```dart
import 'package:vida_ativa/core/utils/app_spacing.dart'; // replace with literal values
import 'package:vida_ativa/features/booking/ui/booking_card.dart'; // replaced by HairlineBookingRow
```

Keep unchanged:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:go_router/go_router.dart';
import 'package:vida_ativa/core/models/booking_model.dart';
import 'package:vida_ativa/features/booking/cubit/booking_cubit.dart';
import 'package:vida_ativa/features/booking/cubit/booking_state.dart';
import 'package:vida_ativa/core/utils/snack_helper.dart';
import 'package:vida_ativa/features/booking/ui/client_booking_detail_sheet.dart';
import 'package:vida_ativa/features/booking/ui/pix_payment_screen.dart';
```

Keep `_showDetailSheet()` and `_confirmCancel()` methods unchanged.

CRITICAL: Hero block must NOT be inside a Card or Container with a background color — transparent background only.
CRITICAL: `AppTheme.display(size: 72)` for hero time — never `SizedBox(height: 72, child: Text(...))`.
CRITICAL: Section headers use `_buildSectionHeader()` — no hardcoded `TextStyle(fontSize: 18, fontWeight: FontWeight.bold)`.
CRITICAL: No `Color(0xFF...)` literals anywhere.
CRITICAL: `AppSpacing` is REMOVED — use literal `16.0`, `8.0`, `12.0` directly.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter analyze lib/features/booking/ui/my_bookings_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AppTheme.display(size: 72" lib/features/booking/ui/my_bookings_screen.dart` returns 1 match
    - `grep -n "appBar:" lib/features/booking/ui/my_bookings_screen.dart` returns 0 matches (AppBar removed)
    - `grep -n "AppBar" lib/features/booking/ui/my_bookings_screen.dart` returns 0 matches
    - `grep -n "HairlineBookingRow" lib/features/booking/ui/my_bookings_screen.dart` returns at least 2 matches (upcoming + past)
    - `grep -n "EM SEGUIDA" lib/features/booking/ui/my_bookings_screen.dart` returns 1 match
    - `grep -n "HISTÓRICO" lib/features/booking/ui/my_bookings_screen.dart` returns 1 match
    - `grep -n "PRÓXIMO" lib/features/booking/ui/my_bookings_screen.dart` returns at least 2 matches (hero eyebrow logic)
    - `grep -n "VER AGENDA" lib/features/booking/ui/my_bookings_screen.dart` returns 1 match (SportBtn label)
    - `grep -n "SportBtn.outlined" lib/features/booking/ui/my_bookings_screen.dart` returns 1 match (empty state)
    - `grep -n "booking_card" lib/features/booking/ui/my_bookings_screen.dart` returns 0 matches (BookingCard removed)
    - `grep -rn "Color(0xFF" lib/features/booking/ui/my_bookings_screen.dart` returns 0 matches
    - `grep -n "SafeArea" lib/features/booking/ui/my_bookings_screen.dart` returns 1 match (inline header structure)
    - `flutter analyze lib/features/booking/ui/my_bookings_screen.dart` exits 0
  </acceptance_criteria>
  <done>MyBookingsScreen has no AppBar; inline header with "VIDA ATIVA" wordmark + "MINHAS RESERVAS" eyebrow; first upcoming booking shown as hero Anton 72px; remaining bookings and past as HairlineBookingRow; "EM SEGUIDA"/"HISTÓRICO" section headers in mono; empty state with SportBtn.outlined "VER AGENDA"</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three Arena-identity components for booking flow:
    1. Plan 01: SportBtn (filled/outlined) + HairlineBookingRow
    2. Plan 02: BookingConfirmationSheet with Anton 88px hero + orange stripe banner + SportBtn buttons
    3. Plan 03: MyBookingsScreen with inline header + Anton 72px hero + hairline rows + mono section headers
  </what-built>
  <how-to-verify>
    Run the app and verify:

    1. BOOKING CONFIRMATION SHEET (BOOK-07, BOOK-08, BOOK-09):
       - Navigate to Schedule tab → tap on an available slot
       - Sheet opens: drag handle at top, then hero block with: date eyebrow in small mono, time "HH:MM" in very large Anton (88px), price in small mono below
       - No icon rows (calendar/clock/money icons should be gone)
       - If arena is in "manual approval" mode and Pix is disabled: banner appears as narrow orange stripe on left — no colored background box
       - Payment buttons: "PAGAR COM PIX" (orange pill) and "PAGAR NA HORA" (outlined pill) — both full-width, Anton uppercase, no icons

    2. MY BOOKINGS SCREEN (BOOK-10, BOOK-11, BOOK-12):
       - Navigate to Minhas Reservas tab
       - No AppBar — header shows "VIDA ATIVA" wordmark with orange pill + "MINHAS RESERVAS" in small mono on right
       - First upcoming booking shows as hero block: orange eyebrow "PRÓXIMO · HOJE/AMANHÃ/[DAY]" + large Anton time (72px) + small mono date below
       - Below hero: section label "EM SEGUIDA" in small mono uppercase (if more upcoming bookings exist)
       - Past bookings under "HISTÓRICO" section label
       - All non-hero bookings render as minimal rows: day number + time + status pill (outline only, no fill)
       - If no bookings: "Nenhuma reserva" text + "VER AGENDA" outlined pill button

    3. GLOBAL:
       - flutter analyze exits 0
       - No green colors visible anywhere in booking flow (old primaryGreen → orange)
  </how-to-verify>
  <resume-signal>Escrever "aprovado" ou descrever problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → Screen | BookingModel data rendered from BookingCubit state — authenticated Firestore reads only |
| Widget → Navigator | Tap handlers route to PixPaymentScreen or ClientBookingDetailSheet — no data written to Firestore by this screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Information Disclosure | Hero block: booking date/time displayed prominently | accept | Data already visible to authenticated user; no PII; booking belongs to the current user |
| T-26-03-02 | Information Disclosure | Status labels ("CONFIRMADO", "CANCELADO") in status pills | accept | Status is non-sensitive enum value; user owns the booking; no other user data leaked |
| T-26-03-03 | Spoofing | `StatefulNavigationShell.of(context).goBranch(0)` in empty state | accept | Navigation is local within authenticated shell; cannot route to unauthenticated screen |
| T-26-03-04 | Information Disclosure | Console/debug: booking IDs logged via Sentry | accept | Sentry integration is in BookingCubit, not in this view; view does not add any logging |
</threat_model>

<verification>
After both tasks complete:
- `flutter analyze` exits 0 on all modified files
- All 3 BOOK requirements (10, 11, 12) verified by acceptance criteria greps
- Checkpoint approved by user after visual verification
</verification>

<success_criteria>
- MyBookingsScreen has no AppBar; uses SafeArea + inline header (BOOK-12 partial — wordmark)
- Hero "Próximo" Anton 72px with dynamic orange eyebrow visible (BOOK-10)
- HairlineBookingRow renders for all non-hero bookings with hairline separator (BOOK-11)
- "EM SEGUIDA" and "HISTÓRICO" in JBM mono uppercase tracked (BOOK-12)
- Empty state uses SportBtn.outlined "VER AGENDA"
- User checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/26-fluxo-de-reserva-cliente/26-03-SUMMARY.md`
</output>
