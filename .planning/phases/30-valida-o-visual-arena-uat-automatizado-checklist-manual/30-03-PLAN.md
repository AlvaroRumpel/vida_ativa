---
phase: 30-valida-o-visual-arena-uat-automatizado-checklist-manual
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
  - 30-02
files_modified: []
autonomous: true
requirements:
  - VAL-05

must_haves:
  truths:
    - "Cada tela coberta tem sua conformidade verificada ponto-a-ponto contra o CONTEXT.md da fase que a implementou"
    - "VALIDATION.md contém seção 'Conformidade Visual por Tela' com resultado por tela"
    - "Issues CRITICAL adicionais encontrados nesta fase têm fix aplicado"
    - "Issues MINOR adicionais documentados com status 'pendente manual'"
  artifacts:
    - path: ".planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/VALIDATION.md"
      provides: "Seção de conformidade visual por tela, com resultado PASS/FAIL por critério"
  key_links:
    - from: "VALIDATION.md conformidade visual"
      to: "26-CONTEXT.md / 27-CONTEXT.md / 28-CONTEXT.md"
      via: "verificação ponto-a-ponto contra decisões do CONTEXT.md original"
      pattern: "D-0[0-9]: .*"
---

<objective>
Ler cada arquivo de tela coberta e verificar conformidade visual ponto-a-ponto contra
as decisões dos CONTEXT.md das fases que os implementaram. Documentar resultado por
critério no VALIDATION.md. Aplicar fixes CRITICAL adicionais não capturados no Plan 01.

Purpose: Garantia de conformidade semântica — não apenas ausência de tokens hardcoded,
mas que os tokens corretos foram escolhidos (ex: Anton 88px e não 72px na confirmação).

Output: VALIDATION.md com seção de conformidade visual completa.
</objective>

<execution_context>
@F:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@F:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-CONTEXT.md
@.planning/phases/27-admin-slots-reservas-usu-rios/27-CONTEXT.md
@.planning/phases/28-admin-pre-os-ajustes/28-CONTEXT.md
@.planning/design-reference.md
@lib/core/theme/app_theme.dart

<!-- Summaries das fases executadas -->
@.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-01-SUMMARY.md
@.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conformidade visual — Booking Flow (confirmação + Pix QR + Minhas Reservas)</name>

  <read_first>
    - lib/features/booking/ui/booking_confirmation_sheet.dart
    - lib/features/booking/ui/pix_payment_screen.dart
    - lib/features/booking/ui/my_bookings_screen.dart
    - lib/features/booking/ui/hairline_booking_row.dart
  </read_first>

  <action>
Verificar cada tela do Booking Flow contra critérios do Phase 26 (BOOK-07 a BOOK-12).

**BookingConfirmationSheet — critérios a verificar:**

1. BOOK-07: Hero block exibe horário em `AppTheme.display(size: 88)` como elemento principal
   - Grep: `display(size: 88` deve existir
   - Grep: sem `Card(` envolvendo o hero

2. BOOK-08: Banner de aprovação manual usa faixa laranja 2px à esquerda, sem container colorido
   - Grep: `Container(width: 2, color: AppTheme.orange)` presente
   - Grep: sem `Container(color:` fora deste padrão de stripe

3. BOOK-09: Botões "PAGAR COM PIX" e "PAGAR NA HORA" são `SportBtn.filled` e `SportBtn.outlined`
   - Grep: `SportBtn.filled(` e `SportBtn.outlined(` presentes
   - Grep: sem `FilledButton(` ou `OutlinedButton(` para ações principais

**PixPaymentScreen — critérios a verificar (após fixes do Plan 01):**

4. QR screen usa AppTheme tokens exclusivamente (verificar resultado do Plan 01)
   - Grep: zero `Color(0x` e zero `Colors.` (exceto transparent)
   - AppBar título "Pagamento Pix" usa AppBarTheme do tema (sem TextStyle inline)

5. Countdown urgente usa `AppTheme.orangeDk` (não vermelho hardcoded)
   - Grep: `AppTheme.orangeDk` presente no countdown

6. Botão "Gerar novo QR" usa `AppTheme.orange` + `AppTheme.paper`
   - Grep: `AppTheme.orange` e `AppTheme.paper` no FilledButton de regeneração

**MyBookingsScreen — critérios a verificar:**

7. BOOK-10: Hero block exibe horário em `AppTheme.display(size: 72)` como elemento principal
   - Grep: `display(size: 72` deve existir
   - Grep: eyebrow em `AppTheme.mono` com `AppTheme.orange`

8. BOOK-11: Eyebrow "PRÓXIMO · HOJE" em `AppTheme.mono(color: AppTheme.orange)`
   - Grep: `AppTheme.orange` no eyebrow do hero

9. BOOK-12: Demais reservas usam `HairlineBookingRow` com hairline divisória
   - Grep: `HairlineBookingRow(` presente no ListView

**HairlineBookingRow — critérios gerais:**

10. Sem `Card(` — usa `DecoratedBox` com `Border`
    - Grep: sem `Card(` no arquivo
    - Grep: `DecoratedBox` e `BorderSide(color: AppTheme.lineHair` presentes

Para cada critério, registrar PASS ou FAIL no VALIDATION.md na seção
"Conformidade Visual por Tela — Booking Flow".

Se encontrar FAIL CRITICAL (tamanho errado, token errado, layout estruturalmente diferente):
aplicar fix inline e marcar "fix aplicado" no VALIDATION.md.
  </action>

  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && grep -n "display(size: 88" lib/features/booking/ui/booking_confirmation_sheet.dart && grep -n "display(size: 72" lib/features/booking/ui/my_bookings_screen.dart</automated>
  </verify>

  <done>
    Seção "Booking Flow" do VALIDATION.md preenchida com resultado PASS/FAIL por critério.
    Anton 88px na confirmação confirmado. Anton 72px no hero de Minhas Reservas confirmado.
    Eventuais issues CRITICAL corrigidos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Conformidade visual — Admin Frame + Slots + Reservas + Usuários + Preços + Ajustes</name>

  <read_first>
    - lib/features/admin/ui/admin_screen.dart
    - lib/features/admin/ui/slot_management_tab.dart
    - lib/features/admin/ui/booking_management_tab.dart
    - lib/features/admin/ui/admin_booking_row.dart
    - lib/features/admin/ui/users_management_tab.dart
    - lib/features/admin/ui/user_detail_sheet.dart
    - lib/features/admin/ui/pricing_tab.dart
    - lib/features/admin/ui/settings_tab.dart
    - lib/core/widgets/sport_btn.dart
  </read_first>

  <action>
Verificar cada tela admin contra critérios dos Phase 25, 27 e 28.

**Admin Frame (admin_screen.dart) — Phase 25:**

1. ADMN-13: TabBar com labels JetBrains Mono uppercase, indicador underline laranja 2px, fundo sand
   - Grep: `Tab(text: '` (uppercase strings) presente
   - Grep: `UnderlineTabIndicator` ou `tabBarTheme` do AppTheme (sem custom indicator override aqui)
   - Grep: sem `backgroundColor:` no TabBar que sobrescreva AppTheme

2. ADMN-14: Header exibe wordmark + "PAINEL ADMIN" mono + "cliente →" laranja
   - Grep: `VIDA` e `ATIVA` em Anton (display)
   - Grep: `PAINEL ADMIN` em `AppTheme.mono`
   - Grep: `cliente →` em `AppTheme.mono(color: AppTheme.orange)`

3. ADMN-15: Notification banner usa faixa laranja 2px à esquerda, sem fundo colorido
   - Grep: `Container(width: 2, color: AppTheme.orange)` em `_buildInlineBanner` e `_NotificationBanner`
   - Grep: sem `Container(color:` de fundo na área do banner (após fixes do Plan 01)

**Aba Slots (slot_management_tab.dart) — Phase 27 D-01 a D-06:**

4. ADMN-16: Slot vazio exibe hora em Anton 32px + preço em Manrope
   - Grep: `display(size: 32` presente no SlotRow
   - Grep: sem `ChoiceChip` e sem `selectedColor: AppTheme.primaryGreen`

5. ADMN-17: Day selector usa underline laranja 2px, botões ← → de navegação
   - Grep: `AdminDaySelector` presente
   - Grep: `Container(width: 20, height: 2, color: isSelected ? AppTheme.orange : Colors.transparent)`
   - (Colors.transparent aqui é aceitável — é invisível, não cor de marca)

6. Slot reservado exibe Anton laranja (D-02)
   - Grep: `color: isBooked ? AppTheme.orange : AppTheme.ink` no display do horário

**Aba Reservas (booking_management_tab.dart + admin_booking_row.dart) — Phase 27 D-07 a D-11:**

7. ADMN-18: AdminBookingRow tem Anton 36px + nome Manrope + status mono uppercase
   - Grep: `display(size: 36` ou similar no admin_booking_row.dart
   - Grep: sem `AdminBookingCard` (deletado)

8. ADMN-19: Pills Confirmar/Recusar visíveis apenas para pending, outline sem fundo colorido
   - Grep: `status == 'pending'` ou similar gate para mostrar pills
   - Grep: sem `backgroundColor:` sólido nas pills

**Aba Usuários (users_management_tab.dart + user_detail_sheet.dart) — Phase 27 D-12 a D-15:**

9. ADMN-20: Avatar circular ink para usuário, laranja para admin — Anton initial
   - Grep: `AppTheme.orange` e `AppTheme.ink` como cores de fundo do avatar
   - Grep: `display(size:` no initial do avatar

10. ADMN-21: UserDetailSheet existe com drag handle + SportBtn para ações de admin
    - Grep: arquivo user_detail_sheet.dart existe (já verificado) e importado
    - Grep: `SportBtn` no user_detail_sheet.dart

**Aba Preços (pricing_tab.dart) — Phase 28 D-01 a D-04:**

11. ADMN-22: Faixas hairline com Anton 30px hora + Anton 44px preço + timeline laranja 3px
    - Grep: `display(size: 30` e `display(size: 44` presentes
    - Grep: `Container(height: 3, color: AppTheme.lineHair)` + `Container(color: AppTheme.orange)` na timeline

12. ADMN-23: "Salvar tabela" é SportBtn.filledInk no rodapé
    - Grep: `SportBtn.filledInk(` presente no footer do _PricingEditor

**Aba Ajustes (settings_tab.dart) — Phase 28 D-05 a D-08:**

13. ADMN-24: Toggle Pix com label "PIX ATIVO" em Anton 26px + Switch sem activeColor hardcoded
    - Grep: `display(size: 26` presente
    - Grep: sem `activeColor:` no Switch (usa AppTheme.switchTheme)

14. ADMN-25: Campos MP como underline fields em mono + ícone olho
    - Grep: `AppTheme.mono` no style dos TextFields de credencial
    - Grep: `Icons.visibility` e `Icons.visibility_off` presentes

**SportBtn (sport_btn.dart) — verificar variantes:**

15. Três variantes existem: filled (orange), outlined (ink), filledInk (ink bg)
    - Grep: `_SportBtnVariant.filledInk` presente
    - Grep: `backgroundColor: AppTheme.ink` no case filledInk

Para cada critério, registrar PASS ou FAIL na seção "Conformidade Visual por Tela — Admin".
Aplicar fixes CRITICAL se encontrar e marcar no VALIDATION.md.
  </action>

  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && grep -n "display(size: 44" lib/features/admin/ui/pricing_tab.dart && grep -n "display(size: 26" lib/features/admin/ui/settings_tab.dart && grep -n "filledInk" lib/core/widgets/sport_btn.dart</automated>
  </verify>

  <done>
    Seção "Admin" do VALIDATION.md preenchida com resultado PASS/FAIL por critério ADMN-13 a ADMN-25.
    Anton 44px em PricingTab confirmado. SportBtn.filledInk confirmado. Eventuais issues CRITICAL corrigidos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit code → VALIDATION.md | Leitura de código + escrita de documentação — sem risco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering | Edições de fix inline neste plan | mitigate | Todo fix aplicado deve ser verificado com grep de confirmação imediata após edição |
| T-30-07 | Information Disclosure | user_detail_sheet.dart — dados de usuário | accept | Dados de usuário (email, nome) vêm de Firebase Auth já autenticado; sem PII adicional exposta |
</threat_model>

<verification>
- grep por `display(size: 88` em booking_confirmation_sheet.dart retorna resultado
- grep por `display(size: 72` em my_bookings_screen.dart retorna resultado
- grep por `display(size: 44` em pricing_tab.dart retorna resultado
- grep por `SportBtn.filledInk` em pricing_tab.dart retorna resultado
- VALIDATION.md contém seções "Booking Flow" e "Admin" com tabelas de conformidade
</verification>

<success_criteria>
- Todas as telas cobertas verificadas ponto-a-ponto contra CONTEXT.md das fases originais
- VALIDATION.md completo com conformidade visual por tela e critério
- Issues CRITICAL adicionais (se encontrados) corrigidos e documentados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-03-SUMMARY.md`
</output>
