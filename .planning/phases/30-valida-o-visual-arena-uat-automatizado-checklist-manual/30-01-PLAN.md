---
phase: 30-valida-o-visual-arena-uat-automatizado-checklist-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/booking/ui/pix_payment_screen.dart
  - lib/features/admin/ui/admin_screen.dart
autonomous: true
requirements:
  - VAL-01
  - VAL-02

must_haves:
  truths:
    - "Nenhum arquivo de UI das telas cobertas usa Color(0x...) ou Colors.* hardcoded (exceto Colors.transparent)"
    - "Todos os TextStyle() hardcoded foram substituídos por AppTheme.display/ui/mono"
    - "VALIDATION.md existe com todos os issues encontrados categorizados por severidade"
    - "Todos os issues CRITICAL encontrados estão marcados como 'fix aplicado' no VALIDATION.md"
  artifacts:
    - path: "lib/features/booking/ui/pix_payment_screen.dart"
      provides: "Pix QR screen com tokens Arena — zero hardcoded colors"
    - path: ".planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/VALIDATION.md"
      provides: "Registro completo de issues com severidade e status do fix"
  key_links:
    - from: "pix_payment_screen.dart"
      to: "AppTheme.*"
      via: "substituição de Color(0xFF...) e TextStyle() por AppTheme.display/ui/mono"
      pattern: "AppTheme\\.(display|ui|mono|orange|ink|concrete|court|lineHair|paper|sand)"
---

<objective>
Realizar grep sistemático por tokens hardcoded em todos os arquivos de UI das telas
cobertas pelo Phase 30, documentar issues em VALIDATION.md, e aplicar fixes CRITICAL
imediatamente. A prioridade é pix_payment_screen.dart (múltiplos hardcoded CRITICAL) e
admin_screen.dart (FCM error banner).

Purpose: Garantir que a identidade Arena Esportivo está 100% implementada via AppTheme
tokens, sem nenhum Color() ou TextStyle() hardcoded escapando.

Output: VALIDATION.md com todos os issues, pix_payment_screen.dart e admin_screen.dart
corrigidos.
</objective>

<execution_context>
@F:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@F:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-CONTEXT.md
@.planning/ROADMAP.md

<!-- AppTheme tokens — fonte de verdade -->
@lib/core/theme/app_theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep por tokens hardcoded + criar VALIDATION.md</name>

  <read_first>
    - lib/features/booking/ui/pix_payment_screen.dart
    - lib/features/booking/ui/booking_confirmation_sheet.dart
    - lib/features/booking/ui/my_bookings_screen.dart
    - lib/features/booking/ui/hairline_booking_row.dart
    - lib/features/admin/ui/admin_screen.dart
    - lib/features/admin/ui/slot_management_tab.dart
    - lib/features/admin/ui/booking_management_tab.dart
    - lib/features/admin/ui/admin_booking_row.dart
    - lib/features/admin/ui/users_management_tab.dart
    - lib/features/admin/ui/user_detail_sheet.dart
    - lib/features/admin/ui/pricing_tab.dart
    - lib/features/admin/ui/settings_tab.dart
    - lib/core/widgets/sport_btn.dart
    - lib/core/theme/app_theme.dart
  </read_first>

  <action>
Execute grep para cada padrão proibido nos arquivos de UI das telas cobertas:

Padrões a buscar (D-01):
- `Color(0x` — hardcoded hex color
- `Colors\.` — exceto `Colors.transparent` (aceitável em overlays)
- `#[0-9A-Fa-f]{6}` — literal hex em strings (raro em Dart, mas verificar)
- `TextStyle(` — TextStyle hardcoded (fora de AppTheme)
- `FontFamily:` — fonte hardcoded
- `AppTheme.primaryGreen` — legacy alias (deve usar AppTheme.court)
- `AppTheme.brandAmber` — legacy alias (deve usar AppTheme.orange)

Escopo de arquivos UI (telas cobertas):
```
lib/features/booking/ui/pix_payment_screen.dart
lib/features/booking/ui/booking_confirmation_sheet.dart
lib/features/booking/ui/my_bookings_screen.dart
lib/features/booking/ui/hairline_booking_row.dart
lib/features/admin/ui/admin_screen.dart
lib/features/admin/ui/slot_management_tab.dart
lib/features/admin/ui/booking_management_tab.dart
lib/features/admin/ui/admin_booking_row.dart
lib/features/admin/ui/users_management_tab.dart
lib/features/admin/ui/user_detail_sheet.dart
lib/features/admin/ui/pricing_tab.dart
lib/features/admin/ui/settings_tab.dart
lib/core/widgets/sport_btn.dart
```

Para cada ocorrência encontrada, determinar severidade:
- CRITICAL: cor errada visível ao usuário (wrong brand color, wrong font family)
- MINOR: espaçamento, tamanho próximo ao correto, legacy alias que ainda renderiza igual

Criar `.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/VALIDATION.md`
com a seguinte estrutura por issue:

```markdown
# VALIDATION.md — Phase 30: Visual Audit

**Audit date:** 2026-06-07
**Auditor:** Claude (automated)

## Issues

| ID | Screen | Severity | Description | File | Line | Fix Status |
|----|--------|----------|-------------|------|------|------------|
| V-01 | Pix QR | CRITICAL | ... | pix_payment_screen.dart | L241 | fix aplicado |
```

Pre-scan mostra que pix_payment_screen.dart tem os seguintes issues conhecidos para incluir:
- `Color(0xFFC62828)` em duas posições (countdown urgente + expirado) — deve ser AppTheme.orangeDk
- `Color(0xFF757575)` no texto de loading — deve ser AppTheme.concrete
- `Color(0xFFF5F5F5)` no container do código pix — deve ser AppTheme.paper
- `Color(0xFF424242)` no texto do código — deve ser AppTheme.ink
- `Color(0xFFFFF8E1)` no container de info — deve ser AppTheme.paper ou AppTheme.sand
- `Color(0xFFFFB300)` na borda do container de info — deve ser AppTheme.orange
- `Color(0xFFE65100)` no texto e ícone de info — deve ser AppTheme.orange
- `TextStyle(fontSize: 14, color: Color(0xFFC62828), fontStyle: italic)` — deve ser AppTheme.ui()
- `TextStyle(fontSize: 24, fontWeight: w700, color: isUrgent ? ...)` — deve ser AppTheme.mono() ou AppTheme.display()
- `TextStyle(fontSize: 16, fontWeight: w500)` em instrução — deve ser AppTheme.ui()
- `TextStyle(fontSize: 12, fontFamily: 'monospace')` no código — deve ser AppTheme.mono()
- `Colors.white` no FilledButton expirado — deve ser AppTheme.paper
- `AppTheme.primaryGreen` (3 ocorrências) — deve ser AppTheme.court
- `Colors.grey.withValues(alpha: 0.5)` no overlay expirado — deve ser AppTheme.ink.withValues(alpha:0.4)
- `BorderRadius.circular(16)` e `circular(12)` no container QR — manter (forma, não cor)
- `boxShadow` no container QR — MINOR (shadow, não cor de marca)

admin_screen.dart issues:
- `Colors.red.withValues(alpha: 0.1)` no FCM error banner — deve ser AppTheme.orangeDk.withValues(alpha:0.1)
- `TextStyle(color: Colors.red, fontSize: 12)` no FCM error — deve ser AppTheme.ui(size: 12, color: AppTheme.orangeDk)
- `TextStyle(fontSize: 13)` no _NotificationBanner — deve ser AppTheme.ui(size: 13)

booking_confirmation_sheet.dart:
- `TextStyle(color: AppTheme.orangeDk)` na linha do error message — MINOR, deve ser AppTheme.ui(color: AppTheme.orangeDk)

Documentar todos os issues encontrados pelo grep, não apenas os listados acima.
  </action>

  <verify>
    <automated>Test-Path "F:\_geral\Projetos\vida_ativa\.planning\phases\30-valida-o-visual-arena-uat-automatizado-checklist-manual\VALIDATION.md"</automated>
  </verify>

  <done>
    VALIDATION.md existe com tabela de issues preenchida. Cada issue tem ID, tela, severidade, descrição, arquivo+linha, e status do fix.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Aplicar fixes CRITICAL em pix_payment_screen.dart e admin_screen.dart</name>

  <read_first>
    - lib/features/booking/ui/pix_payment_screen.dart
    - lib/features/admin/ui/admin_screen.dart
    - lib/features/booking/ui/booking_confirmation_sheet.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/VALIDATION.md
  </read_first>

  <action>
Aplicar todos os fixes CRITICAL identificados na Task 1.

**pix_payment_screen.dart — substituições obrigatórias:**

1. `_buildCountdown()` — texto "QR expirado":
   - `TextStyle(fontSize: 14, color: Color(0xFFC62828), fontStyle: FontStyle.italic)` →
     `AppTheme.ui(size: 14, color: AppTheme.orangeDk).copyWith(fontStyle: FontStyle.italic)`

2. `_buildCountdown()` — countdown timer color:
   - `color: isUrgent ? const Color(0xFFC62828) : AppTheme.primaryGreen` →
     `color: isUrgent ? AppTheme.orangeDk : AppTheme.court`
   - `TextStyle(fontSize: 24, fontWeight: FontWeight.w700, color: ...)` →
     `AppTheme.display(size: 24, color: isUrgent ? AppTheme.orangeDk : AppTheme.court)`

3. `_buildCountdown()` — FilledButton "Gerar novo QR":
   - `backgroundColor: AppTheme.primaryGreen` → `backgroundColor: AppTheme.orange`
   - `foregroundColor: Colors.white` → `foregroundColor: AppTheme.paper`
   - `shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))` →
     `shape: const StadiumBorder()` (alinha com SportBtn.filled pattern)

4. `_buildLoading()` — texto "Gerando QR...":
   - `TextStyle(fontSize: 16, color: Color(0xFF757575))` →
     `AppTheme.ui(size: 16, color: AppTheme.concrete)`

5. `_buildQrContent()` — instrução header:
   - `TextStyle(fontSize: 16, fontWeight: FontWeight.w500)` →
     `AppTheme.ui(size: 16, weight: FontWeight.w500)`

6. `_buildQrContent()` — container do QR image (boxShadow MINOR — manter, mas cor do container):
   - `color: Colors.white` → `color: AppTheme.paper`
   - `boxShadow` com `Colors.black.withValues(alpha: 0.08)` → manter (shadow é MINOR)

7. `_buildQrContent()` — overlay QR expirado:
   - `color: Colors.grey.withValues(alpha: 0.5)` → `color: AppTheme.ink.withValues(alpha: 0.4)`

8. `_buildQrContent()` — divider "ou use o codigo" texto:
   - `color: Colors.grey[600]` → `AppTheme.concrete`
   - `fontSize: 13` inline → `AppTheme.ui(size: 13, color: AppTheme.concrete)`

9. `_buildQrContent()` — container do código pix (copia-e-cola):
   - `color: const Color(0xFFF5F5F5)` → `color: AppTheme.paper`
   - `BorderRadius.circular(12)` → manter (forma OK)
   - `TextStyle(fontSize: 12, fontFamily: 'monospace', color: Color(0xFF424242))` →
     `AppTheme.mono(size: 12, color: AppTheme.ink)`

10. `_buildQrContent()` — OutlinedButton copiar:
    - `foregroundColor: AppTheme.primaryGreen` → `AppTheme.court` (ou AppTheme.orange — prefer orange para consistência com SportBtn; usar AppTheme.orange)
    - `side: BorderSide(color: _copied ? AppTheme.primaryGreen : AppTheme.primaryGreen.withValues(alpha:0.5))` →
      `side: BorderSide(color: _copied ? AppTheme.orange : AppTheme.orange.withValues(alpha:0.5))`
    - Manter shape: RoundedRectangleBorder (aceitável para este botão secundário especializado)

11. `_buildQrContent()` — container info amarelo:
    - `color: const Color(0xFFFFF8E1)` → `color: AppTheme.sand`
    - `border: Border.all(color: const Color(0xFFFFB300), width: 1)` → `border: Border.all(color: AppTheme.orange, width: 1)`
    - `Icon(size: 16, color: Color(0xFFE65100))` → `Icon(size: 16, color: AppTheme.orange)`
    - `TextStyle(fontSize: 12, color: Color(0xFFE65100))` → `AppTheme.ui(size: 12, color: AppTheme.orange)`

**admin_screen.dart — fixes CRITICAL:**

12. FCM error banner (AdminFcmError branch no BlocBuilder):
    - `color: Colors.red.withValues(alpha: 0.1)` → `color: AppTheme.orangeDk.withValues(alpha: 0.1)`
    - `TextStyle(color: Colors.red, fontSize: 12)` → `AppTheme.ui(size: 12, color: AppTheme.orangeDk)`

13. _NotificationBanner widget — `TextStyle(fontSize: 13)`:
    - Linha com `'Ative as notificações...'` usa `style: TextStyle(fontSize: 13)` →
      `style: AppTheme.ui(size: 13)`

**booking_confirmation_sheet.dart — fix MINOR (aplicar mesmo sendo minor):**

14. Error message text:
    - `const TextStyle(color: AppTheme.orangeDk)` →
      `AppTheme.ui(size: 13, color: AppTheme.orangeDk)`

Após cada substituição, atualizar o status do fix em VALIDATION.md para "fix aplicado".

Verificar com grep que não restam `Color(0x`, `Colors\.` (exceto `Colors.transparent`),
`TextStyle(` em nenhum dos 3 arquivos modificados.
  </action>

  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && grep -rn "Color(0x\|Colors\.\|TextStyle(" lib/features/booking/ui/pix_payment_screen.dart lib/features/admin/ui/admin_screen.dart lib/features/booking/ui/booking_confirmation_sheet.dart | grep -v "Colors.transparent" | wc -l</automated>
  </verify>

  <done>
    Os três arquivos não contêm mais Color(0x...), Colors.* (exceto transparent), ou TextStyle() hardcoded.
    VALIDATION.md tem todos os issues com status "fix aplicado" ou "pendente manual" (apenas MINOR não críticos).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → codebase | Leitura de arquivos fonte — sem risco de injeção |
| VALIDATION.md → git | Arquivo de documentação — sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | pix_payment_screen.dart — QR code display | accept | QR é dado temporal gerado pelo backend; sem PII no código |
| T-30-02 | Tampering | Edições em pix_payment_screen.dart | mitigate | Rodar flutter analyze após edições para garantir zero erros de compilação |
| T-30-03 | Denial of Service | CountdownTimer sem cleanup | accept | dispose() já cancela _countdownTimer; padrão correto presente |
</threat_model>

<verification>
Após execução:
- grep por `Color(0x` em lib/features/booking/ui/pix_payment_screen.dart retorna 0 resultados
- grep por `Colors\.` (exceto transparent) retorna 0 resultados nos 3 arquivos modificados
- grep por `AppTheme.primaryGreen` retorna 0 resultados nos arquivos modificados
- VALIDATION.md existe e tem ao menos 10 issues documentados
- Todos os issues CRITICAL marcados como "fix aplicado"
</verification>

<success_criteria>
- pix_payment_screen.dart usa exclusivamente AppTheme tokens (zero Color(0x...) hardcoded)
- admin_screen.dart FCM error banner usa AppTheme.orangeDk (zero Colors.red)
- VALIDATION.md completo e commitado com todos os issues da fase
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-01-SUMMARY.md`
</output>
