---
phase: 30-valida-o-visual-arena-uat-automatizado-checklist-manual
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - VAL-03
  - VAL-04

must_haves:
  truths:
    - "flutter analyze retorna 0 errors (warnings de info-level são aceitáveis)"
    - "flutter build web --release completa sem erros"
    - "Testes de widget existentes para fases 26-29 passam sem falhas"
  artifacts:
    - path: ".planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/VALIDATION.md"
      provides: "Seção de build/analyze gate adicionada com resultado dos comandos"
  key_links:
    - from: "lib/features/**/*.dart"
      to: "flutter analyze"
      via: "Dart analyzer — deve retornar 0 errors"
      pattern: "No issues found"
---

<objective>
Executar o gate de build e análise estática para garantir que o codebase compila
limpo após todas as alterações da v6.0. Verificar cobertura de widget tests para
as telas implementadas nas fases 26-29.

Purpose: Garantir que nenhuma edição da v6.0 introduziu erros de compilação ou
regressões, e que existe cobertura mínima de testes de widget para as novas telas.

Output: Resultado de flutter analyze + flutter build web documentado no VALIDATION.md.
Falhas de teste documentadas com severidade.
</objective>

<execution_context>
@F:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@F:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: flutter analyze — gate de análise estática</name>

  <read_first>
    - pubspec.yaml
  </read_first>

  <action>
Executar flutter analyze no diretório do projeto:

```
cd F:/_geral/Projetos/vida_ativa && flutter analyze
```

Critério de pass (D-04): zero errors. Warnings de info-level (`info •`) são tolerados.

Interpretar saída:
- `No issues found!` → PASS
- `N issue(s) found.` onde todos são `info •` ou `warning •` → PASS (documentar warnings)
- Qualquer `error •` → FAIL (adicionar ao VALIDATION.md como CRITICAL, investigar e corrigir)

Erros comuns a investigar se aparecerem:
- Import não resolvido → arquivo deletado ou movido
- Type mismatch → mudança de tipo em model/cubit
- Undefined name → widget renomeado sem atualizar importadores

Adicionar seção ao VALIDATION.md:

```markdown
## Build & Analyze Gate

### flutter analyze
**Status:** PASS / FAIL
**Command:** `flutter analyze`
**Output summary:** [N issues found / No issues found]
**Errors (if any):**
- [erro, arquivo:linha]
```
  </action>

  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter analyze 2>&1 | tail -5</automated>
  </verify>

  <done>
    flutter analyze retorna 0 errors. Resultado documentado no VALIDATION.md.
    Se houver errors: corrigidos antes de prosseguir.
  </done>
</task>

<task type="auto">
  <name>Task 2: flutter build web + cobertura de widget tests</name>

  <read_first>
    - test/features/admin/ui/admin_booking_row_test.dart
    - test/features/admin/ui/slot_management_tab_test.dart
    - test/features/admin/ui/user_detail_sheet_test.dart
    - test/features/admin/ui/dashboard_tab_test.dart
    - pubspec.yaml
  </read_first>

  <action>
**Parte A — Widget tests das fases 26-29:**

Executar os testes de widget existentes:
```
cd F:/_geral/Projetos/vida_ativa && flutter test test/features/admin/ui/ test/features/booking/ --reporter compact 2>&1
```

Ler os arquivos de teste antes de executar para entender o que cada um cobre.
Verificar quais telas das fases 26-29 têm cobertura (D-06):
- Phase 26: HairlineBookingRow, BookingConfirmationSheet, MyBookingsScreen
- Phase 27: AdminBookingRow (admin_booking_row_test.dart), SlotManagementTab, UserDetailSheet
- Phase 28: PricingTab, SettingsTab (verificar se existem)
- Phase 29: DashboardTab (dashboard_tab_test.dart)

Listar telas SEM cobertura de widget test. Documentar no VALIDATION.md:
```markdown
### Widget Test Coverage (Fases 26-29)

| Screen | Test File | Status |
|--------|-----------|--------|
| HairlineBookingRow | ? | presente / ausente |
| BookingConfirmationSheet | ? | presente / ausente |
| ...
```

Se um teste falhar: documentar no VALIDATION.md como CRITICAL se relacionado a
token Arena (cor/fonte errada detectada pelo test), ou MINOR se falha de mock/infra.

**Parte B — flutter build web --release (D-05):**

Executar:
```
cd F:/_geral/Projetos/vida_ativa && flutter build web --release 2>&1 | tail -20
```

Este comando é lento (~2-5 min). Aguardar conclusão completa.

Interpretar:
- `Built build/web` → PASS
- Qualquer `Error:` ou `Exception:` → FAIL (documentar, investigar)

Adicionar ao VALIDATION.md:
```markdown
### flutter build web --release
**Status:** PASS / FAIL
**Output:** Built build/web (N.NMB) / [error message]
```
  </action>

  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter test test/features/admin/ui/ --reporter compact 2>&1 | tail -3</automated>
  </verify>

  <done>
    flutter build web --release completa com "Built build/web". Widget tests passam.
    Cobertura de testes documentada no VALIDATION.md (telas com e sem cobertura).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flutter build → web artifacts | Build de produção — output em build/web/ |
| test runner → Firebase mocks | Testes usam mocks — sem acesso real ao Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | build/web artifacts | accept | Build é local/staging — não é deploy de produção neste plan |
| T-30-05 | Information Disclosure | flutter analyze output | accept | Saída do analyzer é interna, sem dados sensíveis |
</threat_model>

<verification>
- flutter analyze: zero linhas com `error •` na saída
- flutter build web: linha "Built build/web" presente na saída
- VALIDATION.md contém seção "Build & Analyze Gate" preenchida
- VALIDATION.md contém tabela de cobertura de widget tests
</verification>

<success_criteria>
- `flutter analyze` retorna 0 errors (D-04)
- `flutter build web --release` completa sem erros (D-05)
- Cobertura de widget tests das fases 26-29 mapeada e documentada (D-06)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/30-valida-o-visual-arena-uat-automatizado-checklist-manual/30-02-SUMMARY.md`
</output>
