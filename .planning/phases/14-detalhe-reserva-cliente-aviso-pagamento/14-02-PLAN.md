---
phase: 14-detalhe-reserva-cliente-aviso-pagamento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/booking/ui/booking_confirmation_sheet.dart
autonomous: true
requirements:
  - BOOK-06

must_haves:
  truths:
    - "Tela de confirmacao de reserva exibe aviso de pagamento antes do botao Reservar"
    - "Aviso e visualmente destacado (banner/container com cor de aviso)"
    - "Texto do aviso explica que a reserva so sera confirmada mediante pagamento"
  artifacts:
    - path: "lib/features/booking/ui/booking_confirmation_sheet.dart"
      provides: "Payment warning banner widget in booking confirmation flow"
      contains: "pagamento"
  key_links:
    - from: "booking_confirmation_sheet.dart"
      to: "user"
      via: "Payment warning banner between price row and participants field"
      pattern: "pagamento"
---

<objective>
Add a payment warning banner to BookingConfirmationSheet, inserted between the price info row and the participants text field.

Purpose: Users currently see no indication that booking confirmation requires payment. The banner sets expectations before they commit, reducing confusion and support requests.
Output: booking_confirmation_sheet.dart updated with a visible payment disclaimer banner.
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add payment warning banner to BookingConfirmationSheet</name>
  <files>lib/features/booking/ui/booking_confirmation_sheet.dart</files>

  <read_first>
    - lib/features/booking/ui/booking_confirmation_sheet.dart (full file — understand current widget tree)
    - lib/core/theme/app_theme.dart (AppTheme.primaryGreen and any warning color constants)
  </read_first>

  <action>
In `booking_confirmation_sheet.dart`, insert a payment warning banner in the `build()` method's Column children list.

Insertion point: after the price `_infoRow` block (after `const SizedBox(height: 16)` that precedes the `TextField`), before the `TextField` for participants.

Concretely, the current sequence is:
```
_infoRow(Icons.attach_money, ...)   // line ~124
const SizedBox(height: 16),          // line ~129
TextField(...)                        // line ~130
```

Change it to:
```
_infoRow(Icons.attach_money, ...)
const SizedBox(height: 16),
// --- INSERT PAYMENT WARNING HERE ---
_paymentWarningBanner(),
const SizedBox(height: 16),
TextField(...)
```

Add a private method `_paymentWarningBanner()` to `_BookingConfirmationSheetState`:

```dart
Widget _paymentWarningBanner() {
  return Container(
    padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 10),
    decoration: BoxDecoration(
      color: const Color(0xFFFFF3E0),
      borderRadius: BorderRadius.circular(10),
      border: Border.all(color: const Color(0xFFFFB300), width: 1),
    ),
    child: Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        const Icon(Icons.info_outline, size: 18, color: Color(0xFFE65100)),
        const SizedBox(width: 8),
        const Expanded(
          child: Text(
            'Esta reserva so sera confirmada apos o pagamento. '
            'Aguarde a confirmacao do estabelecimento.',
            style: TextStyle(
              fontSize: 13,
              color: Color(0xFFE65100),
              height: 1.4,
            ),
          ),
        ),
      ],
    ),
  );
}
```

Color rationale:
- Background `0xFFFFF3E0` (amber 50) and border `0xFFFFB300` (amber 600): standard warning palette, no new theme constants needed
- Icon and text `0xFFE65100` (deep orange 900): high contrast on amber background, readable

No new imports needed — `Container`, `Row`, `Icon`, `Text` are all from `package:flutter/material.dart` which is already imported.

Do NOT alter any other part of the file (the submit logic, error handling, or other info rows).
  </action>

  <verify>
    <automated>grep -n "pagamento\|_paymentWarningBanner\|FFF3E0" "F:/_geral/Projetos/vida_ativa/lib/features/booking/ui/booking_confirmation_sheet.dart"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_paymentWarningBanner"` returns at least 2 matches (method definition + call site)
    - `grep -n "pagamento"` returns a match with the warning text
    - `grep -n "FFB300"` returns a match (border color confirming the banner container exists)
    - `grep -n "info_outline"` returns a match (warning icon)
    - `grep -n "TextField"` still returns a match (participants field not removed)
    - `grep -n "FilledButton"` still returns a match (Reservar button not removed)
    - The banner call site appears AFTER the price infoRow SizedBox and BEFORE the TextField (verify line order by reading the file)
  </acceptance_criteria>

  <done>Tela de confirmacao de reserva exibe banner amarelo-laranja com icone de info e texto "Esta reserva so sera confirmada apos o pagamento. Aguarde a confirmacao do estabelecimento." entre o preco e o campo de participantes.</done>
</task>

</tasks>

<verification>
After task completes, run:
```
flutter analyze lib/features/booking/ui/booking_confirmation_sheet.dart
```
Expect: no errors.

Manual check: Tap a slot on the schedule to open the confirmation sheet. The payment warning banner should appear between the price row and the participants input field, before the "Reservar" button.
</verification>

<success_criteria>
1. BookingConfirmationSheet exibe banner de aviso de pagamento entre o preco e o campo de participantes
2. Banner tem cor de destaque (fundo amarelo claro, borda amber, texto laranja escuro)
3. Texto comunica claramente que confirmacao depende de pagamento
4. Nenhum outro comportamento da tela alterado (submit, validacao, campos existentes)
</success_criteria>

<output>
After completion, create `.planning/phases/14-detalhe-reserva-cliente-aviso-pagamento/14-02-SUMMARY.md`
</output>
