---
phase: 23-design-system-navigationbar
plan: 03
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified: []
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "flutter analyze --no-fatal-infos exits 0 with zero errors and zero warnings"
    - "flutter build web --release completes without compilation errors (exit code 0)"
    - "No Color(0xFF...) or Colors.* remains in the 2 audited files"
    - "assets/google_fonts/ contains exactly 3 .ttf files"
    - "pubspec.yaml lists assets/google_fonts/ under flutter.assets"
    - "app_shell.dart uses AppTheme.lineHair for nav bar border"
  artifacts:
    - path: "build/web/main.dart.js"
      provides: "Compiled Flutter web artifact — proof build succeeded"
  key_links:
    - from: "flutter analyze"
      to: "all Dart source files in lib/"
      via: "Dart analyzer static analysis"
      pattern: "No issues found"
    - from: "flutter build web --release"
      to: "build/web/"
      via: "Dart2JS compilation"
      pattern: "Built build/web"
---

<objective>
Run full build verification to confirm Phase 23 changes compile cleanly and introduce no analyzer warnings or errors.

Purpose: Closes Phase 23 with evidence — a clean build is the completion criterion (D-03). No visual staging verification required this phase (deferred per CONTEXT.md deferred decisions).
Output: `flutter analyze` and `flutter build web --release` both exit 0. Phase 23 is done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-design-system-navigationbar/23-CONTEXT.md
@.planning/phases/23-design-system-navigationbar/23-VALIDATION.md
@.planning/phases/23-design-system-navigationbar/23-01-SUMMARY.md
@.planning/phases/23-design-system-navigationbar/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full analyzer + build verification</name>
  <files></files>
  <read_first>
    - .planning/phases/23-design-system-navigationbar/23-02-SUMMARY.md (confirm color audit completed)
    - .planning/phases/23-design-system-navigationbar/23-01-SUMMARY.md (confirm fonts bundled + border fixed)
  </read_first>
  <action>
Run the full verification sequence:

**Step 1 — Pre-flight: confirm prior work is in place:**
```
# Fonts exist
ls -la assets/google_fonts/

# pubspec updated
grep "google_fonts" pubspec.yaml

# Border fix in place
grep "AppTheme.lineHair" lib/app_shell.dart

# Color audit done (must return 0 for each)
grep -c "Color(0x" lib/features/admin/ui/admin_booking_card.dart
grep -c "Color(0x" lib/features/booking/ui/booking_confirmation_sheet.dart
grep -c "Colors\." lib/features/admin/ui/admin_booking_card.dart
grep -c "Colors\." lib/features/booking/ui/booking_confirmation_sheet.dart
```

If any pre-flight check fails, fix the issue before proceeding (refer to 23-01-PLAN.md or 23-02-PLAN.md for the corrective action).

**Step 2 — Flutter analyzer (fast, ~10–30 seconds):**
```
flutter analyze --no-fatal-infos
```
Expected output: `No issues found!` or only hint-level items.
If any WARNING or ERROR appears: fix in the relevant file and re-run before proceeding to Step 3.

**Step 3 — Full web build (slow, ~2–4 minutes):**
```
flutter build web --release
```
Expected output ends with: `Built build/web` (exit code 0).
If build fails: read the error message carefully. Common causes:
  - Unused import in modified file → remove it
  - `const` on TextStyle with AppTheme token → remove `const` keyword from the TextStyle (AppTheme tokens ARE const but TextStyle() with named arg may not be)
  - Missing `import 'package:vida_ativa/core/theme/app_theme.dart';` → verify import exists in edited files

After both commands exit 0, the phase is complete.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos 2>&amp;1 | tail -5 && flutter build web --release 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `flutter analyze --no-fatal-infos` exits 0 with zero errors and zero warnings (hints are acceptable)
    - `flutter build web --release` exits 0 and outputs "Built build/web"
    - build/web/main.dart.js exists (or build/web/flutter_service_worker.js)
    - Phase 23 completion criteria (D-03) are fully satisfied
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output → Firebase Hosting | Compiled JS/HTML served statically — no server-side trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | build/web/ output | accept | Build artifacts verified by flutter build exit code; no additional signing needed for staging |
</threat_model>

<verification>
Final phase verification (all must pass):
1. `flutter analyze --no-fatal-infos` → exits 0
2. `flutter build web --release` → exits 0
3. `ls assets/google_fonts/` → 3 .ttf files
4. `grep -n "AppTheme.lineHair" lib/app_shell.dart` → shows border line
5. `grep -c "Color(0x" lib/features/admin/ui/admin_booking_card.dart` → 0
6. `grep -c "Color(0x" lib/features/booking/ui/booking_confirmation_sheet.dart` → 0
</verification>

<success_criteria>
- `flutter analyze --no-fatal-infos` exits 0 — zero errors, zero warnings
- `flutter build web --release` exits 0 — web artifact produced in build/web/
- All 6 Phase 23 requirements (DS-01, DS-02, DS-03, DS-04, NAV-01, NAV-02) satisfied
- Phase 23 state can be marked Complete in .planning/STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-system-navigationbar/23-03-SUMMARY.md`
</output>
