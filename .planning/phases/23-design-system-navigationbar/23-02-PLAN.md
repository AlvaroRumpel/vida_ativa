---
phase: 23-design-system-navigationbar
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - lib/features/admin/ui/admin_booking_card.dart
  - lib/features/booking/ui/booking_confirmation_sheet.dart
autonomous: true
requirements:
  - DS-01
  - DS-03
  - DS-04

must_haves:
  truths:
    - "admin_booking_card.dart contains zero Color(0xFF...) or Colors.* hardcoded values"
    - "booking_confirmation_sheet.dart contains zero Color(0xFF...) hardcoded values"
    - "All color references in both files use AppTheme.* static const tokens"
    - "flutter analyze --no-fatal-infos exits 0 after replacements"
  artifacts:
    - path: "lib/features/admin/ui/admin_booking_card.dart"
      provides: "AdminBookingCard with all colors from AppTheme tokens"
      contains: "AppTheme."
    - path: "lib/features/booking/ui/booking_confirmation_sheet.dart"
      provides: "BookingConfirmationSheet with all colors from AppTheme tokens"
      contains: "AppTheme."
  key_links:
    - from: "admin_booking_card.dart _statusColor()"
      to: "AppTheme.sun, AppTheme.court, AppTheme.orange, AppTheme.concrete"
      via: "switch expression return values"
      pattern: "AppTheme\\.(sun|court|orange|concrete|ink|orangeDk)"
    - from: "admin_booking_card.dart _sportBgColors / _sportFgColors"
      to: "AppTheme.paper, AppTheme.ink, AppTheme.court, AppTheme.orange, AppTheme.orangeDk, AppTheme.sun"
      via: "static const List<Color>"
      pattern: "AppTheme\\."
    - from: "booking_confirmation_sheet.dart _paymentWarningBanner()"
      to: "AppTheme.paper, AppTheme.sun, AppTheme.orange"
      via: "Container BoxDecoration color + border + Icon color"
      pattern: "AppTheme\\."
---

<objective>
Replace all hardcoded Color(0xFF...) and Colors.* values in admin_booking_card.dart and booking_confirmation_sheet.dart with AppTheme.* tokens.

Purpose: Eliminates color drift from v5.0 Material palette — these files still use old colors (blues, greens, purples) that clash with the Arena Esportivo design system. After this plan, all color values are centralized in AppTheme, so palette changes cascade globally.
Output: Both files use only AppTheme.* color tokens. Zero hardcoded Color(0xFF...) remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-design-system-navigationbar/23-CONTEXT.md
@.planning/phases/23-design-system-navigationbar/23-RESEARCH.md
@.planning/phases/23-design-system-navigationbar/23-01-SUMMARY.md

<interfaces>
<!-- AppTheme token reference (lib/core/theme/app_theme.dart — DO NOT MODIFY) -->
```dart
static const Color sand      = Color(0xFFF4EFE2); // bg principal
static const Color paper     = Color(0xFFFBF8F0); // surface lifted / light background
static const Color ink       = Color(0xFF0E0E0C); // near-black / dark text
static const Color inkSoft   = Color(0xFF2A2A26);
static const Color concrete  = Color(0xFF6B6B66); // secondary/dim text, icons
static const Color line      = Color(0xFFD9D2BE); // divider
static const Color lineHair  = Color(0xFFEAE3CE); // thin hairline divider
static const Color orange    = Color(0xFFFF4D17); // primary accent
static const Color orangeDk  = Color(0xFFC9300A); // darker orange / error / danger
static const Color court     = Color(0xFF1B5E2A); // success green
static const Color sun       = Color(0xFFFFB800); // yellow / warning
// Legacy aliases:
static const Color primaryGreen = court;   // same as court
static const Color brandAmber   = orange;  // same as orange
```

<!-- Hardcoded colors found in admin_booking_card.dart (lines 19–52) -->
```dart
// _statusColor() — 5 hardcoded values:
('pending', _) => Colors.orange,               // → AppTheme.orange
('pending_payment', _) => const Color(0xFFFFC107),  // → AppTheme.sun
('confirmed', 'pix') => const Color(0xFF4CAF50),    // → AppTheme.court
('confirmed', 'on_arrival') => const Color(0xFF2196F3), // → AppTheme.ink (blue→ink for Arena)
('expired', _) => Colors.grey,                 // → AppTheme.concrete
('rejected', _) => Colors.red,                 // → AppTheme.orangeDk

// _sportBgColors — 8 hardcoded values (background chips):
Color(0xFFE3F2FD), // blue bg   → AppTheme.paper
Color(0xFFE8F5E9), // green bg  → AppTheme.paper
Color(0xFFFFF3E0), // orange bg → AppTheme.paper
Color(0xFFF3E5F5), // purple bg → AppTheme.paper
Color(0xFFFCE4EC), // pink bg   → AppTheme.paper
Color(0xFFE0F7FA), // teal bg   → AppTheme.paper
Color(0xFFF9FBE7), // lime bg   → AppTheme.paper
Color(0xFFFFF8E1), // amber bg  → AppTheme.paper

// _sportFgColors — 8 hardcoded values (foreground chips):
Color(0xFF1565C0), // blue fg   → AppTheme.ink
Color(0xFF2E7D32), // green fg  → AppTheme.court
Color(0xFFE65100), // orange fg → AppTheme.orange
Color(0xFF6A1B9A), // purple fg → AppTheme.ink
Color(0xFFC62828), // red fg    → AppTheme.orangeDk
Color(0xFF00695C), // teal fg   → AppTheme.court
Color(0xFF558B2F), // lime fg   → AppTheme.court
Color(0xFFF57F17), // amber fg  → AppTheme.sun

// participants/time icon color:
const Icon(Icons.group, size: 14, color: Colors.grey)       // → AppTheme.concrete
const TextStyle(color: Colors.grey, fontSize: 13)           // → AppTheme.concrete
const Icon(Icons.access_time, size: 14, color: Colors.grey) // → AppTheme.concrete
const TextStyle(color: Colors.grey)                         // → AppTheme.concrete
const Icon(Icons.attach_money, size: 14, color: Colors.grey) // → AppTheme.concrete
const TextStyle(color: Colors.grey)                         // → AppTheme.concrete

// OutlinedButton reject foreground:
OutlinedButton.styleFrom(foregroundColor: Colors.red)       // → AppTheme.orangeDk
```

<!-- Hardcoded colors found in booking_confirmation_sheet.dart -->
```dart
// _paymentWarningBanner() — 3 hardcoded:
color: const Color(0xFFFFF3E0),                             // → AppTheme.paper (light bg)
border: Border.all(color: const Color(0xFFFFB300), width: 1) // → AppTheme.sun
Icon(Icons.info_outline, size: 18, color: Color(0xFFE65100)) // → AppTheme.orange
TextStyle(color: Color(0xFFE65100), ...)                    // → AppTheme.orange

// Drag handle — line 277:
color: const Color(0xFFD0CAC0)                              // → AppTheme.line

// Error text — line 389:
const TextStyle(color: Color(0xFFC62828))                   // → AppTheme.orangeDk

// _infoRow icon bg: AppTheme.primaryGreen.withValues(alpha:0.10) — keep as-is (already token)
// Switch: activeThumbColor: AppTheme.primaryGreen — keep as-is (already token)
// CircularProgressIndicator: color: Colors.white — → AppTheme.paper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors in admin_booking_card.dart</name>
  <files>
    lib/features/admin/ui/admin_booking_card.dart
  </files>
  <read_first>
    - lib/features/admin/ui/admin_booking_card.dart (full file — verify current state before editing)
    - lib/core/theme/app_theme.dart lines 7–22 (token values for reference — DO NOT modify)
  </read_first>
  <action>
Run grep first to confirm all hardcoded colors present before editing:
```
grep -n "Color(0x\|Colors\." lib/features/admin/ui/admin_booking_card.dart
```

Apply ALL of the following replacements in admin_booking_card.dart:

**1. _statusColor() switch expression (lines 19–28):**
```dart
// BEFORE:
('pending', _) => Colors.orange,
('pending_payment', _) => const Color(0xFFFFC107),
('confirmed', 'pix') => const Color(0xFF4CAF50),
('confirmed', 'on_arrival') => const Color(0xFF2196F3),
('confirmed', _) => AppTheme.primaryGreen,
('expired', _) => Colors.grey,
('rejected', _) => Colors.red,
_ => Colors.grey,

// AFTER:
('pending', _) => AppTheme.orange,
('pending_payment', _) => AppTheme.sun,
('confirmed', 'pix') => AppTheme.court,
('confirmed', 'on_arrival') => AppTheme.ink,
('confirmed', _) => AppTheme.court,
('expired', _) => AppTheme.concrete,
('rejected', _) => AppTheme.orangeDk,
_ => AppTheme.concrete,
```

**2. _sportBgColors list (lines 32–41) — replace entire list:**
```dart
// AFTER (all sport backgrounds → paper, consistent Arena style):
static const List<Color> _sportBgColors = [
  AppTheme.paper, // slot 0
  AppTheme.paper, // slot 1
  AppTheme.paper, // slot 2
  AppTheme.paper, // slot 3
  AppTheme.paper, // slot 4
  AppTheme.paper, // slot 5
  AppTheme.paper, // slot 6
  AppTheme.paper, // slot 7
];
```

**3. _sportFgColors list (lines 43–52) — replace entire list:**
```dart
// AFTER (map to Arena palette — nearest semantic token):
static const List<Color> _sportFgColors = [
  AppTheme.ink,      // was blue fg
  AppTheme.court,    // was green fg
  AppTheme.orange,   // was orange fg
  AppTheme.ink,      // was purple fg → ink (no purple in Arena)
  AppTheme.orangeDk, // was red fg
  AppTheme.court,    // was teal fg → court
  AppTheme.court,    // was lime fg → court
  AppTheme.sun,      // was amber fg
];
```

**4. Icon and TextStyle Colors.grey references (participants section, time/price section):**
Replace every `color: Colors.grey` with `color: AppTheme.concrete`.
Replace every `TextStyle(color: Colors.grey, ...)` with `TextStyle(color: AppTheme.concrete, ...)`.
There are 6 occurrences total.

**5. OutlinedButton reject foreground:**
```dart
// BEFORE:
OutlinedButton.styleFrom(foregroundColor: Colors.red)

// AFTER:
OutlinedButton.styleFrom(foregroundColor: AppTheme.orangeDk)
```

After all replacements, run:
```
grep -n "Color(0x\|Colors\." lib/features/admin/ui/admin_booking_card.dart
```
This must return 0 results. If any remain, fix them.

Then run:
```
flutter analyze --no-fatal-infos lib/features/admin/ui/admin_booking_card.dart
```
  </action>
  <verify>
    <automated>grep -c "Color(0x\|Colors\." lib/features/admin/ui/admin_booking_card.dart; flutter analyze --no-fatal-infos lib/features/admin/ui/admin_booking_card.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `grep -c "Color(0x\|Colors\." lib/features/admin/ui/admin_booking_card.dart` returns 0
    - All _sportBgColors entries are AppTheme.paper
    - All _sportFgColors entries use AppTheme.* tokens
    - _statusColor() uses AppTheme.sun, AppTheme.court, AppTheme.ink, AppTheme.concrete, AppTheme.orangeDk, AppTheme.orange
    - `flutter analyze --no-fatal-infos lib/features/admin/ui/admin_booking_card.dart` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in booking_confirmation_sheet.dart</name>
  <files>
    lib/features/booking/ui/booking_confirmation_sheet.dart
  </files>
  <read_first>
    - lib/features/booking/ui/booking_confirmation_sheet.dart (full file — verify current state before editing)
    - lib/core/theme/app_theme.dart lines 7–22 (token values — DO NOT modify)
  </read_first>
  <action>
Run grep first to confirm all hardcoded colors:
```
grep -n "Color(0x\|Colors\." lib/features/booking/ui/booking_confirmation_sheet.dart
```

Apply ALL of the following replacements in booking_confirmation_sheet.dart:

**1. _paymentWarningBanner() — Container decoration (around line 232–254):**
```dart
// BEFORE:
color: const Color(0xFFFFF3E0),
border: Border.all(color: const Color(0xFFFFB300), width: 1),
// ...
Icon(Icons.info_outline, size: 18, color: Color(0xFFE65100)),
// ...
TextStyle(fontSize: 13, color: Color(0xFFE65100), height: 1.4)

// AFTER:
color: AppTheme.paper,
border: Border.all(color: AppTheme.sun, width: 1),
// ...
Icon(Icons.info_outline, size: 18, color: AppTheme.orange),
// ...
TextStyle(fontSize: 13, color: AppTheme.orange, height: 1.4)
```

**2. Drag handle color (around line 277):**
```dart
// BEFORE:
color: const Color(0xFFD0CAC0),

// AFTER:
color: AppTheme.line,
```

**3. Error text TextStyle (around line 389):**
```dart
// BEFORE:
const TextStyle(color: Color(0xFFC62828))

// AFTER:
TextStyle(color: AppTheme.orangeDk)
```
Note: remove `const` keyword since AppTheme.orangeDk is a static const but TextStyle with a non-literal parameter cannot be const in older Dart. If analyzer requires const, use `const TextStyle(color: AppTheme.orangeDk)` — AppTheme.orangeDk IS const so this is valid.

**4. CircularProgressIndicator color — two occurrences:**
```dart
// BEFORE:
color: Colors.white,

// AFTER:
color: AppTheme.paper,
```

After all replacements, verify:
```
grep -n "Color(0x\|Colors\." lib/features/booking/ui/booking_confirmation_sheet.dart
```
This must return 0 results. If any remain, fix them.

Then:
```
flutter analyze --no-fatal-infos lib/features/booking/ui/booking_confirmation_sheet.dart
```
  </action>
  <verify>
    <automated>grep -c "Color(0x\|Colors\." lib/features/booking/ui/booking_confirmation_sheet.dart; flutter analyze --no-fatal-infos lib/features/booking/ui/booking_confirmation_sheet.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `grep -c "Color(0x\|Colors\." lib/features/booking/ui/booking_confirmation_sheet.dart` returns 0
    - _paymentWarningBanner() uses AppTheme.paper (bg), AppTheme.sun (border), AppTheme.orange (icon + text)
    - Drag handle uses AppTheme.line
    - Error text uses AppTheme.orangeDk
    - CircularProgressIndicator uses AppTheme.paper
    - `flutter analyze --no-fatal-infos lib/features/booking/ui/booking_confirmation_sheet.dart` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget layer → AppTheme | Color token lookup via static const — no runtime resolution, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Information Disclosure | Sport chip color indices | accept | Color selection is deterministic hash of sport name string — no PII, colors are UI-only |
| T-23-02-02 | Tampering | AppTheme.* static consts | accept | Static const values compiled into binary; no runtime override possible |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "Color(0x" lib/features/admin/ui/admin_booking_card.dart` → 0
2. `grep -c "Colors\." lib/features/admin/ui/admin_booking_card.dart` → 0
3. `grep -c "Color(0x" lib/features/booking/ui/booking_confirmation_sheet.dart` → 0
4. `grep -c "Colors\." lib/features/booking/ui/booking_confirmation_sheet.dart` → 0
5. `flutter analyze --no-fatal-infos` across both files → exits 0
</verification>

<success_criteria>
- Zero Color(0xFF...) or Colors.* hardcoded values remain in admin_booking_card.dart
- Zero Color(0xFF...) or Colors.* hardcoded values remain in booking_confirmation_sheet.dart
- All color references use AppTheme.* static const tokens
- `flutter analyze --no-fatal-infos` exits 0 for both files
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-system-navigationbar/23-02-SUMMARY.md`
</output>
