---
phase: 28-admin-pre-os-ajustes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/widgets/sport_btn.dart
autonomous: true
requirements:
  - ADMN-23
  - ADMN-24

must_haves:
  truths:
    - "SportBtn.filledInk renders with ink background and paper text"
    - "SportBtn.filledInk uses StadiumBorder and minimumSize full-width 52px"
    - "SportBtn.filledInk uses Anton 15px typography (same as other variants)"
  artifacts:
    - path: "lib/core/widgets/sport_btn.dart"
      provides: "FilledInk variant constructor"
      contains: "SportBtn.filledInk"
  key_links:
    - from: "SportBtn.filledInk"
      to: "AppTheme.ink / AppTheme.paper"
      via: "FilledButton.styleFrom backgroundColor/foregroundColor"
      pattern: "backgroundColor: AppTheme\\.ink"
---

<objective>
Add `SportBtn.filledInk` named constructor to the existing SportBtn widget.

Purpose: PricingTab footer (Plan 02) uses SportBtn.filledInk for "Salvar tabela". This variant must exist before that plan runs.
Output: sport_btn.dart with three named constructors: filled (orange), outlined (transparent ink border), filledInk (ink bg + paper text).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-admin-pre-os-ajustes/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SportBtn.filledInk variant</name>
  <files>lib/core/widgets/sport_btn.dart</files>

  <read_first>
    - lib/core/widgets/sport_btn.dart — current file with filled and outlined variants
    - lib/core/theme/app_theme.dart — AppTheme.ink, AppTheme.paper, AppTheme.display()
  </read_first>

  <action>
Extend SportBtn with a third named constructor `SportBtn.filledInk`.

Current file has `_filled` bool distinguishing filled vs outlined. The cleanest approach is to add a `_variant` enum or a third field. Use a `_SportBtnVariant` enum with values `filled`, `outlined`, `filledInk` to replace the `_filled` bool — this avoids ambiguous state with two fields.

Exact implementation:

```dart
enum _SportBtnVariant { filled, outlined, filledInk }

class SportBtn extends StatelessWidget {
  final String label;
  final VoidCallback? onPressed;
  final _SportBtnVariant _variant;

  const SportBtn.filled(this.label, {super.key, required this.onPressed})
      : _variant = _SportBtnVariant.filled;

  const SportBtn.outlined(this.label, {super.key, required this.onPressed})
      : _variant = _SportBtnVariant.outlined;

  const SportBtn.filledInk(this.label, {super.key, required this.onPressed})
      : _variant = _SportBtnVariant.filledInk;

  @override
  Widget build(BuildContext context) {
    return switch (_variant) {
      _SportBtnVariant.filled => FilledButton(
          onPressed: onPressed,
          style: FilledButton.styleFrom(
            backgroundColor: AppTheme.orange,
            foregroundColor: AppTheme.paper,
            minimumSize: const Size(double.infinity, 52),
            textStyle: AppTheme.display(size: 15, color: AppTheme.paper),
            shape: const StadiumBorder(),
            padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 14),
          ),
          child: Text(label, overflow: TextOverflow.ellipsis),
        ),
      _SportBtnVariant.outlined => OutlinedButton(
          onPressed: onPressed,
          style: OutlinedButton.styleFrom(
            foregroundColor: AppTheme.ink,
            side: const BorderSide(color: AppTheme.ink, width: 1.5),
            minimumSize: const Size(double.infinity, 52),
            textStyle: AppTheme.display(size: 15, color: AppTheme.ink),
            shape: const StadiumBorder(),
            padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 14),
          ),
          child: Text(label, overflow: TextOverflow.ellipsis),
        ),
      _SportBtnVariant.filledInk => FilledButton(
          onPressed: onPressed,
          style: FilledButton.styleFrom(
            backgroundColor: AppTheme.ink,
            foregroundColor: AppTheme.paper,
            minimumSize: const Size(double.infinity, 52),
            textStyle: AppTheme.display(size: 15, color: AppTheme.paper),
            shape: const StadiumBorder(),
            padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 14),
          ),
          child: Text(label, overflow: TextOverflow.ellipsis),
        ),
    };
  }
}
```

Update the doc comment at the top of the class to document all three variants including filledInk (ink background, paper text, primary footer CTA).
  </action>

  <verify>
    <automated>cd F:/_geral/Projetos/vida_ativa && flutter analyze lib/core/widgets/sport_btn.dart 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep "SportBtn.filledInk" lib/core/widgets/sport_btn.dart returns a match
    - grep "backgroundColor: AppTheme.ink" lib/core/widgets/sport_btn.dart returns a match
    - grep "foregroundColor: AppTheme.paper" lib/core/widgets/sport_btn.dart returns at least 2 matches (filled + filledInk)
    - flutter analyze lib/core/widgets/sport_btn.dart exits with 0 errors
    - Existing callers of SportBtn.filled and SportBtn.outlined still compile (no breaking change)
  </acceptance_criteria>

  <done>sport_btn.dart has three variants. SportBtn.filledInk is callable with ink bg + paper text + Anton 15px + StadiumBorder + full-width 52px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget layer | Pure UI widget — no data access, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | sport_btn.dart | accept | Read-only UI widget, no data mutation, no user input |
</threat_model>

<verification>
- flutter analyze lib/core/widgets/sport_btn.dart passes with no errors
- All three named constructors are accessible: SportBtn.filled, SportBtn.outlined, SportBtn.filledInk
- No existing code that calls SportBtn.filled or SportBtn.outlined breaks (enum replaces bool, same public API)
</verification>

<success_criteria>
sport_btn.dart compiles cleanly with three named constructors. Wave 2 plans can import and use SportBtn.filledInk without modification to this file.
</success_criteria>

<output>
After completion, create `.planning/phases/28-admin-pre-os-ajustes/28-01-SUMMARY.md`
</output>
