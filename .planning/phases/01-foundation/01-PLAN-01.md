---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - web/manifest.json
  - firebase.json
  - firestore.rules
  - lib/core/models/user_model.dart
  - lib/core/models/slot_model.dart
  - lib/core/models/booking_model.dart
  - lib/core/models/blocked_date_model.dart
autonomous: true
requirements: [INFRA-01, INFRA-02, PWA-01]

must_haves:
  truths:
    - "All four Dart model classes round-trip correctly through Firestore serialization (toFirestore/fromFirestore)"
    - "firestore.rules denies unauthenticated writes to all collections"
    - "web/manifest.json has name 'Vida Ativa', display standalone, maskable icons, and green theme_color"
    - "firebase.json has SPA rewrite and no-cache header for flutter_service_worker.js"
    - "pubspec.yaml includes flutter_riverpod and go_router dependencies"
  artifacts:
    - path: "lib/core/models/user_model.dart"
      provides: "UserModel with Firestore serialization"
      contains: "class UserModel"
    - path: "lib/core/models/slot_model.dart"
      provides: "SlotModel with Firestore serialization"
      contains: "class SlotModel"
    - path: "lib/core/models/booking_model.dart"
      provides: "BookingModel with Firestore serialization"
      contains: "class BookingModel"
    - path: "lib/core/models/blocked_date_model.dart"
      provides: "BlockedDateModel with Firestore serialization"
      contains: "class BlockedDateModel"
    - path: "firestore.rules"
      provides: "Firestore security rules denying unauthenticated writes"
      contains: "rules_version"
    - path: "web/manifest.json"
      provides: "PWA manifest with correct branding"
      contains: "Vida Ativa"
    - path: "firebase.json"
      provides: "Firebase Hosting SPA config"
      contains: "rewrites"
  key_links:
    - from: "lib/core/models/booking_model.dart"
      to: "Firestore /bookings collection"
      via: "deterministic doc ID pattern {slotId}_{date}"
      pattern: "slotId.*date"
    - from: "firestore.rules"
      to: "all Firestore collections"
      via: "isAuthenticated() guard on writes"
      pattern: "request\\.auth != null"
---

<objective>
Install project dependencies, create the four core data models with Firestore serialization, configure PWA manifest and Firebase Hosting, and bootstrap Firestore security rules.

Purpose: Every subsequent phase depends on these models, config, and security foundations. Doing this first means auth, schedule, booking, and admin features build on a tested base.
Output: 4 model files, updated pubspec.yaml, updated manifest.json, updated firebase.json, new firestore.rules
</objective>

<execution_context>
@C:/Users/alvar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- No prior plan interfaces needed -- this is the first plan. -->
<!-- Existing files to modify: -->

From pubspec.yaml (current dependencies):
```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  cloud_firestore: ^6.1.3
```

From web/manifest.json (current state):
```json
{
  "name": "vida_ativa",
  "short_name": "vida_ativa",
  "background_color": "#0175C2",
  "theme_color": "#0175C2",
  "description": "A new Flutter project."
}
```

From firebase.json (current state -- FlutterFire CLI metadata only):
```json
{"flutter":{"platforms":{"dart":{"lib/firebase_options.dart":{"projectId":"vida-ativa-94ba0","configurations":{"web":"1:1020952880974:web:05faae57258c3914b8e01f"}}}}}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and configure PWA manifest + Firebase Hosting</name>
  <files>pubspec.yaml, web/manifest.json, firebase.json</files>
  <read_first>
    - pubspec.yaml
    - web/manifest.json
    - firebase.json
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/research/STACK.md
  </read_first>
  <action>
**1. pubspec.yaml — add new dependencies:**

Run the following commands to add dependencies (do NOT manually edit pubspec.yaml — use `flutter pub add` for correct version resolution):

```bash
flutter pub add flutter_riverpod go_router equatable
```

Do NOT add table_calendar, intl, shared_preferences, uuid, riverpod_annotation, build_runner, riverpod_generator, mockito, or fake_cloud_firestore yet -- those are for later phases. Phase 1 only needs flutter_riverpod, go_router, and equatable.

After running `flutter pub add`, verify the dependencies appear in pubspec.yaml.

**2. web/manifest.json — update branding per CONTEXT.md decisions:**

Replace the entire manifest.json content with:
```json
{
    "name": "Vida Ativa",
    "short_name": "Vida Ativa",
    "start_url": ".",
    "display": "standalone",
    "background_color": "#FFFFFF",
    "theme_color": "#2E7D32",
    "description": "Reserve sua quadra na Academia Vida Ativa",
    "orientation": "portrait-primary",
    "prefer_related_applications": false,
    "icons": [
        {
            "src": "icons/Icon-192.png",
            "sizes": "192x192",
            "type": "image/png"
        },
        {
            "src": "icons/Icon-512.png",
            "sizes": "512x512",
            "type": "image/png"
        },
        {
            "src": "icons/Icon-maskable-192.png",
            "sizes": "192x192",
            "type": "image/png",
            "purpose": "maskable"
        },
        {
            "src": "icons/Icon-maskable-512.png",
            "sizes": "512x512",
            "type": "image/png",
            "purpose": "maskable"
        }
    ]
}
```

Key changes from current: `name` from "vida_ativa" to "Vida Ativa", `short_name` from "vida_ativa" to "Vida Ativa", `background_color` from "#0175C2" to "#FFFFFF", `theme_color` from "#0175C2" to "#2E7D32" (Material green 800), `description` from generic to "Reserve sua quadra na Academia Vida Ativa".

**3. firebase.json — add hosting config while preserving existing flutter metadata:**

Replace the entire firebase.json with:
```json
{
    "flutter": {
        "platforms": {
            "dart": {
                "lib/firebase_options.dart": {
                    "projectId": "vida-ativa-94ba0",
                    "configurations": {
                        "web": "1:1020952880974:web:05faae57258c3914b8e01f"
                    }
                }
            }
        }
    },
    "hosting": {
        "public": "build/web",
        "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
        "rewrites": [
            {
                "source": "**",
                "destination": "/index.html"
            }
        ],
        "headers": [
            {
                "source": "**/*.@(js|css)",
                "headers": [
                    {
                        "key": "Cache-Control",
                        "value": "max-age=31536000"
                    }
                ]
            },
            {
                "source": "flutter_service_worker.js",
                "headers": [
                    {
                        "key": "Cache-Control",
                        "value": "no-cache"
                    }
                ]
            }
        ]
    },
    "firestore": {
        "rules": "firestore.rules"
    }
}
```

Key additions: `hosting` block with SPA rewrite (`** -> /index.html`), cache headers (long-lived for JS/CSS, no-cache for service worker), and `firestore.rules` reference.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/vida_ativa" && flutter pub get 2>&1 | tail -5 && grep "flutter_riverpod" pubspec.yaml && grep "go_router" pubspec.yaml && grep "equatable" pubspec.yaml && grep "Vida Ativa" web/manifest.json && grep "2E7D32" web/manifest.json && grep "rewrites" firebase.json && grep "flutter_service_worker" firebase.json && grep "firestore.rules" firebase.json</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `flutter_riverpod:` dependency line
    - pubspec.yaml contains `go_router:` dependency line
    - pubspec.yaml contains `equatable:` dependency line
    - `flutter pub get` exits 0
    - web/manifest.json contains `"name": "Vida Ativa"`
    - web/manifest.json contains `"theme_color": "#2E7D32"`
    - web/manifest.json contains `"display": "standalone"`
    - web/manifest.json contains `"purpose": "maskable"`
    - firebase.json contains `"rewrites"` with `"destination": "/index.html"`
    - firebase.json contains `"flutter_service_worker.js"` with `"no-cache"`
    - firebase.json contains `"firestore"` section with `"rules": "firestore.rules"`
    - firebase.json still contains `"projectId": "vida-ativa-94ba0"` (existing metadata preserved)
  </acceptance_criteria>
  <done>Dependencies installed, manifest branded, firebase.json configured for SPA hosting with service worker cache control</done>
</task>

<task type="auto">
  <name>Task 2: Create four core data models with Firestore serialization</name>
  <files>
    lib/core/models/user_model.dart,
    lib/core/models/slot_model.dart,
    lib/core/models/booking_model.dart,
    lib/core/models/blocked_date_model.dart
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
Create the `lib/core/models/` directory and 4 model files. All models use Equatable for value equality. All models have `factory fromFirestore(DocumentSnapshot doc)` and `Map<String, dynamic> toFirestore()` methods. Import `cloud_firestore` for `DocumentSnapshot` and `Timestamp` types.

**1. lib/core/models/user_model.dart:**
```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:equatable/equatable.dart';

class UserModel extends Equatable {
  final String uid;
  final String email;
  final String displayName;
  final String role; // "client" or "admin"
  final String? phone;

  const UserModel({
    required this.uid,
    required this.email,
    required this.displayName,
    required this.role,
    this.phone,
  });

  factory UserModel.fromFirestore(DocumentSnapshot<Map<String, dynamic>> doc) {
    final data = doc.data()!;
    return UserModel(
      uid: doc.id,
      email: data['email'] as String,
      displayName: data['displayName'] as String,
      role: data['role'] as String,
      phone: data['phone'] as String?,
    );
  }

  Map<String, dynamic> toFirestore() {
    return {
      'email': email,
      'displayName': displayName,
      'role': role,
      if (phone != null) 'phone': phone,
    };
  }

  bool get isAdmin => role == 'admin';

  @override
  List<Object?> get props => [uid, email, displayName, role, phone];
}
```

Note: `uid` is NOT in `toFirestore()` because it is the document ID, not a field.

**2. lib/core/models/slot_model.dart:**
```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:equatable/equatable.dart';

class SlotModel extends Equatable {
  final String id;
  final int dayOfWeek; // 1=Monday..7=Sunday (Dart DateTime.weekday convention)
  final String startTime; // "HH:mm" format, e.g. "08:00"
  final double price;
  final bool isActive;

  const SlotModel({
    required this.id,
    required this.dayOfWeek,
    required this.startTime,
    required this.price,
    required this.isActive,
  });

  factory SlotModel.fromFirestore(DocumentSnapshot<Map<String, dynamic>> doc) {
    final data = doc.data()!;
    return SlotModel(
      id: doc.id,
      dayOfWeek: data['dayOfWeek'] as int,
      startTime: data['startTime'] as String,
      price: (data['price'] as num).toDouble(),
      isActive: data['isActive'] as bool,
    );
  }

  Map<String, dynamic> toFirestore() {
    return {
      'dayOfWeek': dayOfWeek,
      'startTime': startTime,
      'price': price,
      'isActive': isActive,
    };
  }

  @override
  List<Object?> get props => [id, dayOfWeek, startTime, price, isActive];
}
```

Note: `price` cast uses `(data['price'] as num).toDouble()` because Firestore may return int or double depending on the stored value.

**3. lib/core/models/booking_model.dart:**
```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:equatable/equatable.dart';

class BookingModel extends Equatable {
  final String id; // Deterministic: "{slotId}_{date}"
  final String slotId;
  final String date; // "YYYY-MM-DD" format
  final String userId;
  final String status; // "pending" | "confirmed" | "cancelled"
  final DateTime createdAt;
  final DateTime? cancelledAt;

  const BookingModel({
    required this.id,
    required this.slotId,
    required this.date,
    required this.userId,
    required this.status,
    required this.createdAt,
    this.cancelledAt,
  });

  /// Generates the deterministic document ID for anti-double-booking.
  /// Always use this instead of Firestore auto-generated IDs.
  static String generateId(String slotId, String date) => '${slotId}_$date';

  factory BookingModel.fromFirestore(DocumentSnapshot<Map<String, dynamic>> doc) {
    final data = doc.data()!;
    return BookingModel(
      id: doc.id,
      slotId: data['slotId'] as String,
      date: data['date'] as String,
      userId: data['userId'] as String,
      status: data['status'] as String,
      createdAt: (data['createdAt'] as Timestamp).toDate(),
      cancelledAt: data['cancelledAt'] != null
          ? (data['cancelledAt'] as Timestamp).toDate()
          : null,
    );
  }

  Map<String, dynamic> toFirestore() {
    return {
      'slotId': slotId,
      'date': date,
      'userId': userId,
      'status': status,
      'createdAt': Timestamp.fromDate(createdAt),
      if (cancelledAt != null) 'cancelledAt': Timestamp.fromDate(cancelledAt!),
    };
  }

  bool get isPending => status == 'pending';
  bool get isConfirmed => status == 'confirmed';
  bool get isCancelled => status == 'cancelled';

  @override
  List<Object?> get props => [id, slotId, date, userId, status, createdAt, cancelledAt];
}
```

Note: `id` is NOT in `toFirestore()` — it is the document ID. The `generateId` static method enforces the `{slotId}_{date}` pattern critical for anti-double-booking.

**4. lib/core/models/blocked_date_model.dart:**
```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:equatable/equatable.dart';

class BlockedDateModel extends Equatable {
  final String date; // "YYYY-MM-DD" format, also used as document ID
  final String createdBy; // UID of admin who created the block

  const BlockedDateModel({
    required this.date,
    required this.createdBy,
  });

  factory BlockedDateModel.fromFirestore(DocumentSnapshot<Map<String, dynamic>> doc) {
    final data = doc.data()!;
    return BlockedDateModel(
      date: doc.id, // Document ID IS the date
      createdBy: data['createdBy'] as String,
    );
  }

  Map<String, dynamic> toFirestore() {
    return {
      'date': date, // Redundant with doc ID but aids serialization
      'createdBy': createdBy,
    };
  }

  @override
  List<Object?> get props => [date, createdBy];
}
```
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/vida_ativa" && flutter analyze lib/core/models/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/models/user_model.dart exists and contains `class UserModel extends Equatable`
    - lib/core/models/user_model.dart contains `factory UserModel.fromFirestore(DocumentSnapshot`
    - lib/core/models/user_model.dart contains `Map<String, dynamic> toFirestore()`
    - lib/core/models/user_model.dart contains `bool get isAdmin`
    - lib/core/models/slot_model.dart exists and contains `class SlotModel extends Equatable`
    - lib/core/models/slot_model.dart contains `(data['price'] as num).toDouble()`
    - lib/core/models/booking_model.dart exists and contains `class BookingModel extends Equatable`
    - lib/core/models/booking_model.dart contains `static String generateId(String slotId, String date)`
    - lib/core/models/booking_model.dart contains `Timestamp` import from cloud_firestore
    - lib/core/models/blocked_date_model.dart exists and contains `class BlockedDateModel extends Equatable`
    - lib/core/models/blocked_date_model.dart contains `date: doc.id` in fromFirestore
    - `flutter analyze lib/core/models/` reports no errors
  </acceptance_criteria>
  <done>Four model classes created with Firestore serialization, value equality via Equatable, deterministic BookingModel ID generation, and zero analyzer errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Firestore security rules bootstrap</name>
  <files>firestore.rules</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/research/STACK.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
Create `firestore.rules` at the project root. Phase 1 rules are a bootstrap: deny all unauthenticated writes, allow authenticated reads. Phase 6 will harden these with granular per-collection rules.

Write the following content to `firestore.rules`:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Phase 1 bootstrap: deny unauthenticated writes to ALL collections.
    // Phase 6 will replace these with granular per-collection rules
    // (isAdmin() gate, client-only-own-bookings, etc.)

    function isAuthenticated() {
      return request.auth != null;
    }

    // Users collection
    match /users/{userId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated() && request.auth.uid == userId;
    }

    // Slots collection
    match /slots/{slotId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }

    // Bookings collection
    match /bookings/{bookingId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }

    // Blocked dates collection
    match /blockedDates/{dateId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }
  }
}
```

Important design notes:
- Phase 1 only enforces authentication (no role-based rules yet) — this is intentional. The `isAdmin()` function requires the `/users/{uid}` documents to exist, which requires the auth feature from Phase 2. Phase 6 adds the full `isAdmin()` gate.
- All reads require authentication — no public data.
- The `users` collection has a stricter write rule (`request.auth.uid == userId`) even in Phase 1 to prevent users creating documents for other UIDs.
- Do NOT deploy these rules to production yet. They will be deployed when the Firebase Hosting deploy is tested.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/vida_ativa" && test -f firestore.rules && grep "rules_version" firestore.rules && grep "isAuthenticated" firestore.rules && grep "/users/" firestore.rules && grep "/slots/" firestore.rules && grep "/bookings/" firestore.rules && grep "/blockedDates/" firestore.rules && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - firestore.rules exists at project root
    - firestore.rules contains `rules_version = '2';`
    - firestore.rules contains `function isAuthenticated()`
    - firestore.rules contains `return request.auth != null;`
    - firestore.rules contains `match /users/{userId}` with `request.auth.uid == userId` write guard
    - firestore.rules contains `match /slots/{slotId}`
    - firestore.rules contains `match /bookings/{bookingId}`
    - firestore.rules contains `match /blockedDates/{dateId}`
    - All write rules include `isAuthenticated()` check (no `allow write: if true`)
  </acceptance_criteria>
  <done>Firestore security rules file exists at project root, denies all unauthenticated writes, referenced by firebase.json</done>
</task>

</tasks>

<verification>
1. `flutter pub get` succeeds (dependencies resolved)
2. `flutter analyze lib/core/models/` reports no errors
3. manifest.json has "Vida Ativa" name, "#2E7D32" theme_color, "standalone" display
4. firebase.json has SPA rewrite, service worker no-cache, and firestore rules reference
5. firestore.rules exists with authentication guards on all collections
</verification>

<success_criteria>
- All 4 model files compile without errors and have fromFirestore/toFirestore methods
- pubspec.yaml has flutter_riverpod, go_router, equatable
- manifest.json is PWA-ready with correct branding
- firebase.json supports SPA routing and service worker freshness
- firestore.rules denies unauthenticated writes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
