---
phase: 19-admin-settings-credenciais-pix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - functions/index.js
autonomous: true
requirements:
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "Cliente Flutter não consegue ler config/mercadopago (permission-denied)"
    - "Admin Flutter consegue escrever em config/mercadopago"
    - "config/settings permanece legível para usuários autenticados (intacto)"
    - "createPixPayment lê config/mercadopago primeiro; se vazio, usa Secret Manager"
    - "handlePixWebhook lê config/mercadopago para ambos accessToken e webhookSecret"
    - "expireUnpaidBookings usa helper getMpAccessToken() — sem mudança de comportamento"
    - "Se Firestore e Secret Manager não têm o token, CF lança HttpsError 'MP_ACCESS_TOKEN not configured'"
  artifacts:
    - path: "firestore.rules"
      provides: "Regra match /config/mercadopago com allow read: if false"
      contains: "allow read: if false"
    - path: "functions/index.js"
      provides: "Helpers getMpAccessToken() e getMpWebhookSecret() + todas as CFs usando helpers"
      exports: ["getMpAccessToken", "getMpWebhookSecret"]
  key_links:
    - from: "functions/index.js getMpAccessToken()"
      to: "config/mercadopago (Firestore Admin SDK)"
      via: "admin.firestore().collection('config').doc('mercadopago').get()"
      pattern: "collection.*config.*doc.*mercadopago"
    - from: "createPixPayment"
      to: "getMpAccessToken(db)"
      via: "const token = await getMpAccessToken(db)"
      pattern: "getMpAccessToken"
    - from: "handlePixWebhook"
      to: "getMpWebhookSecret(db) + getMpAccessToken(db)"
      via: "const secret = await getMpWebhookSecret(db)"
      pattern: "getMpWebhookSecret"
---

<objective>
Adicionar regra Firestore para isolamento de credenciais e atualizar Cloud Functions para ler credenciais do Firestore como fonte primária.

Purpose: Credenciais MP configuradas pelo admin via SettingsTab ficam disponíveis imediatamente nas CFs sem redeploy. Regras de segurança impedem leitura das credenciais pelo client Flutter.
Output: firestore.rules com regra write-only para config/mercadopago; functions/index.js com helpers getMpAccessToken/getMpWebhookSecret usados em createPixPayment, handlePixWebhook, expireUnpaidBookings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-admin-settings-credenciais-pix/19-CONTEXT.md
@.planning/phases/19-admin-settings-credenciais-pix/19-RESEARCH.md

<interfaces>
<!-- Interfaces extraídas do codebase para o executor usar diretamente. -->

From firestore.rules (estrutura atual):
```
match /config/{docId} {
  allow read: if isAuthenticated();
  allow write: if isAdmin();
}
```
→ Adicionar bloco override para mercadopago DENTRO do match /config/{docId}:
```
match /mercadopago {
  allow read: if false;
  allow write: if isAdmin();
}
```
NOTA: Firestore rules NÃO suportam nested match dentro de match de documento com {docId}.
A regra correta é:
```
match /config/{docId} {
  allow read: if isAuthenticated();
  allow write: if isAdmin();
}
match /config/mercadopago {
  allow read: if false;
  allow write: if isAdmin();
}
```
Regra mais específica (`/config/mercadopago`) tem precedência sobre a genérica (`/config/{docId}`).

From functions/index.js (linhas críticas com mpAccessToken.value()):
- Linha 214: `accessToken: mpAccessToken.value()` em createPixPayment → substituir por helper
- Linha 374: `accessToken: mpAccessToken.value()` em handlePixWebhook (fetch order) → substituir por helper
- Linha 349-351: `mpWebhookSecret.value()` em handlePixWebhook (verifyMpSignature) → substituir por helper
- Linha 563: `accessToken: mpAccessToken.value()` em cancelPixPayment → NÃO alterar (cancelPixPayment não está no escopo D-12)
- Linha 650-654: `accessToken: mpAccessToken.value()` em adminConfirmPixPayment → NÃO alterar (não está em D-12)

Funções em D-12 (a alterar): createPixPayment, handlePixWebhook, expireUnpaidBookings
Funções fora de escopo (NÃO alterar): cancelPixPayment, adminConfirmPixPayment, updateSlotPricesFromTiers, notifyAdminNewBooking

From functions/index.js (padrão defineSecret atual):
```javascript
const mpAccessToken = defineSecret('MP_ACCESS_TOKEN');
const mpWebhookSecret = defineSecret('MP_WEBHOOK_SECRET');
```
→ Manter esses defineSecret — são necessários para o fallback e para declarar secrets nas CFs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar firestore.rules com regra write-only para config/mercadopago</name>
  <files>
    firestore.rules
  </files>
  <read_first>
    - firestore.rules (ler arquivo completo antes de modificar — ver regra atual de /config/{docId})
  </read_first>
  <action>
Adicionar regra específica para `config/mercadopago` com `allow read: if false` (per D-09).

**Regra a adicionar** logo após o bloco `match /config/{docId}`:

```
match /config/mercadopago {
  allow read: if false;
  allow write: if isAdmin();
}
```

**Posicionamento:** No nível `match /databases/{database}/documents`, após o bloco `match /config/{docId}`. Regras mais específicas têm precedência no Firestore.

Resultado esperado do arquivo:
```
match /config/{docId} {
  allow read: if isAuthenticated();
  allow write: if isAdmin();
}

match /config/mercadopago {
  allow read: if false;
  allow write: if isAdmin();
}
```

**NÃO alterar** nenhuma outra regra existente — especialmente `/config/{docId}` que cobre `config/booking` e `config/settings` (per D-10).

**Por que essa abordagem funciona:** No Firestore Security Rules v2, quando há múltiplas regras correspondendo ao mesmo path, a mais específica (match literal vs wildcard) tem precedência. `match /config/mercadopago` é mais específico que `match /config/{docId}`.
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && grep -n "allow read: if false" firestore.rules</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allow read: if false" firestore.rules` retorna pelo menos 1 resultado
    - `grep -n "match /config/mercadopago" firestore.rules` retorna resultado
    - `grep -n "match /config/{docId}" firestore.rules` ainda retorna resultado (não removido)
    - `grep -n "isAuthenticated" firestore.rules` retorna pelo menos 3 resultados (regras existentes intactas)
    - Bloco `match /config/mercadopago` contém exatamente: `allow read: if false;` e `allow write: if isAdmin();`
  </acceptance_criteria>
  <done>firestore.rules tem regra `allow read: if false` para config/mercadopago. Regras existentes (config/booking, config/settings via config/{docId}) intactas.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar helpers getMpAccessToken/getMpWebhookSecret nas Cloud Functions</name>
  <files>
    functions/index.js
  </files>
  <read_first>
    - functions/index.js (ler arquivo completo — mapear todos os usos de mpAccessToken.value() e mpWebhookSecret.value())
  </read_first>
  <action>
**Passo 1 — Adicionar funções helper** logo após `admin.initializeApp();` (linha 13 atual):

```javascript
/**
 * Reads MP_ACCESS_TOKEN from Firestore config/mercadopago first.
 * Falls back to Secret Manager if Firestore field is empty or document missing.
 * Per D-11: Firestore is primary source, Secret Manager is fallback.
 * @param {admin.firestore.Firestore} db
 * @returns {Promise<string>}
 */
async function getMpAccessToken(db) {
  try {
    const configSnap = await db.collection('config').doc('mercadopago').get();
    const token = configSnap.data()?.accessToken;
    if (token && token.trim()) {
      console.log('getMpAccessToken: using token from Firestore');
      return token.trim();
    }
  } catch (err) {
    console.warn('getMpAccessToken: Firestore read failed, falling back to Secret Manager:', err.message);
  }
  const token = mpAccessToken.value();
  console.log('getMpAccessToken: using token from Secret Manager');
  return token;
}

/**
 * Reads MP_WEBHOOK_SECRET from Firestore config/mercadopago first.
 * Falls back to Secret Manager if Firestore field is empty or document missing.
 * Per D-11: Firestore is primary source, Secret Manager is fallback.
 * @param {admin.firestore.Firestore} db
 * @returns {Promise<string>}
 */
async function getMpWebhookSecret(db) {
  try {
    const configSnap = await db.collection('config').doc('mercadopago').get();
    const secret = configSnap.data()?.webhookSecret;
    if (secret && secret.trim()) {
      console.log('getMpWebhookSecret: using secret from Firestore');
      return secret.trim();
    }
  } catch (err) {
    console.warn('getMpWebhookSecret: Firestore read failed, falling back to Secret Manager:', err.message);
  }
  const secret = mpWebhookSecret.value();
  console.log('getMpWebhookSecret: using secret from Secret Manager');
  return secret;
}
```

**Passo 2 — Atualizar `createPixPayment`** (per D-12):

Localizar linha: `const client = new MercadoPagoConfig({ accessToken: mpAccessToken.value(), ... })`

Substituir por:
```javascript
const token = await getMpAccessToken(db);
if (!token) {
  throw new HttpsError('failed-precondition', 'MP_ACCESS_TOKEN not configured');
}
const client = new MercadoPagoConfig({
  accessToken: token,
  options: { timeout: 10000 },
});
```

NOTA: `const db = admin.firestore();` já existe em createPixPayment (linha ~168). Usar o mesmo `db`.

**Passo 3 — Atualizar `handlePixWebhook`** (per D-12):

A função usa `mpAccessToken.value()` em dois lugares e `mpWebhookSecret.value()` em um lugar.

3a. Antes da verificação de assinatura, adicionar leitura do db e dos secrets:
```javascript
const db = admin.firestore();
const [accessToken, webhookSecret] = await Promise.all([
  getMpAccessToken(db),
  getMpWebhookSecret(db),
]);

if (!webhookSecret) {
  console.error('handlePixWebhook: MP_WEBHOOK_SECRET not configured');
  res.status(202).send({ success: true });
  return;
}
```

3b. Substituir `mpWebhookSecret.value()` na chamada `verifyMpSignature(...)` por `webhookSecret`.

3c. Substituir `mpAccessToken.value()` em `new MercadoPagoConfig({ accessToken: mpAccessToken.value() })` por `accessToken`.

3d. Adicionar guard após obter accessToken:
```javascript
if (!accessToken) {
  console.error('handlePixWebhook: MP_ACCESS_TOKEN not configured');
  res.status(202).send({ success: true });
  return;
}
```

POSICIONAMENTO CRÍTICO: O `res.status(202)` inicial (que retorna antes de qualquer async) deve ser MANTIDO como primeira linha. Os reads de Firestore acontecem APÓS o 202 inicial ser enviado. Na prática, `handlePixWebhook` usa `onRequest` e o response é enviado ao final — verificar a estrutura atual: a função retorna 202 ao FINAL após processar (não no início). Manter o padrão atual onde 202 é retornado após processamento.

**Passo 4 — Atualizar `expireUnpaidBookings`** (per D-12):

Esta função não usa MP credentials atualmente. Verificar o arquivo — se não há uso de mpAccessToken/mpWebhookSecret nela, nenhuma mudança é necessária. Apenas confirmar ausência e documentar no SUMMARY.

**Passo 5 — NÃO alterar** as seguintes funções (fora de D-12):
- `cancelPixPayment` — continua usando `mpAccessToken.value()` diretamente
- `adminConfirmPixPayment` — continua usando `mpAccessToken.value()` diretamente
- `updateSlotPricesFromTiers` — não usa MP credentials
- `notifyAdminNewBooking` — não usa MP credentials
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa/functions && node -e "require('./index.js'); console.log('syntax ok')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async function getMpAccessToken" functions/index.js` retorna resultado
    - `grep -n "async function getMpWebhookSecret" functions/index.js` retorna resultado
    - `grep -n "getMpAccessToken(db)" functions/index.js` retorna pelo menos 1 resultado (usado em createPixPayment)
    - `grep -n "getMpWebhookSecret(db)" functions/index.js` retorna resultado (usado em handlePixWebhook)
    - `grep -n "MP_ACCESS_TOKEN not configured" functions/index.js` retorna pelo menos 1 resultado (per D-13)
    - `grep -n "collection.*config.*doc.*mercadopago" functions/index.js` retorna pelo menos 2 resultados (nos dois helpers)
    - `node -e "require('./index.js'); console.log('ok')"` executado em functions/ retorna "syntax ok" ou "ok" (sem erros de sintaxe)
    - createPixPayment: `grep -A5 "createPixPayment" functions/index.js | grep -c "getMpAccessToken"` retorna 1
    - handlePixWebhook: `grep -c "getMpWebhookSecret" functions/index.js` retorna pelo menos 1
    - `grep -c "mpWebhookSecret.value()" functions/index.js` retorna 0 (substituído pelo helper em handlePixWebhook)
    - Linha com `mpAccessToken.value()` em handlePixWebhook deve ser 0 (substituída)
  </acceptance_criteria>
  <done>Helpers getMpAccessToken/getMpWebhookSecret adicionados. createPixPayment e handlePixWebhook usam helpers com fallback. expireUnpaidBookings confirmada como sem uso de credenciais MP. Sintaxe JS válida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Flutter → Firestore rules | Qualquer usuário autenticado (incluindo admin Flutter) tentando ler config/mercadopago |
| Cloud Functions Admin SDK → Firestore | CF lê config/mercadopago — bypass de regras de segurança |
| Mercado Pago → handlePixWebhook | Requisições externas ao endpoint de webhook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Information Disclosure | firestore.rules /config/mercadopago | mitigate | `allow read: if false` impede leitura pelo SDK cliente de qualquer usuário, incluindo admin Flutter. Admin SDK em CFs bypassa regras — comportamento esperado e seguro. |
| T-19-07 | Spoofing | handlePixWebhook signature verification | mitigate | getMpWebhookSecret() garante que o secret de verificação de assinatura MP vem da fonte correta (Firestore ou Secret Manager). Se secret não configurado, webhook retorna 202 silenciosamente (log + não processa). |
| T-19-08 | Information Disclosure | CF logs com credenciais | mitigate | Helpers usam `console.log` apenas para indicar a FONTE (Firestore vs Secret Manager) — nunca loggam o valor do token. Accepted: CF logs acessíveis apenas por admins do projeto Firebase. |
| T-19-09 | Denial of Service | Firestore read por invocação | accept | Cada invocação de CF lê config/mercadopago (1 doc read). Custo: ~$0.000006/read. Para volume típico de academia pequena (< 100 pagamentos/dia), custo negligenciável. Cache de estado global não implementado por design (per RESEARCH.md). |
| T-19-10 | Elevation of Privilege | config/mercadopago write via Firestore rules | mitigate | `allow write: if isAdmin()` verifica role no documento /users/{uid}. Apenas admins do sistema podem escrever credenciais via Flutter client SDK. |
</threat_model>

<verification>
Após execução do plano:

1. Firestore rules:
   - `grep "allow read: if false" firestore.rules` — presente
   - `grep "match /config/mercadopago" firestore.rules` — presente
   - Testar no Firebase Rules Playground: leitura de /config/mercadopago por usuário autenticado → "DENIED"
   - Testar no Firebase Rules Playground: escrita em /config/mercadopago por admin → "ALLOWED"
   - Testar leitura de /config/booking por usuário autenticado → "ALLOWED" (intacto)

2. Cloud Functions:
   - `node -e "require('./index.js'); console.log('ok')"` em functions/ — sem erros de sintaxe
   - Deploy functions com `firebase deploy --only functions`
   - Teste manual: salvar token via SettingsTab → criar reserva Pix → QR gerado (token lido do Firestore)
   - Teste de fallback: limpar config/mercadopago → criar reserva Pix → QR gerado (token lido do Secret Manager)
</verification>

<success_criteria>
- firestore.rules tem `allow read: if false` para config/mercadopago — client Flutter não consegue ler credenciais
- config/settings e config/booking continuam acessíveis para usuários autenticados (sem regressão)
- functions/index.js tem helpers getMpAccessToken(db) e getMpWebhookSecret(db)
- createPixPayment e handlePixWebhook usam helpers — credenciais lidas do Firestore em primeira instância
- Se Firestore e Secret Manager não têm token → erro "MP_ACCESS_TOKEN not configured" (per D-13)
- Node.js syntax check passes: `node -e "require('./index.js')"` sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-admin-settings-credenciais-pix/19-02-SUMMARY.md`
</output>
