---
phase: 19-admin-settings-credenciais-pix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/admin/cubit/settings_cubit.dart
  - lib/features/admin/cubit/settings_state.dart
  - lib/features/admin/ui/settings_tab.dart
  - lib/features/admin/ui/admin_screen.dart
  - lib/features/admin/cubit/admin_booking_cubit.dart
  - lib/features/admin/ui/booking_management_tab.dart
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "Admin vê 6ª aba 'Config' no TabBar do AdminScreen"
    - "SettingsTab exibe seção Mercado Pago com campos accessToken e webhookSecret"
    - "Campos exibem indicador 'Configurado' (checkmark verde) quando credencial já existe no Firestore"
    - "Campos são obscureText por padrão com botão show/hide"
    - "Ao carregar, cubit lê apenas se campo existe (booleano) — nunca retorna valor ao Flutter"
    - "Botão 'Salvar Credenciais' escreve em config/mercadopago com SetOptions(merge: true)"
    - "Kill switch pixEnabled movido para SettingsTab; removido de BookingManagementTab"
  artifacts:
    - path: "lib/features/admin/cubit/settings_cubit.dart"
      provides: "SettingsCubit — carrega status de credenciais e salva no Firestore"
      exports: ["SettingsCubit"]
    - path: "lib/features/admin/cubit/settings_state.dart"
      provides: "SettingsInitial, SettingsLoaded, SettingsError — apenas flags booleanas"
      exports: ["SettingsState", "SettingsInitial", "SettingsLoaded", "SettingsError"]
    - path: "lib/features/admin/ui/settings_tab.dart"
      provides: "SettingsTab com campos mascarados e toggle pixEnabled"
      exports: ["SettingsTab"]
  key_links:
    - from: "lib/features/admin/ui/admin_screen.dart"
      to: "lib/features/admin/ui/settings_tab.dart"
      via: "TabBarView child + BlocProvider<SettingsCubit>"
      pattern: "SettingsTab"
    - from: "lib/features/admin/cubit/settings_cubit.dart"
      to: "config/mercadopago (Firestore)"
      via: "_firestore.collection('config').doc('mercadopago')"
      pattern: "collection.*config.*doc.*mercadopago"
    - from: "lib/features/admin/ui/settings_tab.dart"
      to: "lib/features/admin/cubit/settings_cubit.dart"
      via: "context.read<SettingsCubit>().saveCredentials()"
      pattern: "SettingsCubit"
---

<objective>
Criar a aba "Config" no painel admin com gerenciamento seguro de credenciais Mercado Pago e toggle pixEnabled.

Purpose: Admin configura credenciais MP pelo painel sem redeploy de Cloud Functions. Kill switch Pix centralizado numa aba de configurações.
Output: SettingsCubit + SettingsState + SettingsTab + AdminScreen atualizado + BookingManagementTab sem pixEnabled
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-admin-settings-credenciais-pix/19-CONTEXT.md
@.planning/phases/19-admin-settings-credenciais-pix/19-RESEARCH.md

<interfaces>
<!-- Interfaces extraídas do codebase para o executor usar diretamente. -->

From lib/features/admin/ui/admin_screen.dart (linhas críticas):
```dart
// TabController(length: 5, ...) → mudar para 6
// tabs: const [ Tab(text: 'Slots'), Tab(text: 'Bloqueios'), Tab(text: 'Reservas'), Tab(text: 'Usuarios'), Tab(text: 'Preços') ]
// TabBarView children: const [ SlotManagementTab(), BlockedDatesTab(), BookingManagementTab(), UsersManagementTab(), PricingTab() ]
// BlocProvider.value wraps FCM cubit apenas — outros cubits são criados inline em TabBarView

// PricingTab não tem BlocProvider em admin_screen — é fornecido via repositório separado.
// Verificar como PricingCubit é provido (pode ser provido em outro ponto).
```

From lib/features/admin/cubit/admin_booking_cubit.dart (padrão a replicar):
```dart
class AdminBookingCubit extends Cubit<AdminBookingState> {
  final FirebaseFirestore _firestore;
  final String _adminUid;
  
  AdminBookingCubit({required FirebaseFirestore firestore, required String adminUid})
      : _firestore = firestore, _adminUid = adminUid, super(const AdminBookingInitial()) {
    _loadConfig().then((_) { ... });
  }
  
  // Firestore write pattern:
  await _firestore.collection('config').doc('booking')
      .set({'pixEnabled': enabled}, SetOptions(merge: true));
  
  // Sentry pattern:
  Sentry.captureException(e, stackTrace: s);
}
```

From lib/features/admin/cubit/admin_booking_state.dart (padrão a seguir — equatable states):
```dart
// AdminBookingInitial, AdminBookingLoaded, AdminBookingError
// Extends sealed class AdminBookingState
// Uses @immutable + const constructors
```

From lib/features/admin/ui/pricing_tab.dart (padrão de form tab):
```dart
// Scaffold > ListView padding: EdgeInsets.all(AppSpacing.md)
// FilledButton com backgroundColor: AppTheme.primaryGreen
// SnackHelper.success(context, 'Mensagem')
// SnackHelper.error(context, 'Erro: $e')
// import 'package:vida_ativa/core/theme/app_spacing.dart';
// import 'package:vida_ativa/core/theme/app_theme.dart';
// import 'package:vida_ativa/core/utils/snack_helper.dart';
```

From lib/features/admin/ui/booking_management_tab.dart (pixEnabled a remover):
```dart
// SwitchListTile — 'Pagamento Pix' (linhas 116-127) a ser REMOVIDO desta tab
// state.pixEnabled referenciado em AdminBookingLoaded — remover campo após migração
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SettingsCubit e SettingsState</name>
  <files>
    lib/features/admin/cubit/settings_state.dart
    lib/features/admin/cubit/settings_cubit.dart
  </files>
  <read_first>
    - lib/features/admin/cubit/admin_booking_cubit.dart (padrão de cubit com Firestore)
    - lib/features/admin/cubit/admin_booking_state.dart (padrão de sealed states com equatable)
    - lib/features/admin/ui/pricing_tab.dart (padrão de imports — AppTheme, AppSpacing, SnackHelper)
    - lib/features/admin/ui/admin_screen.dart (como outros cubits são instanciados — verificar PricingCubit)
  </read_first>
  <action>
**settings_state.dart** — Criar sealed class com 3 estados (padrão idêntico ao AdminBookingState):

```dart
import 'package:equatable/equatable.dart';
import 'package:flutter/foundation.dart';

sealed class SettingsState extends Equatable {
  const SettingsState();
}

@immutable
final class SettingsInitial extends SettingsState {
  const SettingsInitial();
  @override
  List<Object?> get props => [];
}

@immutable
final class SettingsLoaded extends SettingsState {
  final bool isAccessTokenConfigured;
  final bool isWebhookSecretConfigured;
  final bool pixEnabled;

  const SettingsLoaded({
    required this.isAccessTokenConfigured,
    required this.isWebhookSecretConfigured,
    required this.pixEnabled,
  });

  @override
  List<Object?> get props => [isAccessTokenConfigured, isWebhookSecretConfigured, pixEnabled];
}

@immutable
final class SettingsError extends SettingsState {
  final String message;
  const SettingsError(this.message);
  @override
  List<Object?> get props => [message];
}
```

**IMPORTANTE — SettingsLoaded inclui `pixEnabled`:** SettingsCubit gerencia AMBOS os documentos (`config/mercadopago` para credenciais e `config/booking` para pixEnabled). Isso evita boilerplate e centraliza toda a lógica de configuração num único cubit. (Responde à Open Question 1 do RESEARCH.md.)

**settings_cubit.dart** — Criar SettingsCubit seguindo padrão de AdminBookingCubit:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:sentry_flutter/sentry_flutter.dart';

import 'package:vida_ativa/features/admin/cubit/settings_state.dart';

class SettingsCubit extends Cubit<SettingsState> {
  final FirebaseFirestore _firestore;

  SettingsCubit({required FirebaseFirestore firestore})
      : _firestore = firestore,
        super(const SettingsInitial()) {
    _loadSettings();
  }

  Future<void> _loadSettings() async {
    try {
      final results = await Future.wait([
        _firestore.collection('config').doc('mercadopago').get(),
        _firestore.collection('config').doc('booking').get(),
      ]);
      final mpData = results[0].data();
      final bookingData = results[1].data();

      emit(SettingsLoaded(
        isAccessTokenConfigured:
            (mpData?['accessToken'] ?? '').toString().isNotEmpty,
        isWebhookSecretConfigured:
            (mpData?['webhookSecret'] ?? '').toString().isNotEmpty,
        pixEnabled: bookingData?['pixEnabled'] ?? true,
      ));
    } catch (e, s) {
      Sentry.captureException(e, stackTrace: s);
      emit(const SettingsError('Erro ao carregar configurações.'));
    }
  }

  /// Salva credenciais em config/mercadopago.
  /// Apenas campos não-vazios são escritos (merge: true).
  /// NUNCA retorna ou armazena o valor do token no estado Flutter.
  Future<void> saveCredentials({
    String? accessToken,
    String? webhookSecret,
  }) async {
    final data = <String, dynamic>{};
    if (accessToken != null && accessToken.trim().isNotEmpty) {
      data['accessToken'] = accessToken.trim();
    }
    if (webhookSecret != null && webhookSecret.trim().isNotEmpty) {
      data['webhookSecret'] = webhookSecret.trim();
    }
    if (data.isEmpty) return;

    data['updatedAt'] = FieldValue.serverTimestamp();

    try {
      await _firestore
          .collection('config')
          .doc('mercadopago')
          .set(data, SetOptions(merge: true));
      await _loadSettings();
    } catch (e, s) {
      Sentry.captureException(e, stackTrace: s);
      rethrow;
    }
  }

  /// Move pixEnabled para/de config/booking (mesmo path que AdminBookingCubit).
  Future<void> setPixEnabled(bool enabled) async {
    try {
      await _firestore
          .collection('config')
          .doc('booking')
          .set({'pixEnabled': enabled}, SetOptions(merge: true));
      await _loadSettings();
    } catch (e, s) {
      Sentry.captureException(e, stackTrace: s);
      rethrow;
    }
  }

  Future<void> reload() => _loadSettings();
}
```

**IMPORTANTE — AdminBookingCubit ainda lê pixEnabled do Firestore via `_loadConfig()`:** Não alterar isso agora. O cubit de bookings pode continuar lendo `config/booking` para uso interno (confirmationMode, pixEnabled para exibição no estado de reservas). O toggle de edição é que migra para SettingsTab. Isso é feito na Task 2.
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/cubit/settings_cubit.dart lib/features/admin/cubit/settings_state.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class SettingsInitial" lib/features/admin/cubit/settings_state.dart` retorna resultado
    - `grep -r "class SettingsLoaded" lib/features/admin/cubit/settings_state.dart` retorna resultado com campos `isAccessTokenConfigured`, `isWebhookSecretConfigured`, `pixEnabled`
    - `grep -r "class SettingsError" lib/features/admin/cubit/settings_state.dart` retorna resultado
    - `grep -r "class SettingsCubit" lib/features/admin/cubit/settings_cubit.dart` retorna resultado
    - `grep -r "saveCredentials" lib/features/admin/cubit/settings_cubit.dart` retorna resultado
    - `grep -r "setPixEnabled" lib/features/admin/cubit/settings_cubit.dart` retorna resultado
    - `grep -r "config.*mercadopago" lib/features/admin/cubit/settings_cubit.dart` retorna resultado
    - `grep -r "accessToken.*value\|value.*accessToken" lib/features/admin/cubit/settings_cubit.dart` NÃO retorna resultado (token nunca armazenado no estado)
    - `flutter analyze` retorna "No issues found" para os dois arquivos
  </acceptance_criteria>
  <done>settings_state.dart e settings_cubit.dart criados e sem erros de análise. Cubit lê only booleans, nunca valores de credenciais.</done>
</task>

<task type="auto">
  <name>Task 2: Criar SettingsTab + atualizar AdminScreen + migrar pixEnabled</name>
  <files>
    lib/features/admin/ui/settings_tab.dart
    lib/features/admin/ui/admin_screen.dart
    lib/features/admin/cubit/admin_booking_cubit.dart
    lib/features/admin/ui/booking_management_tab.dart
  </files>
  <read_first>
    - lib/features/admin/ui/admin_screen.dart (estrutura atual com 5 tabs — adicionar 6ª)
    - lib/features/admin/ui/pricing_tab.dart (padrão de form tab com Scaffold > ListView > FilledButton)
    - lib/features/admin/ui/booking_management_tab.dart (SwitchListTile pixEnabled a remover)
    - lib/features/admin/cubit/admin_booking_cubit.dart (método setPixEnabled + campo _pixEnabled a remover)
    - lib/features/admin/cubit/admin_booking_state.dart (campo pixEnabled em AdminBookingLoaded a remover)
    - lib/features/admin/cubit/settings_cubit.dart (interface de saveCredentials/setPixEnabled — criado na Task 1)
  </read_first>
  <action>
**1. settings_tab.dart** — Criar SettingsTab seguindo padrão de PricingTab (Scaffold > ListView):

Estrutura:
- `BlocBuilder<SettingsCubit, SettingsState>` com switch:
  - `SettingsInitial()` → `CircularProgressIndicator`
  - `SettingsError(:final message)` → `Text(message, color: red)`
  - `SettingsLoaded()` → `_SettingsForm`

`_SettingsForm` é `StatefulWidget` com:
- `TextEditingController _accessTokenController` e `_webhookSecretController`
- `bool _showAccessToken = false` e `_showWebhookSecret = false`
- `bool _isSaving = false`

Layout do form (ListView padding: `EdgeInsets.all(AppSpacing.md)`):
1. Seção "Mercado Pago" — `Text('Mercado Pago', style: textTheme.titleMedium)`
2. Campo Access Token:
   - `TextField(controller: _accessTokenController, obscureText: !_showAccessToken)`
   - `decoration: InputDecoration(labelText: 'Access Token', hintText: state.isAccessTokenConfigured ? '••••••••••••••••' : 'Cole o Access Token de produção', suffixIcon: IconButton(icon: Icon(_showAccessToken ? Icons.visibility_off : Icons.visibility), onPressed: () => setState(() => _showAccessToken = !_showAccessToken)))`
   - Se `state.isAccessTokenConfigured`: mostrar `Icon(Icons.check_circle, color: AppTheme.primaryGreen)` ao lado do campo (Row com Expanded + trailing icon)
3. `SizedBox(height: AppSpacing.sm)`
4. Campo Webhook Secret (estrutura idêntica ao Access Token com `_showWebhookSecret`)
   - `labelText: 'Webhook Secret'`, `hintText: state.isWebhookSecretConfigured ? '••••••••••••••••' : 'Cole o Webhook Secret'`
5. `SizedBox(height: AppSpacing.md)`
6. `FilledButton(onPressed: _isSaving ? null : _saveCredentials, style: FilledButton.styleFrom(backgroundColor: AppTheme.primaryGreen), child: _isSaving ? CircularProgressIndicator(strokeWidth: 2, color: Colors.white, height: 20, width: 20) : Text('Salvar Credenciais'))`
7. `SizedBox(height: AppSpacing.lg)` + `Divider()`
8. Seção "Pix" — `Text('Pix', style: textTheme.titleMedium)`
9. `SwitchListTile(title: Text('Pagamento Pix'), subtitle: Text(state.pixEnabled ? 'Usuários podem pagar com Pix' : 'Apenas pagamento na hora'), value: state.pixEnabled, onChanged: (v) => context.read<SettingsCubit>().setPixEnabled(v))`

`_saveCredentials()`:
```dart
Future<void> _saveCredentials() async {
  setState(() => _isSaving = true);
  try {
    await context.read<SettingsCubit>().saveCredentials(
      accessToken: _accessTokenController.text,
      webhookSecret: _webhookSecretController.text,
    );
    if (mounted) {
      SnackHelper.success(context, 'Credenciais salvas.');
      _accessTokenController.clear();
      _webhookSecretController.clear();
    }
  } catch (e) {
    if (mounted) SnackHelper.error(context, 'Erro ao salvar: $e');
  } finally {
    if (mounted) setState(() => _isSaving = false);
  }
}
```

**2. admin_screen.dart** — Adicionar 6ª aba (per D-01):
- `TabController(length: 5, ...)` → `TabController(length: 6, ...)`
- Adicionar `Tab(text: 'Config')` ao final da lista de tabs
- Adicionar `SettingsTab()` ao final da lista de TabBarView children, envolta em `BlocProvider`:
  ```dart
  BlocProvider(
    create: (_) => SettingsCubit(
      firestore: FirebaseFirestore.instance,
    ),
    child: const SettingsTab(),
  ),
  ```
- Adicionar import de `settings_cubit.dart`, `settings_tab.dart`, `cloud_firestore`

**3. admin_booking_cubit.dart** — Remover setPixEnabled + _pixEnabled (per D-02):
- Remover campo `bool _pixEnabled = true;`
- Remover leitura de `pixEnabled` em `_loadConfig()`: remover linha `_pixEnabled = configSnap.data()?['pixEnabled'] ?? true;`
- Remover método `setPixEnabled(bool enabled)`
- Em `selectDate()`: remover `pixEnabled: _pixEnabled` do `emit(AdminBookingLoaded(...))`
- Em `setConfirmationMode()`: remover `pixEnabled: _pixEnabled` do emit

**4. admin_booking_state.dart** — Remover campo pixEnabled de AdminBookingLoaded:
- Ler o arquivo antes de modificar
- Remover `final bool pixEnabled;` do AdminBookingLoaded
- Remover do construtor `required this.pixEnabled`
- Remover de `props`

**5. booking_management_tab.dart** — Remover SwitchListTile pixEnabled (per D-02):
- Remover o bloco `SwitchListTile` com `title: const Text('Pagamento Pix')` (linhas 116-127 aprox.)
- Remover qualquer referência a `state.pixEnabled`
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/vida_ativa && flutter analyze lib/features/admin/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class SettingsTab" lib/features/admin/ui/settings_tab.dart` retorna resultado
    - `grep -r "obscureText" lib/features/admin/ui/settings_tab.dart` retorna pelo menos 2 resultados (um por campo)
    - `grep -r "check_circle" lib/features/admin/ui/settings_tab.dart` retorna resultado (badge configurado)
    - `grep -r "isAccessTokenConfigured.*••••\|••••.*isAccessTokenConfigured" lib/features/admin/ui/settings_tab.dart` retorna resultado (placeholder mascarado)
    - `grep -r "Pagamento Pix" lib/features/admin/ui/settings_tab.dart` retorna resultado (toggle migrado)
    - `grep -r "Pagamento Pix" lib/features/admin/ui/booking_management_tab.dart` NÃO retorna resultado (toggle removido)
    - `grep -r "setPixEnabled" lib/features/admin/cubit/admin_booking_cubit.dart` NÃO retorna resultado (método removido)
    - `grep -r "pixEnabled" lib/features/admin/cubit/admin_booking_cubit.dart` NÃO retorna resultado
    - `grep -rn "length: 6" lib/features/admin/ui/admin_screen.dart` retorna resultado (TabController atualizado)
    - `grep -r "Tab(text: .Config" lib/features/admin/ui/admin_screen.dart` retorna resultado
    - `grep -r "SettingsTab" lib/features/admin/ui/admin_screen.dart` retorna resultado
    - `flutter analyze lib/features/admin/` retorna "No issues found"
  </acceptance_criteria>
  <done>SettingsTab renderiza com campos mascarados + badge + toggle pixEnabled. AdminScreen tem 6 tabs. BookingManagementTab sem toggle Pix. AdminBookingCubit sem setPixEnabled/pixEnabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → Firestore | Client tenta ler/escrever config/mercadopago |
| Admin Flutter → Firestore | Admin escreve credenciais MP via SDK cliente |
| Cloud Functions (Admin SDK) → Firestore | CF lê credenciais — bypass de regras |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | config/mercadopago (Firestore) | mitigate | Firestore rule `allow read: if false` implementada em 19-02-PLAN.md Task 1. Cubit Flutter nunca emite valores de token — apenas flags booleanas `isAccessTokenConfigured`. Verificado em acceptance_criteria da Task 1. |
| T-19-02 | Information Disclosure | SettingsCubit state | mitigate | Estado SettingsLoaded contém apenas `bool isAccessTokenConfigured` e `bool isWebhookSecretConfigured` — nunca String com valor do token. Acceptance criteria verifica ausência de padrão `accessToken.*value`. |
| T-19-03 | Elevation of Privilege | config/mercadopago write | mitigate | Firestore rule `allow write: if isAdmin()` implementada em 19-02-PLAN.md. Apenas usuários com role == "admin" no documento /users/{uid} podem escrever credenciais. |
| T-19-04 | Tampering | TextEditingController after save | mitigate | `_saveCredentials()` chama `_accessTokenController.clear()` após save bem-sucedido — evita token residual em memória da UI. |
| T-19-05 | Information Disclosure | Flutter logs/Sentry | accept | SnackHelper e catch blocks não loggam valor de token (apenas erro genérico). Sentry.captureException captura exceção sem incluir credenciais. Risco baixo — aceito para simplificar. |
</threat_model>

<verification>
Após execução do plano:

1. `flutter analyze lib/features/admin/` — zero issues
2. `flutter build web --no-tree-shake-icons` — build completa sem erros
3. Manual: abrir painel admin → aba "Config" visível no TabBar
4. Manual: campos Access Token e Webhook Secret exibem obscureText por padrão
5. Manual: botão show/hide funciona em cada campo
6. Manual: após salvar credenciais, SnackBar "Credenciais salvas." aparece e campos limpam
7. Manual: recarregar SettingsTab → campos mostram placeholder "••••••••••••••••" + checkmark verde
8. Manual: toggle Pix na aba Config funciona; aba Reservas não tem mais o toggle Pix
</verification>

<success_criteria>
- SettingsTab visível como 6ª aba "Config" no painel admin
- Campos de credenciais MP mascarados por padrão (obscureText: true)
- Badge "Configurado" (Icons.check_circle verde) visível quando credencial existe no Firestore
- Salvar credenciais escreve em config/mercadopago via SetOptions(merge: true)
- Estado Flutter NUNCA contém valor de token — apenas flags booleanas
- Kill switch pixEnabled movido para SettingsTab; removido de BookingManagementTab e AdminBookingCubit
- `flutter analyze` e `flutter build web` passam sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-admin-settings-credenciais-pix/19-01-SUMMARY.md`
</output>
