---
phase: 20-infraestrutura-de-esporte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/models/booking_model.dart
  - lib/features/admin/cubit/sport_config_cubit.dart
  - lib/features/admin/cubit/sport_config_state.dart
  - lib/features/admin/ui/admin_screen.dart
autonomous: true
requirements:
  - SPORT-03
  - SPORT-04
tags:
  - flutter
  - bloc
  - firestore
  - model
must_haves:
  truths:
    - "BookingModel aceita campo sport opcional sem quebrar reservas existentes"
    - "SportConfigCubit emite SportConfigLoaded com lista de esportes via stream em /config/sports"
    - "Se /config/sports não existe ou tem lista vazia, SportConfigCubit escreve defaults Vôlei, Beach Tênis, Futevôlei automaticamente"
    - "SportConfigCubit está disponível dentro de AdminScreen para SettingsTab consumir"
  artifacts:
    - path: "lib/core/models/booking_model.dart"
      provides: "Campo sport opcional, fromFirestore, toFirestore, props"
      contains: "final String? sport"
    - path: "lib/features/admin/cubit/sport_config_state.dart"
      provides: "Sealed state com Initial/Loaded/Error"
      contains: "sealed class SportConfigState"
    - path: "lib/features/admin/cubit/sport_config_cubit.dart"
      provides: "Stream listener em /config/sports + writeDefaults + saveSports"
      contains: "class SportConfigCubit extends Cubit<SportConfigState>"
    - path: "lib/features/admin/ui/admin_screen.dart"
      provides: "MultiBlocProvider injeta SportConfigCubit junto com SettingsCubit no SettingsTab"
      contains: "SportConfigCubit"
  key_links:
    - from: "lib/features/admin/cubit/sport_config_cubit.dart"
      to: "/config/sports (Firestore)"
      via: "_firestore.collection('config').doc('sports').snapshots().listen"
      pattern: "collection\\('config'\\)\\.doc\\('sports'\\)\\.snapshots"
    - from: "lib/features/admin/ui/admin_screen.dart"
      to: "lib/features/admin/cubit/sport_config_cubit.dart"
      via: "MultiBlocProvider envolvendo SettingsTab"
      pattern: "BlocProvider.*SportConfigCubit"
---

<objective>
Plano 01 entrega a fundação de dados e estado para Phase 20: campo `sport?` no `BookingModel` (backward compatible com reservas existentes), novo `SportConfigCubit` modelado em `PricingCubit` com `StreamSubscription` em `/config/sports`, escrita automática de defaults se doc ausente ou vazio (D-12, D-13, SPORT-03), e provisão do cubit no `AdminScreen` para que `SettingsTab` (Plano 03) consuma.

Purpose: SPORT-04 exige campo nullable que não quebra reservas existentes. SPORT-03 exige inicialização automática dos defaults. Sem esta fundação, Planos 02 e 03 não têm modelo nem dados.

Output: BookingModel estendido + cubit/state novos + AdminScreen provisionando SportConfigCubit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md
@.planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md

@lib/core/models/booking_model.dart
@lib/features/admin/cubit/pricing_cubit.dart
@lib/features/admin/cubit/pricing_state.dart
@lib/features/admin/ui/admin_screen.dart

<interfaces>
<!-- BookingModel atual: já tem 10 campos nullable seguindo padrão `if (field != null)` em toFirestore. -->
<!-- Padrão verificado em paymentMethod (linha 17), expiresAt (18), paymentId (19). -->

From lib/core/models/booking_model.dart (atual — antes do plano):
```dart
class BookingModel extends Equatable {
  final String id;
  final String slotId;
  final String date;
  final String userId;
  final String status;
  final DateTime createdAt;
  final DateTime? cancelledAt;
  final String? startTime;
  final double? price;
  final String? userDisplayName;
  final String? participants;
  final String? recurrenceGroupId;
  final String? paymentMethod;
  final DateTime? expiresAt;
  final String? paymentId;
  // construtor, fromFirestore, toFirestore, props seguem este padrão
}
```

From lib/features/admin/cubit/pricing_cubit.dart (referência exata para SportConfigCubit):
```dart
class PricingCubit extends Cubit<PricingState> {
  final FirebaseFirestore _firestore;
  StreamSubscription<DocumentSnapshot<Map<String, dynamic>>>? _sub;

  PricingCubit({required FirebaseFirestore firestore})
      : _firestore = firestore,
        super(const PricingInitial()) {
    _startStream();
  }

  void _startStream() {
    _sub = _firestore.collection('config').doc('pricing').snapshots().listen(
      (snap) { emit(PricingLoaded(PriceTierModel.listFromFirestore(snap))); },
      onError: (e, s) {
        Sentry.captureException(e, stackTrace: s);
        emit(const PricingError('Erro ao carregar preços.'));
      },
    );
  }

  @override
  Future<void> close() { _sub?.cancel(); return super.close(); }
}
```

From lib/features/admin/ui/admin_screen.dart (linha 135-140 — ponto de inserção):
```dart
BlocProvider(
  create: (_) => SettingsCubit(firestore: FirebaseFirestore.instance),
  child: const SettingsTab(),
),
```
Substituir por MultiBlocProvider com SettingsCubit + SportConfigCubit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender BookingModel com campo sport opcional</name>
  <files>lib/core/models/booking_model.dart</files>
  <read_first>
    - lib/core/models/booking_model.dart (arquivo a modificar — ver padrão paymentMethod/expiresAt/paymentId)
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md (D-11 backward compat)
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Pattern 3 (snippet exato)
  </read_first>
  <action>
    Adicionar campo `final String? sport;` ao BookingModel, seguindo padrão exato dos campos nullable existentes (`paymentMethod`, `paymentId`).

    Mudanças concretas no arquivo `lib/core/models/booking_model.dart`:

    1. Declarar field após a linha `final String? paymentId;` (linha 19 atual):
    ```dart
    final String? sport; // SPORT-04: nullable, backward compat
    ```

    2. Adicionar parâmetro no construtor após `this.paymentId,` (linha 36 atual):
    ```dart
    this.sport,
    ```

    3. Adicionar parsing em `fromFirestore` após `paymentId: data['paymentId'] as String?,` (linha 64 atual):
    ```dart
    sport: data['sport'] as String?,
    ```

    4. Adicionar serialização em `toFirestore` após `if (paymentId != null) 'paymentId': paymentId,` (linha 83 atual), com o guard nullable (D-11):
    ```dart
    if (sport != null) 'sport': sport,
    ```

    5. Adicionar `sport` ao final da lista de `props` (linha 99-101 atual):
    ```dart
    @override
    List<Object?> get props => [id, slotId, date, userId, status, createdAt,
        cancelledAt, startTime, price, userDisplayName, participants,
        recurrenceGroupId, paymentMethod, expiresAt, paymentId, sport];
    ```

    NÃO criar testes — política do projeto proíbe (feedback_no_tests.md).
    NÃO adicionar getters booleanos para sport — não há valores enum, apenas string livre.
    NÃO usar `set` com merge em outros lugares — esta mudança é apenas no model.
  </action>
  <verify>
    <automated>flutter analyze lib/core/models/booking_model.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/core/models/booking_model.dart` contains `final String? sport;`
    - File contains `this.sport,` no construtor
    - File contains `sport: data['sport'] as String?,` em fromFirestore
    - File contains `if (sport != null) 'sport': sport,` em toFirestore
    - File contains `sport]` no final da lista de props
    - `flutter analyze lib/core/models/booking_model.dart` exits 0 (sem novos warnings nem errors)
  </acceptance_criteria>
  <done>BookingModel tem campo sport nullable; reservas antigas fromFirestore retornam sport=null; toFirestore omite a chave quando null; props inclui sport.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar SportConfigState e SportConfigCubit com auto-init de defaults</name>
  <files>lib/features/admin/cubit/sport_config_state.dart, lib/features/admin/cubit/sport_config_cubit.dart</files>
  <read_first>
    - lib/features/admin/cubit/pricing_cubit.dart (padrão exato a replicar)
    - lib/features/admin/cubit/pricing_state.dart (padrão sealed class)
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Pattern 1, §Code Examples (snippet exato)
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Common Pitfalls §Pitfall 2 (evitar loop infinito de _writeDefaults)
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md D-12, D-13
  </read_first>
  <action>
    Criar dois arquivos novos seguindo exatamente o padrão de `pricing_state.dart` e `pricing_cubit.dart`.

    **Arquivo 1: `lib/features/admin/cubit/sport_config_state.dart`**

    ```dart
    import 'package:equatable/equatable.dart';

    sealed class SportConfigState extends Equatable {
      const SportConfigState();
    }

    class SportConfigInitial extends SportConfigState {
      const SportConfigInitial();
      @override
      List<Object?> get props => [];
    }

    class SportConfigLoaded extends SportConfigState {
      final List<String> sports;
      const SportConfigLoaded(this.sports);
      @override
      List<Object?> get props => [sports];
    }

    class SportConfigError extends SportConfigState {
      final String message;
      const SportConfigError(this.message);
      @override
      List<Object?> get props => [message];
    }
    ```

    **Arquivo 2: `lib/features/admin/cubit/sport_config_cubit.dart`**

    ```dart
    import 'dart:async';

    import 'package:cloud_firestore/cloud_firestore.dart';
    import 'package:flutter_bloc/flutter_bloc.dart';
    import 'package:sentry_flutter/sentry_flutter.dart';
    import 'package:vida_ativa/features/admin/cubit/sport_config_state.dart';

    class SportConfigCubit extends Cubit<SportConfigState> {
      static const List<String> defaultSports = ['Vôlei', 'Beach Tênis', 'Futevôlei'];

      final FirebaseFirestore _firestore;
      StreamSubscription<DocumentSnapshot<Map<String, dynamic>>>? _sub;
      bool _initializingDefaults = false; // guard contra Pitfall 2 (loop infinito)

      SportConfigCubit({required FirebaseFirestore firestore})
          : _firestore = firestore,
            super(const SportConfigInitial()) {
        _startStream();
      }

      void _startStream() {
        _sub = _firestore
            .collection('config')
            .doc('sports')
            .snapshots()
            .listen(
          (snap) {
            final data = snap.data();
            final sports = (data?['sports'] as List<dynamic>?)
                    ?.map((e) => e as String)
                    .toList() ??
                <String>[];
            if (!snap.exists || sports.isEmpty) {
              if (!_initializingDefaults) {
                _writeDefaults();
              }
            } else {
              _initializingDefaults = false;
              emit(SportConfigLoaded(sports));
            }
          },
          onError: (e, s) {
            Sentry.captureException(e, stackTrace: s);
            emit(const SportConfigError('Erro ao carregar esportes.'));
          },
        );
      }

      Future<void> _writeDefaults() async {
        _initializingDefaults = true;
        try {
          await _firestore.collection('config').doc('sports').set({
            'sports': defaultSports,
          });
          // Stream listener vai emitir SportConfigLoaded automaticamente após o write completar.
        } catch (e, s) {
          _initializingDefaults = false;
          Sentry.captureException(e, stackTrace: s);
          emit(const SportConfigError('Erro ao inicializar esportes.'));
        }
      }

      Future<void> saveSports(List<String> sports) async {
        try {
          await _firestore.collection('config').doc('sports').set({
            'sports': sports,
          });
          // Stream vai emitir SportConfigLoaded automaticamente após o write.
        } catch (e, s) {
          Sentry.captureException(e, stackTrace: s);
          rethrow;
        }
      }

      @override
      Future<void> close() {
        _sub?.cancel();
        return super.close();
      }
    }
    ```

    Notas obrigatórias:
    - Usar `set` (sem `merge: true`) em ambos `_writeDefaults` e `saveSports`, conforme RESEARCH §Anti-Patterns ("Não usar set com merge para a lista de esportes").
    - Defaults exatos: `['Vôlei', 'Beach Tênis', 'Futevôlei']` com acentos UTF-8 corretos (D-13).
    - Flag `_initializingDefaults` resolve Pitfall 2.
    - Sentry.captureException em todos os catch blocks (padrão do projeto).
    - NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/cubit/sport_config_cubit.dart lib/features/admin/cubit/sport_config_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/admin/cubit/sport_config_state.dart` exists and contains `sealed class SportConfigState`
    - File contains `class SportConfigInitial`, `class SportConfigLoaded`, `class SportConfigError`
    - File `lib/features/admin/cubit/sport_config_cubit.dart` exists and contains `class SportConfigCubit extends Cubit<SportConfigState>`
    - File contains `static const List<String> defaultSports = ['Vôlei', 'Beach Tênis', 'Futevôlei'];`
    - File contains `collection('config').doc('sports').snapshots().listen`
    - File contains `if (!snap.exists || sports.isEmpty)`
    - File contains `_initializingDefaults` (Pitfall 2 guard)
    - File contains `Future<void> saveSports(List<String> sports)`
    - File contains `Sentry.captureException` in catch blocks
    - File does NOT contain `SetOptions(merge: true)` (anti-pattern documented in research)
    - `flutter analyze` on both files exits 0
  </acceptance_criteria>
  <done>Dois arquivos novos criados; cubit lê stream em /config/sports; escreve defaults se ausente/vazio; flag previne loop; saveSports persiste com `set` sobrescrevendo o doc.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Provisionar SportConfigCubit no AdminScreen junto com SettingsCubit</name>
  <files>lib/features/admin/ui/admin_screen.dart</files>
  <read_first>
    - lib/features/admin/ui/admin_screen.dart (arquivo a modificar — linha 135-140 é o ponto)
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Pattern 7
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md (Claude's Discretion: provisão no AdminScreen)
  </read_first>
  <action>
    Substituir o `BlocProvider` único que envolve `SettingsTab` por um `MultiBlocProvider` que injeta `SettingsCubit` E `SportConfigCubit`. Isso permite que o Plano 03 (SettingsTab seção Esportes) leia o estado do `SportConfigCubit` via `BlocBuilder`.

    Mudanças concretas em `lib/features/admin/ui/admin_screen.dart`:

    1. Adicionar import logo após `import 'package:vida_ativa/features/admin/cubit/settings_cubit.dart';` (linha 9 atual):
    ```dart
    import 'package:vida_ativa/features/admin/cubit/sport_config_cubit.dart';
    ```

    2. Substituir o bloco das linhas 135-140 atual:
    ```dart
    BlocProvider(
      create: (_) => SettingsCubit(
        firestore: FirebaseFirestore.instance,
      ),
      child: const SettingsTab(),
    ),
    ```

    Por:
    ```dart
    MultiBlocProvider(
      providers: [
        BlocProvider(
          create: (_) => SettingsCubit(
            firestore: FirebaseFirestore.instance,
          ),
        ),
        BlocProvider(
          create: (_) => SportConfigCubit(
            firestore: FirebaseFirestore.instance,
          ),
        ),
      ],
      child: const SettingsTab(),
    ),
    ```

    `MultiBlocProvider` já está disponível via `package:flutter_bloc/flutter_bloc.dart` (já importado na linha 3 atual). NÃO adicionar import duplicado de flutter_bloc.

    NÃO mexer em outras abas (Slots, Bloqueios, Reservas, Usuários, Preços) — escopo é apenas o SettingsTab.
    NÃO provisionar o `SportConfigCubit` no nível de app — provisão apenas no AdminScreen (Opção A do RESEARCH para a área do cliente, que vai receber a lista resolvida via parâmetro no Plano 02).
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/admin_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/admin/ui/admin_screen.dart` contains `import 'package:vida_ativa/features/admin/cubit/sport_config_cubit.dart';`
    - File contains `MultiBlocProvider(`
    - File contains `create: (_) => SportConfigCubit(`
    - File contains `firestore: FirebaseFirestore.instance,` inside the SportConfigCubit create
    - File still contains `create: (_) => SettingsCubit(` (settings cubit não removido)
    - File still contains `child: const SettingsTab(),`
    - `flutter analyze lib/features/admin/ui/admin_screen.dart` exits 0
  </acceptance_criteria>
  <done>AdminScreen agora provisiona ambos os cubits acima do SettingsTab via MultiBlocProvider; SettingsTab no Plano 03 pode usar `context.read<SportConfigCubit>()` e `BlocBuilder<SportConfigCubit, SportConfigState>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Flutter) → Firestore /config/sports | Apenas admin deve escrever; cliente normal não pode |
| Firestore /bookings (existente) → Flutter (fromFirestore) | Docs antigos sem campo sport precisam abrir sem crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | /config/sports write by non-admin | mitigate | Regras Firestore existentes em /config/{document} cobrem admin-only (verificadas pelo padrão das outras configs como /config/pricing e /config/mercadopago); cubit escreve apenas dentro de fluxo admin. Sem código adicional necessário neste plano; documentado em UAT. |
| T-20-02 | Denial of Service | _writeDefaults loop infinito | mitigate | Flag `_initializingDefaults` bloqueia chamadas reentrantes (Pitfall 2 do RESEARCH); guard `!snap.exists || sports.isEmpty` impede write quando doc já tem dados. |
| T-20-03 | Information Disclosure | BookingModel.fromFirestore vaza dados | accept | Campo sport é nome curto de esporte, não é PII; risco baixo. |
| T-20-04 | Repudiation / Backward break | Reservas antigas sem campo sport | mitigate | `data['sport'] as String?` retorna null para docs sem o campo; `if (sport != null)` em toFirestore não adiciona a chave se null. Backward compat garantida por padrão estabelecido (paymentMethod, paymentId seguem o mesmo molde). |
</threat_model>

<verification>
- `flutter analyze` em todos os arquivos modificados → 0 erros, 0 warnings novos.
- Manual smoke (executor reporta no SUMMARY): abrir app em modo dev, navegar para Admin > Ajustes; verificar Firestore Console que `/config/sports` foi criado com `{sports: ['Vôlei', 'Beach Tênis', 'Futevôlei']}`.
- Manual smoke: abrir reserva antiga (sem campo sport) — confirma que não há crash em fromFirestore.
</verification>

<success_criteria>
- BookingModel aceita e serializa campo sport opcional sem quebrar reservas antigas (SPORT-04).
- SportConfigCubit existe e está provisionado no AdminScreen.
- Primeira execução em ambiente sem /config/sports cria o doc com defaults Vôlei, Beach Tênis, Futevôlei (SPORT-03).
- Stream emite SportConfigLoaded com a lista quando doc existe e tem itens.
- `flutter analyze` limpo em todos os arquivos tocados.
</success_criteria>

<output>
Criar `.planning/phases/20-infraestrutura-de-esporte/20-01-SUMMARY.md` ao final, listando arquivos modificados, comportamento verificado e qualquer surpresa encontrada (especialmente sobre regras Firestore se houver bloqueio de escrita no primeiro run).
</output>
