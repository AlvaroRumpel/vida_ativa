---
phase: 20-infraestrutura-de-esporte
plan: 02
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - lib/features/booking/ui/booking_confirmation_sheet.dart
  - lib/features/booking/cubit/booking_cubit.dart
  - lib/features/schedule/ui/slot_day_view.dart
  - lib/features/schedule/ui/slot_list.dart
autonomous: true
requirements:
  - SPORT-01
tags:
  - flutter
  - bloc
  - ui
  - booking
must_haves:
  truths:
    - "Cliente vê dropdown 'Esporte (opcional)' no BookingConfirmationSheet após o campo de participantes quando lista de esportes não está vazia"
    - "Quando lista está vazia, dropdown é completamente escondido (não renderizado)"
    - "Cliente seleciona esporte e o valor selecionado é propagado para BookingCubit.bookSlot e BookingCubit.bookRecurring"
    - "Reservas criadas (Pix e on_arrival, single e recorrente) persistem o campo sport no doc Firestore quando selecionado"
  artifacts:
    - path: "lib/features/booking/ui/booking_confirmation_sheet.dart"
      provides: "DropdownButtonFormField<String?> de esporte + parâmetro List<String> sports no construtor + estado _selectedSport"
      contains: "DropdownButtonFormField<String?>"
    - path: "lib/features/booking/cubit/booking_cubit.dart"
      provides: "Parâmetro String? sport opcional em bookSlot e bookRecurring; propagação na criação do BookingModel"
      contains: "String? sport"
    - path: "lib/features/schedule/ui/slot_day_view.dart"
      provides: "Caller lê SportConfigCubit antes de abrir sheet e passa List<String> sports"
      contains: "SportConfigCubit"
    - path: "lib/features/schedule/ui/slot_list.dart"
      provides: "Caller lê SportConfigCubit antes de abrir sheet e passa List<String> sports"
      contains: "SportConfigCubit"
  key_links:
    - from: "lib/features/booking/ui/booking_confirmation_sheet.dart"
      to: "lib/features/booking/cubit/booking_cubit.dart"
      via: "bookSlot(..., sport: _selectedSport) e bookRecurring(..., sport: _selectedSport)"
      pattern: "sport: _selectedSport"
    - from: "lib/features/schedule/ui/slot_day_view.dart"
      to: "lib/features/admin/cubit/sport_config_cubit.dart"
      via: "context.read<SportConfigCubit>() OU widget tree provider acima"
      pattern: "SportConfigCubit"
---

<objective>
Plano 02 entrega a experiência do cliente para SPORT-01: dropdown opcional de esporte no formulário de reserva, propagação do campo até `BookingModel` via `bookSlot` e `bookRecurring`, e wiring dos callers (`slot_day_view.dart`, `slot_list.dart`) para passar a lista resolvida ao sheet.

Purpose: SPORT-01 exige que o cliente possa selecionar um esporte opcional via dropdown na reserva. Sem esta UI nem a propagação no cubit, o campo sport do Plano 01 nunca é populado.

Output: BookingConfirmationSheet com dropdown condicional, BookingCubit recebendo e persistindo sport, callers no schedule provisionando o SportConfigCubit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md
@.planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md
@.planning/phases/20-infraestrutura-de-esporte/20-UI-SPEC.md

@lib/features/booking/ui/booking_confirmation_sheet.dart
@lib/features/booking/cubit/booking_cubit.dart
@lib/features/schedule/ui/slot_day_view.dart
@lib/features/schedule/ui/slot_list.dart
@lib/features/admin/cubit/sport_config_cubit.dart
@lib/features/admin/cubit/sport_config_state.dart

<interfaces>
<!-- Contratos relevantes — extraídos do código existente + cubit criado em Plano 01. -->

From lib/features/admin/cubit/sport_config_cubit.dart (criado em Plano 01):
```dart
class SportConfigCubit extends Cubit<SportConfigState> {
  static const List<String> defaultSports = ['Vôlei', 'Beach Tênis', 'Futevôlei'];
  SportConfigCubit({required FirebaseFirestore firestore});
  Future<void> saveSports(List<String> sports);
}

sealed class SportConfigState extends Equatable { ... }
class SportConfigInitial extends SportConfigState { ... }
class SportConfigLoaded extends SportConfigState { final List<String> sports; ... }
class SportConfigError extends SportConfigState { final String message; ... }
```

From lib/features/booking/cubit/booking_cubit.dart (assinatura atual de bookSlot):
```dart
Future<void> bookSlot({
  required String slotId,
  required String dateString,
  required double price,
  required String startTime,
  required String userDisplayName,
  required String paymentMethod,
  String? participants,
  String? recurrenceGroupId,
}) async { ... }

Future<List<RecurrenceOutcome>> bookRecurring({
  required List<RecurrenceEntry> entries,
  required String startTime,
  required String userDisplayName,
  required String paymentMethod,
  String? participants,
}) async { ... }
```

From lib/features/booking/ui/booking_confirmation_sheet.dart (construtor atual):
```dart
const BookingConfirmationSheet({
  super.key,
  required this.viewModel,
  required this.bookingCubit,
  this.pixEnabled = true,
});
```

From lib/features/schedule/ui/slot_day_view.dart (linha 127, ponto de abertura do sheet):
```dart
builder: (_) => BookingConfirmationSheet(
  viewModel: viewModel,
  bookingCubit: bookingCubit,
  pixEnabled: bookingCubit.pixEnabled,
),
```

From lib/features/schedule/ui/slot_list.dart (linha 68, idem):
```dart
builder: (_) => BookingConfirmationSheet(
  viewModel: viewModel,
  bookingCubit: bookingCubit,
  pixEnabled: bookingCubit.pixEnabled,
),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender BookingCubit.bookSlot e bookRecurring com parâmetro sport</name>
  <files>lib/features/booking/cubit/booking_cubit.dart</files>
  <read_first>
    - lib/features/booking/cubit/booking_cubit.dart (arquivo a modificar — linhas 68-124 bookSlot; 130-167 bookRecurring)
    - lib/core/models/booking_model.dart (já estendido em Plano 01 com sport)
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Pattern 4, §Common Pitfalls §Pitfall 4 (propagar sport em bookRecurring)
  </read_first>
  <action>
    Adicionar parâmetro opcional `String? sport` em ambos os métodos e propagá-lo até o `BookingModel` criado dentro da transação.

    Mudanças concretas em `lib/features/booking/cubit/booking_cubit.dart`:

    1. **bookSlot — adicionar parâmetro** logo após `String? recurrenceGroupId,` (linha 76 atual), antes do `}) async {`:
    ```dart
        String? sport,
    ```

    2. **bookSlot — propagar no BookingModel** dentro da transação (linha 107-120 atual). Adicionar `sport: sport,` na construção do BookingModel logo após `paymentMethod: paymentMethod,`:
    ```dart
    final booking = BookingModel(
      id: docId,
      slotId: slotId,
      date: dateString,
      userId: _userId,
      status: initialStatus,
      createdAt: DateTime.now(),
      startTime: startTime,
      price: price,
      userDisplayName: userDisplayName,
      participants: participants,
      recurrenceGroupId: recurrenceGroupId,
      paymentMethod: paymentMethod,
      sport: sport,
    );
    ```

    3. **bookRecurring — adicionar parâmetro** logo após `String? participants,` (linha 135 atual), antes do `}) async {`:
    ```dart
        String? sport,
    ```

    4. **bookRecurring — propagar para bookSlot** dentro do `entries.map` (linha 142-151 atual). Adicionar `sport: sport,` à chamada de `bookSlot`:
    ```dart
    await bookSlot(
      slotId: entry.slotId,
      dateString: entry.dateString,
      price: entry.price,
      startTime: startTime,
      userDisplayName: userDisplayName,
      participants: participants,
      recurrenceGroupId: groupId,
      paymentMethod: paymentMethod,
      sport: sport,
    );
    ```

    NÃO mudar a assinatura de RecurrenceEntry — sport não é por entry, é o mesmo para o grupo todo (vem do dropdown do sheet, um único valor).
    NÃO mexer em cancelBooking, updateParticipants, cancelGroupFuture — escopo é apenas as duas funções de criação.
    NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/booking/cubit/booking_cubit.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/booking/cubit/booking_cubit.dart` contains the pattern `String? sport,` appearing at least twice (one in bookSlot signature, one in bookRecurring signature)
    - File contains `sport: sport,` appearing at least twice (one in BookingModel construction inside bookSlot, one in the bookSlot call inside bookRecurring)
    - File still contains `paymentMethod: paymentMethod,` (não removido por engano)
    - `flutter analyze lib/features/booking/cubit/booking_cubit.dart` exits 0
  </acceptance_criteria>
  <done>bookSlot e bookRecurring aceitam sport opcional; reservas criadas com sport != null persistem o campo no Firestore via BookingModel.toFirestore (já guardado por if-not-null do Plano 01); chamadas sem sport continuam funcionando idênticas (default null).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar parâmetro sports e dropdown ao BookingConfirmationSheet</name>
  <files>lib/features/booking/ui/booking_confirmation_sheet.dart</files>
  <read_first>
    - lib/features/booking/ui/booking_confirmation_sheet.dart (arquivo a modificar — construtor linha 17-32; state linha 34-39; build com TextField participants linha 332-350; calls de bookSlot linha 119-132 _handlePayPix, linha 165-177 _handlePayOnArrival, linha 69-79 _handleConfirmRecurring)
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md D-01, D-02, D-03, D-04
    - .planning/phases/20-infraestrutura-de-esporte/20-UI-SPEC.md §Component Inventory `Modified: BookingConfirmationSheet`, §Interaction States
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Code Examples `Dropdown no BookingConfirmationSheet`
  </read_first>
  <action>
    Adicionar (a) parâmetro `final List<String> sports` ao construtor, (b) state `String? _selectedSport`, (c) widget `DropdownButtonFormField<String?>` condicional após o `TextField` de participantes, (d) propagação de `_selectedSport` nas três chamadas de `bookSlot`/`bookRecurring`.

    Mudanças concretas em `lib/features/booking/ui/booking_confirmation_sheet.dart`:

    1. **Adicionar parâmetro no construtor** (linha 17-32 atual):
    ```dart
    class BookingConfirmationSheet extends StatefulWidget {
      final SlotViewModel viewModel;
      final BookingCubit bookingCubit;
      final bool pixEnabled;
      final List<String> sports; // SPORT-01 — D-04: vazia = dropdown escondido

      const BookingConfirmationSheet({
        super.key,
        required this.viewModel,
        required this.bookingCubit,
        this.pixEnabled = true,
        this.sports = const <String>[],
      });
      // ...
    }
    ```

    2. **Adicionar field de estado** dentro de `_BookingConfirmationSheetState` (após `_participantsController`, linha 37 atual):
    ```dart
    String? _selectedSport;
    ```

    3. **Adicionar dropdown condicional no build** — entre o `TextField` de participantes (que termina em `textCapitalization: TextCapitalization.words,` linha 349) e a linha do `if (_errorMessage != null) ...[` (linha 351). Inserir:
    ```dart
    if (widget.sports.isNotEmpty) ...[
      const SizedBox(height: 16),
      DropdownButtonFormField<String?>(
        value: _selectedSport,
        decoration: const InputDecoration(
          labelText: 'Esporte (opcional)',
          border: OutlineInputBorder(
            borderRadius: BorderRadius.all(Radius.circular(12)),
          ),
          enabledBorder: OutlineInputBorder(
            borderRadius: BorderRadius.all(Radius.circular(12)),
          ),
          focusedBorder: OutlineInputBorder(
            borderRadius: BorderRadius.all(Radius.circular(12)),
          ),
        ),
        items: [
          const DropdownMenuItem<String?>(
            value: null,
            child: Text('Não informado'),
          ),
          ...widget.sports.map(
            (s) => DropdownMenuItem<String?>(value: s, child: Text(s)),
          ),
        ],
        onChanged: (v) => setState(() => _selectedSport = v),
      ),
    ],
    ```

    4. **Propagar `sport: _selectedSport` em três chamadas**:

    - `_handlePayPix` (linha 119-132 atual) — adicionar logo após `participants: ...,`:
    ```dart
    sport: _selectedSport,
    ```

    - `_handlePayOnArrival` (linha 165-177 atual) — adicionar logo após `participants: ...,`:
    ```dart
    sport: _selectedSport,
    ```

    - `_handleConfirmRecurring` (linha 69-79 atual) — adicionar logo após `participants: ...,` na chamada `bookRecurring`:
    ```dart
    sport: _selectedSport,
    ```

    Detalhes obrigatórios da UI:
    - Label exato: `'Esporte (opcional)'` (D-02).
    - Item null com texto `'Não informado'` permite o usuário voltar para "sem seleção".
    - Empty state: o `if (widget.sports.isNotEmpty)` esconde tudo (D-04).
    - Borda `BorderRadius.circular(12)` em todos os estados (idêntica ao TextField de participantes, linha 337-345).
    - NÃO renderizar `const SizedBox(height: 16)` antes da seção condicional fora do `if` — coloca dentro para não criar gap fantasma quando lista vazia.

    NÃO usar `BlocBuilder<SportConfigCubit, SportConfigState>` aqui — a lista é passada como parâmetro (Opção A do RESEARCH §Pattern 6). Isso evita acoplar o sheet ao cubit do admin.
    NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/booking/ui/booking_confirmation_sheet.dart</automated>
  </verify>
  <acceptance_criteria>
    - File contains `final List<String> sports;` in the widget class
    - File contains `this.sports = const <String>[],` in the constructor
    - File contains `String? _selectedSport;` in the state
    - File contains `if (widget.sports.isNotEmpty)`
    - File contains `DropdownButtonFormField<String?>`
    - File contains `labelText: 'Esporte (opcional)'`
    - File contains `value: null` and `child: Text('Não informado')` for the null option
    - File contains `sport: _selectedSport,` appearing at least 3 times (one per booking call)
    - File contains `BorderRadius.circular(12)` (dropdown border matches TextField pattern)
    - `flutter analyze lib/features/booking/ui/booking_confirmation_sheet.dart` exits 0
  </acceptance_criteria>
  <done>Sheet expõe parâmetro sports; dropdown renderiza só quando lista não vazia; seleção persiste em _selectedSport; valor propaga para bookSlot (Pix + on_arrival) e bookRecurring; valor null = "Não informado" e BookingModel.toFirestore omite a chave.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire callers em slot_day_view e slot_list para passar sports ao sheet</name>
  <files>lib/features/schedule/ui/slot_day_view.dart, lib/features/schedule/ui/slot_list.dart</files>
  <read_first>
    - lib/features/schedule/ui/slot_day_view.dart (linha 127, criação do sheet)
    - lib/features/schedule/ui/slot_list.dart (linha 68, criação do sheet)
    - lib/features/admin/cubit/sport_config_cubit.dart (criado em Plano 01)
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Open Questions Q1, §Pattern 6 Opção A
  </read_first>
  <action>
    Os dois callers precisam (a) ter `SportConfigCubit` provisionado acima e (b) ler a lista de esportes ao abrir o sheet, passando-a como parâmetro `sports`.

    Como o sheet é aberto na área do cliente (não no AdminScreen), provisionamos o `SportConfigCubit` **localmente em cada arquivo** envolvendo a função/widget que chama `showModalBottomSheet`. Padrão exato: `BlocProvider` no topo do widget que contém o método de abertura do sheet.

    ### Decisão de provisão (Opção A do RESEARCH):

    Em vez de injetar globalmente, cada arquivo cria seu próprio `SportConfigCubit` no nível do widget que tem o método de abertura do sheet. Como o cubit lê de Firestore, múltiplas instâncias são aceitáveis (cada uma abre seu próprio stream) — mas para reduzir leituras, prefere-se uma única instância por screen. Implementação: usar `BlocProvider` envolvendo o widget de schedule no nível mais próximo possível.

    Inspecionar os dois arquivos primeiro para descobrir a estrutura do widget. Se ambos forem `StatelessWidget` chamados por uma screen pai (ex.: `ScheduleScreen`), prover o cubit lá.

    **Abordagem pragmática (sem precisar editar a árvore mais alta):** Em cada caller, antes de abrir o sheet, criar uma única instância do `SportConfigCubit` localmente, ler o estado, e descartar. Mas streams são assíncronos — pode não estar pronto. Solução: usar `FirebaseFirestore.instance.collection('config').doc('sports').get()` **diretamente** dentro do handler antes de abrir o sheet, e passar a lista resolvida.

    Mudanças concretas:

    **A) `lib/features/schedule/ui/slot_day_view.dart`** — modificar o método que abre o sheet (contém linha 127):

    1. Adicionar import no topo do arquivo (se ainda não presente):
    ```dart
    import 'package:cloud_firestore/cloud_firestore.dart';
    ```

    2. Localizar o método que contém o `showModalBottomSheet` (a função que envolve a linha 127). Tornar `async` (se ainda não for) e ler a lista de esportes antes de abrir o sheet:

    Antes:
    ```dart
    builder: (_) => BookingConfirmationSheet(
      viewModel: viewModel,
      bookingCubit: bookingCubit,
      pixEnabled: bookingCubit.pixEnabled,
    ),
    ```

    Depois:
    ```dart
    // Read sports list from Firestore before opening sheet (Opção A do RESEARCH).
    // Doc may not exist yet on first launch — handle null gracefully.
    final sportsSnap = await FirebaseFirestore.instance
        .collection('config')
        .doc('sports')
        .get();
    final sports = (sportsSnap.data()?['sports'] as List<dynamic>?)
            ?.map((e) => e as String)
            .toList() ??
        const <String>[];
    if (!context.mounted) return;
    await showModalBottomSheet(
      // ... existing params (context, isScrollControlled, shape) ...
      builder: (_) => BookingConfirmationSheet(
        viewModel: viewModel,
        bookingCubit: bookingCubit,
        pixEnabled: bookingCubit.pixEnabled,
        sports: sports,
      ),
    );
    ```

    Preservar todos os outros parâmetros existentes do `showModalBottomSheet` (`context`, `isScrollControlled: true`, `shape: const RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(16)))`) — ler arquivo atual para copiar exatamente.

    **B) `lib/features/schedule/ui/slot_list.dart`** — mesma mudança no método que contém a linha 68. Replicar exatamente o padrão acima.

    Notas obrigatórias:
    - `if (!context.mounted) return;` é obrigatório após o await em ambos os arquivos (Flutter analyzer flag warning sem isso).
    - Se a função onde a chamada está atualmente não retornar `Future` nem for `async`, marcá-la como `async`. Se for um callback dentro de `onTap:` lambda, basta tornar a lambda `async`.
    - NÃO usar polling nem stream listener — get one-shot é suficiente porque a lista muda raramente e qualquer mudança feita pelo admin já refletirá no próximo open.
    - NÃO provisionar `SportConfigCubit` no widget tree do cliente para evitar acoplamento; o get direto resolve o caso da Opção A do RESEARCH.
    - Se algum dos dois callers já não tem acesso ao `FirebaseFirestore.instance` no scope, adicionar import.

    NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/schedule/ui/slot_day_view.dart lib/features/schedule/ui/slot_list.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/schedule/ui/slot_day_view.dart` contains `collection('config').doc('sports').get()`
    - File contains `sports: sports,` no construtor de BookingConfirmationSheet
    - File contains `if (!context.mounted) return;` após o await
    - File `lib/features/schedule/ui/slot_list.dart` contains `collection('config').doc('sports').get()`
    - File contains `sports: sports,` no construtor de BookingConfirmationSheet
    - File contains `if (!context.mounted) return;` após o await
    - Ambos os arquivos: handler do tap é `async` ou retorna Future
    - `flutter analyze lib/features/schedule/ui/slot_day_view.dart lib/features/schedule/ui/slot_list.dart` exits 0 (sem warnings novos)
  </acceptance_criteria>
  <done>Cliente que toca em um slot dispara um read one-shot de /config/sports; sheet abre com a lista resolvida; quando doc não existe (primeiro launch antes do admin abrir Ajustes), lista é vazia e dropdown não renderiza — comportamento esperado por D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Flutter) → Firestore /config/sports (read) | Cliente normal pode ler lista para popular dropdown; sem dados sensíveis |
| client (Flutter) → Firestore /bookings (write via bookSlot) | Cliente cria reserva com sport opcional; valor é string livre vindo do dropdown |
| User input → BookingModel.sport | Valor restrito a opções do dropdown OU null; sem entrada livre de texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | Cliente injeta string arbitrária em sport via Firestore client SDK direto | mitigate | Embora o dropdown limite a UI, o cliente Flutter ainda pode escrever qualquer string no doc booking. Tamanho é limitado naturalmente pela Firestore (1MB doc); valor é exibido apenas como chip no admin (sem renderização HTML/eval). Risco: usuário admin vê string estranha — não compromete sistema. Aceitar; mitigação adicional (validar contra `/config/sports`) seria server-side em Cloud Function — fora de escopo. |
| T-20-06 | Information Disclosure | Read de /config/sports por cliente não autenticado | accept | Lista de esportes não é PII e não é sensível; já é exibida na UI publicamente. Regras Firestore existentes para `/config/*` provavelmente permitem read autenticado; aceitar comportamento. |
| T-20-07 | Denial of Service | Cliente abre sheet em loop e gera muitos reads de /config/sports | mitigate | Read one-shot por tap (não stream contínuo); Firestore tem rate limiting nativo; custo desprezível (1 doc pequeno). Aceitar. |
| T-20-08 | Repudiation | Reserva criada com sport vazio quando dropdown estava escondido (lista vazia) | mitigate | `_selectedSport` default null; quando dropdown não renderiza, valor fica null; `if (sport != null)` em toFirestore omite a chave (Plano 01). Backward compat garantida. |
</threat_model>

<verification>
- `flutter analyze` em todos os arquivos modificados → 0 erros, 0 warnings novos.
- Manual smoke (executor reporta no SUMMARY):
  1. Abrir slot, ver dropdown "Esporte (opcional)" entre participantes e botões.
  2. Selecionar "Vôlei", confirmar reserva on_arrival, abrir o doc no Firestore Console → campo `sport: "Vôlei"` presente.
  3. Não selecionar nada (deixar "Não informado"), confirmar — doc Firestore SEM campo sport.
  4. Limpar `/config/sports` manualmente no console, abrir slot — dropdown não renderiza (D-04).
  5. Reserva recorrente com sport selecionado → todos os bookings do grupo têm `sport: "X"`.
</verification>

<success_criteria>
- Cliente vê dropdown "Esporte (opcional)" após campo de participantes (D-01, D-02, D-03).
- Dropdown escondido quando lista vazia (D-04).
- Valor selecionado persiste no BookingModel via bookSlot e bookRecurring (SPORT-01).
- Reservas sem sport continuam funcionando (default null, omitido no doc).
- `flutter analyze` limpo em todos os arquivos tocados.
</success_criteria>

<output>
Criar `.planning/phases/20-infraestrutura-de-esporte/20-02-SUMMARY.md` ao final, listando arquivos modificados, comportamento manual verificado e qualquer ajuste necessário no scope dos callers (ex.: se houver mais que dois arquivos abrindo o sheet, registrar).
</output>
