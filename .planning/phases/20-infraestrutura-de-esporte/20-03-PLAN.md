---
phase: 20-infraestrutura-de-esporte
plan: 03
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - lib/features/admin/ui/settings_tab.dart
  - lib/features/admin/ui/admin_booking_card.dart
  - lib/features/admin/ui/admin_booking_detail_sheet.dart
autonomous: true
requirements:
  - SPORT-02
tags:
  - flutter
  - bloc
  - admin
  - ui
must_haves:
  truths:
    - "Admin vê seção 'Esportes' no SettingsTab após a seção Mercado Pago"
    - "Admin pode adicionar um esporte via TextField inline + botão de adicionar"
    - "Admin pode remover um esporte com IconButton de delete por item"
    - "Admin pode reordenar esportes via drag handles no ReorderableListView"
    - "Botão 'Salvar Esportes' persiste a lista em /config/sports via SportConfigCubit.saveSports"
    - "Admin vê chip colorido de esporte em AdminBookingCard e AdminBookingDetailSheet apenas quando booking.sport != null"
  artifacts:
    - path: "lib/features/admin/ui/settings_tab.dart"
      provides: "Nova seção 'Esportes' com ReorderableListView, add/remove, save button"
      contains: "ReorderableListView"
    - path: "lib/features/admin/ui/admin_booking_card.dart"
      provides: "Chip de esporte renderizado quando booking.sport != null"
      contains: "booking.sport"
    - path: "lib/features/admin/ui/admin_booking_detail_sheet.dart"
      provides: "Linha de info com ícone + label do esporte quando booking.sport != null"
      contains: "booking.sport"
  key_links:
    - from: "lib/features/admin/ui/settings_tab.dart"
      to: "lib/features/admin/cubit/sport_config_cubit.dart"
      via: "BlocBuilder<SportConfigCubit, SportConfigState> + context.read<SportConfigCubit>().saveSports"
      pattern: "BlocBuilder<SportConfigCubit"
    - from: "lib/features/admin/ui/admin_booking_card.dart"
      to: "lib/core/models/booking_model.dart"
      via: "leitura direta de booking.sport"
      pattern: "booking\\.sport"
    - from: "lib/features/admin/ui/admin_booking_detail_sheet.dart"
      to: "lib/core/models/booking_model.dart"
      via: "leitura direta de booking.sport"
      pattern: "booking\\.sport"
---

<objective>
Plano 03 entrega a UX do admin para Phase 20: nova seção "Esportes" no `SettingsTab` com add/remove/reorder/save (SPORT-02, D-05..D-08), e exibição de chip colorido de esporte em `AdminBookingCard` e `AdminBookingDetailSheet` (D-09, D-10, D-11).

Purpose: SPORT-02 exige gestão completa (adicionar, remover, reordenar). D-09..D-11 exigem que admin veja o esporte das reservas. Sem este plano, o admin não pode configurar a lista nem visualizar o esporte das reservas existentes.

Output: SettingsTab com nova seção, dois arquivos de card/sheet com chip condicional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md
@.planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md
@.planning/phases/20-infraestrutura-de-esporte/20-UI-SPEC.md

@lib/features/admin/ui/settings_tab.dart
@lib/features/admin/ui/admin_booking_card.dart
@lib/features/admin/ui/admin_booking_detail_sheet.dart
@lib/features/admin/cubit/sport_config_cubit.dart
@lib/features/admin/cubit/sport_config_state.dart
@lib/core/theme/app_spacing.dart
@lib/core/theme/app_theme.dart
@lib/core/utils/snack_helper.dart

<interfaces>
<!-- Contratos extraídos do código existente e do Plano 01. -->

From lib/features/admin/cubit/sport_config_cubit.dart (criado em Plano 01):
```dart
class SportConfigCubit extends Cubit<SportConfigState> {
  static const List<String> defaultSports = ['Vôlei', 'Beach Tênis', 'Futevôlei'];
  Future<void> saveSports(List<String> sports);
}

sealed class SportConfigState extends Equatable {}
class SportConfigInitial extends SportConfigState {}
class SportConfigLoaded extends SportConfigState {
  final List<String> sports;
}
class SportConfigError extends SportConfigState {
  final String message;
}
```

From lib/features/admin/ui/admin_screen.dart (modificado em Plano 01):
```dart
MultiBlocProvider(
  providers: [
    BlocProvider(create: (_) => SettingsCubit(...)),
    BlocProvider(create: (_) => SportConfigCubit(firestore: FirebaseFirestore.instance)),
  ],
  child: const SettingsTab(),
),
```

SettingsTab portanto pode usar `BlocBuilder<SportConfigCubit, SportConfigState>` e `context.read<SportConfigCubit>().saveSports(...)`.

From lib/core/theme/app_spacing.dart (já no projeto):
```dart
class AppSpacing {
  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
  static const double xl = 32;
}
```

From lib/core/theme/app_theme.dart:
```dart
class AppTheme {
  static const Color primaryGreen = Color(0xFF7B5D0A);
  // ...
}
```

From lib/core/utils/snack_helper.dart:
```dart
class SnackHelper {
  static void success(BuildContext context, String message);
  static void error(BuildContext context, String message);
}
```

From lib/core/models/booking_model.dart (modificado em Plano 01):
```dart
class BookingModel extends Equatable {
  // ... outros campos
  final String? sport;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar seção 'Esportes' no SettingsTab com ReorderableListView</name>
  <files>lib/features/admin/ui/settings_tab.dart</files>
  <read_first>
    - lib/features/admin/ui/settings_tab.dart (arquivo a modificar — estrutura atual: BlocBuilder<SettingsCubit> envolvendo _SettingsForm; ListView de items)
    - lib/features/admin/cubit/sport_config_cubit.dart (criado em Plano 01 — usa context.read)
    - lib/features/admin/cubit/sport_config_state.dart (sealed states)
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md D-05, D-06, D-07, D-08
    - .planning/phases/20-infraestrutura-de-esporte/20-UI-SPEC.md §Component Inventory `Modified: SettingsTab`, §Accessibility, §Copywriting Contract, §Interaction States ReorderableListView
    - .planning/phases/20-infraestrutura-de-esporte/20-RESEARCH.md §Common Pitfalls §Pitfall 3 (key obrigatória), §Code Examples ReorderableListView
  </read_first>
  <action>
    Adicionar uma nova seção "Esportes" ao `_SettingsForm` após a seção Mercado Pago (após o `FilledButton` "Salvar Credenciais" linha 195 atual). A seção é um sub-widget novo `_SportsSection` (StatefulWidget) que encapsula sua própria lógica.

    Mudanças concretas em `lib/features/admin/ui/settings_tab.dart`:

    1. **Adicionar imports** no topo:
    ```dart
    import 'package:vida_ativa/features/admin/cubit/sport_config_cubit.dart';
    import 'package:vida_ativa/features/admin/cubit/sport_config_state.dart';
    ```

    2. **Adicionar `_SportsSection` ao ListView** dentro do `_SettingsFormState.build` — após o `FilledButton` "Salvar Credenciais" (depois do `child: ... 'Salvar Credenciais')` linha 195 atual). Inserir:
    ```dart
    const SizedBox(height: AppSpacing.lg),
    const Divider(),
    Text('Esportes', style: textTheme.titleMedium),
    const SizedBox(height: AppSpacing.sm),
    const _SportsSection(),
    ```

    3. **Criar widget `_SportsSection` no mesmo arquivo** (após `_SettingsFormState` no final do arquivo):

    ```dart
    class _SportsSection extends StatefulWidget {
      const _SportsSection();

      @override
      State<_SportsSection> createState() => _SportsSectionState();
    }

    class _SportsSectionState extends State<_SportsSection> {
      final TextEditingController _addController = TextEditingController();
      List<String> _localSports = const <String>[];
      bool _isSaving = false;
      bool _initialized = false;

      @override
      void dispose() {
        _addController.dispose();
        super.dispose();
      }

      void _syncFromState(List<String> stateSports) {
        if (!_initialized) {
          _localSports = List<String>.from(stateSports);
          _initialized = true;
        }
      }

      void _addSport() {
        final name = _addController.text.trim();
        if (name.isEmpty) return;
        if (_localSports.contains(name)) {
          SnackHelper.error(context, 'Esporte já existe.');
          return;
        }
        if (name.length > 50) {
          SnackHelper.error(context, 'Nome muito longo (máx 50 caracteres).');
          return;
        }
        setState(() {
          _localSports.add(name);
          _addController.clear();
        });
      }

      void _removeSport(String sport) {
        setState(() => _localSports.remove(sport));
      }

      void _reorder(int oldIndex, int newIndex) {
        setState(() {
          if (newIndex > oldIndex) newIndex--;
          final item = _localSports.removeAt(oldIndex);
          _localSports.insert(newIndex, item);
        });
      }

      Future<void> _save() async {
        setState(() => _isSaving = true);
        try {
          await context.read<SportConfigCubit>().saveSports(_localSports);
          if (mounted) SnackHelper.success(context, 'Esportes salvos.');
        } catch (_) {
          if (mounted) SnackHelper.error(context, 'Erro ao salvar esportes. Tente novamente.');
        } finally {
          if (mounted) setState(() => _isSaving = false);
        }
      }

      @override
      Widget build(BuildContext context) {
        return BlocBuilder<SportConfigCubit, SportConfigState>(
          builder: (context, state) {
            return switch (state) {
              SportConfigInitial() => const Padding(
                  padding: EdgeInsets.symmetric(vertical: AppSpacing.md),
                  child: Center(child: CircularProgressIndicator()),
                ),
              SportConfigError(:final message) => Center(
                  child: Text(message, style: const TextStyle(color: Colors.red)),
                ),
              SportConfigLoaded(:final sports) => () {
                  _syncFromState(sports);
                  return Column(
                    crossAxisAlignment: CrossAxisAlignment.stretch,
                    children: [
                      if (_localSports.isEmpty)
                        const Padding(
                          padding: EdgeInsets.symmetric(vertical: AppSpacing.md),
                          child: Text(
                            'Nenhum esporte cadastrado. Adicione um acima.',
                            textAlign: TextAlign.center,
                          ),
                        )
                      else
                        ReorderableListView(
                          shrinkWrap: true,
                          physics: const NeverScrollableScrollPhysics(),
                          onReorder: _reorder,
                          children: [
                            for (final sport in _localSports)
                              ListTile(
                                key: ValueKey('sport_$sport'),
                                title: Text(sport),
                                trailing: Row(
                                  mainAxisSize: MainAxisSize.min,
                                  children: [
                                    IconButton(
                                      icon: const Icon(Icons.delete_outline),
                                      color: Colors.red,
                                      tooltip: 'Remover esporte',
                                      onPressed: () => _removeSport(sport),
                                    ),
                                    const Icon(Icons.drag_handle),
                                  ],
                                ),
                              ),
                          ],
                        ),
                      const SizedBox(height: AppSpacing.sm),
                      Row(
                        children: [
                          Expanded(
                            child: TextField(
                              controller: _addController,
                              maxLength: 50,
                              decoration: const InputDecoration(
                                hintText: 'Nome do esporte',
                                border: OutlineInputBorder(),
                                counterText: '',
                              ),
                              onSubmitted: (_) => _addSport(),
                            ),
                          ),
                          const SizedBox(width: AppSpacing.sm),
                          IconButton(
                            icon: const Icon(Icons.add),
                            color: AppTheme.primaryGreen,
                            tooltip: 'Adicionar esporte',
                            onPressed: _addSport,
                          ),
                        ],
                      ),
                      const SizedBox(height: AppSpacing.md),
                      FilledButton(
                        onPressed: _isSaving ? null : _save,
                        style: FilledButton.styleFrom(
                          backgroundColor: AppTheme.primaryGreen,
                        ),
                        child: _isSaving
                            ? const SizedBox(
                                height: 20,
                                width: 20,
                                child: CircularProgressIndicator(
                                  strokeWidth: 2,
                                  color: Colors.white,
                                ),
                              )
                            : const Text('Salvar Esportes'),
                      ),
                    ],
                  );
                }(),
            };
          },
        );
      }
    }
    ```

    Notas obrigatórias:
    - `key: ValueKey('sport_$sport')` em cada item do `ReorderableListView` (Pitfall 3).
    - `maxLength: 50` + validação manual `name.length > 50` no addSport (V5 Input Validation).
    - Checar duplicata antes de adicionar (`_localSports.contains(name)`).
    - `_initialized` flag evita resetar lista local toda vez que o stream emite (admin pode editar localmente antes de salvar — não queremos perder edits quando o stream se reconecta).
    - Quando admin salva, `saveSports` faz `set` sobrescrevendo o doc (sem merge) — a lista local é a fonte de verdade após save.
    - `SnackHelper.success/error` para feedback (padrão do projeto).
    - Strings exatas conforme UI-SPEC §Copywriting Contract: `'Esportes'`, `'Salvar Esportes'`, `'Nome do esporte'`, `'Nenhum esporte cadastrado. Adicione um acima.'`, `'Esportes salvos.'`, `'Erro ao salvar esportes. Tente novamente.'`, `'Esporte já existe.'`, `'Nome muito longo (máx 50 caracteres).'`, `'Remover esporte'`, `'Adicionar esporte'`.
    - `counterText: ''` no TextField para esconder contador de chars (visual mais limpo).
    - NÃO mostrar diálogo de confirmação para remover (UI-SPEC: "No confirmation dialog — immediate removal").
    - NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/settings_tab.dart</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import 'package:vida_ativa/features/admin/cubit/sport_config_cubit.dart';`
    - File contains `import 'package:vida_ativa/features/admin/cubit/sport_config_state.dart';`
    - File contains `class _SportsSection extends StatefulWidget`
    - File contains `BlocBuilder<SportConfigCubit, SportConfigState>`
    - File contains `ReorderableListView`
    - File contains `key: ValueKey('sport_$sport')`
    - File contains `IconButton` with `tooltip: 'Remover esporte'`
    - File contains `IconButton` with `tooltip: 'Adicionar esporte'`
    - File contains exact string `'Salvar Esportes'`
    - File contains exact string `'Nome do esporte'`
    - File contains exact string `'Nenhum esporte cadastrado. Adicione um acima.'`
    - File contains `maxLength: 50`
    - File contains `context.read<SportConfigCubit>().saveSports(`
    - File contains `Text('Esportes', style: textTheme.titleMedium)`
    - `flutter analyze lib/features/admin/ui/settings_tab.dart` exits 0
  </acceptance_criteria>
  <done>SettingsTab renderiza nova seção Esportes após Mercado Pago; admin pode add/remove/reorder localmente; clicar Salvar chama saveSports e mostra SnackHelper.success; lista vazia mostra empty state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Renderizar chip de esporte em AdminBookingCard</name>
  <files>lib/features/admin/ui/admin_booking_card.dart</files>
  <read_first>
    - lib/features/admin/ui/admin_booking_card.dart (arquivo a modificar — estrutura atual com row de participantes linha 152-170 e row de horário linha 171-194)
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md D-09, D-10, D-11
    - .planning/phases/20-infraestrutura-de-esporte/20-UI-SPEC.md §Color "Sport chip color palette", §Component Inventory `New: SportChip widget`, `Modified: AdminBookingCard`
  </read_first>
  <action>
    Adicionar um chip colorido renderizando `booking.sport` quando não-null. Inserir como helper privado dentro do mesmo arquivo (sem widget separado novo — segue o padrão do `_statusColor`/`_statusLabel` no mesmo arquivo).

    Mudanças concretas em `lib/features/admin/ui/admin_booking_card.dart`:

    1. **Adicionar helpers privados** no escopo da classe `AdminBookingCard` (ao lado de `_statusColor` linha 18-29 atual). Inserir antes de `String _statusLabel`:

    ```dart
    // Paleta determinística para chip de esporte (UI-SPEC §Color)
    static const List<Color> _sportBgColors = [
      Color(0xFFE3F2FD), // blue bg
      Color(0xFFE8F5E9), // green bg
      Color(0xFFFFF3E0), // orange bg
      Color(0xFFF3E5F5), // purple bg
      Color(0xFFFCE4EC), // pink bg
      Color(0xFFE0F7FA), // teal bg
      Color(0xFFF9FBE7), // lime bg
      Color(0xFFFFF8E1), // amber bg
    ];

    static const List<Color> _sportFgColors = [
      Color(0xFF1565C0),
      Color(0xFF2E7D32),
      Color(0xFFE65100),
      Color(0xFF6A1B9A),
      Color(0xFFC62828),
      Color(0xFF00695C),
      Color(0xFF558B2F),
      Color(0xFFF57F17),
    ];

    int _sportColorIndex(String sport) =>
        sport.hashCode.abs() % _sportBgColors.length;

    Widget _buildSportChip(String sport) {
      final i = _sportColorIndex(sport);
      return Container(
        padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
        decoration: BoxDecoration(
          color: _sportBgColors[i],
          borderRadius: BorderRadius.circular(12),
        ),
        child: Text(
          sport,
          style: TextStyle(
            color: _sportFgColors[i],
            fontSize: 14,
            fontWeight: FontWeight.w600,
          ),
        ),
      );
    }
    ```

    2. **Renderizar o chip condicionalmente** no método `build`, **entre** o bloco de participantes (linha 152-170 atual `if (booking.participants != null && booking.participants!.isNotEmpty) ...[`) e o bloco de startTime (linha 171 atual `if (booking.startTime != null) ...[`). Inserir:

    ```dart
    if (booking.sport != null) ...[
      const SizedBox(height: 4),
      _buildSportChip(booking.sport!),
    ],
    ```

    Notas obrigatórias:
    - Paleta de 8 cores conforme UI-SPEC §Color "Sport chip color palette" (índices 0-7 fixos).
    - Algoritmo `sport.hashCode.abs() % 8` — `.abs()` é OBRIGATÓRIO porque `hashCode` em Dart pode ser negativo, e `negativo % positivo` no Dart retorna sempre positivo, mas usar `.abs()` deixa explícito e defensivo (RESEARCH §Pattern 5).
    - Padding `EdgeInsets.symmetric(horizontal: 6, vertical: 2)` (UI-SPEC).
    - Border radius 12 (UI-SPEC: "matches status chip").
    - Fonte 14 / w600 (UI-SPEC §Typography).
    - Renderizar **apenas quando** `booking.sport != null` (D-11).
    - SizedBox(height: 4) antes — matches gap das outras rows existentes.

    NÃO usar Material `Chip` widget — UI-SPEC explicita "stateless `Container` with `BoxDecoration` — same pattern as status chip".
    NÃO criar widget separado `SportChip` em arquivo novo — UI-SPEC sugere mas para consistência com `_statusColor`/`_statusLabel` no mesmo arquivo, manter local. (Plano 03 escolhe local; se houver duplicação com Plano sheet, refatorar em fase futura.)
    NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/admin_booking_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - File contains `_sportBgColors` (lista de 8 cores de background)
    - File contains `_sportFgColors` (lista de 8 cores de foreground)
    - File contains `sport.hashCode.abs() % _sportBgColors.length`
    - File contains `Widget _buildSportChip(String sport)`
    - File contains `if (booking.sport != null) ...[`
    - File contains `_buildSportChip(booking.sport!)`
    - File contains `padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2)`
    - File contains `BorderRadius.circular(12)` (dentro do _buildSportChip)
    - File contains `fontSize: 14` and `fontWeight: FontWeight.w600` em conjunto no chip
    - `flutter analyze lib/features/admin/ui/admin_booking_card.dart` exits 0
  </acceptance_criteria>
  <done>AdminBookingCard renderiza chip colorido apenas quando booking.sport não-null; cor determinada por hash da string (mesma string sempre = mesma cor); reservas antigas sem sport não exibem chip (D-11).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Renderizar info-row de esporte em AdminBookingDetailSheet</name>
  <files>lib/features/admin/ui/admin_booking_detail_sheet.dart</files>
  <read_first>
    - lib/features/admin/ui/admin_booking_detail_sheet.dart (arquivo a modificar — info rows linha 242-253 atual, segue padrão `_infoRow(icon, text)`)
    - .planning/phases/20-infraestrutura-de-esporte/20-CONTEXT.md D-09, D-11
    - .planning/phases/20-infraestrutura-de-esporte/20-UI-SPEC.md §Component Inventory `Modified: AdminBookingDetailSheet`
  </read_first>
  <action>
    No `AdminBookingDetailSheet`, exibir o esporte como uma linha de info usando o helper `_infoRow` existente (linha 56-71 atual), com ícone `Icons.sports`. Renderizar condicionalmente (D-11).

    Mudanças concretas em `lib/features/admin/ui/admin_booking_detail_sheet.dart`:

    1. **Renderizar info-row condicionalmente** dentro do método `build` no `_AdminBookingDetailSheetState`. Inserir **logo após** o bloco de participantes (linha 249-253 atual `if (booking.participants != null && booking.participants!.isNotEmpty) ...[ ... _infoRow(Icons.group, booking.participants!), ]`). Adicionar:

    ```dart
    if (booking.sport != null) ...[
      const SizedBox(height: 10),
      _infoRow(Icons.sports, booking.sport!),
    ],
    ```

    Notas obrigatórias:
    - Ícone `Icons.sports` (Material Icons built-in — UI-SPEC §Component Inventory `Modified: AdminBookingDetailSheet`).
    - Reusar o helper `_infoRow(IconData, String)` existente — sem criar widget novo.
    - SizedBox(height: 10) antes — matches o padding entre as outras info rows (consistente com linha 243, 245, 247, 248 atual).
    - Renderizar **apenas quando** `booking.sport != null`.

    NÃO renderizar chip aqui — D-09 menciona "chip" para `AdminBookingCard`, mas para o `DetailSheet` o padrão é info-rows verticais. UI-SPEC confirma: detail sheet usa `_infoRow(Icons.sports, booking.sport!)`.
    NÃO criar testes.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/admin_booking_detail_sheet.dart</automated>
  </verify>
  <acceptance_criteria>
    - File contains `if (booking.sport != null) ...[`
    - File contains `_infoRow(Icons.sports, booking.sport!)`
    - File contains `const SizedBox(height: 10)` adjacent to the new sport row
    - `flutter analyze lib/features/admin/ui/admin_booking_detail_sheet.dart` exits 0
  </acceptance_criteria>
  <done>Detail sheet exibe linha "Esporte" com ícone Icons.sports quando booking.sport != null; reservas sem sport não exibem a linha; layout consistente com outras info-rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin (Flutter) → Firestore /config/sports (write) | Apenas admin pode chamar saveSports; UI exposta apenas no SettingsTab que está atrás de role-based guard |
| Admin input → SportConfigCubit.saveSports | Strings de esporte com max 50 chars; sem caracteres especiais perigosos pois exibidos como text plain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering | Admin escreve string muito longa ou maliciosa em sport | mitigate | `maxLength: 50` no TextField; validação manual `name.length > 50` no _addSport com mensagem de erro; Text rendering sem eval (Flutter Text é seguro contra XSS). |
| T-20-10 | Tampering | Duplicatas na lista de esportes | mitigate | Checagem `_localSports.contains(name)` antes de adicionar; SnackHelper.error informa usuário. |
| T-20-11 | DoS | Lista com 10000 esportes destrói UI | mitigate | Cada add é manual (admin não automatiza); na prática lista terá <20 itens; aceitar limite natural; sem hard cap necessário. |
| T-20-12 | Information Disclosure | Chip de esporte em cards admin vaza dado de cliente | accept | Sport é dado público da reserva (não PII); chip visível apenas no painel admin (já restrito por role). |
| T-20-13 | Repudiation | Admin remove esporte que está em uso por reservas existentes | mitigate | Reservas mantêm o campo `sport` como string própria (não FK), então remover do /config não invalida nem corrompe reservas — elas continuam exibindo o chip com o nome antigo. Comportamento documentado no SUMMARY. |
</threat_model>

<verification>
- `flutter analyze` em todos os arquivos modificados → 0 erros, 0 warnings novos.
- Manual smoke (executor reporta no SUMMARY):
  1. Abrir Admin > Ajustes; ver seção "Esportes" com lista Vôlei, Beach Tênis, Futevôlei (defaults do Plano 01).
  2. Adicionar "Tênis", arrastar para o topo, remover "Futevôlei", clicar Salvar Esportes → SnackHelper.success "Esportes salvos.", Firestore Console reflete nova lista.
  3. Tentar adicionar esporte duplicado → SnackHelper.error "Esporte já existe."
  4. Tentar adicionar nome >50 chars → SnackHelper.error com mensagem de tamanho.
  5. Criar reserva com sport selecionado, abrir aba Reservas no admin: card mostra chip colorido com o nome do esporte; abrir bottomsheet do detalhe: linha "Esporte" com ícone Icons.sports.
  6. Reserva sem sport (anterior): card NÃO mostra chip; detail sheet NÃO mostra linha.
</verification>

<success_criteria>
- Admin gerencia lista de esportes (add/remove/reorder/save) na seção Esportes do SettingsTab (SPORT-02, D-05..D-08).
- Reordenar usa ReorderableListView com keys (Pitfall 3).
- AdminBookingCard mostra chip colorido com cor determinística por hash (D-09, D-10).
- AdminBookingDetailSheet mostra linha de esporte (D-09).
- Reservas sem sport não exibem chip nem linha (D-11, SPORT-04 backward compat preservada).
- `flutter analyze` limpo em todos os arquivos tocados.
</success_criteria>

<output>
Criar `.planning/phases/20-infraestrutura-de-esporte/20-03-SUMMARY.md` ao final, listando arquivos modificados, comportamento manual verificado, e qualquer ajuste de UI específico (ex.: se a paleta de 8 cores precisa ser ajustada visualmente após teste real no dispositivo).
</output>
