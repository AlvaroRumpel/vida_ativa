---
phase: 18
slug: webhook-confirmacao-tempo-real
status: draft
framework: Flutter
design_system_tool: Material 3 + AppTheme
created: 2026-04-08
---

# Phase 18 — UI Design Contract

> Visual and interaction contract for webhook + real-time confirmation phase.
> Generated by gsd-ui-researcher. Framework: Flutter, Material 3 design system.

---

## Design System

| Property | Value |
|----------|-------|
| Framework | Flutter + Material 3 |
| Design Tool | AppTheme (lib/core/theme/app_theme.dart) |
| Font | Nunito (Google Fonts) |
| Icon Library | Material Icons (built-in Flutter) |
| Brand Colors | primaryGreen (#7B5D0A), brandAmber (#D4A800), surface (#FDFAf5), error (#C62828) |

---

## Spacing Scale

Declared values (Flutter EdgeInsets, multiples of 4px):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4 | Icon gaps, tight spacing |
| sm | 8 | Compact element spacing, small gaps |
| md | 16 | Default element padding, card gaps |
| lg | 24 | Section padding, larger gaps |
| xl | 32 | Layout gaps, visual separation |
| 2xl | 48 | Major section breaks |

**Source:** Detected from existing code patterns in `pix_payment_screen.dart`, `booking_card.dart`. Exceptions: none — all phase UI follows 4-point scale.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 (regular) | 1.5 | Default text, instructions, card content |
| Label | 14px | 400 (regular) | 1.5 | Input labels, helper text, timestamps |
| Heading (AppBar) | 18px | 700 (bold) | 1.2 | Screen titles, section headings |
| Countdown (Timer) | 24px | 600 (semibold) | 1.2 | Prominent timer display on payment screen |
| Badge Text | 12px | 500 (medium) | 1.4 | Admin payment status badges |

**Font Family:** Nunito (from `google_fonts` package)

**Source:** AppTheme.lightTheme uses `GoogleFonts.nunitoTextTheme()`. Phase 18 adds countdown display at 24px for prominence.

---

## Color Contract

| Role | Value (Hex) | Semantics | Usage |
|------|-------------|-----------|-------|
| **Dominant (60%)** | #FDFAf5 | Surface background | Screen backgrounds, SafeArea fill |
| **Secondary (30%)** | #F5F1E8 | Card/container background | Card surfaces, bottom sheets |
| **Primary Brand** | #7B5D0A | Primary action, text | AppBar title, FilledButton, primary text |
| **Accent Brand** | #D4A800 | Secondary action | Secondary buttons, emphasis text |
| **Semantic: Success** | #4CAF50 | Confirmed/paid state | Badge "Pix pago", confirmed status icon |
| **Semantic: Warning** | #FFC107 | Awaiting payment | Badge "Aguardando Pix", pending state |
| **Semantic: Expired** | #9E9E9E | Expired/inactive | Badge "Expirada", greyed QR overlay |
| **Semantic: Info** | #2196F3 | On-arrival payment | Badge "Pagar na hora" |
| **Semantic: Error** | #C62828 | Error/destructive | Error text, loading spinner color, reject button |

**Accent reserved for:**
- FilledButton primary actions ("Gerar novo QR", "Confirmar pagamento manual")
- Primary text highlighting (active state indicators)
- Important interactive elements in PixPaymentScreen

**Source:** Extracted from `AppTheme` (primaryGreen, brandAmber, error) and Material 3 ColorScheme. Additional semantic colors follow Material 3 standards.

---

## Component Specifications

### PixPaymentScreen — Countdown Timer Addition

**Location:** `lib/features/booking/ui/pix_payment_screen.dart`

**Countdown Display:**
- **Format:** "MM:SS restantes" (minutes:seconds remaining), centered
- **Position:** Below QR code, above copy-paste code section
- **Font size:** 24px, weight 600 (semibold), color primaryGreen by default
- **Color override:** Change to error red (#C62828) when **< 2 minutes remaining**
- **Implementation:** `Timer.periodic(Duration(seconds: 1), ...)` updating `setState()`
- **Start/stop:** Initiated in `initState()`, cancelled in `dispose()` (CRITICAL for memory)
- **Update frequency:** Every 1 second tick

**"Gerar novo QR" Button (Regeneration):**
- **Triggered:** When countdown reaches 00:00
- **Button type:** `FilledButton.icon` with `Icons.refresh`
- **Button text:** "Gerar novo QR"
- **Button color:** primaryGreen background, white text
- **Button size:** Full width, 48px height
- **Spacing:** 16px gap above (replacing countdown when expired)
- **Behavior:** Calls `_generateQr()` → new `createPixPayment` CF call → new PaymentRecord → countdown restarts with fresh 30min window
- **State transitions:**
  1. Countdown visible + ticking → QR enabled
  2. Countdown = 00:00 → Countdown disappears, button appears, QR gets 0.5 opacity overlay
  3. User taps "Gerar novo QR" → Loading state, new timer starts

**QR Visual When Expired:**
- **Overlay effect:** Semi-transparent grey (Colors.grey.withOpacity(0.3)) over QR image
- **Text below QR:** "QR expirado. Gere um novo acima." (12px, error color, italic)

**Copy-paste Code Section:**
- **No change** to existing layout below countdown/button

---

### PixPaymentScreen — Real-Time Update Listener

**Location:** `lib/features/booking/ui/pix_payment_screen.dart`

**StreamSubscription Pattern:**
- **Source:** `FirebaseFirestore.instance.collection('bookings').doc(widget.bookingId).snapshots()`
- **Initialization:** `initState()` after QR generation
- **Listener:**
  - If booking.status changes to `confirmed`:
    - Cancel countdown timer immediately
    - Show SnackBar: "Pagamento confirmado! Reserva garantida." (2s duration)
    - Navigate: `context.go('/bookings')` (via go_router, not Navigator.pop)
  - If booking.status changes to `expired`:
    - Cancel countdown timer
    - Show SnackBar: "Pagamento expirado. Gere novo QR." (3s duration)
    - UI remains on screen (user can regenerate)
- **Cleanup:** Cancel subscription in `dispose()` (CRITICAL)
- **Guard:** Check `if (!mounted) return;` before setState/navigation

---

### AdminBookingCard — Payment Status Badges

**Location:** `lib/features/admin/ui/admin_booking_card.dart`

**Updated `_statusColor()` Method:**
Extend existing switch to handle `(status, paymentMethod)` tuple:
```dart
Color _statusColor(String status, String? paymentMethod) {
  return switch ((status, paymentMethod)) {
    ('pending', _) => Colors.orange,
    ('pending_payment', _) => Colors.amber,        // NEW
    ('confirmed', 'pix') => AppTheme.primaryGreen, // NEW
    ('confirmed', 'on_arrival') => Colors.blue,    // EXISTING
    ('confirmed', _) => AppTheme.primaryGreen,
    ('expired', _) => Colors.grey,                 // NEW
    ('rejected', _) => Colors.red,
    _ => Colors.grey,
  };
}
```

**Updated `_statusLabel()` Method:**
Extend switch to handle `(status, paymentMethod)` tuple:
```dart
String _statusLabel(String status, String? paymentMethod) {
  return switch ((status, paymentMethod)) {
    ('pending', _) => 'Aguardando',
    ('pending_payment', _) => 'Aguardando Pix',        // NEW
    ('confirmed', 'pix') => 'Pix pago',               // NEW
    ('confirmed', 'on_arrival') => 'Pagar na hora',   // EXISTING
    ('confirmed', _) => 'Confirmado',
    ('expired', _) => 'Expirada',                     // NEW
    ('rejected', _) => 'Recusado',
    _ => 'Cancelado',
  };
}
```

**Badge Styling:**
- **Font size:** 12px, weight 500 (medium)
- **Padding:** 8px horizontal, 4px vertical
- **Border radius:** 4px
- **Background colors:**
  - "Aguardando Pix" (amber): #FFC107
  - "Pix pago" (green): #4CAF50
  - "Expirada" (grey): #9E9E9E
  - "Pagar na hora" (blue): #2196F3
- **Text color:** White on dark backgrounds, dark text on light backgrounds (Material 3 contrast)

**Card Integration:**
- Badge appears in card header row, after booking date/time
- Uses existing Padding/Container pattern already in AdminBookingCard
- No layout restructuring

---

### AdminBookingDetailSheet — Manual Payment Confirmation Button

**Location:** `lib/features/admin/ui/admin_booking_detail_sheet.dart`

**Conditional Button:**
- **Visible when:** `booking.status == 'pending_payment'`
- **Label:** "Confirmar pagamento manual"
- **Type:** `FilledButton.icon` with `Icons.check_circle`
- **Color:** primaryGreen background, white text
- **Size:** Full width, 48px height
- **Position:** Above existing "Confirmar" / "Recusar" buttons (if they exist), or as primary action row

**Behavior on Tap:**
1. Show confirmation dialog: "Confirmar pagamento Pix?" / "Deseja marcar este pagamento como confirmado?"
2. If user confirms:
   - Call `adminBookingCubit.confirmBooking(booking.id)` (existing method)
   - Also update PaymentRecord status to 'paid' via Firestore transaction
   - Show loading spinner during update
   - Show snackbar on success: "Pagamento confirmado manualmente."
   - Close bottom sheet on success

**Loading State:**
- Disable button during API call
- Show CircularProgressIndicator inside button (or replace button text temporarily)
- Use `_isSubmitting` pattern already established in this widget

---

### Cloud Functions UI Contract (Backend → Frontend)

**`handlePixWebhook` Response:**
- **HTTP Status:** 202 Accepted (returned immediately)
- **Body:** `{ "success": true }` (minimal, no error details exposed)
- **Processing:** Happens asynchronously after 202 sent
- **Side effect:** Booking document updated in Firestore (triggers BookingCubit stream, which auto-updates PixPaymentScreen listener)

**`expireUnpaidBookings` Response:**
- **Type:** Scheduled function, no HTTP response
- **Side effect:** Marks booking status as 'expired', which triggers stream listeners in PixPaymentScreen and MyBookingsScreen

---

## Copywriting Contract

| Element | Copy | Context |
|---------|------|---------|
| **Countdown Label** | "{MM}:{SS} restantes" | Timer display above QR, updates every second |
| **Countdown Expired Label** | "QR expirado. Gere um novo acima." | Appears below greyed QR image when timer = 00:00 |
| **Regenerate Button** | "Gerar novo QR" | CTA button when countdown expires, triggers new CF call |
| **Payment Confirmed** (SnackBar) | "Pagamento confirmado! Reserva garantida." | Auto-shown when webhook updates booking to 'confirmed' |
| **Payment Expired** (SnackBar) | "Pagamento expirado. Gere novo QR." | Shown if user stays on screen after expiration |
| **Manual Confirm Button** | "Confirmar pagamento manual" | Admin detail sheet, visible only when `status == 'pending_payment'` |
| **Manual Confirm Dialog Title** | "Confirmar pagamento Pix?" | Confirmation before admin override |
| **Manual Confirm Dialog Body** | "Deseja marcar este pagamento como confirmado?" | Explanation of action |
| **Manual Confirm Success** | "Pagamento confirmado manualmente." | SnackBar after admin confirms |
| **Badge: Pending Payment** | "Aguardando Pix" | Admin card, amber background |
| **Badge: Paid (Pix)** | "Pix pago" | Admin card, green background |
| **Badge: Expired** | "Expirada" | Admin card, grey background |
| **Badge: On Arrival** | "Pagar na hora" | Admin card, blue background (existing) |

**Language:** Portuguese (pt-BR). Consistent with existing app copy.

---

## State Machines

### PixPaymentScreen Timer States

```
┌─────────────────────────────────────────────────┐
│ [QR LOADING]                                    │
│   _isLoading = true                             │
│   CF call in progress                           │
└───────────┬─────────────────────────────────────┘
            │ _generateQr() complete
            ▼
┌─────────────────────────────────────────────────┐
│ [COUNTDOWN ACTIVE]                              │
│   _remaining > Duration.zero                    │
│   Timer ticking down                            │
│   QR visible, countdown MM:SS restantes         │
│   Color: primaryGreen                           │
└───────────┬─────────────────────────────────────┘
            │ <2 min remaining
            │ (Color change to error red)
            │
            │ Countdown = 00:00
            ▼
┌─────────────────────────────────────────────────┐
│ [EXPIRED + REGENERATE AVAILABLE]                │
│   _remaining = Duration.zero                    │
│   Timer cancelled                               │
│   QR greyed (0.5 opacity) + "QR expirado"       │
│   Button "Gerar novo QR" visible                │
└───────────┬──────────────┬───────────────────────┘
            │              │
    Tap button    Webhook confirms
            │              │
            ▼              ▼
    [LOADING]        [CONFIRMED]
    (restart)    (auto-navigate)

┌─────────────────────────────────────────────────┐
│ [CONFIRMED]                                     │
│   Booking.status = 'confirmed'                  │
│   SnackBar: "Pagamento confirmado! ..."         │
│   Auto-navigate context.go('/bookings')         │
└─────────────────────────────────────────────────┘
```

---

## Interaction Patterns

### Countdown Update Loop
1. `initState()` → `_startCountdown()` initializes `Timer.periodic(Duration(seconds: 1), ...)`
2. Each tick: Calculate `_expiresAt.difference(DateTime.now())`
3. If negative: Cancel timer, set `_remaining = Duration.zero`, trigger UI rebuild
4. `dispose()` → Cancel timer (CRITICAL — prevents memory leak)

### Real-Time Payment Confirmation
1. PixPaymentScreen active, user waiting for payment
2. User pays via Mercado Pago app
3. Mercado Pago sends webhook to `handlePixWebhook` CF
4. CF verifies signature, updates booking to `status: 'confirmed'` in Firestore
5. BookingCubit stream listener detects change
6. PixPaymentScreen's `_bookingSubscription` listener fires
7. SnackBar shown: "Pagamento confirmado! Reserva garantida."
8. Navigation: `context.go('/bookings')` (go_router)
9. MyBookingsScreen displays, BookingCard shows "Confirmado" badge

### Admin Manual Fallback
1. Admin opens AdminBookingDetailSheet for `pending_payment` booking
2. Button "Confirmar pagamento manual" visible
3. Admin taps → Confirmation dialog
4. Admin confirms → `adminBookingCubit.confirmBooking()` called
5. PaymentRecord updated to `status: 'paid'` (atomic transaction)
6. Booking status → `'confirmed'`
7. AdminBookingCard badge updated to "Pix pago"

---

## Checklist for Phase 18 Execution

- [ ] **PixPaymentScreen countdown:**
  - Timer.periodic(Duration(seconds: 1)) in initState
  - Format: MM:SS restantes
  - Color: primaryGreen normal, error red < 2 min
  - Cancel in dispose()
  
- [ ] **QR regeneration button:**
  - Appears when countdown = 00:00
  - FilledButton.icon with Icons.refresh
  - Calls _generateQr() → new CF invocation
  - Disables during loading
  
- [ ] **PixPaymentScreen stream listener:**
  - Listen to /bookings/{bookingId} snapshots
  - Auto-navigate on status = 'confirmed'
  - SnackBar "Pagamento confirmado! Reserva garantida."
  - Cancel subscription in dispose()
  
- [ ] **AdminBookingCard badge updates:**
  - _statusColor() extended: (status, paymentMethod) tuple
  - _statusLabel() extended: new labels for pending_payment/expired/pix
  - Renders in existing card header row
  
- [ ] **AdminBookingDetailSheet manual button:**
  - Visible only when status = 'pending_payment'
  - Dialog confirmation before action
  - Updates PaymentRecord status to 'paid'
  - SnackBar on success
  
- [ ] **Cloud Functions (backend, not UI, but affects UI):**
  - handlePixWebhook: 202 response, signature verification, atomic update
  - expireUnpaidBookings: Scheduled every 15 min, marks bookings expired
  - (Covered in Phase 18 PLAN, not UI-SPEC, but listed for completeness)

---

## Design System Alignment

**Spacing:** All padding/margin values are multiples of 4px, consistent with Material 3 and existing app patterns.

**Typography:** Uses Nunito (existing) at declared sizes (16px body, 24px countdown, 12px badge), weights 400/500/600/700 only.

**Color:** Leverages AppTheme.primaryGreen, brandAmber, error, + Material 3 semantic colors (amber, green, grey, blue) for status badges. No custom color mixing.

**Material 3 Components:** FilledButton, OutlinedButton, Card, SnackBar, AlertDialog — all built-in Flutter Material 3 widgets. No external component library required.

**Patterns:** Reuses established patterns — Timer lifecycle (BookingCubit already uses streams), StatefulWidget lifecycle (dispose cleanup), Firestore listeners (existing pattern).

---

## No External Registry Required

This phase uses **only Material 3 built-in components** (Flutter defaults). No shadcn, Radix UI, or third-party component registry involvement. **Safety Gate:** not required.

---

## Pre-Populated From

| Source | Decisions Used |
|--------|----------------|
| CONTEXT.md | 8 decisions: countdown format, regeneration flow, webhook response, idempotency, auto-nav, expiration logic, badge labels, manual fallback |
| RESEARCH.md | Timer pattern, StreamSubscription pattern, HMAC verification, scheduled function pattern |
| app_theme.dart | Colors, fonts, Material 3 theme configuration |
| Existing code patterns | AdminBookingCard switch pattern, PixPaymentScreen structure, BookingCubit stream pattern |

---

## Status

**Draft:** Ready for gsd-ui-checker validation.

Approval: Pending

---

*UI-SPEC generated: 2026-04-08*  
*Phase: 18-webhook-confirmacao-tempo-real*  
*Framework: Flutter Material 3*
