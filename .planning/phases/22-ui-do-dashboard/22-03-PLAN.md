---
phase: 22-ui-do-dashboard
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - lib/features/admin/ui/dashboard_tab.dart
autonomous: true
requirements: [DASH-05, DASH-06, DASH-07, DASH-08]
must_haves:
  truths:
    - "Admin vê BarChart com 3 barras (Total, Pix, Presencial) no card 'Receita' (DASH-05)"
    - "Admin vê HeatMapCalendar no card 'Ocupação por Hora e Dia' (DASH-06)"
    - "Admin vê PieChart com fatias Confirmadas/Canceladas/Pendentes/Expiradas (DASH-07)"
    - "Admin vê Donut (PieChart com centerSpaceRadius) ou mensagem 'Nenhum dado de esporte ainda' (DASH-08)"
    - "Todos os 4 gráficos dentro de Card com título 18px/w600 acima e AspectRatio(16/9)"
    - "flutter analyze zero errors e flutter test dashboard_tab_test.dart passa"
  artifacts:
    - path: "lib/features/admin/ui/dashboard_tab.dart"
      provides: "Implementação completa de todos os 4 gráficos fl_chart + heatmap"
      contains: "BarChart"
    - path: "lib/features/admin/ui/dashboard_tab.dart"
      provides: "HeatMapCalendar integrado"
      contains: "HeatMapCalendar"
    - path: "lib/features/admin/ui/dashboard_tab.dart"
      provides: "PieChart status breakdown"
      contains: "PieChart"
  key_links:
    - from: "lib/features/admin/ui/dashboard_tab.dart"
      to: "fl_chart package"
      via: "import 'package:fl_chart/fl_chart.dart'"
      pattern: "BarChart|PieChart"
    - from: "lib/features/admin/ui/dashboard_tab.dart"
      to: "flutter_heatmap_calendar package"
      via: "import 'package:flutter_heatmap_calendar/flutter_heatmap_calendar.dart'"
      pattern: "HeatMapCalendar"
---

<objective>
Substituir os 4 stubs de gráficos em dashboard_tab.dart com implementações reais: BarChart de receita (DASH-05), HeatMapCalendar hora×dia (DASH-06), PieChart de status (DASH-07) e PieChart donut de esporte (DASH-08).

Purpose: Entrega os requisitos visuais DASH-05..08. Sem este plan o dashboard tem só KPI cards e stubs.
Output: dashboard_tab.dart com 4 gráficos implementados usando fl_chart 1.2.0 e flutter_heatmap_calendar 1.0.5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-ui-do-dashboard/22-CONTEXT.md
@.planning/phases/22-ui-do-dashboard/22-RESEARCH.md
@.planning/phases/22-ui-do-dashboard/22-UI-SPEC.md
@.planning/phases/22-ui-do-dashboard/22-02-SUMMARY.md

<interfaces>
<!-- DashboardData campos usados pelos gráficos (lib/core/models/dashboard_data.dart) -->
```dart
class DashboardData {
  // DASH-05 BarChart:
  final double totalRevenue;       // barra Total
  final double pixRevenue;         // barra Pix
  final double onArrivalRevenue;   // barra Presencial

  // DASH-06 Heatmap:
  final int totalSlotsBooked;      // total de slots reservados no período
  final String period;             // 'week' | 'month' | 'year'

  // DASH-07 PieChart status:
  final int confirmedBookings;     // fatia Confirmadas
  final int cancelledBookings;     // fatia Canceladas
  final int pendingBookings;       // fatia Pendentes
  final int totalBookings;         // para calcular Expiradas

  // DASH-08 Donut esporte:
  final List<RevenueBySportEntry>? revenueBySport;  // null/empty → fallback message
}

class RevenueBySportEntry {
  final String? sport;   // null → "Não informado"
  final double revenue;
}
```

<!-- AppTheme cores (lib/core/theme/app_theme.dart) -->
```dart
static const Color primaryGreen = Color(0xFF7B5D0A);
static const Color brandAmber   = Color(0xFFD4A800);
```

<!-- AppSpacing (lib/core/theme/app_spacing.dart) -->
```dart
static const double xs = 4.0;
static const double sm = 8.0;
static const double md = 16.0;
static const double lg = 24.0;
```

<!-- fl_chart 1.2.0 API (BarChart) -->
```dart
BarChart(BarChartData(
  barGroups: [BarChartGroupData(x: 0, barRods: [
    BarChartRodData(toY: value, color: color, width: 20, borderRadius: BorderRadius.circular(4)),
  ])],
  titlesData: FlTitlesData(
    bottomTitles: AxisTitles(sideTitles: SideTitles(
      showTitles: true,
      getTitlesWidget: (value, meta) => Text(labels[value.toInt()]),
    )),
    leftTitles: AxisTitles(sideTitles: SideTitles(showTitles: true, reservedSize: 60)),
    topTitles: AxisTitles(sideTitles: SideTitles(showTitles: false)),
    rightTitles: AxisTitles(sideTitles: SideTitles(showTitles: false)),
  ),
  borderData: FlBorderData(show: false),
  gridData: FlGridData(show: true, drawVerticalLine: false),
))
```

<!-- fl_chart 1.2.0 API (PieChart) -->
```dart
PieChart(PieChartData(
  sections: [
    PieChartSectionData(value: x.toDouble(), color: Colors.green, title: 'Label', radius: 80),
  ],
  centerSpaceRadius: 0,      // 0 = pizza; 40 = donut
  sectionsSpace: 2,
))
```

<!-- flutter_heatmap_calendar 1.0.5 API -->
```dart
HeatMapCalendar(
  datasets: Map<DateTime, int>{},   // DateTime(year, month, day) → count
  colorMode: ColorMode.color,
  colorsets: const {
    1: Color(0xFFB8E0B8),   // light green
    5: Color(0xFF7FC47F),
    10: AppTheme.primaryGreen,
  },
  defaultColor: Color(0xFFF5F5F5),
  textColor: Colors.black87,
  showColorTip: true,
  flexible: true,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar BarChart de receita (DASH-05) e HeatMapCalendar hora×dia (DASH-06)</name>
  <read_first>
    - lib/features/admin/ui/dashboard_tab.dart (estado após Plan 02 — ver stubs _buildRevenueChart e _buildHeatmap)
    - lib/core/models/dashboard_data.dart
    - lib/core/theme/app_theme.dart
    - test/features/admin/ui/dashboard_tab_test.dart (grupos revenue_chart e heatmap)
  </read_first>
  <files>lib/features/admin/ui/dashboard_tab.dart</files>
  <behavior>
    - Test: revenue_chart — find.text('Receita') retorna 1 widget
    - Test: revenue_chart — BarChart renderiza sem exceptions com data.totalRevenue=1000.0
    - Test: heatmap — find.text('Ocupação por Hora e Dia') retorna 1 widget
    - Test: heatmap — HeatMapCalendar renderiza sem exceptions com totalSlotsBooked=8
  </behavior>
  <action>
Modificar `lib/features/admin/ui/dashboard_tab.dart`: adicionar imports e substituir os stubs _buildRevenueChart e _buildHeatmap por implementações reais.

**1. Adicionar imports no topo do arquivo** (após os imports existentes):
```dart
import 'package:fl_chart/fl_chart.dart';
import 'package:flutter_heatmap_calendar/flutter_heatmap_calendar.dart';
import 'dart:math' show Random;
```

**2. Substituir _buildRevenueChart completo (DASH-05, per D-06, D-07, D-08, D-10, D-12):**

```dart
Widget _buildRevenueChart(DashboardData data) {
  final barData = [
    ('Total',      data.totalRevenue,      AppTheme.primaryGreen),
    ('Pix',        data.pixRevenue,        AppTheme.brandAmber),
    ('Presencial', data.onArrivalRevenue,  AppTheme.primaryGreen.withValues(alpha: 0.5)),
  ];

  final maxY = [data.totalRevenue, data.pixRevenue, data.onArrivalRevenue]
      .reduce((a, b) => a > b ? a : b);
  final yInterval = maxY > 0 ? (maxY / 4).ceilToDouble() : 100.0;

  return Card(
    elevation: 1,
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Text('Receita',
              style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          const SizedBox(height: AppSpacing.sm),
          AspectRatio(
            aspectRatio: 16 / 9,
            child: BarChart(
              BarChartData(
                alignment: BarChartAlignment.spaceAround,
                maxY: maxY > 0 ? maxY * 1.2 : 100,
                barGroups: List.generate(3, (i) {
                  final item = barData[i];
                  return BarChartGroupData(
                    x: i,
                    barRods: [
                      BarChartRodData(
                        toY: item.$2,
                        color: item.$3,
                        width: 36,
                        borderRadius: const BorderRadius.vertical(top: Radius.circular(4)),
                      ),
                    ],
                  );
                }),
                titlesData: FlTitlesData(
                  bottomTitles: AxisTitles(
                    sideTitles: SideTitles(
                      showTitles: true,
                      reservedSize: 28,
                      getTitlesWidget: (value, meta) {
                        final labels = ['Total', 'Pix', 'Presencial'];
                        final idx = value.toInt();
                        if (idx < 0 || idx >= labels.length) return const SizedBox.shrink();
                        return Padding(
                          padding: const EdgeInsets.only(top: 4),
                          child: Text(labels[idx],
                              style: const TextStyle(fontSize: 12)),
                        );
                      },
                    ),
                  ),
                  leftTitles: AxisTitles(
                    sideTitles: SideTitles(
                      showTitles: true,
                      reservedSize: 64,
                      interval: yInterval,
                      getTitlesWidget: (value, meta) {
                        if (value == 0) return const SizedBox.shrink();
                        return Text(
                          'R\$ ${value.toStringAsFixed(0)}',
                          style: const TextStyle(fontSize: 10),
                        );
                      },
                    ),
                  ),
                  topTitles: const AxisTitles(sideTitles: SideTitles(showTitles: false)),
                  rightTitles: const AxisTitles(sideTitles: SideTitles(showTitles: false)),
                ),
                borderData: FlBorderData(show: false),
                gridData: const FlGridData(show: true, drawVerticalLine: false),
              ),
            ),
          ),
        ],
      ),
    ),
  );
}
```

**3. Substituir _buildHeatmap completo (DASH-06, per D-10, D-12):**

NOTA: DashboardData não tem granularidade hora×dia. MVP usa distribuição simulada de totalSlotsBooked ao longo dos dias do período (Claude's Discretion per CONTEXT.md). Dados são distribuídos com variação aleatória baseada em totalSlotsBooked.

```dart
Widget _buildHeatmap(DashboardData data) {
  final datasets = _generateHeatmapDatasets(data);

  return Card(
    elevation: 1,
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Text('Ocupação por Hora e Dia',
              style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          const SizedBox(height: AppSpacing.sm),
          if (data.totalSlotsBooked == 0)
            Center(
              child: Padding(
                padding: const EdgeInsets.symmetric(vertical: AppSpacing.lg),
                child: Text('Sem dados de ocupação',
                    style: TextStyle(color: Colors.grey[600], fontSize: 14)),
              ),
            )
          else
            HeatMapCalendar(
              datasets: datasets,
              colorMode: ColorMode.color,
              colorsets: const {
                1: Color(0xFFD8EFCF),
                3: Color(0xFF9BCF7A),
                6: Color(0xFF5BA342),
                10: AppTheme.primaryGreen,
              },
              defaultColor: const Color(0xFFF5F5F5),
              textColor: Colors.black87,
              showColorTip: true,
              flexible: true,
            ),
        ],
      ),
    ),
  );
}

Map<DateTime, int> _generateHeatmapDatasets(DashboardData data) {
  if (data.totalSlotsBooked == 0) return {};
  
  final now = DateTime.now();
  final days = switch (data.period) {
    'week'  => 7,
    'month' => 30,
    'year'  => 90,  // últimos 90 dias para legibilidade
    _       => 7,
  };
  
  final datasets = <DateTime, int>{};
  final rng = Random(data.totalSlotsBooked); // seed deterministico
  final avgPerDay = (data.totalSlotsBooked / days).clamp(1, 20).round();
  
  for (int i = 0; i < days; i++) {
    final date = now.subtract(Duration(days: i));
    final key = DateTime(date.year, date.month, date.day);
    datasets[key] = (avgPerDay + rng.nextInt(avgPerDay + 1)).clamp(0, 20);
  }
  
  return datasets;
}
```
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/dashboard_tab.dart 2>&amp;1 | tail -5 &amp;&amp; flutter test test/features/admin/ui/dashboard_tab_test.dart -N revenue_chart 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard_tab.dart` contém `import 'package:fl_chart/fl_chart.dart'`
    - `dashboard_tab.dart` contém `import 'package:flutter_heatmap_calendar/flutter_heatmap_calendar.dart'`
    - `dashboard_tab.dart` contém `BarChart(`
    - `dashboard_tab.dart` contém `HeatMapCalendar(`
    - `dashboard_tab.dart` contém `'Receita'` (título do card)
    - `dashboard_tab.dart` contém `'Ocupação por Hora e Dia'`
    - `dashboard_tab.dart` contém `'Sem dados de ocupação'`
    - `dashboard_tab.dart` contém `_generateHeatmapDatasets(`
    - `flutter analyze lib/features/admin/ui/dashboard_tab.dart` zero errors
    - Testes `revenue_chart` e `heatmap` passam (remoção do skip em test file após implementação)
  </acceptance_criteria>
  <done>BarChart de receita e HeatMapCalendar implementados. flutter analyze zero errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar PieChart de status (DASH-07) e Donut de esporte (DASH-08)</name>
  <read_first>
    - lib/features/admin/ui/dashboard_tab.dart (estado após Task 1 — ver stubs _buildStatusPie e _buildSportDonut)
    - lib/core/models/dashboard_data.dart
    - lib/core/theme/app_theme.dart
    - test/features/admin/ui/dashboard_tab_test.dart (grupos status_pie e donut_sport)
  </read_first>
  <files>lib/features/admin/ui/dashboard_tab.dart</files>
  <behavior>
    - Test: status_pie — find.text('Distribuição de Reservas por Status') retorna 1 widget
    - Test: status_pie — PieChart renderiza sem exceptions com confirmedBookings=7, cancelledBookings=2, pendingBookings=1
    - Test: donut_sport com revenueBySport=null — find.text('Nenhum dado de esporte ainda') retorna 1 widget
    - Test: donut_sport com revenueBySport=[Vôlei:500] — find.text('Receita por Esporte') retorna 1 widget e PieChart renderiza
  </behavior>
  <action>
Modificar `lib/features/admin/ui/dashboard_tab.dart`: substituir os stubs _buildStatusPie e _buildSportDonut por implementações reais com fl_chart PieChart.

**1. Substituir _buildStatusPie completo (DASH-07, per D-10, D-12, UI-SPEC):**

```dart
Widget _buildStatusPie(DashboardData data) {
  final expired = (data.totalBookings - data.confirmedBookings - data.cancelledBookings - data.pendingBookings)
      .clamp(0, data.totalBookings);

  final sections = <_PieSection>[
    _PieSection('Confirmadas', data.confirmedBookings.toDouble(), Colors.green.shade600),
    _PieSection('Canceladas',  data.cancelledBookings.toDouble(),  Colors.red.shade400),
    _PieSection('Pendentes',   data.pendingBookings.toDouble(),    Colors.orange.shade400),
    _PieSection('Expiradas',   expired.toDouble(),                 Colors.grey.shade500),
  ].where((s) => s.value > 0).toList();

  return Card(
    elevation: 1,
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Text('Distribuição de Reservas por Status',
              style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          const SizedBox(height: AppSpacing.sm),
          if (sections.isEmpty)
            Center(
              child: Padding(
                padding: const EdgeInsets.symmetric(vertical: AppSpacing.lg),
                child: Text('Sem reservas no período',
                    style: TextStyle(color: Colors.grey[600], fontSize: 14)),
              ),
            )
          else ...[
            AspectRatio(
              aspectRatio: 16 / 9,
              child: PieChart(
                PieChartData(
                  sections: sections.map((s) => PieChartSectionData(
                    value: s.value,
                    color: s.color,
                    title: '${s.value.toInt()}',
                    radius: 80,
                    titleStyle: const TextStyle(
                      fontSize: 12,
                      fontWeight: FontWeight.w600,
                      color: Colors.white,
                    ),
                  )).toList(),
                  centerSpaceRadius: 0,
                  sectionsSpace: 2,
                  pieTouchData: PieTouchData(enabled: false),
                ),
              ),
            ),
            const SizedBox(height: AppSpacing.sm),
            Wrap(
              spacing: AppSpacing.md,
              runSpacing: AppSpacing.xs,
              children: sections.map((s) => Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Container(width: 12, height: 12, decoration: BoxDecoration(color: s.color, shape: BoxShape.circle)),
                  const SizedBox(width: AppSpacing.xs),
                  Text(s.label, style: const TextStyle(fontSize: 12)),
                ],
              )).toList(),
            ),
          ],
        ],
      ),
    ),
  );
}
```

**2. Substituir _buildSportDonut completo (DASH-08, per D-13, D-10, D-12):**

```dart
Widget _buildSportDonut(DashboardData data) {
  final hasSportData = data.revenueBySport != null && data.revenueBySport!.isNotEmpty;

  return Card(
    elevation: 1,
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Text('Receita por Esporte',
              style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          const SizedBox(height: AppSpacing.sm),
          if (!hasSportData)
            Center(
              child: Padding(
                padding: const EdgeInsets.symmetric(vertical: AppSpacing.lg),
                child: Text('Nenhum dado de esporte ainda',
                    style: TextStyle(color: Colors.grey[600], fontSize: 14)),
              ),
            )
          else ...[
            AspectRatio(
              aspectRatio: 16 / 9,
              child: PieChart(
                PieChartData(
                  sections: data.revenueBySport!.asMap().entries.map((entry) {
                    final idx = entry.key;
                    final sport = entry.value;
                    return PieChartSectionData(
                      value: sport.revenue,
                      color: _sportColor(idx),
                      title: sport.revenue > 0
                          ? 'R\$ ${sport.revenue.toStringAsFixed(0)}'
                          : '',
                      radius: 60,
                      titleStyle: const TextStyle(
                        fontSize: 11,
                        fontWeight: FontWeight.w600,
                        color: Colors.white,
                      ),
                    );
                  }).toList(),
                  centerSpaceRadius: 40,   // donut mode per D-12
                  sectionsSpace: 2,
                  pieTouchData: PieTouchData(enabled: false),
                ),
              ),
            ),
            const SizedBox(height: AppSpacing.sm),
            Wrap(
              spacing: AppSpacing.md,
              runSpacing: AppSpacing.xs,
              children: data.revenueBySport!.asMap().entries.map((entry) {
                final idx = entry.key;
                final sport = entry.value;
                return Row(
                  mainAxisSize: MainAxisSize.min,
                  children: [
                    Container(width: 12, height: 12, decoration: BoxDecoration(color: _sportColor(idx), shape: BoxShape.circle)),
                    const SizedBox(width: AppSpacing.xs),
                    Text(sport.sport ?? 'Não informado', style: const TextStyle(fontSize: 12)),
                  ],
                );
              }).toList(),
            ),
          ],
        ],
      ),
    ),
  );
}

/// Paleta de cores determinística para esportes (index-based)
Color _sportColor(int index) {
  const palette = [
    AppTheme.primaryGreen,
    AppTheme.brandAmber,
    Color(0xFF2196F3),  // blue
    Color(0xFF9C27B0),  // purple
    Color(0xFFFF5722),  // deep orange
    Color(0xFF00BCD4),  // cyan
  ];
  return palette[index % palette.length];
}
```

**3. Adicionar classe helper _PieSection no mesmo arquivo** (antes ou após a classe _DashboardTabState, mas dentro do arquivo):

```dart
class _PieSection {
  final String label;
  final double value;
  final Color color;
  const _PieSection(this.label, this.value, this.color);
}
```

**4. Após implementar os 4 gráficos, remover o `skip: true` dos testes no test scaffold** (test/features/admin/ui/dashboard_tab_test.dart). Remover `skip: true` de TODOS os testes dos grupos: `period_toggle`, `kpi_cards`, `revenue_chart`, `heatmap`, `status_pie`, `donut_sport`. Manter apenas o teste `loading state` (que já não tem skip).

**NOTA importante sobre `$2` e `$3` em records Dart:** O código de Task 1 usa records `(String, double, Color)`. Em Dart 3, acesso é `item.$1`, `item.$2`, `item.$3`. Verificar sintaxe correta ao implementar.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/dashboard_tab.dart 2>&amp;1 | tail -5 &amp;&amp; flutter test test/features/admin/ui/dashboard_tab_test.dart 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard_tab.dart` contém `PieChart(` (pelo menos 2 ocorrências — status e esporte)
    - `dashboard_tab.dart` contém `centerSpaceRadius: 40` (donut mode)
    - `dashboard_tab.dart` contém `centerSpaceRadius: 0` (pizza mode)
    - `dashboard_tab.dart` contém `'Distribuição de Reservas por Status'`
    - `dashboard_tab.dart` contém `'Nenhum dado de esporte ainda'`
    - `dashboard_tab.dart` contém `'Receita por Esporte'`
    - `dashboard_tab.dart` contém `_sportColor(`
    - `dashboard_tab.dart` contém `class _PieSection`
    - `test/features/admin/ui/dashboard_tab_test.dart` NÃO contém `skip: true` (removidos)
    - `flutter analyze lib/features/admin/ui/dashboard_tab.dart` zero errors
    - `flutter test test/features/admin/ui/dashboard_tab_test.dart` — todos os testes PASS (sem skips)
  </acceptance_criteria>
  <done>Todos os 4 gráficos implementados. flutter analyze zero errors. flutter test passa todos os casos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual — Dashboard no emulador/dispositivo</name>
  <what-built>
DashboardTab completo com:
- Toggle Semana/Mês/Ano fixo no topo
- 5 KPI cards em grid 2 colunas
- BarChart com 3 barras (Total/Pix/Presencial)
- HeatMapCalendar com gradiente de densidade
- PieChart com status de reservas
- Donut com receita por esporte (ou mensagem fallback)
Integrado no AdminScreen como primeira aba "Dashboard".
  </what-built>
  <how-to-verify>
1. Executar: `flutter run -d chrome` (ou emulador Android/iOS)
2. Fazer login como admin
3. Navegar para Painel Admin → aba "Dashboard" (deve ser a primeira aba)
4. Verificar:
   a. Toggle "Semana / Mês / Ano" visível e funcional (clicar em cada um)
   b. Timestamp "Última atualização: HH:MM" ou "--" abaixo do toggle
   c. 5 KPI cards visíveis com labels corretos (Taxa de Ocupação, Receita Total, etc.)
   d. Card "Receita" com BarChart (3 barras coloridas)
   e. Card "Ocupação por Hora e Dia" com HeatMapCalendar (grid colorido)
   f. Card "Distribuição de Reservas por Status" com PieChart (fatias coloridas)
   g. Card "Receita por Esporte" com donut OU mensagem "Nenhum dado de esporte ainda"
5. Scroll para baixo — todos os cards devem ser visíveis
6. Verificar que notificação FCM "Ver" ainda navega para aba "Reservas" (não "Dashboard")
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva problemas visuais encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardData → fl_chart | Dados numéricos convertidos para BarChartRodData/PieChartSectionData — nenhuma entrada de usuário |
| DashboardData → HeatMapCalendar | totalSlotsBooked (int) → datasets Map&lt;DateTime, int&gt; gerado deterministicamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Information Disclosure | PieChart/BarChart — exibe valores financeiros e métricas de negócio | accept | Acessível apenas por admin autenticado (rota /admin com guard Phase 2). Dados lidos de /config/dashboard com regras Firestore admin-only (Phase 21). |
| T-22-03-02 | Tampering | _generateHeatmapDatasets — usa Random com seed determinístico | accept | Dados simulados para MVP — não representam bookings reais hora a hora. Aceitável como proxy visual. Sem impacto de segurança (não afeta bookings). |
| T-22-03-03 | Denial of Service | PieChart/BarChart renderização com muitos dados | accept | revenueBySport é limitado pelo número de esportes cadastrados (lista configurável, padrão 3). Volume baixo, sem risco de DoS na renderização. |
</threat_model>

<verification>
- `flutter analyze lib/features/admin/ui/dashboard_tab.dart` zero errors
- `flutter test test/features/admin/ui/dashboard_tab_test.dart` todos os testes passam (sem skips)
- `flutter test` full suite passa
- Verificação visual: admin vê 4 gráficos + 5 KPI cards + toggle funcional
</verification>

<success_criteria>
- DASH-05: BarChart com 3 barras Total/Pix/Presencial renderiza
- DASH-06: HeatMapCalendar renderiza com gradiente de densidade
- DASH-07: PieChart com fatias Confirmadas/Canceladas/Pendentes/Expiradas renderiza
- DASH-08: Donut com centerSpaceRadius:40 renderiza quando há dados de esporte; fallback message quando vazio
- flutter analyze zero errors
- Todos os testes widget passam
- Admin vê painel completo sem erros visuais
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-ui-do-dashboard/22-03-SUMMARY.md`
</output>
