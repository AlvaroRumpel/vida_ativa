---
phase: 22-ui-do-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - test/features/admin/ui/dashboard_tab_test.dart
autonomous: true
requirements: [DASH-05, DASH-06, DASH-07, DASH-08]
must_haves:
  truths:
    - "flutter pub get executa sem erro após adição dos packages"
    - "test scaffold existe e compila — flutter test dashboard_tab_test.dart passa (todos pendentes)"
    - "fl_chart e flutter_heatmap_calendar aparecem em pubspec.yaml"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Declaração de dependências fl_chart e flutter_heatmap_calendar"
      contains: "fl_chart"
    - path: "test/features/admin/ui/dashboard_tab_test.dart"
      provides: "Scaffold de testes para DashboardTab"
      exports: []
  key_links:
    - from: "pubspec.yaml"
      to: "lib/features/admin/ui/dashboard_tab.dart"
      via: "import 'package:fl_chart/fl_chart.dart'"
      pattern: "fl_chart"
---

<objective>
Instalar as duas dependências novas (fl_chart e flutter_heatmap_calendar) no pubspec.yaml e criar o scaffold de testes widget para DashboardTab com todos os casos de teste pendentes (failing gracefully — skip ou pending).

Purpose: Sem fl_chart e flutter_heatmap_calendar não compila. Sem test scaffold o plano 02/03 não tem onde registrar verificações automatizadas.
Output: pubspec.yaml atualizado + test/features/admin/ui/dashboard_tab_test.dart criado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ui-do-dashboard/22-CONTEXT.md
@.planning/phases/22-ui-do-dashboard/22-RESEARCH.md
@.planning/phases/22-ui-do-dashboard/22-VALIDATION.md

<interfaces>
<!-- DashboardState hierarchy (lib/features/admin/cubit/dashboard_state.dart) -->
```dart
abstract class DashboardState extends Equatable {}
class DashboardLoading extends DashboardState {}
class DashboardLoaded extends DashboardState {
  final DashboardData week;
  final DashboardData month;
  final DashboardData year;
}
class DashboardError extends DashboardState {
  final String message;
}
```

<!-- DashboardData fields relevantes (lib/core/models/dashboard_data.dart) -->
```dart
class DashboardData {
  final String period;
  final DateTime? updatedAt;
  final int totalBookings;
  final int confirmedBookings;
  final int cancelledBookings;
  final int pendingBookings;
  final int totalSlotsBooked;
  final double totalRevenue;
  final double pixRevenue;
  final double onArrivalRevenue;
  // NULLABLE (D+1 delay):
  final double? occupancyRate;
  final double? avgTicket;
  final double? conversionRate;
  final double? noShowRate;
  final List<RevenueBySportEntry>? revenueBySport;
  static DashboardData empty(String period);
}
class RevenueBySportEntry { final String? sport; final double revenue; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar fl_chart e flutter_heatmap_calendar ao pubspec.yaml</name>
  <read_first>
    - pubspec.yaml (estado atual das dependências — ver seção dependencies:)
  </read_first>
  <files>pubspec.yaml</files>
  <action>
Adicionar as duas dependências novas na seção `dependencies:` do pubspec.yaml, após a linha `google_fonts: ^6.2.1`:

```yaml
  fl_chart: ^1.2.0
  flutter_heatmap_calendar: ^1.0.5
```

Após editar o arquivo, executar: `flutter pub get`

O comando deve completar sem erros. Se houver conflito de versão, usar a versão mais recente compatível com Flutter >=3.11.3 (Dart ^3.11.3).
  </action>
  <verify>
    <automated>flutter pub get 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contém a linha `fl_chart: ^1.2.0`
    - pubspec.yaml contém a linha `flutter_heatmap_calendar: ^1.0.5`
    - `flutter pub get` retorna exit code 0 (sem erros)
    - pubspec.lock é atualizado com fl_chart e flutter_heatmap_calendar resolvidos
  </acceptance_criteria>
  <done>Ambos os packages instalados, flutter pub get passa sem erro.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar scaffold de testes widget para DashboardTab</name>
  <read_first>
    - pubspec.yaml (confirmar bloc_test e mocktail em dev_dependencies)
    - lib/features/admin/cubit/dashboard_state.dart
    - lib/core/models/dashboard_data.dart
  </read_first>
  <files>test/features/admin/ui/dashboard_tab_test.dart</files>
  <action>
Criar o arquivo `test/features/admin/ui/dashboard_tab_test.dart` com o scaffold completo de testes widget. O arquivo deve:

1. Importar os packages necessários:
```dart
import 'package:bloc_test/bloc_test.dart';
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:vida_ativa/core/models/dashboard_data.dart';
import 'package:vida_ativa/features/admin/cubit/dashboard_cubit.dart';
import 'package:vida_ativa/features/admin/cubit/dashboard_state.dart';
```

2. Definir mock do DashboardCubit:
```dart
class MockDashboardCubit extends MockCubit<DashboardState> implements DashboardCubit {}
```

3. Definir helper para DashboardData de teste:
```dart
DashboardData testDashboardData({
  double totalRevenue = 1000.0,
  double pixRevenue = 600.0,
  double onArrivalRevenue = 400.0,
  int totalBookings = 10,
  int confirmedBookings = 7,
  int cancelledBookings = 2,
  int pendingBookings = 1,
  int totalSlotsBooked = 8,
  double? occupancyRate = 0.8,
  double? avgTicket = 100.0,
  double? conversionRate = 0.7,
  double? noShowRate = 0.1,
  List<RevenueBySportEntry>? revenueBySport,
}) => DashboardData(
  period: 'week',
  startDate: '2026-01-01',
  endDate: '2026-01-07',
  updatedAt: DateTime(2026, 5, 21, 10, 30),
  totalBookings: totalBookings,
  confirmedBookings: confirmedBookings,
  cancelledBookings: cancelledBookings,
  pendingBookings: pendingBookings,
  totalSlotsBooked: totalSlotsBooked,
  totalRevenue: totalRevenue,
  pixRevenue: pixRevenue,
  onArrivalRevenue: onArrivalRevenue,
  totalSlotsAvailable: 10,
  occupancyRate: occupancyRate,
  avgTicket: avgTicket,
  conversionRate: conversionRate,
  noShowRate: noShowRate,
  uniqueClients: 5,
  newClients: 2,
  returnRate: 0.6,
  topClients: null,
  revenueBySport: revenueBySport,
);
```

4. Criar helper para pump do widget com BlocProvider:
```dart
Widget buildSubject(MockDashboardCubit cubit) => MaterialApp(
  home: BlocProvider<DashboardCubit>.value(
    value: cubit,
    child: const Scaffold(body: DashboardTab()),
  ),
);
```

5. Criar grupo de testes com casos PENDENTES (skip: true) para cada requisito:

```dart
void main() {
  late MockDashboardCubit cubit;
  setUp(() { cubit = MockDashboardCubit(); });

  group('DashboardTab', () {
    group('loading state', () {
      testWidgets('mostra CircularProgressIndicator quando DashboardLoading', (tester) async {
        when(() => cubit.state).thenReturn(const DashboardLoading());
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.byType(CircularProgressIndicator), findsOneWidget);
      });
    });

    group('period_toggle', () {
      testWidgets('SegmentedButton com 3 segmentos Semana/Mes/Ano visivel', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Semana'), findsOneWidget);
        expect(find.text('Mês'), findsOneWidget);
        expect(find.text('Ano'), findsOneWidget);
      }, skip: true); // Wave 0 — DashboardTab nao existe ainda
    });

    group('kpi_cards', () {
      testWidgets('mostra 5 KPI cards com labels corretos', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Taxa de Ocupação'), findsOneWidget);
        expect(find.text('Receita Total'), findsOneWidget);
        expect(find.text('Ticket Médio'), findsOneWidget);
        expect(find.text('Taxa de Conversão'), findsOneWidget);
        expect(find.text('Taxa de No-Show'), findsOneWidget);
      }, skip: true);

      testWidgets('mostra -- quando metricas nullable sao null', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(occupancyRate: null, avgTicket: null, conversionRate: null, noShowRate: null),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('--'), findsWidgets);
      }, skip: true);
    });

    group('revenue_chart', () {
      testWidgets('BarChart de receita renderiza com dados', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Receita'), findsOneWidget);
      }, skip: true);
    });

    group('heatmap', () {
      testWidgets('heatmap ocupa e dia renderiza titulo', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Ocupação por Hora e Dia'), findsOneWidget);
      }, skip: true);
    });

    group('status_pie', () {
      testWidgets('PieChart de status renderiza titulo', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Distribuição de Reservas por Status'), findsOneWidget);
      }, skip: true);
    });

    group('donut_sport', () {
      testWidgets('mostra mensagem quando revenueBySport e null', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(revenueBySport: null),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Nenhum dado de esporte ainda'), findsOneWidget);
      }, skip: true);

      testWidgets('Donut chart renderiza quando revenueBySport tem dados', (tester) async {
        when(() => cubit.state).thenReturn(DashboardLoaded(
          week: testDashboardData(revenueBySport: [
            const RevenueBySportEntry(sport: 'Vôlei', revenue: 500.0),
          ]),
          month: testDashboardData(),
          year: testDashboardData(),
        ));
        await tester.pumpWidget(buildSubject(cubit));
        expect(find.text('Receita por Esporte'), findsOneWidget);
      }, skip: true);
    });
  });
}
```

NOTA: O import de `DashboardTab` será:
```dart
import 'package:vida_ativa/features/admin/ui/dashboard_tab.dart';
```
Adicionar este import ao topo do arquivo. O arquivo ainda não existe (Plan 02 cria), mas o test scaffold compila pois todos os testes com o widget estão marcados `skip: true`.

IMPORTANTE: O teste `loading state` NÃO usa skip — ele testa apenas o estado DashboardLoading com CircularProgressIndicator que Plan 02 criará. Este teste ficará vermelho até Plan 02 completar (aceitável).
  </action>
  <verify>
    <automated>flutter test test/features/admin/ui/dashboard_tab_test.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `test/features/admin/ui/dashboard_tab_test.dart` existe
    - Arquivo contém `class MockDashboardCubit extends MockCubit`
    - Arquivo contém grupos: `period_toggle`, `kpi_cards`, `revenue_chart`, `heatmap`, `status_pie`, `donut_sport`
    - Testes com `skip: true` compilam sem erro de sintaxe
    - `flutter test test/features/admin/ui/dashboard_tab_test.dart` executa (pode falhar no loading state até Plan 02)
  </acceptance_criteria>
  <done>Test scaffold criado com todos os casos mapeados. flutter pub get + test scaffold compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → pub.dev | Dependências externas (fl_chart, flutter_heatmap_calendar) adicionadas ao projeto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | pubspec.yaml — package versions | accept | Packages verificados em pub.dev em 2026-05-21. fl_chart 1.2.0 e flutter_heatmap_calendar 1.0.5 são estáveis sem CVEs conhecidos. |
| T-22-01-02 | Denial of Service | pub.dev network call durante flutter pub get | accept | Desenvolimento local; offline mirror não é requisito. Fallback: pubspec.lock já existente pode ser reusado. |
</threat_model>

<verification>
- `flutter pub get` retorna exit code 0
- `grep "fl_chart" pubspec.yaml` retorna linha com versão
- `grep "flutter_heatmap_calendar" pubspec.yaml` retorna linha com versão
- `flutter test test/features/admin/ui/dashboard_tab_test.dart` compila e executa (skips são OK)
</verification>

<success_criteria>
- fl_chart ^1.2.0 e flutter_heatmap_calendar ^1.0.5 em pubspec.yaml
- flutter pub get passa sem erro
- Test scaffold criado com 7 grupos de test cases (skip até Plan 02/03)
- Teste `loading state` (não-skip) pode estar vermelho — aceitável nesta fase
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-ui-do-dashboard/22-01-SUMMARY.md`
</output>
