---
phase: 22-ui-do-dashboard
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - lib/features/admin/ui/dashboard_tab.dart
  - lib/features/admin/ui/admin_screen.dart
autonomous: true
requirements: [DASH-05, DASH-06, DASH-07, DASH-08]
must_haves:
  truths:
    - "AdminScreen tem 7 tabs; 'Dashboard' é a primeira (índice 0)"
    - "_reservasTabIndex vale 3 em admin_screen.dart"
    - "DashboardTab é StatefulWidget com BlocBuilder — mostra CircularProgressIndicator no loading"
    - "Toggle SegmentedButton<String> com 3 segmentos Semana/Mês/Ano está fixo no topo (fora do scroll)"
    - "Timestamp 'Última atualização: HH:MM' aparece abaixo do toggle"
    - "5 KPI cards em GridView 2 colunas — labels, valores e tratamento de null com '--'"
    - "Estrutura de scroll: Column → toggle+divider → Expanded(SingleChildScrollView)"
  artifacts:
    - path: "lib/features/admin/ui/dashboard_tab.dart"
      provides: "Widget principal DashboardTab com states loading/loaded/error"
      min_lines: 150
    - path: "lib/features/admin/ui/admin_screen.dart"
      provides: "AdminScreen com 7 tabs, Dashboard em índice 0"
      contains: "_reservasTabIndex = 3"
  key_links:
    - from: "lib/features/admin/ui/admin_screen.dart"
      to: "lib/features/admin/ui/dashboard_tab.dart"
      via: "import + TabBarView children[0]"
      pattern: "DashboardTab()"
    - from: "lib/features/admin/ui/dashboard_tab.dart"
      to: "lib/features/admin/cubit/dashboard_cubit.dart"
      via: "BlocBuilder<DashboardCubit, DashboardState>"
      pattern: "BlocBuilder<DashboardCubit"
---

<objective>
Criar o widget DashboardTab com estrutura completa (toggle de período, KPI cards, estados loading/error) e integrar no AdminScreen como primeira aba (índice 0).

Purpose: Estrutura base que Plan 03 completa com os 4 gráficos (BarChart, Heatmap, PieChart, Donut).
Output: dashboard_tab.dart com StatefulWidget funcional + admin_screen.dart com 7 tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-ui-do-dashboard/22-CONTEXT.md
@.planning/phases/22-ui-do-dashboard/22-RESEARCH.md
@.planning/phases/22-ui-do-dashboard/22-UI-SPEC.md

<interfaces>
<!-- lib/features/admin/cubit/dashboard_state.dart -->
```dart
abstract class DashboardState extends Equatable {}
class DashboardLoading extends DashboardState {}
class DashboardLoaded extends DashboardState {
  final DashboardData week;
  final DashboardData month;
  final DashboardData year;
}
class DashboardError extends DashboardState {
  final String message;
}
```

<!-- lib/core/models/dashboard_data.dart — campos disponíveis -->
```dart
class DashboardData {
  final String period;
  final DateTime? updatedAt;
  final int totalBookings;
  final int confirmedBookings;
  final int cancelledBookings;
  final int pendingBookings;
  final int totalSlotsBooked;
  final double totalRevenue;
  final double pixRevenue;
  final double onArrivalRevenue;
  // NULLABLE — D+1 delay (mostrar '--' quando null):
  final double? occupancyRate;   // multiplicar por 100 para %
  final double? avgTicket;       // R$
  final double? conversionRate;  // multiplicar por 100 para %
  final double? noShowRate;      // multiplicar por 100 para %
  final List<RevenueBySportEntry>? revenueBySport;
  static DashboardData empty(String period);
}
class RevenueBySportEntry { final String? sport; final double revenue; }
```

<!-- lib/core/theme/app_theme.dart -->
```dart
class AppTheme {
  static const Color primaryGreen = Color(0xFF7B5D0A);
  static const Color brandAmber = Color(0xFFD4A800);
  // SnackBarTheme já configurado: backgroundColor: Color(0xFF2D2318), actionTextColor: brandAmber
}
```

<!-- lib/core/theme/app_spacing.dart -->
```dart
class AppSpacing {
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 16.0;
  static const double lg = 24.0;
  static const double xl = 32.0;
}
```

<!-- lib/features/admin/ui/admin_screen.dart — estado atual -->
// TabController length: 6
// _reservasTabIndex = 2 (constante estática)
// Tabs (índice 0..5): Slots, Bloqueios, Reservas, Usuarios, Preços, Ajustes
// DashboardCubit JÁ PROVISIONADO em BlocProvider na linha 75-76:
//   create: (_) => DashboardCubit(firestore: FirebaseFirestore.instance)
// _goToReservas() chama _tabController.animateTo(_reservasTabIndex)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar DashboardTab — estrutura base, toggle, KPI cards e estados</name>
  <read_first>
    - lib/features/admin/cubit/dashboard_state.dart
    - lib/core/models/dashboard_data.dart
    - lib/core/theme/app_theme.dart
    - lib/core/theme/app_spacing.dart
    - lib/features/admin/ui/booking_management_tab.dart (padrão de estrutura existente)
    - test/features/admin/ui/dashboard_tab_test.dart (casos de teste a satisfazer)
  </read_first>
  <files>lib/features/admin/ui/dashboard_tab.dart</files>
  <behavior>
    - Test: DashboardLoading → mostra CircularProgressIndicator centralizado
    - Test: DashboardError → mostra SnackBar com mensagem e botão "Tentar Novamente"
    - Test: DashboardLoaded → mostra SegmentedButton com textos "Semana", "Mês", "Ano"
    - Test: DashboardLoaded → mostra timestamp "Última atualização: 10:30" quando updatedAt != null
    - Test: DashboardLoaded → mostra timestamp "Última atualização: --" quando updatedAt == null
    - Test: DashboardLoaded → mostra 5 labels: "Taxa de Ocupação", "Receita Total", "Ticket Médio", "Taxa de Conversão", "Taxa de No-Show"
    - Test: DashboardLoaded com métricas null → mostra '--' nos cards
  </behavior>
  <action>
Criar `lib/features/admin/ui/dashboard_tab.dart` com implementação completa.

**Imports obrigatórios:**
```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:intl/intl.dart';
import 'package:vida_ativa/core/models/dashboard_data.dart';
import 'package:vida_ativa/core/theme/app_spacing.dart';
import 'package:vida_ativa/core/theme/app_theme.dart';
import 'package:vida_ativa/features/admin/cubit/dashboard_cubit.dart';
import 'package:vida_ativa/features/admin/cubit/dashboard_state.dart';
// fl_chart e flutter_heatmap_calendar — STUB neste plan (importar mas placeholders)
import 'package:fl_chart/fl_chart.dart';
import 'package:flutter_heatmap_calendar/flutter_heatmap_calendar.dart';
```

**Classe DashboardTab** (StatefulWidget, per D-02):
```dart
class DashboardTab extends StatefulWidget {
  const DashboardTab({super.key});
  @override
  State<DashboardTab> createState() => _DashboardTabState();
}
```

**Classe _DashboardTabState:**
- `String _selectedPeriod = 'week'` — estado local (per D-03, Claude's discretion)
- `build()` retorna `BlocConsumer<DashboardCubit, DashboardState>` (usa BlocConsumer para capturar DashboardError e exibir SnackBar per D-18):
  ```dart
  BlocConsumer<DashboardCubit, DashboardState>(
    listener: (context, state) {
      if (state is DashboardError) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: Text(state.message),
            action: SnackBarAction(
              label: 'Tentar Novamente',
              onPressed: () {
                // DashboardCubit não tem retry() público — recriar stream via BlocProvider.of
                // Workaround: fechar e reabrir o cubit não é possível aqui.
                // Implementar chamando método privado exposto ou usando context.read<DashboardCubit>()
                // Por ora: mostrar SnackBar apenas (cubit reabre automaticamente via Stream)
              },
            ),
          ),
        );
      }
    },
    builder: (context, state) => switch (state) {
      DashboardLoading() => const Center(child: CircularProgressIndicator()),
      DashboardError() => const Center(child: CircularProgressIndicator()), // SnackBar já exibido no listener
      DashboardLoaded() => _buildDashboard(context, state as DashboardLoaded),
      _ => const Center(child: CircularProgressIndicator()),
    },
  )
  ```

**Método _buildDashboard(context, DashboardLoaded state):**
- Chama `_selectData(state)` para obter `DashboardData data`
- Retorna Column (per D-04):
  ```dart
  Column(
    children: [
      Padding(
        padding: const EdgeInsets.fromLTRB(AppSpacing.md, AppSpacing.md, AppSpacing.md, 0),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            SegmentedButton<String>(
              segments: const [
                ButtonSegment(value: 'week', label: Text('Semana')),
                ButtonSegment(value: 'month', label: Text('Mês')),
                ButtonSegment(value: 'year', label: Text('Ano')),
              ],
              selected: {_selectedPeriod},
              onSelectionChanged: (newSelection) {
                setState(() => _selectedPeriod = newSelection.first);
              },
            ),
            const SizedBox(height: AppSpacing.xs),
            Align(
              alignment: Alignment.centerRight,
              child: Text(
                'Última atualização: ${_formatTime(data.updatedAt)}',
                style: TextStyle(fontSize: 11, color: Colors.grey[600]),
              ),
            ),
          ],
        ),
      ),
      const Divider(height: 1),
      Expanded(
        child: SingleChildScrollView(
          padding: const EdgeInsets.symmetric(horizontal: AppSpacing.md, vertical: AppSpacing.md),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              _buildKpiGrid(data),
              const SizedBox(height: AppSpacing.lg),
              _buildRevenueChart(data),   // Stub em Plan 02 → implementado Plan 03
              const SizedBox(height: AppSpacing.lg),
              _buildHeatmap(data),        // Stub em Plan 02 → implementado Plan 03
              const SizedBox(height: AppSpacing.lg),
              _buildStatusPie(data),      // Stub em Plan 02 → implementado Plan 03
              const SizedBox(height: AppSpacing.lg),
              _buildSportDonut(data),     // Stub em Plan 02 → implementado Plan 03
              const SizedBox(height: AppSpacing.xl),
            ],
          ),
        ),
      ),
    ],
  )
  ```

**Método _selectData(DashboardLoaded state) → DashboardData:**
```dart
DashboardData _selectData(DashboardLoaded state) => switch (_selectedPeriod) {
  'week'  => state.week,
  'month' => state.month,
  'year'  => state.year,
  _       => state.week,
};
```

**Método _formatTime(DateTime? dt) → String:**
```dart
String _formatTime(DateTime? dt) {
  if (dt == null) return '--';
  return DateFormat('HH:mm').format(dt);
}
```

**Método _buildKpiGrid(DashboardData data) → Widget:**

Retorna GridView.count com crossAxisCount: 2, shrinkWrap: true, physics: NeverScrollableScrollPhysics(), crossAxisSpacing: AppSpacing.sm, mainAxisSpacing: AppSpacing.sm, childAspectRatio: 1.3.

5 KPI cards na ordem (per UI-SPEC.md):
1. Label: "Taxa de Ocupação", value: `occupancyRate != null ? '${(occupancyRate! * 100).toStringAsFixed(1)}%' : '--'`, unit: '' 
2. Label: "Receita Total", value: `NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$').format(data.totalRevenue)`, unit: ''
3. Label: "Ticket Médio", value: `data.avgTicket != null ? NumberFormat.currency(locale: 'pt_BR', symbol: 'R\$').format(data.avgTicket!) : '--'`, unit: ''
4. Label: "Taxa de Conversão", value: `data.conversionRate != null ? '${(data.conversionRate! * 100).toStringAsFixed(1)}%' : '--'`, unit: ''
5. Label: "Taxa de No-Show", value: `data.noShowRate != null ? '${(data.noShowRate! * 100).toStringAsFixed(1)}%' : '--'`, unit: ''

Cada card segue este padrão (per D-11, UI-SPEC KPI Card):
```dart
Card(
  elevation: 1,
  child: Padding(
    padding: const EdgeInsets.all(12.0),
    child: Column(
      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(label, style: TextStyle(fontSize: 12, color: Colors.grey[600])),
        Text(
          value,
          style: TextStyle(
            fontSize: 24,
            fontWeight: FontWeight.bold,
            color: AppTheme.primaryGreen,
          ),
        ),
        if (unit.isNotEmpty)
          Text(unit, style: TextStyle(fontSize: 11, color: Colors.grey[600])),
      ],
    ),
  ),
)
```

**Stubs para gráficos (Plan 03 irá substituir):**

```dart
Widget _buildRevenueChart(DashboardData data) {
  return Card(
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: const [
          Text('Receita', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          SizedBox(height: AppSpacing.sm),
          AspectRatio(aspectRatio: 16 / 9, child: Center(child: Text('Carregando...'))),
        ],
      ),
    ),
  );
}

Widget _buildHeatmap(DashboardData data) {
  return Card(
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: const [
          Text('Ocupação por Hora e Dia', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          SizedBox(height: AppSpacing.sm),
          Center(child: Text('Carregando...')),
        ],
      ),
    ),
  );
}

Widget _buildStatusPie(DashboardData data) {
  return Card(
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: const [
          Text('Distribuição de Reservas por Status', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          SizedBox(height: AppSpacing.sm),
          AspectRatio(aspectRatio: 16 / 9, child: Center(child: Text('Carregando...'))),
        ],
      ),
    ),
  );
}

Widget _buildSportDonut(DashboardData data) {
  final hasSportData = data.revenueBySport != null && data.revenueBySport!.isNotEmpty;
  return Card(
    child: Padding(
      padding: const EdgeInsets.all(AppSpacing.md),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Text('Receita por Esporte', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
          const SizedBox(height: AppSpacing.sm),
          if (!hasSportData)
            Center(
              child: Padding(
                padding: const EdgeInsets.symmetric(vertical: AppSpacing.lg),
                child: Text('Nenhum dado de esporte ainda', style: TextStyle(color: Colors.grey[600], fontSize: 14)),
              ),
            )
          else
            const AspectRatio(aspectRatio: 16 / 9, child: Center(child: Text('Carregando...'))),
        ],
      ),
    ),
  );
}
```

NOTA: Os stubs garantem que Plan 02 compila e os testes de estrutura (toggle, KPI, labels) passam. Plan 03 substitui os stubs por implementações reais de fl_chart e flutter_heatmap_calendar.

Não importar fl_chart nem flutter_heatmap_calendar neste arquivo ainda — os stubs não precisam. Remover os imports `fl_chart` e `flutter_heatmap_calendar` da lista de imports para evitar warnings "unused import". Plan 03 adicionará esses imports ao substituir os stubs.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/dashboard_tab.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/admin/ui/dashboard_tab.dart` existe
    - Arquivo contém `class DashboardTab extends StatefulWidget`
    - Arquivo contém `class _DashboardTabState extends State&lt;DashboardTab&gt;`
    - Arquivo contém `String _selectedPeriod = 'week'`
    - Arquivo contém `SegmentedButton&lt;String&gt;`
    - Arquivo contém `ButtonSegment(value: 'week', label: Text('Semana'))`
    - Arquivo contém `ButtonSegment(value: 'month', label: Text('Mês'))`
    - Arquivo contém `ButtonSegment(value: 'year', label: Text('Ano'))`
    - Arquivo contém `'Última atualização: '`
    - Arquivo contém `'Taxa de Ocupação'`
    - Arquivo contém `'Receita Total'`
    - Arquivo contém `'Ticket Médio'`
    - Arquivo contém `'Taxa de Conversão'`
    - Arquivo contém `'Taxa de No-Show'`
    - Arquivo contém `'Nenhum dado de esporte ainda'`
    - Arquivo contém `'Receita por Esporte'`
    - `flutter analyze lib/features/admin/ui/dashboard_tab.dart` retorna "No issues found" (ou zero errors)
  </acceptance_criteria>
  <done>DashboardTab compilando sem erros. Toggle, KPI cards e estados loading/error implementados. Stubs de gráficos presentes para Plan 03.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar DashboardTab no AdminScreen como aba índice 0</name>
  <read_first>
    - lib/features/admin/ui/admin_screen.dart (estado atual completo — ver linha a linha)
  </read_first>
  <files>lib/features/admin/ui/admin_screen.dart</files>
  <action>
Modificar `lib/features/admin/ui/admin_screen.dart` com as seguintes alterações exatas (per D-01, CONTEXT.md código_contexto):

**1. Adicionar import no topo do arquivo** (após os imports existentes):
```dart
import 'package:vida_ativa/features/admin/ui/dashboard_tab.dart';
```

**2. Alterar a constante `_reservasTabIndex`** (per D-01):
```dart
// ANTES:
static const int _reservasTabIndex = 2;
// DEPOIS:
static const int _reservasTabIndex = 3;
```

**3. Alterar `TabController(length:...)`** na linha de initState:
```dart
// ANTES:
_tabController = TabController(length: 6, vsync: this);
// DEPOIS:
_tabController = TabController(length: 7, vsync: this);
```

**4. Inserir Tab "Dashboard" como PRIMEIRO elemento no TabBar** (per D-01):
```dart
// ANTES (tabs lista):
tabs: const [
  Tab(text: 'Slots'),
  Tab(text: 'Bloqueios'),
  Tab(text: 'Reservas'),
  Tab(text: 'Usuarios'),
  Tab(text: 'Preços'),
  Tab(text: 'Ajustes'),
],
// DEPOIS:
tabs: const [
  Tab(text: 'Dashboard'),   // índice 0 — NOVO
  Tab(text: 'Slots'),
  Tab(text: 'Bloqueios'),
  Tab(text: 'Reservas'),
  Tab(text: 'Usuarios'),
  Tab(text: 'Preços'),
  Tab(text: 'Ajustes'),
],
```

**5. Inserir DashboardTab() como PRIMEIRO filho no TabBarView** (per D-01):
```dart
// ANTES (children lista):
children: [
  const SlotManagementTab(),
  const BlockedDatesTab(),
  const BookingManagementTab(),
  ...
],
// DEPOIS:
children: [
  const DashboardTab(),    // índice 0 — NOVO
  const SlotManagementTab(),
  const BlockedDatesTab(),
  const BookingManagementTab(),
  const UsersManagementTab(),
  const PricingTab(),
  BlocProvider(
    create: (_) => SettingsCubit(
      firestore: FirebaseFirestore.instance,
    ),
    child: const SettingsTab(),
  ),
],
```

NOTA: DashboardCubit JÁ está provisionado no BlocProvider externo (linha 75-76 do arquivo original). Não adicionar outro BlocProvider para DashboardCubit — DashboardTab acessa via `context.read<DashboardCubit>()` ou BlocBuilder direto.
  </action>
  <verify>
    <automated>flutter analyze lib/features/admin/ui/admin_screen.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `admin_screen.dart` contém `_reservasTabIndex = 3`
    - `admin_screen.dart` contém `TabController(length: 7`
    - `admin_screen.dart` contém `Tab(text: 'Dashboard')`
    - `admin_screen.dart` contém `const DashboardTab()`
    - `admin_screen.dart` contém `import 'package:vida_ativa/features/admin/ui/dashboard_tab.dart'`
    - `flutter analyze lib/features/admin/ui/admin_screen.dart` retorna zero errors
    - `flutter test test/features/admin/ui/dashboard_tab_test.dart` — teste `loading state` passa (sem skip)
  </acceptance_criteria>
  <done>AdminScreen com 7 tabs. Dashboard em índice 0. _reservasTabIndex=3. Notificação FCM "Ver" ainda navega para Reservas (índice 3 = correto).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardCubit → Firestore | Leitura de /config/dashboard/periods — dados já validados pela Phase 21 |
| AdminScreen → DashboardTab | Widget aninhado acessa DashboardCubit via BlocProvider ancestral |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Information Disclosure | DashboardTab — exibe métricas financeiras | accept | Acesso restrito por AdminScreen (rota /admin requer role=admin via go_router guard, Phase 2). Dados lidos de /config/dashboard com regras Firestore que bloqueiam clientes (Phase 21). |
| T-22-02-02 | Tampering | DashboardData exibida no UI | accept | UI é read-only. DashboardTab não escreve no Firestore. Cubit apenas lê stream. |
| T-22-02-03 | Denial of Service | SegmentedButton period toggle gera rebuild excessivo | accept | setState apenas atualiza _selectedPeriod local; BlocBuilder não refaz query Firestore — apenas re-renderiza com dados em memória. |
</threat_model>

<verification>
- `flutter analyze lib/features/admin/ui/dashboard_tab.dart` zero errors
- `flutter analyze lib/features/admin/ui/admin_screen.dart` zero errors  
- `flutter test test/features/admin/ui/dashboard_tab_test.dart` — loading state test PASS
- Testes skip ainda skippados (aguardam Plan 03 para gráficos)
</verification>

<success_criteria>
- DashboardTab criado com StatefulWidget, BlocConsumer, toggle, KPI grid e stubs de gráficos
- AdminScreen com 7 tabs, Dashboard em índice 0, _reservasTabIndex=3
- `flutter analyze` zero errors em ambos os arquivos
- Teste `loading state` passa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-ui-do-dashboard/22-02-SUMMARY.md`
</output>
